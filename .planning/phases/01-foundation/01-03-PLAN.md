---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tabs/FinansialTab.tsx
  - src/App.tsx
autonomous: false
requirements:
  - NAV-01
tags: [react, navigation, tabs, shadcn]
must_haves:
  truths:
    - "Tab in top nav is labeled 'Finansial' (not 'Goals')"
    - "Tab value stays 'goals' so persisted state from useTabStore does not break"
    - "Clicking the 'Finansial' tab shows two sub-tabs: Kekayaan and Goals"
    - "Sub-tab 'Kekayaan' is active by default when the tab opens"
    - "Sub-tab 'Kekayaan' renders a placeholder (no Net Worth UI yet — that is Phase 2)"
    - "Sub-tab 'Goals' renders existing GoalsTab content unchanged"
    - "All other tabs (Dashboard, Transaksi, Investasi, Pensiun, Laporan, Catatan, Pengaturan) continue to work"
    - "TypeScript build (tsc --noEmit) has zero errors"
  artifacts:
    - path: "src/tabs/FinansialTab.tsx"
      provides: "Default-exported React component wrapping GoalsTab + Kekayaan placeholder in shadcn sub-tabs"
      exports: ["default"]
      contains: "defaultValue=\"kekayaan\""
    - path: "src/App.tsx"
      provides: "Updated TABS array with Finansial label + FinansialTab Comp"
      contains: "label: 'Finansial'"
  key_links:
    - from: "src/App.tsx TABS array entry value='goals'"
      to: "src/tabs/FinansialTab.tsx"
      via: "Comp: FinansialTab"
      pattern: "Comp: FinansialTab"
    - from: "src/tabs/FinansialTab.tsx"
      to: "src/tabs/GoalsTab.tsx"
      via: "import GoalsTab from '@/tabs/GoalsTab'"
      pattern: "import GoalsTab from '@/tabs/GoalsTab'"
    - from: "src/tabs/FinansialTab.tsx Tabs"
      to: "shadcn Tabs component"
      via: "import from '@/components/ui/tabs'"
      pattern: "from '@/components/ui/tabs'"
---

<objective>
Restruktur navigasi: tab top-level `Goals` di-rename menjadi `Finansial`, dan komponen tab-nya diganti dari `GoalsTab` menjadi wrapper baru `FinansialTab` yang berisi 2 sub-tab (Kekayaan + Goals). Sub-tab Kekayaan default aktif dengan placeholder. Sub-tab Goals me-render `GoalsTab` yang tidak berubah.

Purpose: Menyiapkan "rumah" UI untuk fitur Net Worth Phase 2 tanpa menambah UI baru. Tab `value` tetap `'goals'` (D-01) agar state tersimpan pengguna existing tidak rusak.
Output: 1 file baru (`FinansialTab.tsx`) + 1 file termodifikasi (`App.tsx`, 3-line patch).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-PATTERNS.md
@src/tabs/PensiunTab.tsx
@src/tabs/GoalsTab.tsx

<interfaces>
<!-- GoalsTab confirmed self-contained — zero props from parent (finding #4) -->
<!-- PensiunTab is the canonical sub-tab pattern -->

From src/components/ui/tabs.tsx (shadcn):
```typescript
export const Tabs: (props: { defaultValue: string; className?: string; children: ReactNode }) => JSX.Element
export const TabsList: (props: { className?: string; children: ReactNode }) => JSX.Element
export const TabsTrigger: (props: { value: string; children: ReactNode }) => JSX.Element
export const TabsContent: (props: { value: string; children: ReactNode }) => JSX.Element
```

From src/tabs/GoalsTab.tsx:
```typescript
export default function GoalsTab(): JSX.Element  // zero props, self-contained
```

Current App.tsx TABS entry (line 32):
```typescript
{ value: 'goals',  label: 'Goals',  icon: Target,  Comp: GoalsTab }
```

Target App.tsx TABS entry:
```typescript
{ value: 'goals',  label: 'Finansial',  icon: Target,  Comp: FinansialTab }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FinansialTab.tsx wrapper with 2 sub-tabs</name>
  <files>src/tabs/FinansialTab.tsx</files>
  <read_first>
    - src/tabs/PensiunTab.tsx (canonical sub-tab pattern — lines around the inner Tabs block)
    - src/tabs/GoalsTab.tsx (confirm zero props consumed from parent)
    - src/components/ui/tabs.tsx (shadcn Tabs component exports)
    - .planning/phases/01-foundation/01-UI-SPEC.md (FinansialTab JSX exactly as specified)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-03, D-04 — defaultValue "kekayaan", PensiunTab pattern)
  </read_first>
  <behavior>
    - Default export is a React function component with signature `() => JSX.Element`
    - Renders a single top-level `<div className="space-y-6">` containing a `<Tabs defaultValue="kekayaan" className="w-full">`
    - Has exactly 2 TabsTrigger: value="kekayaan" (label "Kekayaan"), value="goals" (label "Goals")
    - TabsTrigger order in JSX is kekayaan FIRST, goals SECOND (D-03)
    - TabsContent value="kekayaan" renders the placeholder div with text "Fitur Kekayaan (Net Worth) akan hadir di Phase 2."
    - TabsContent value="goals" renders `<GoalsTab />` with zero props
    - No state, no hooks, no useEffect — purely structural
  </behavior>
  <action>
    Create new file `src/tabs/FinansialTab.tsx` with this EXACT content:

    ```tsx
    import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs'
    import GoalsTab from '@/tabs/GoalsTab'

    export default function FinansialTab() {
      return (
        <div className="space-y-6">
          <Tabs defaultValue="kekayaan" className="w-full">
            <TabsList className="mb-4">
              <TabsTrigger value="kekayaan">Kekayaan</TabsTrigger>
              <TabsTrigger value="goals">Goals</TabsTrigger>
            </TabsList>
            <TabsContent value="kekayaan">
              <div className="flex flex-col items-center justify-center py-20 text-center">
                <span className="text-muted-foreground text-sm">
                  Fitur Kekayaan (Net Worth) akan hadir di Phase 2.
                </span>
              </div>
            </TabsContent>
            <TabsContent value="goals">
              <GoalsTab />
            </TabsContent>
          </Tabs>
        </div>
      )
    }
    ```

    Hard rules:
    - Do NOT modify `src/tabs/GoalsTab.tsx` AT ALL (finding #8 — zero changes)
    - Do NOT add props to GoalsTab — it is self-contained (finding #4, verified)
    - Do NOT use relative imports (`../components/`) — use `@/` alias per project convention
    - Do NOT add any state, useEffect, or hooks — this is a purely structural wrapper
    - Keep the outer `<div className="space-y-6">` — matches PensiunTab pattern (D-04)
    - `defaultValue` on outer Tabs MUST be the string literal `"kekayaan"` (D-03)
    - Order of TabsTrigger: kekayaan first, goals second
  </action>
  <verify>
    <automated>test -f src/tabs/FinansialTab.tsx && grep -q 'defaultValue="kekayaan"' src/tabs/FinansialTab.tsx && grep -q 'export default function FinansialTab' src/tabs/FinansialTab.tsx && grep -q "from '@/components/ui/tabs'" src/tabs/FinansialTab.tsx && grep -q "from '@/tabs/GoalsTab'" src/tabs/FinansialTab.tsx && grep -q "Fitur Kekayaan (Net Worth) akan hadir di Phase 2." src/tabs/FinansialTab.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `src/tabs/FinansialTab.tsx` exists
    - `grep "export default function FinansialTab"` matches 1 line
    - `grep 'defaultValue="kekayaan"'` matches 1 line (D-03)
    - `grep -c 'TabsTrigger value='` returns 2
    - `grep 'TabsTrigger value="kekayaan"'` matches 1 line
    - `grep 'TabsTrigger value="goals"'` matches 1 line
    - In the file, `value="kekayaan"` TabsTrigger appears BEFORE `value="goals"` TabsTrigger (use `grep -n` to confirm line order — kekayaan line number < goals line number)
    - `grep "from '@/components/ui/tabs'"` matches 1 line
    - `grep "from '@/tabs/GoalsTab'"` matches 1 line
    - `grep "Fitur Kekayaan (Net Worth) akan hadir di Phase 2."` matches 1 line (exact copy from UI-SPEC)
    - `grep "<GoalsTab />"` matches 1 line (rendered with zero props)
    - File does NOT contain `useState`, `useEffect`, `useQuery`, or any hook import (structural-only)
    - File does NOT contain `../` (no relative imports)
    - `npx tsc --noEmit` exits 0
    - `src/tabs/GoalsTab.tsx` has ZERO modifications compared to HEAD — verify with `git diff --stat HEAD src/tabs/GoalsTab.tsx` showing no changes
  </acceptance_criteria>
  <done>FinansialTab.tsx created matching UI-SPEC exactly, TypeScript clean, GoalsTab.tsx untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update App.tsx TABS array — swap to Finansial + FinansialTab</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (current state — lines 1-40 where imports and TABS array live)
    - src/tabs/FinansialTab.tsx (just created — confirms the import target exists)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01, D-02 — value stays 'goals', icon stays Target, only label and Comp change)
    - .planning/phases/01-foundation/01-PATTERNS.md (Critical Warnings — MUST remove GoalsTab import from App.tsx to avoid unused import error, finding #5)
  </read_first>
  <behavior>
    - Top-level tab value remains the literal string `'goals'` (D-01 — prevents useTabStore state corruption)
    - Top-level tab label changes from `'Goals'` to `'Finansial'`
    - Top-level tab Comp changes from `GoalsTab` to `FinansialTab`
    - Top-level tab icon remains `Target` (D-02)
    - `GoalsTab` import removed from App.tsx (no longer referenced there; FinansialTab imports it directly) — finding #5 mandates this to avoid TS unused-import error
    - `FinansialTab` import added at the top of App.tsx
    - All OTHER TABS entries (dashboard, transactions, investments, pensiun, reports, notes, settings) are unchanged byte-for-byte
    - TypeScript build clean (`npx tsc --noEmit` returns 0)
  </behavior>
  <action>
    Make exactly these changes to `src/App.tsx`:

    **Change 1 — Remove the GoalsTab import:**
    Find and DELETE this line (currently around line 16):
    ```typescript
    import GoalsTab from '@/tabs/GoalsTab'
    ```

    **Change 2 — Add the FinansialTab import** (insert where GoalsTab's line was, or adjacent to other tab imports, to keep the import block tidy):
    ```typescript
    import FinansialTab from '@/tabs/FinansialTab'
    ```

    **Change 3 — Update the TABS array entry for 'goals'** (currently line 32):
    Replace:
    ```typescript
    { value: 'goals',        label: 'Goals',      icon: Target,          Comp: GoalsTab },
    ```
    With:
    ```typescript
    { value: 'goals',        label: 'Finansial',  icon: Target,          Comp: FinansialTab },
    ```

    Constraints (ALL must hold):
    - Do NOT change the `value:` field — it MUST remain `'goals'` (D-01)
    - Do NOT change the `icon:` field — it MUST remain `Target` (D-02)
    - Do NOT remove/change the `Target` import from lucide-react (still used for this tab)
    - Do NOT change any other TABS array entry
    - Do NOT re-order the TABS array entries
    - Do NOT add or remove any other imports
    - Do NOT modify any function body in App.tsx — this is imports + TABS array only

    Anti-pattern to avoid: a naive search-and-replace that changes `'goals'` to `'finansial'` — that would break persisted tab state for existing users (D-01). ONLY change the label and Comp fields.
  </action>
  <verify>
    <automated>grep -q "import FinansialTab from '@/tabs/FinansialTab'" src/App.tsx && ! grep -q "import GoalsTab from '@/tabs/GoalsTab'" src/App.tsx && grep -q "value: 'goals'" src/App.tsx && grep -q "label: 'Finansial'" src/App.tsx && grep -q "Comp: FinansialTab" src/App.tsx && ! grep -q "Comp: GoalsTab" src/App.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import FinansialTab from '@/tabs/FinansialTab'" src/App.tsx` matches 1 line
    - `grep "import GoalsTab" src/App.tsx` returns NO matches (empty output)
    - `grep "value: 'goals'" src/App.tsx` still matches 1 line (D-01 preserved)
    - `grep "label: 'Finansial'" src/App.tsx` matches 1 line
    - `grep "Comp: FinansialTab" src/App.tsx` matches 1 line
    - `grep "Comp: GoalsTab" src/App.tsx` returns NO matches
    - `grep "label: 'Goals'" src/App.tsx` returns NO matches
    - `grep -c "Comp:" src/App.tsx` returns 8 (same count as before — all other entries intact)
    - `grep "icon: Target" src/App.tsx` matches 1 line (Target icon preserved per D-02)
    - `grep "import { " src/App.tsx | grep "Target"` confirms Target still imported from lucide-react
    - `git diff --stat src/App.tsx` shows changes only on the 3 expected lines (GoalsTab import removed, FinansialTab import added, one TABS entry modified)
    - `npx tsc --noEmit` returns exit code 0 — zero TypeScript errors (this catches the unused-import issue)
    - `npm run build` (if available) completes without errors
  </acceptance_criteria>
  <done>App.tsx has new FinansialTab import, old GoalsTab import removed, TABS entry updated with `label: 'Finansial'` and `Comp: FinansialTab` while `value: 'goals'` and `icon: Target` are preserved. TS build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — navigation works end-to-end</name>
  <files>(no files modified — verification only)</files>
  <action>Execute the verification steps described in <how-to-verify> below. Report PASS/FAIL for each numbered step. Do not modify any code in this task.</action>
  <what-built>
    Tab "Goals" has been renamed to "Finansial" at the top-level nav, now rendering a new `FinansialTab` wrapper that contains two sub-tabs: "Kekayaan" (default active, shows a Phase 2 placeholder) and "Goals" (shows the existing unchanged GoalsTab content).
  </what-built>
  <how-to-verify>
    1. Run the app locally (`npm run dev` or equivalent) and log in.
    2. Check top nav: the 4th tab (between Investasi and Pensiun) should now read "Finansial" with the Target icon. "Goals" should no longer appear as a top-level tab.
    3. Click the "Finansial" tab. It should show two sub-tabs: "Kekayaan" and "Goals" (in that order).
    4. "Kekayaan" sub-tab should be active by default (highlighted). Its content area should show the centered placeholder text: "Fitur Kekayaan (Net Worth) akan hadir di Phase 2."
    5. Click the "Goals" sub-tab. The existing Goals content (Tujuan/Goals list and CRUD) should appear exactly as it did before — including any data you previously created.
    6. Click back to "Kekayaan" — placeholder reappears.
    7. Regression check — click through ALL other top tabs and confirm each still works normally: Dashboard, Transaksi, Investasi, Pensiun, Laporan, Catatan, Pengaturan. No blank screens, no console errors.
    8. Persistence check: refresh the page while on the "Finansial" tab. After reload, "Finansial" tab should still be active (because `useTabStore` persists the value `'goals'` which still matches this entry — D-01 guarantee).
    9. Open browser devtools console — should have ZERO errors and ZERO new warnings from these changes.
  </how-to-verify>
  
  <verify>
    <automated>MISSING — human-only verification; the checks listed in how-to-verify must all be confirmed by the user before resume</automated>
  </verify>
  <done>User replies with "approved" indicating all numbered checks in how-to-verify passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → client React state | Tab navigation is client-side only; no new data crosses trust boundaries in this plan |
| Persisted state (`useTabStore`) → component tree | Existing users have `activeTab='goals'` persisted; new code must still accept that value (D-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | Broken persisted tab state leaves users stuck on a non-existent tab | mitigate | D-01 — keep `value: 'goals'` unchanged; persisted state remains valid |
| T-03-02 | Tampering | Import cycle or missing import breaks build | mitigate | `npx tsc --noEmit` in acceptance criteria catches this; Task 2 explicitly removes the now-unused GoalsTab import (finding #5) |
| T-03-03 | Information Disclosure | Net Worth placeholder leaks future-feature details | accept | Placeholder text is intentional user communication, not sensitive data |
| T-03-04 | Spoofing | N/A — no auth or identity changes | accept | This plan touches only view-layer navigation |
</threat_model>

<verification>
- FinansialTab.tsx matches UI-SPEC exactly (Task 1 grep checks)
- App.tsx has minimal diff — 3 line-level changes, no other touches (Task 2 grep checks)
- `npx tsc --noEmit` clean
- Human verification confirms end-to-end navigation works and other tabs regress cleanly (Task 3)
- `src/tabs/GoalsTab.tsx` has zero modifications (finding #8)
</verification>

<success_criteria>
- NAV-01 satisfied: tab "Goals" renamed to "Finansial" with 2 sub-tabs (Kekayaan, Goals) — navigasi berfungsi tanpa error
- Phase-level success criterion 3 met: tab structure works, sub-tab navigation works, no errors
- Phase-level success criterion 4 met: all existing features still work (verified in Task 3 step 7)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
