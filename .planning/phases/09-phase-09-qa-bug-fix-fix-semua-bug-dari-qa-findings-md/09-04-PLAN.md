---
phase: 09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-VERIFICATION.md
  - .planning/STATE.md
autonomous: false
requirements: [QA-CRITICAL-1, QA-CRITICAL-2, QA-MEDIUM-3, QA-MEDIUM-4, QA-MEDIUM-5, QA-MEDIUM-6, QA-LOW-7, QA-LOW-8]
must_haves:
  truths:
    - "All 8 QA-FINDINGS.md bugs verified fixed via combination of static check + live UAT"
    - "VERIFICATION.md exists and records evidence per bug (PASS/FAIL + evidence pointer)"
    - "STATE.md updated to reflect Phase 9 completion"
    - "No regression introduced — existing app flows (login, transaksi CRUD, kekayaan, pensiun, laporan) still work"
  artifacts:
    - path: ".planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-VERIFICATION.md"
      provides: "Evidence-backed verification record for all 8 bugs"
      contains: "Bug #1, Bug #2, Bug #3, Bug #4, Bug #5, Bug #6, Bug #7, Bug #8"
    - path: ".planning/STATE.md"
      provides: "Updated project state reflecting Phase 9 complete"
      contains: "Phase 9"
  key_links:
    - from: ".planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-VERIFICATION.md"
      to: "QA-FINDINGS.md"
      via: "1-to-1 bug mapping with evidence"
      pattern: "Bug #[1-8]"
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 9 status update"
      pattern: "Phase 9.*complete|Phase 9.*PASS"
---

<objective>
Final verification gate untuk Phase 9. Eksekusi static checks (grep/tsc/build) untuk 6 bug frontend, eksekusi live UAT (browser MCP) untuk 5 scenarios yang mencakup semua 8 bug, lalu tulis VERIFICATION.md dan update STATE.md.

Purpose: Tutup phase secara formal dengan evidence terdokumentasi. Tanpa task ini, fix mungkin sudah deployed tapi tidak ada single-source-of-truth yang membuktikan setiap dari 8 bug verified PASS.

Output: 1 VERIFICATION.md + STATE.md updated. Phase 9 official complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-CONTEXT.md
@.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-01-PLAN.md
@.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-02-PLAN.md
@.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-03-PLAN.md
@QA-FINDINGS.md

<interfaces>
<!-- VERIFICATION.md structure (mirror Phase 5/6/7 VERIFICATION.md style) -->

```markdown
# Phase 9 Verification — QA Bug Fix

**Verifier:** Claude Sonnet 4.6 + user (manual UAT)
**Date:** YYYY-MM-DD
**Scope:** Semua 8 bug dari QA-FINDINGS.md (audit 2026-05-01)

## Summary

| Bug | Severity | Verification Method | Status |
|-----|----------|---------------------|--------|
| #1 — Link Investasi (FOR UPDATE+aggregate) | Critical | Studio query + live UAT | PASS |
| #2 — Tambah Uang (ambiguous column) | Critical | Studio query + live UAT | PASS |
| #3 — GoalsTab filter state reset | Medium | Live UAT | PASS |
| ... |

## Evidence per Bug
...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Static verification — grep all 6 frontend changes from Plan 09-02</name>
  <files>—</files>
  <read_first>
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-02-PLAN.md (acceptance criteria per task — runs as input checklist)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-02-SUMMARY.md (build/lint output recorded)
  </read_first>
  <action>
Eksekusi 6 grep checks untuk membuktikan setiap fix frontend ada di kode (post-deploy ke production):

```bash
# Bug #3 — FinansialTab forceMount on goals only
grep -c 'TabsContent value="goals" forceMount' src/tabs/FinansialTab.tsx
# Expected: 1
grep -c 'TabsContent value="kekayaan" forceMount' src/tabs/FinansialTab.tsx
# Expected: 0 (kekayaan tidak boleh forceMount)

# Bug #4 — AuthProvider toast + signOut
grep -c "Sesi berakhir, silakan login kembali" src/auth/AuthProvider.tsx
# Expected: 2

# Bug #5 — AddMoneyDialog remaining calc
grep -c "goal.target_amount - goal.current_amount - (investedValue ?? 0)" src/components/AddMoneyDialog.tsx
# Expected: 1

# Bug #6 — GoalDialog label + helper
grep -c "Dana Kas Terkumpul (Rp)" src/components/GoalDialog.tsx
# Expected: 1
grep -c "Investasi terhubung dihitung otomatis dari portofolio" src/components/GoalDialog.tsx
# Expected: 1

# Bug #7a — GoalDialog DialogDescription
grep -c "Buat atau edit goal keuangan Anda" src/components/GoalDialog.tsx
# Expected: 1

# Bug #7b — LinkInvestmentDialog DialogDescription
grep -c "Hubungkan investasi ke goal ini dengan menentukan persentase alokasi" src/components/LinkInvestmentDialog.tsx
# Expected: 1

# Bug #8 — ReportsTab "Ekspor PDF"
grep -c "Ekspor PDF" src/tabs/ReportsTab.tsx
# Expected: 1
grep -c "Export PDF" src/tabs/ReportsTab.tsx
# Expected: 0 (old text harus tidak ada)
```

Catat hasil masing-masing dengan format `Bug #N — <name>: <count> (expected <expected>)` ke memori untuk Task 4 nanti.

Jika ada `(expected !=expected)` mismatch, STOP dan laporkan — tandanya Plan 09-02 belum sepenuhnya selesai atau ada regresi.
  </action>
  <verify>
    <automated>grep -c 'TabsContent value="goals" forceMount' src/tabs/FinansialTab.tsx | xargs test 1 -eq && grep -c "Sesi berakhir, silakan login kembali" src/auth/AuthProvider.tsx | xargs test 2 -eq && grep -c "goal.target_amount - goal.current_amount - (investedValue ?? 0)" src/components/AddMoneyDialog.tsx | xargs test 1 -eq && grep -c "Dana Kas Terkumpul (Rp)" src/components/GoalDialog.tsx | xargs test 1 -eq && grep -c "Hubungkan investasi ke goal ini dengan menentukan persentase alokasi" src/components/LinkInvestmentDialog.tsx | xargs test 1 -eq && grep -c "Ekspor PDF" src/tabs/ReportsTab.tsx | xargs test 1 -eq</automated>
  </verify>
  <acceptance_criteria>
    - Semua 9 grep counts match expected values (lihat action body).
    - Tidak ada count mismatch.
    - Output dari semua grep dicatat verbatim untuk Task 4.
  </acceptance_criteria>
  <done>Semua 6 fix frontend verified present di codebase via static grep — siap untuk live UAT.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DB-side static verification — pg_proc inspection of fixed functions</name>
  <files>—</files>
  <read_first>
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-03-SUMMARY.md (Studio paste evidence captured by Plan 09-03)
    - supabase/migrations/0025_fix_goal_bugs.sql
  </read_first>
  <action>
Re-confirm Studio paste hasil dari Plan 09-03 masih persistent (tidak di-rollback). User diminta untuk run query berikut di Studio SQL Editor dan paste output ke chat:

```sql
-- Critical #1 verification
SELECT
  proname,
  CASE
    WHEN prosrc ~ 'FROM \(\s+SELECT allocation_pct[\s\S]+FOR UPDATE\s+\) sub' THEN 'PASS'
    ELSE 'FAIL'
  END AS critical_1_subquery_for_update
FROM pg_proc
WHERE proname = 'enforce_goal_investment_total'
  AND pronamespace = 'public'::regnamespace;

-- Critical #2 verification
SELECT
  proname,
  CASE
    WHEN prosrc ~ 'FROM goals g\s' AND prosrc ~ 'WHERE g\.id = p_id AND g\.user_id = v_uid' THEN 'PASS'
    ELSE 'FAIL'
  END AS critical_2_alias_pattern
FROM pg_proc
WHERE proname = 'add_money_to_goal'
  AND pronamespace = 'public'::regnamespace;

-- Trigger + GRANT cross-check
SELECT
  (SELECT count(*) FROM pg_trigger WHERE tgname = 'goal_investments_total_check') AS trigger_count,
  (SELECT count(*) FROM information_schema.routine_privileges
   WHERE routine_name = 'add_money_to_goal' AND grantee = 'authenticated' AND privilege_type = 'EXECUTE') AS grant_count;
```

Expected output:
- Row 1: `enforce_goal_investment_total | PASS`
- Row 2: `add_money_to_goal | PASS`
- Row 3: `trigger_count=1 | grant_count=1`

Jika ada FAIL atau count != 1, regress Plan 09-03: re-paste 0025 ke Studio.

Catat output verbatim untuk Task 4.
  </action>
  <verify>
    <automated>echo "User must paste Studio output to chat — cannot automate"</automated>
  </verify>
  <acceptance_criteria>
    - User pastes 3 rows of query output to chat.
    - Row 1 shows `PASS`.
    - Row 2 shows `PASS`.
    - Row 3 shows `trigger_count=1`, `grant_count=1`.
    - Output saved verbatim ke memory untuk Task 4 (VERIFICATION.md content).
  </acceptance_criteria>
  <done>DB state verified post-deploy — Critical #1 dan #2 fix confirmed persistent di production database.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live UAT — 5 browser scenarios covering all 8 bugs</name>
  <what-built>
    Phase 9 fix yang sudah di-deploy via Plan 09-02 (frontend) + Plan 09-03 (DB). Sekarang user perlu manually verify behavior di production app.
  </what-built>
  <how-to-verify>
**Setup:** Login ke production app `https://kantongpintar.vercel.app` (atau staging) dengan akun user normal (bukan admin) — atau admin out-of-View-As mode.

**UAT-1 — Critical #1: Link Investasi sukses (Bug #1)**
1. Pergi ke tab Finansial → Goals.
2. Pilih satu goal (atau buat baru jika kosong).
3. Klik tombol "Hubungkan Investasi" (atau ikon link).
4. Pilih satu investasi dari dropdown.
5. Masukkan alokasi 50%.
6. Klik "Simpan".
**Expected:** Toast sukses, dialog tertutup, link investasi muncul di card goal. TIDAK boleh ada toast `FOR UPDATE is not allowed with aggregate functions`.

**UAT-2 — Critical #2 + Medium #5 + Medium #6 + Low #7 (Goals dialogs): Tambah Uang sukses + sisa akurat + label kas-only + a11y (Bug #2, #5, #6, #7-Goal)**
1. Di tab Goals, pilih goal yang sudah punya linked investasi (dari UAT-1 atau seed).
2. Buka DevTools Console.
3. Klik tombol "Tambah Uang" → dialog AddMoneyDialog terbuka.
   - **Bug #5 check:** Verifikasi "Sisa yang perlu dikumpulkan" sudah memperhitungkan investasi (Sisa = target − cash − investasi). Compare visually dengan badge progress di card goal.
   - **Bug #7 check:** Console TIDAK boleh ada warning `Missing Description or aria-describedby` saat AddMoneyDialog terbuka (sudah ada DialogDescription sejak Phase 7).
4. Masukkan jumlah Rp 1.000.
5. Klik "Simpan".
   - **Expected:** Toast sukses, dialog tertutup, current_amount goal naik Rp 1.000. TIDAK boleh ada toast `column reference "current_amount" is ambiguous`.
6. Klik tombol Edit goal (icon pensil) → dialog GoalDialog terbuka.
   - **Bug #6 check:** Label field di tengah berbunyi "Dana Kas Terkumpul (Rp)" (bukan "Sudah Terkumpul").
   - **Bug #6 check:** Di bawah input ada teks "Investasi terhubung dihitung otomatis dari portofolio" dengan style muted.
   - **Bug #7 check:** Console TIDAK boleh ada warning `Missing Description or aria-describedby` saat GoalDialog terbuka.
7. Klik "Batal" → dialog tertutup.

**UAT-3 — Low #7 (LinkInvestmentDialog a11y): Hubungkan Investasi tanpa console warning (Bug #7-Link)**
1. Di tab Goals, klik tombol "Hubungkan Investasi" pada salah satu goal.
2. Buka DevTools Console.
   - **Bug #7 check:** Saat dialog LinkInvestmentDialog terbuka, console TIDAK boleh ada warning `Missing Description or aria-describedby`.
3. Klik "Batal" → dialog tertutup.

**UAT-4 — Medium #3: GoalsTab filter state preserved (Bug #3)**
1. Pergi ke tab Finansial → Goals (default Kekayaan, klik tab Goals).
2. Ketik "Dana" di search bar GoalsTab (atau set status filter ke "Aktif").
3. Klik tab Kekayaan → KekayaanTab tampil.
4. Klik tab Goals lagi.
   - **Bug #3 check:** Search bar masih berisi "Dana" (atau status filter masih "Aktif"). TIDAK boleh kembali ke default.
5. Hapus filter, kembalikan ke default.

**UAT-5 — Medium #4: Auth refresh-token failure handling (Bug #4)**
Catatan: Bug #4 sulit di-trigger manual (butuh clock skew). Sebagai proxy, lakukan force-signOut + reload:
1. Buka DevTools → Application → Local Storage → cari key Supabase auth (e.g. `sb-<project>-auth-token`).
2. Edit nilai refresh_token jadi `"invalid-fake-token"`.
3. Reload halaman.
   - **Bug #4 check:** Toast muncul dengan teks "Sesi berakhir, silakan login kembali".
   - **Bug #4 check:** User di-redirect ke halaman login (signOut dipanggil).
   - **Bug #4 check:** Console mencetak `[AuthProvider] ... error` atau `Token refresh failed` (tapi UI feedback yang penting — toast tampil).

**UAT-6 — Low #8: Ekspor PDF (Bug #8)**
1. Pergi ke tab Laporan.
2. Cari tombol di pojok kanan atas dengan icon download.
   - **Bug #8 check:** Tombol bertuliskan "Ekspor PDF" (bukan "Export PDF").
3. (Optional) Klik tombol untuk verify export tetap functional.
  </how-to-verify>
  <acceptance_criteria>
    - UAT-1 PASS: Link Investasi sukses (no FOR UPDATE error).
    - UAT-2 PASS: Tambah Uang sukses (no ambiguous column error) + Sisa akurat + label "Dana Kas Terkumpul" + helper text + GoalDialog tanpa warning.
    - UAT-3 PASS: LinkInvestmentDialog tanpa warning aria-describedby.
    - UAT-4 PASS: GoalsTab filter "Dana" / status filter persists across tab switch.
    - UAT-5 PASS: Toast "Sesi berakhir, silakan login kembali" muncul saat refresh token invalid.
    - UAT-6 PASS: Tombol "Ekspor PDF" rendered.
    - User attaches screenshot atau text confirmation untuk minimum UAT-1, UAT-2, UAT-4, UAT-5, UAT-6 (UAT-3 cukup text "no warning").
    - Tidak ada regression: Login normal, transaksi CRUD, kekayaan, pensiun, laporan still functional (spot-check).
  </acceptance_criteria>
  <resume-signal>Type "uat-pass" jika 6 UAT scenarios PASS. Type "uat-fail: [scenario], [issue]" jika ada yang FAIL — agent akan diagnose dan trigger gap closure.</resume-signal>
  <files>—</files>
  <action>See <how-to-verify> block above — user follows UAT-1 through UAT-6 di production app dan attaches screenshots/text untuk setiap scenario. Claude tidak dapat eksekusi browser UAT secara autonomous; tugas Claude di task selanjutnya (Task 4) adalah merangkum hasil ke VERIFICATION.md.</action>
  <verify>
    <automated>echo "Manual UAT checkpoint — user must paste UAT outcomes to chat per resume-signal"</automated>
  </verify>
  <done>User typed "uat-pass" + provided evidence (screenshots/text) untuk minimum UAT-1, UAT-2, UAT-4, UAT-5, UAT-6 (UAT-3 cukup text), dan no regression detected pada spot-check non-Phase-9 flows.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write 09-VERIFICATION.md with all evidence</name>
  <files>.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-VERIFICATION.md</files>
  <read_first>
    - QA-FINDINGS.md (8 bugs source-of-truth)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-01-SUMMARY.md, 09-02-SUMMARY.md, 09-03-SUMMARY.md (collected evidence per plan)
    - .planning/phases/05*/05-VERIFICATION.md (style reference jika ada)
  </read_first>
  <action>
Tulis file `.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-VERIFICATION.md` dengan struktur exact ini:

```markdown
# Phase 9 Verification — QA Bug Fix

**Verifier:** Claude Sonnet 4.6 + user (manual UAT)
**Date:** <YYYY-MM-DD WIB>
**Scope:** Semua 8 bug dari QA-FINDINGS.md (audit 2026-05-01)
**Verdict:** <PASS | PASS-WITH-NOTES | FAIL>

---

## Summary

| Bug | Severity | Source | Verification Method | Status |
|-----|----------|--------|---------------------|--------|
| #1 Link Investasi (FOR UPDATE+aggregate) | Critical | QA-FINDINGS.md Goals row 1 | Studio pg_proc query + UAT-1 | PASS |
| #2 Tambah Uang (ambiguous column) | Critical | QA-FINDINGS.md Goals row 2 | Studio pg_proc query + UAT-2 | PASS |
| #3 GoalsTab filter reset | Medium | QA-FINDINGS.md Finansial row 6 | grep + UAT-4 | PASS |
| #4 Auth refresh-token error | Medium | QA-FINDINGS.md Dashboard row 7 | grep + UAT-5 | PASS |
| #5 AddMoneyDialog Sisa overstated | Medium | QA-FINDINGS.md Goals row 3 | grep + UAT-2 | PASS |
| #6 GoalDialog label kas-only | Medium | QA-FINDINGS.md Goals row 4 | grep + UAT-2 | PASS |
| #7 Missing aria-describedby | Low | QA-FINDINGS.md Goals row 5 | grep + UAT-2/3 | PASS |
| #8 Export PDF inkonsisten | Low | QA-FINDINGS.md Laporan row 8 | grep + UAT-6 | PASS |

---

## Static Verification (Task 1 + 2)

### Frontend (grep counts dari Task 1)

<paste verbatim 9 grep results dari Task 1>

### Backend (Studio pg_proc dari Task 2)

<paste verbatim 3 query rows dari Task 2>

---

## Live UAT (Task 3)

### UAT-1 — Bug #1 Link Investasi
- Steps: <copy dari Task 3 how-to-verify UAT-1>
- Result: <PASS> — <user confirmation text + screenshot path jika ada>

### UAT-2 — Bug #2 + #5 + #6 + #7-Goal
- Steps: <copy dari Task 3>
- Result: <PASS>
- Evidence: <screenshots + console output>

### UAT-3 — Bug #7-LinkInvestment
- Steps: <copy>
- Result: <PASS>

### UAT-4 — Bug #3 Filter state preserved
- Steps: <copy>
- Result: <PASS>
- Evidence: <text describe before/after switch>

### UAT-5 — Bug #4 Auth refresh handler
- Steps: <copy>
- Result: <PASS>
- Evidence: <console log + toast screenshot>

### UAT-6 — Bug #8 Ekspor PDF
- Steps: <copy>
- Result: <PASS>

---

## Regression Check

Spot-check skenario non-Phase 9:
- [ ] Login + signOut tetap berfungsi.
- [ ] Tambah Transaksi (Pemasukan + Pengeluaran) tetap berfungsi.
- [ ] Kekayaan tab — Net Worth calculation tetap akurat.
- [ ] Investasi tab — list dan filter tetap berfungsi.
- [ ] Pensiun tab — simulasi tetap berfungsi.
- [ ] Laporan tab — chart pie + bar tetap render.
- [ ] Pengaturan — tema toggle tetap berfungsi.
- [ ] Catatan — empty state + filter tetap.

---

## Deferred / Notes

- D-14 raw NUMERIC formatting di withdraw_from_goal error message — tetap defer ke v1.2 (out of scope Phase 9, see CONTEXT.md `<deferred>`).
- net_worth_snapshots auto-insert 42501 saat View-As — tetap defer ke v1.2.
- Edge Function fetch-prices CORS — tetap defer ke v1.2.

---

## Artifacts

- `supabase/migrations/0025_fix_goal_bugs.sql` (created Plan 09-01)
- 6 frontend files modified (lihat 09-02-SUMMARY.md)
- Studio paste applied (lihat 09-03-SUMMARY.md)

---

## Sign-off

- [ ] All 8 bugs PASS
- [ ] No regression
- [ ] Evidence recorded
- [ ] STATE.md updated
- [ ] Verdict: PASS

Verifier: <name>
Date: <YYYY-MM-DD>
```

Isi placeholder `<...>` dengan data real dari Task 1, 2, 3 yang sudah di-collect. Jangan biarkan placeholder text seperti `<copy>` di file final.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-VERIFICATION.md && grep -c "Bug #1\|Bug #2\|Bug #3\|Bug #4\|Bug #5\|Bug #6\|Bug #7\|Bug #8" .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-VERIFICATION.md` exists.
    - File contains all 8 bug references (`Bug #1` ... `Bug #8`).
    - File contains string `Verdict: PASS` (atau `PASS-WITH-NOTES` jika ada minor caveat).
    - File contains a Summary table dengan 8 rows (header + 8 bug rows).
    - File contains Static Verification section dengan output verbatim dari Task 1 + Task 2.
    - File contains UAT-1 sampai UAT-6 sections dengan result + evidence.
    - File contains Regression Check section dengan minimum 8 checkboxes.
    - File TIDAK mengandung placeholder unfilled seperti `<copy>`, `<paste verbatim>`, `<YYYY-MM-DD>`.
  </acceptance_criteria>
  <done>VERIFICATION.md tertulis dengan evidence lengkap untuk semua 8 bug, ready untuk archived sebagai Phase 9 closure document.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Update STATE.md + ROADMAP.md to reflect Phase 9 complete</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (full file — focus on Current Position + Pending Todos + Roadmap Evolution)
    - .planning/ROADMAP.md (full file — focus on Phase 9 entry)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-VERIFICATION.md (verdict yang baru ditulis Task 4)
  </read_first>
  <action>
**Edit 1 — STATE.md:**

1. Update `last_updated` ke timestamp ISO sekarang (UTC).
2. Update `last_activity` ke tanggal hari ini WIB.
3. Update `progress`:
   - `completed_phases`: increment dari current value (kemungkinan 5 → 5 jika Phase 8 belum, atau 6 → 6+1).
   - `total_plans`: tambah 4 (Phase 9 plans).
   - `completed_plans`: tambah 4.
4. Update "Current Position" → set Phase ke "9 (QA Bug Fix) — Complete".
5. Tambah section baru di "Accumulated Context > Decisions" — judul `### Decisions (v1.1 execution-time, post-Phase-9)`:
   ```markdown
   - **Phase 9 PASS.** Semua 8 bug dari QA-FINDINGS.md (audit 2026-05-01) verified fixed via Plan 09-01..09-04. Migration 0025 applied via Studio paste (de-facto channel continues). 6 frontend files modified (FinansialTab, AuthProvider, AddMoneyDialog, GoalDialog, LinkInvestmentDialog, ReportsTab). VERIFICATION.md tersedia di .planning/phases/09-phase-09-qa-bug-fix-.../09-VERIFICATION.md.
   - **Lesson Phase 9 — RETURNS TABLE name clash double-check.** Bug Critical #2 (add_money_to_goal ambiguous column) adalah variant dari hotfix yang sudah dipelajari di Phase 7 untuk withdraw_from_goal — tapi tidak diaplikasikan ke add_money_to_goal di file yang sama. Lesson: saat fix RETURNS TABLE clash di salah satu fungsi, search ALL fungsi di file yang punya pattern serupa dan apply fix sekaligus.
   - **Lesson Phase 9 — FOR UPDATE+aggregate.** PostgreSQL melarang `FOR UPDATE` bersamaan dengan aggregate (e.g. SUM). Pattern aman: pisahkan ke subquery dengan `FOR UPDATE`, aggregate di outer query. Sudah dicatat di pattern map untuk future migrations.
   ```
6. Update "Roadmap Evolution" — tambah entry: `- Phase 9 completed (YYYY-MM-DD): All 8 QA-FINDINGS bugs fixed and verified. PASS verdict.`
7. Update "Session Continuity":
   - `Last session`: timestamp baru.
   - `Stopped at`: `Phase 9 complete`.
   - `Resume command`: `/gsd-plan-phase <next>` (atau jika v1.1 sudah lengkap → `/gsd-milestone-close v1.1` — tergantung context).

**Edit 2 — ROADMAP.md:**

1. Cari section "### Phase 9: Phase 09 — QA Bug Fix..."
2. Update Goal field (saat ini `[To be planned]`) dengan goal real dari CONTEXT.md `<domain>`:
   ```
   **Goal**: Fix semua 8 bug dari QA-FINDINGS.md (audit 2026-05-01). 2 Critical (DB), 4 Medium (frontend), 2 Low (a11y + i18n). Tidak ada fitur baru, tidak ada refactor di luar scope bug.
   ```
3. Update Requirements: `**Requirements**: QA-CRITICAL-1, QA-CRITICAL-2, QA-MEDIUM-3, QA-MEDIUM-4, QA-MEDIUM-5, QA-MEDIUM-6, QA-LOW-7, QA-LOW-8`
4. Update Plans count: `**Plans:** 4 plans`
5. Update Plans list:
   ```
   Plans:
   - [x] 09-01-PLAN.md — Create migration 0025_fix_goal_bugs.sql (Critical #1 + #2 fixes)
   - [x] 09-02-PLAN.md — Frontend fixes 6 files (Bug #3-#8)
   - [x] 09-03-PLAN.md — [BLOCKING] Studio paste 0025 to production
   - [x] 09-04-PLAN.md — [BLOCKING] Verification + UAT + write VERIFICATION.md
   ```
6. Mark Phase 9 row di "Progress" table di bagian bawah sebagai Complete dengan tanggal hari ini.

CRITICAL constraints:
- TIDAK mengubah Phase 5/6/7/8 entries (sudah completed).
- Pakai backtick + plus/minus consistent dengan existing pattern di file.
- Tanggal pakai format `YYYY-MM-DD` (WIB), timestamp ISO pakai UTC dengan `Z`.
  </action>
  <verify>
    <automated>grep -c "Phase 9 PASS\|Phase 9 complete" .planning/STATE.md && grep -c "09-04-PLAN.md" .planning/ROADMAP.md && grep -c "QA-CRITICAL-1, QA-CRITICAL-2" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains literal string `Phase 9 PASS` atau `Phase 9 complete` (at least one).
    - `.planning/STATE.md` `last_updated` di-update ke timestamp ISO baru.
    - `.planning/STATE.md` Decisions section mengandung "Lesson Phase 9 — RETURNS TABLE name clash" dan "Lesson Phase 9 — FOR UPDATE+aggregate".
    - `.planning/ROADMAP.md` Phase 9 entry memiliki Goal real (bukan `[To be planned]`).
    - `.planning/ROADMAP.md` Phase 9 mengandung `**Plans:** 4 plans`.
    - `.planning/ROADMAP.md` Phase 9 plan list mengandung `09-01-PLAN.md`, `09-02-PLAN.md`, `09-03-PLAN.md`, `09-04-PLAN.md` (semua dengan `[x]`).
    - `.planning/ROADMAP.md` mengandung `QA-CRITICAL-1, QA-CRITICAL-2` di Requirements line Phase 9.
  </acceptance_criteria>
  <done>STATE.md dan ROADMAP.md updated; Phase 9 secara formal tercatat sebagai complete dengan lesson learned terdokumentasi untuk milestone audit nanti.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User UAT confirmation → VERIFICATION.md | User attestation crosses into permanent project record |
| pg_proc query result → VERIFICATION.md | DB inspection is read-only, but content recorded affects audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-17 | Repudiation (false PASS claim) | UAT scenarios | mitigate | Each UAT requires user-attached evidence (screenshot atau verbatim text) recorded di VERIFICATION.md. Acceptance criteria menyebut "user attaches screenshot atau text confirmation". |
| T-09-18 | Tampering (VERIFICATION.md fabricated) | 09-VERIFICATION.md content | accept | Solo developer trust model — verifier adalah developer sendiri. VERIFICATION.md committed ke git provides audit trail; future audit dapat replay UAT steps. |
| T-09-19 | Information Disclosure (Studio query in VERIFICATION.md) | pg_proc prosrc bodies | accept | Function bodies sudah open-source di repo. No secret leaked. |
| T-09-20 | Denial of Service (regression introduced by Phase 9) | Spot-check Regression Check section | mitigate | Task 3 UAT-3 + Regression Check section di VERIFICATION.md memaksa user spot-check 8 non-Phase-9 flows. Failure menjadi gap closure trigger. |

</threat_model>

<verification>
- 09-VERIFICATION.md committed dengan all 8 bugs PASS dan evidence terdokumentasi.
- STATE.md updated (last_updated baru, Phase 9 marked complete, lesson learned tercatat).
- ROADMAP.md Phase 9 entry filled (Goal, Requirements, Plans).
- User typed "uat-pass" di Task 3 (atau equivalent confirmation).
- Studio queries di Task 2 returned PASS untuk Critical #1 dan #2.
- Static greps di Task 1 returned correct counts untuk semua 6 frontend changes.
</verification>

<success_criteria>
1. All 8 bugs verified PASS via combination of static + UAT.
2. VERIFICATION.md exists with full evidence + verdict PASS (atau PASS-WITH-NOTES).
3. STATE.md updated dengan Phase 9 completion + 2 lesson learned.
4. ROADMAP.md updated dengan Phase 9 Goal/Requirements/Plans.
5. No regression detected dalam Regression Check spot-check.
6. Phase 9 closed cleanly — siap untuk milestone v1.1 close gate atau Phase 10 jika ada.
</success_criteria>

<output>
After completion, create `.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-04-SUMMARY.md` documenting:
- Verification verdict (PASS / PASS-WITH-NOTES / FAIL).
- 8 bug status grid.
- Files updated (VERIFICATION.md, STATE.md, ROADMAP.md).
- Pointer ke commit SHA jika sudah committed.
- Next step (milestone close atau next phase).
</output>
