---
phase: 12-kesehatan-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shell/navConfig.ts
  - src/routes.tsx
  - src/tabs/kesehatan/KesehatanLayout.tsx
  - src/tabs/kesehatan/KesehatanLanding.tsx
  - src/tabs/kesehatan/PiramidaShell.tsx
  - src/tabs/kesehatan/KalkulatorBanner.tsx
  - src/tabs/kesehatan/ModulCard.tsx
  - src/tabs/kesehatan/modulCatalog.ts
autonomous: false
requirements: [STRAT-01, STRAT-02]
must_haves:
  truths:
    - "User klik grup 'Strategi' → 'Kesehatan' di sidebar → masuk ke route /kesehatan"
    - "Route /kesehatan render KesehatanLayout dengan Outlet untuk sub-route Phase 15"
    - "Default content /kesehatan adalah KesehatanLanding dengan 3 section: piramida shell + banner kalkulator + grid 6 card modul"
    - "Piramida shell render 4 trapezoid tier (Proteksi/Akumulasi/Pertumbuhan/Warisan) dengan label, klik tier → toast 'Coming soon' (Phase 13 akan wire ke panel)"
    - "Banner kalkulator render card dengan ikon + copy + link 'Buka kalkulator →' ke /kesehatan/kalkulator (404 acceptable di Phase 12)"
    - "Grid 6 card modul render 2 kolom × 3 baris di desktop (≥md), single kolom di mobile, masing-masing card link ke /kesehatan/<slug>"
    - "Sidebar mobile drawer tetap sehat dengan grup 'Strategi' tambahan (no overflow/clipping di viewport ≤640px)"
    - "Active state sidebar — pas user di /kesehatan, item 'Kesehatan' highlighted (isActive=true via location.pathname startsWith)"
  artifacts:
    - path: "src/shell/navConfig.ts"
      provides: "NAV_GROUPS dengan grup baru 'Strategi' antara 'Tujuan' dan footer"
      contains: "label: 'Strategi'"
    - path: "src/routes.tsx"
      provides: "Route /kesehatan dengan nested children dan KesehatanLanding sebagai index"
      contains: "path: 'kesehatan'"
    - path: "src/tabs/kesehatan/KesehatanLayout.tsx"
      provides: "Layout shell dengan <Outlet />"
      min_lines: 10
    - path: "src/tabs/kesehatan/KesehatanLanding.tsx"
      provides: "Landing default content (piramida + banner + grid)"
      min_lines: 30
    - path: "src/tabs/kesehatan/PiramidaShell.tsx"
      provides: "4-tier SVG/CSS pyramid component (gray shell, no data wiring di Phase 12)"
      min_lines: 30
    - path: "src/tabs/kesehatan/KalkulatorBanner.tsx"
      provides: "Banner card untuk teaser kalkulator"
      min_lines: 15
    - path: "src/tabs/kesehatan/ModulCard.tsx"
      provides: "Reusable card component untuk 6 modul"
      min_lines: 15
    - path: "src/tabs/kesehatan/modulCatalog.ts"
      provides: "Const list 6 modul (slug, label, description, icon)"
      contains: "arus-kas"
  key_links:
    - from: "src/shell/navConfig.ts NAV_GROUPS"
      to: "src/shell/AppSidebar.tsx render loop"
      via: "iteration over NAV_GROUPS dengan label 'Strategi'"
      pattern: "Strategi"
    - from: "src/routes.tsx /kesehatan path"
      to: "src/tabs/kesehatan/KesehatanLayout"
      via: "lazy-import atau direct import + element"
      pattern: "kesehatan"
    - from: "KesehatanLayout"
      to: "KesehatanLanding via index route"
      via: "<Outlet /> rendering child route"
      pattern: "Outlet"
    - from: "KesehatanLanding grid"
      to: "modulCatalog.ts MODUL_CATALOG"
      via: ".map((modul) => <ModulCard ... />)"
      pattern: "MODUL_CATALOG"
---

<objective>
Buat foundation halaman `/kesehatan`: sidebar entry baru di grup "Strategi", react-router nested route dengan layout + landing default, dan landing component dengan **3 section shell** (piramida grayed, banner kalkulator, grid 6 card modul).

Phase 12 hanya **shell** — tidak ada data layer, tidak ada interaksi. Klik tier piramida → toast "Coming soon", klik banner/modul card → arahkan ke route yang akan dibuild Phase 15 (wildcard fallback ke `/dashboard` di routes.tsx sudah handle 404 case).

Purpose: Subsequent phases (13/14/15) butuh (a) sidebar route mounted, (b) layout shell punya slot untuk fill content, (c) PiramidaShell sebagai komponen yang akan diisi data oleh Phase 13. Tanpa shell ini, Phase 13 plan tidak bisa start.

Output:
- 1 modified file: `navConfig.ts` (tambah grup Strategi + 1 item)
- 1 modified file: `routes.tsx` (tambah route /kesehatan dengan children + index)
- 6 new files di `src/tabs/kesehatan/`: layout, landing, piramida shell, banner, modul card, modul catalog

Implements:
- STRAT-01 (sidebar grup Strategi + item Kesehatan + route)
- STRAT-02 (landing layout 3 section: piramida + banner + grid 6 modul)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-kesehatan-foundation/12-CONTEXT.md
@docs/superpowers/specs/2026-05-08-framework-page-design.md
@src/shell/navConfig.ts
@src/shell/AppShell.tsx
@src/shell/AppSidebar.tsx
@src/routes.tsx

<interfaces>
<!-- Existing patterns yang harus diikuti — extracted from codebase -->

```ts
// src/shell/navConfig.ts (current shape)
export type NavBadge = 'recurring-due'

export type NavItem = {
  to: string
  label: string
  icon: LucideIcon
  badge?: NavBadge
}

export type NavGroup = {
  label?: string
  items: NavItem[]
  isFooter?: boolean
}

export const NAV_GROUPS: NavGroup[] = [
  // existing 5 groups...
]
```

```tsx
// src/shell/AppSidebar.tsx — active state pattern (already exists)
const isActive =
  location.pathname === to || location.pathname.startsWith(to + '/')
```

```tsx
// src/routes.tsx — current shape uses createBrowserRouter dengan AppShell sebagai parent
export const router = createBrowserRouter([
  {
    path: '/',
    element: <AppShell />,
    children: [
      { index: true, element: <Navigate to="/dashboard" replace /> },
      { path: 'dashboard', element: <DashboardTab /> },
      // ... etc
      { path: '*', element: <Navigate to="/dashboard" replace /> },
    ],
  },
])
```

```tsx
// src/shell/AppShell.tsx — already renders <Outlet /> di main, jadi nested route children langsung mounted di sini
<main className="p-6">
  <Outlet />
</main>
```

```ts
// shadcn/ui Card — existing dependency (used di KekayaanTab, GoalsTab, dst)
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card'
```

```ts
// sonner toast — existing dependency
import { toast } from 'sonner'
toast.info('Coming soon — fitur ini akan tersedia di Phase 13')
```

```ts
// Icon imports — lucide-react existing dependency
import { HeartPulse, Calculator, Wallet, Target, PieChart, Briefcase, Coins, Brain } from 'lucide-react'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Buat 6 file foundation di src/tabs/kesehatan/ — modulCatalog, ModulCard, KalkulatorBanner, PiramidaShell, KesehatanLayout, KesehatanLanding</name>
  <files>src/tabs/kesehatan/modulCatalog.ts, src/tabs/kesehatan/ModulCard.tsx, src/tabs/kesehatan/KalkulatorBanner.tsx, src/tabs/kesehatan/PiramidaShell.tsx, src/tabs/kesehatan/KesehatanLayout.tsx, src/tabs/kesehatan/KesehatanLanding.tsx</files>
  <action>
Create 6 file baru di `src/tabs/kesehatan/`. Buat folder dulu kalau belum ada.

**File 1: `src/tabs/kesehatan/modulCatalog.ts`** — single source of truth untuk 6 modul (per design spec §6 + lampiran):

```ts
import { Wallet, Target, PieChart, Briefcase, Coins, Brain } from 'lucide-react'
import type { LucideIcon } from 'lucide-react'

export type ModulItem = {
  slug: string         // sub-route segment, mis. 'arus-kas'
  label: string        // judul card di grid
  description: string  // 1-line teaser di card
  icon: LucideIcon     // lucide icon
}

// Source: design spec §6 + lampiran "Mapping konten file source ke modul React"
export const MODUL_CATALOG: ModulItem[] = [
  {
    slug: 'arus-kas',
    label: 'Pondasi & Cash Flow',
    description: 'Anggaran, dana darurat, dan disiplin arus kas bulanan.',
    icon: Wallet,
  },
  {
    slug: 'tujuan',
    label: 'Tujuan & Risiko',
    description: 'Time horizon, profil risiko, dan setting target finansial.',
    icon: Target,
  },
  {
    slug: 'alokasi-aset',
    label: 'Alokasi Aset & Diversifikasi',
    description: 'Mengatur porsi saham, obligasi, emas, dan kelas aset lainnya.',
    icon: PieChart,
  },
  {
    slug: 'instrumen',
    label: 'Instrumen Indonesia & Global',
    description: 'Reksa dana, saham IDX, ETF global, dan emas fisik vs digital.',
    icon: Briefcase,
  },
  {
    slug: 'pajak-biaya-inflasi',
    label: 'Pajak, Biaya & Inflasi',
    description: 'Real return setelah inflasi, biaya transaksi, dan efisiensi pajak.',
    icon: Coins,
  },
  {
    slug: 'perilaku',
    label: 'Behavioral Finance & Disiplin',
    description: 'Bias kognitif, FOMO, dan strategi dollar-cost averaging.',
    icon: Brain,
  },
]
```

**File 2: `src/tabs/kesehatan/ModulCard.tsx`** — reusable card untuk grid:

```tsx
import { useNavigate } from 'react-router-dom'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import type { ModulItem } from './modulCatalog'

export default function ModulCard({ modul }: { modul: ModulItem }) {
  const navigate = useNavigate()
  const Icon = modul.icon
  const to = `/kesehatan/${modul.slug}`

  return (
    <Card
      role="button"
      tabIndex={0}
      onClick={() => navigate(to)}
      onKeyDown={(e) => {
        if (e.key === 'Enter' || e.key === ' ') {
          e.preventDefault()
          navigate(to)
        }
      }}
      className="cursor-pointer transition-shadow hover:shadow-md focus:outline-none focus:ring-2 focus:ring-ring"
    >
      <CardHeader>
        <div className="flex items-center gap-3">
          <div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-lg bg-primary/10 text-primary">
            <Icon className="h-5 w-5" />
          </div>
          <CardTitle className="text-base">{modul.label}</CardTitle>
        </div>
      </CardHeader>
      <CardContent>
        <CardDescription>{modul.description}</CardDescription>
      </CardContent>
    </Card>
  )
}
```

**File 3: `src/tabs/kesehatan/KalkulatorBanner.tsx`** — banner card:

```tsx
import { useNavigate } from 'react-router-dom'
import { Calculator, ArrowRight } from 'lucide-react'
import { Card, CardContent } from '@/components/ui/card'
import { Button } from '@/components/ui/button'

export default function KalkulatorBanner() {
  const navigate = useNavigate()
  return (
    <Card className="bg-gradient-to-r from-primary/5 to-primary/10 border-primary/20">
      <CardContent className="flex flex-col gap-3 py-5 sm:flex-row sm:items-center sm:justify-between">
        <div className="flex items-start gap-3 sm:items-center">
          <div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-lg bg-primary/15 text-primary">
            <Calculator className="h-5 w-5" />
          </div>
          <div>
            <p className="text-sm font-medium">
              Hitung target investasimu dengan kalkulator compound interest
            </p>
            <p className="text-xs text-muted-foreground">
              Lihat proyeksi tahun-per-tahun dari setoran rutin kamu.
            </p>
          </div>
        </div>
        <Button
          variant="default"
          size="sm"
          className="shrink-0"
          onClick={() => navigate('/kesehatan/kalkulator')}
        >
          Buka kalkulator
          <ArrowRight className="ml-2 h-4 w-4" />
        </Button>
      </CardContent>
    </Card>
  )
}
```

**File 4: `src/tabs/kesehatan/PiramidaShell.tsx`** — 4-tier SVG pyramid shell (no data wiring di Phase 12):

```tsx
import { toast } from 'sonner'

type Tier = {
  id: 1 | 2 | 3 | 4
  label: string  // "PROTEKSI" etc
  subtitle: string  // "Tier 1" etc
}

const TIERS: Tier[] = [
  { id: 4, label: 'WARISAN', subtitle: 'Tier 4' },
  { id: 3, label: 'PERTUMBUHAN', subtitle: 'Tier 3' },
  { id: 2, label: 'AKUMULASI', subtitle: 'Tier 2' },
  { id: 1, label: 'PROTEKSI', subtitle: 'Tier 1' },
]

/**
 * Phase 12 shell — semua tier render gray (no data wiring).
 * Phase 13 akan terima props `indicators` dan render warna hijau/kuning/merah.
 *
 * Visual: 4 trapezoid stacked, top (Tier 4) paling sempit, bottom (Tier 1) paling lebar.
 * Implementation: pakai CSS clip-path trapezoid via inline style + Tailwind untuk spacing.
 * Klik tier → toast "Coming soon" (Phase 13 akan replace dengan slide-down panel).
 */
export default function PiramidaShell({
  variant = 'default',
}: {
  variant?: 'default' | 'grayed-empty'
}) {
  const isEmpty = variant === 'grayed-empty'

  return (
    <div className="mx-auto flex max-w-md flex-col items-center gap-1 py-4">
      {TIERS.map((tier, index) => {
        // Width percent: tier 4 = 50%, tier 3 = 65%, tier 2 = 80%, tier 1 = 95%
        const widthPercent = 50 + index * 15
        return (
          <button
            key={tier.id}
            type="button"
            onClick={() =>
              toast.info(
                isEmpty
                  ? `Yuk isi data dulu — Tier ${tier.id} ${tier.label} akan terbuka setelah ada minimal 3 data poin.`
                  : `Tier ${tier.id} ${tier.label}: detail panel akan tersedia di update berikutnya.`,
              )
            }
            aria-label={`Tier ${tier.id} ${tier.label}`}
            className="group relative w-full max-w-[420px] cursor-pointer transition-transform hover:-translate-y-0.5 focus:outline-none focus:ring-2 focus:ring-ring rounded"
            style={{ width: `${widthPercent}%` }}
          >
            <div
              className={`flex h-14 items-center justify-between gap-3 px-5 text-sm font-semibold tracking-wide ${
                isEmpty
                  ? 'bg-gray-200 text-gray-500 dark:bg-gray-700 dark:text-gray-400'
                  : 'bg-gray-300 text-gray-700 dark:bg-gray-600 dark:text-gray-200'
              }`}
              style={{
                clipPath: 'polygon(8% 0, 92% 0, 100% 100%, 0 100%)',
              }}
            >
              <span className="text-[10px] opacity-70">{tier.subtitle}</span>
              <span>{tier.label}</span>
              <span className="text-[10px] opacity-0">.</span>
            </div>
          </button>
        )
      })}
      {isEmpty && (
        <p className="mt-3 text-center text-sm text-muted-foreground">
          Yuk mulai isi data untuk lihat warna hijau/kuning/merah di setiap tier.
        </p>
      )}
    </div>
  )
}
```

Notes untuk PiramidaShell:
- Pakai CSS `clip-path: polygon(...)` untuk efek trapezoid — simpler dari SVG, responsive dengan parent width.
- 4 tier render dari atas (Tier 4 Warisan) ke bawah (Tier 1 Proteksi) per design spec §4 struktur. Trapezoid bottom paling lebar = visual stability.
- Width incremental 50/65/80/95% untuk visual hierarchy. Bisa adjust kalau visual rough — tidak ada hard requirement di spec selain "4 tier".
- Variant `grayed-empty` di-set oleh DIAG-11 (Plan 12-03). Default variant ini juga gray di Phase 12 karena no data wiring; Phase 13 akan add varian colored via prop indicators.
- Klik tier toast "Coming soon" — sesuai CONTEXT.md decisions ("klik tier belum aksi anything di Phase 12 — atau toast 'Coming soon'").

**File 5: `src/tabs/kesehatan/KesehatanLayout.tsx`** — nested route layout:

```tsx
import { Outlet } from 'react-router-dom'

/**
 * Layout untuk route /kesehatan dengan children sub-routes.
 * Phase 12: cukup wrapper minimum + Outlet — sub-route Phase 15 nanti
 * akan tambah breadcrumb di sini.
 */
export default function KesehatanLayout() {
  return (
    <div className="space-y-6">
      <Outlet />
    </div>
  )
}
```

**File 6: `src/tabs/kesehatan/KesehatanLanding.tsx`** — default landing content:

```tsx
import PiramidaShell from './PiramidaShell'
import KalkulatorBanner from './KalkulatorBanner'
import ModulCard from './ModulCard'
import { MODUL_CATALOG } from './modulCatalog'

/**
 * Phase 12 landing — 3 section shell:
 *  1. HERO: PiramidaShell (4-tier, grayed di Phase 12)
 *  2. BANNER kalkulator
 *  3. GRID 6 card modul (2 kol desktop, 1 kol mobile)
 *
 * Phase 13 akan inject `indicators` ke PiramidaShell + tambah TierPanel.
 * DIAG-11 empty state di Plan 12-03 akan branch render di sini berdasarkan total count.
 */
export default function KesehatanLanding() {
  return (
    <div className="space-y-6">
      <header className="space-y-1">
        <h1 className="text-2xl font-bold tracking-tight">Kesehatan Finansial</h1>
        <p className="text-sm text-muted-foreground">
          Lihat kondisi keuangan kamu lewat piramida 4 tier, lalu pelajari konsep finansial dari modul edukasi.
        </p>
      </header>

      {/* HERO: Piramida shell */}
      <section aria-labelledby="piramida-heading">
        <h2 id="piramida-heading" className="sr-only">Piramida Kesehatan</h2>
        <PiramidaShell />
      </section>

      {/* BANNER kalkulator */}
      <section aria-labelledby="kalkulator-heading">
        <h2 id="kalkulator-heading" className="sr-only">Kalkulator Compound Interest</h2>
        <KalkulatorBanner />
      </section>

      {/* GRID 6 card modul */}
      <section aria-labelledby="modul-heading" className="space-y-3">
        <h2 id="modul-heading" className="text-lg font-semibold">Modul Edukasi</h2>
        <div className="grid gap-3 sm:grid-cols-2">
          {MODUL_CATALOG.map((modul) => (
            <ModulCard key={modul.slug} modul={modul} />
          ))}
        </div>
      </section>
    </div>
  )
}
```

Notes untuk KesehatanLanding:
- Grid `sm:grid-cols-2` = 2 kolom dari breakpoint `sm` (640px) ke atas; di bawah itu single kolom (mobile). 6 modul × 2 kolom = 3 baris di desktop, 6 baris di mobile.
- `<header>` + `<section>` + `aria-labelledby` untuk a11y — konsisten dengan pattern existing (lihat DashboardTab atau KekayaanTab).
- DIAG-11 empty state akan diintegrasikan di Plan 12-03 — landing tetap satu file, tapi akan ada conditional render based on total count query. Plan 12-03 akan modify file ini.
  </action>
  <verify>
    <automated>test -d src/tabs/kesehatan && test -f src/tabs/kesehatan/modulCatalog.ts && test -f src/tabs/kesehatan/ModulCard.tsx && test -f src/tabs/kesehatan/KalkulatorBanner.tsx && test -f src/tabs/kesehatan/PiramidaShell.tsx && test -f src/tabs/kesehatan/KesehatanLayout.tsx && test -f src/tabs/kesehatan/KesehatanLanding.tsx && grep -q "MODUL_CATALOG" src/tabs/kesehatan/modulCatalog.ts && grep -q "Outlet" src/tabs/kesehatan/KesehatanLayout.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>6 files exist, MODUL_CATALOG has 6 entries dengan slugs ['arus-kas', 'tujuan', 'alokasi-aset', 'instrumen', 'pajak-biaya-inflasi', 'perilaku'], TypeScript compiles</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire sidebar + routes — modify navConfig.ts dan routes.tsx</name>
  <files>src/shell/navConfig.ts, src/routes.tsx</files>
  <action>
**Modify `src/shell/navConfig.ts`** — tambah grup baru "Strategi" antara grup "Tujuan" dan footer (per CONTEXT.md decisions):

1. Tambah `HeartPulse` ke import dari `lucide-react` (icon kandidat per CONTEXT.md "prefer HeartPulse karena align dengan 'kesehatan keuangan'").

2. Tambah grup baru di array `NAV_GROUPS` dengan posisi **antara grup "Tujuan" dan footer**. Hasil akhir array:

```ts
import {
  LayoutDashboard,
  Wallet,
  Calendar,
  BarChart3,
  StickyNote,
  TrendingUp,
  Landmark,
  Target,
  PiggyBank,
  HeartPulse,           // ← NEW
  BookOpen,
  Settings as SettingsIcon,
} from 'lucide-react'
import type { LucideIcon } from 'lucide-react'

// ... type definitions tetap ...

export const NAV_GROUPS: NavGroup[] = [
  {
    items: [{ to: '/dashboard', label: 'Dashboard', icon: LayoutDashboard }],
  },
  {
    label: 'Keuangan',
    items: [ /* unchanged */ ],
  },
  {
    label: 'Aset',
    items: [ /* unchanged */ ],
  },
  {
    label: 'Tujuan',
    items: [ /* unchanged */ ],
  },
  {
    label: 'Strategi',                                                  // ← NEW group
    items: [
      { to: '/kesehatan', label: 'Kesehatan', icon: HeartPulse },
    ],
  },
  {
    isFooter: true,
    items: [ /* unchanged */ ],
  },
]
```

Critical placement: grup "Strategi" HARUS berada **sebelum** grup `isFooter: true` (Panduan/Pengaturan). Per design spec §2 sidebar restructure preview.

**Modify `src/routes.tsx`** — tambah nested route `/kesehatan` dengan `KesehatanLayout` + index `KesehatanLanding`:

1. Tambah imports:
```tsx
import KesehatanLayout from '@/tabs/kesehatan/KesehatanLayout'
import KesehatanLanding from '@/tabs/kesehatan/KesehatanLanding'
```

2. Tambah route entry **sebelum** wildcard fallback (`{ path: '*', element: <Navigate to="/dashboard" replace /> }`). Wildcard harus tetap last — kalau /kesehatan setelah wildcard, tidak akan pernah match.

```tsx
// ... existing imports ...
import KesehatanLayout from '@/tabs/kesehatan/KesehatanLayout'
import KesehatanLanding from '@/tabs/kesehatan/KesehatanLanding'

export const router = createBrowserRouter([
  {
    path: '/',
    element: <AppShell />,
    children: [
      { index: true, element: <Navigate to="/dashboard" replace /> },
      { path: 'dashboard', element: <DashboardTab /> },
      { path: 'transaksi', element: <TransactionsTab /> },
      { path: 'periode-gaji', element: <PeriodeGajiTab /> },
      { path: 'laporan', element: <ReportsTab /> },
      { path: 'catatan', element: <NotesTab /> },
      { path: 'investasi', element: <InvestmentsTab /> },
      { path: 'kekayaan', element: <KekayaanTab /> },
      { path: 'goals', element: <GoalsTab /> },
      { path: 'pensiun', element: <PensiunTab /> },
      // ← NEW: nested /kesehatan route
      {
        path: 'kesehatan',
        element: <KesehatanLayout />,
        children: [
          { index: true, element: <KesehatanLanding /> },
          // Phase 15 akan tambah:
          //   { path: 'kalkulator', element: <KalkulatorCompound /> },
          //   { path: 'arus-kas', element: <ArusKasModul /> },
          //   ... etc per MODUL_CATALOG slugs
        ],
      },
      { path: 'finansial', element: <Navigate to="/kekayaan" replace /> },
      { path: 'pengaturan', element: <SettingsTab /> },
      { path: 'panduan', element: <PanduanFullPage /> },
      { path: 'panduan/:slug', element: <PanduanFullPage /> },
      { path: '*', element: <Navigate to="/dashboard" replace /> },  // ← wildcard tetap LAST
    ],
  },
])
```

Notes:
- Direct import (bukan lazy) untuk konsistensi dengan tab existing — pfm-web belum pakai lazy-loading di route layer; tetap simple.
- Phase 15 nanti akan tambah child routes (kalkulator + 6 modul slugs). Phase 12 sengaja **tidak** add stub routes untuk modul/kalkulator — biarin wildcard `*` redirect ke /dashboard. Acceptable interim per CONTEXT.md ("link ke `/kesehatan/<slug>` (juga coming-soon di Phase 12)" → wildcard redirect adalah implementation pilihan).
- `KesehatanLayout` punya `<Outlet />` jadi `KesehatanLanding` (sebagai index child) akan di-render saat user ada di `/kesehatan` exact path.
- Sidebar `isActive` logic existing (`location.pathname === to || location.pathname.startsWith(to + '/')`) sudah handle nested case — pas user ada di `/kesehatan/arus-kas` (Phase 15), item "Kesehatan" sidebar tetap highlighted.
  </action>
  <verify>
    <automated>grep -q "label: 'Strategi'" src/shell/navConfig.ts && grep -q "HeartPulse" src/shell/navConfig.ts && grep -q "to: '/kesehatan'" src/shell/navConfig.ts && grep -q "path: 'kesehatan'" src/routes.tsx && grep -q "KesehatanLayout" src/routes.tsx && grep -q "KesehatanLanding" src/routes.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>navConfig has new Strategi group + Kesehatan item dengan HeartPulse icon. routes.tsx has /kesehatan path dengan KesehatanLayout + index KesehatanLanding child. TypeScript compiles, no lint errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual UAT — verify sidebar nav + landing render</name>
  <files>(visual UAT — no file changes; sanity check only)</files>
  <action>
**What's been built:**
- Sidebar grup "Strategi" baru dengan item "Kesehatan" (HeartPulse icon) — antara grup "Tujuan" dan footer
- Route `/kesehatan` rendering `KesehatanLayout` dengan default content `KesehatanLanding`
- Landing 3 section: piramida shell (4 trapezoid gray) + banner kalkulator + grid 6 card modul (2 kolom desktop, 1 kolom mobile)
- Klik tier piramida → toast "Coming soon" (Sonner)
- Klik banner "Buka kalkulator" atau modul card → navigate ke `/kesehatan/...` → wildcard redirect ke `/dashboard` (acceptable, Phase 15 akan fill)

**Steps untuk human:**
**Setup:**
```bash
npm run dev
```
Open http://localhost:5173 (atau port yang muncul) di Chrome desktop.

**Test 1 — Sidebar grup Strategi muncul (desktop):**
1. Login (kalau belum) — pakai akun admin/user existing.
2. Lihat sidebar kiri. Expected order grup:
   - Dashboard (no group label)
   - **Keuangan**: Transaksi / Periode Gaji / Laporan / Catatan
   - **Aset**: Investasi / Kekayaan
   - **Tujuan**: Goals / Pensiun
   - **Strategi**: Kesehatan ← NEW
   - (footer bottom-aligned): Panduan / Pengaturan
3. Verify icon di item "Kesehatan" adalah HeartPulse (heart shape dengan pulse line).

**Test 2 — Klik sidebar → navigate ke /kesehatan:**
1. Klik item "Kesehatan" di sidebar.
2. URL bar berubah jadi `http://localhost:5173/kesehatan`.
3. Sidebar item "Kesehatan" highlighted (active state — background lebih gelap).
4. Main content area menampilkan:
   - Header: "Kesehatan Finansial" (h1) + tagline 1 baris.
   - Section piramida: 4 trapezoid gray stacked, top sempit (Tier 4 WARISAN) ke bottom lebar (Tier 1 PROTEKSI).
   - Banner ungu/biru muda dengan ikon kalkulator + copy + tombol "Buka kalkulator →".
   - Grid 6 card modul: 2 kolom × 3 baris. Card berisi icon + judul + 1-baris description.

**Test 3 — Klik tier piramida → toast:**
1. Klik tier "Tier 1 PROTEKSI" (yang paling lebar, paling bawah).
2. Expected: toast bottom-right "Tier 1 PROTEKSI: detail panel akan tersedia di update berikutnya."
3. Klik tier "Tier 4 WARISAN" → toast similar untuk Tier 4.

**Test 4 — Klik banner kalkulator:**
1. Klik tombol "Buka kalkulator →".
2. URL bar sebentar `/kesehatan/kalkulator`, lalu redirect ke `/dashboard` (wildcard fallback). Acceptable — Phase 15 akan fill.

**Test 5 — Klik card modul:**
1. Klik card "Pondasi & Cash Flow".
2. URL bar sebentar `/kesehatan/arus-kas`, lalu redirect ke `/dashboard`. Acceptable.

**Test 6 — Mobile responsive (sidebar drawer):**
1. Resize Chrome ke ≤640px width (atau buka DevTools mobile emulation iPhone SE).
2. Sidebar collapse jadi drawer button di top-left.
3. Klik drawer button → sidebar slide in. Verify grup "Strategi" muncul, tidak overflow/clipped.
4. Klik "Kesehatan" → drawer close + navigate ke /kesehatan.
5. Landing render: piramida tetap centered, grid modul jadi single kolom (6 card stacked vertically).

**Test 7 — Active state nested route (kalau Phase 15 future-test):**
1. Manual ke URL `http://localhost:5173/kesehatan/arus-kas` — akan wildcard redirect, jadi Test 7 ini skip kecuali Phase 15 sudah ada. Acceptable di Phase 12.

**Test 8 — Build production:**
```bash
npm run build
```
Expected: build success, no TypeScript error, no missing import. Vite bundle size delta acceptable (< 30KB increase untuk 6 file kecil).

**Report back:**
- Screenshot sidebar dengan grup Strategi visible (Test 1)
- Screenshot landing /kesehatan (Test 2 — full page)
- Screenshot mobile drawer dengan grup Strategi (Test 6)
- Confirmation tier piramida click → toast
- Build output line "✓ built in Xms" + total bundle size
  </action>
  <verify>
    <automated>echo "Manual visual verification — see action steps"</automated>
  </verify>
  <done>Visual UAT pass dengan screenshots/output reported, no regression</done>
  <resume-signal>Type "approved" + tempel screenshots/output, atau describe issues yang harus di-fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client routing | Phase 12 tidak ada server-side state; semua client-side React Router. Tidak ada attack vector baru — route guard inherits dari AppShell auth check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | I | /kesehatan accessible by unauthenticated | accept | Phase 12 landing tidak punya user-data display (pure shell). Kalaupun unauthenticated user reach /kesehatan via deep link, AppShell auth guard existing (yang sudah protect /dashboard etc) akan redirect ke login. Tidak ada bypass. |
| T-12-07 | T | Sidebar config tampering via DevTools | accept | NAV_GROUPS adalah static config di bundle; user bisa modify runtime tapi tidak ada effect server-side. Standard SPA model. |
| T-12-08 | D | Piramida click handler infinite toast loop | mitigate | Sonner toast library sudah handle dedup; klik cepat 10x → max 3 toast visible (default config). Acceptable. |
</threat_model>

<verification>
- 6 file baru di `src/tabs/kesehatan/` exist + TypeScript compile
- `navConfig.ts` punya grup "Strategi" dengan 1 item Kesehatan + HeartPulse icon
- `routes.tsx` punya nested `/kesehatan` route dengan KesehatanLayout + index KesehatanLanding
- Visual UAT pass (Test 1-8) dengan screenshot konfirmasi
- Mobile drawer behavior intact
- Production build success
</verification>

<success_criteria>
1. User klik sidebar grup "Strategi" → "Kesehatan" → masuk ke `/kesehatan` dengan landing 3 section
2. Sidebar grup "Strategi" muncul **antara** grup "Tujuan" dan footer (Panduan/Pengaturan)
3. Piramida shell render 4 tier trapezoid (gray, urutan top→bottom: Warisan, Pertumbuhan, Akumulasi, Proteksi)
4. Banner kalkulator render dengan tombol "Buka kalkulator →"
5. Grid 6 modul render 2 kolom desktop (≥640px), 1 kolom mobile, label/description sesuai modulCatalog
6. Klik tier piramida → Sonner toast "Coming soon"
7. Mobile drawer tetap sehat, grup "Strategi" tidak break layout
8. Active state sidebar — pas user di `/kesehatan`, item "Kesehatan" highlighted
9. `npm run build` success
</success_criteria>

<output>
After completion, create `.planning/phases/12-kesehatan-foundation/12-02-SUMMARY.md` dengan:
- Files created/modified list
- Visual UAT results dengan screenshots
- Build output (bundle delta)
- Note: KesehatanLanding akan dimodifikasi di Plan 12-03 untuk DIAG-11 empty state branching
- Hand-off note: Phase 13 akan extend PiramidaShell dengan props `indicators` + tambah TierPanel; Phase 15 akan tambah child routes di routes.tsx (kalkulator + 6 modul)
</output>
