---
phase: 09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tabs/FinansialTab.tsx
  - src/auth/AuthProvider.tsx
  - src/components/AddMoneyDialog.tsx
  - src/components/GoalDialog.tsx
  - src/components/LinkInvestmentDialog.tsx
  - src/tabs/ReportsTab.tsx
autonomous: true
requirements: [QA-MEDIUM-3, QA-MEDIUM-4, QA-MEDIUM-5, QA-MEDIUM-6, QA-LOW-7, QA-LOW-8]
must_haves:
  truths:
    - "GoalsTab filter state (search, status filter) tetap bertahan saat user pindah ke tab Kekayaan lalu kembali ke Goals"
    - "Refresh token failure di startup menampilkan toast 'Sesi berakhir, silakan login kembali' dan memanggil signOut"
    - "AddMoneyDialog 'Sisa' menampilkan target − current_amount − investedValue (bukan target − current_amount saja)"
    - "GoalDialog field 'Sudah Terkumpul' diganti label menjadi 'Dana Kas Terkumpul (Rp)' dengan helper text di bawahnya"
    - "GoalDialog dan LinkInvestmentDialog memiliki <DialogDescription> sehingga tidak ada console warning aria-describedby"
    - "Tombol export di ReportsTab bertuliskan 'Ekspor PDF' (bukan 'Export PDF')"
  artifacts:
    - path: "src/tabs/FinansialTab.tsx"
      provides: "Tab wrapper dengan forceMount pada TabsContent goals (bug #3 fix)"
      contains: 'TabsContent value="goals" forceMount'
    - path: "src/auth/AuthProvider.tsx"
      provides: "Auth provider dengan refresh-token error handler (bug #4 fix)"
      contains: "Sesi berakhir, silakan login kembali"
    - path: "src/components/AddMoneyDialog.tsx"
      provides: "Dialog dengan kalkulasi remaining yang akurat (bug #5 fix)"
      contains: "(investedValue ?? 0)"
    - path: "src/components/GoalDialog.tsx"
      provides: "Dialog dengan label kas-only + helper text + DialogDescription (bug #6 + #7 fix)"
      contains: "Dana Kas Terkumpul (Rp)"
    - path: "src/components/LinkInvestmentDialog.tsx"
      provides: "Dialog dengan DialogDescription (bug #7 fix)"
      contains: "Hubungkan investasi ke goal ini dengan menentukan persentase alokasi"
    - path: "src/tabs/ReportsTab.tsx"
      provides: "Reports tab dengan tombol bahasa Indonesia (bug #8 fix)"
      contains: "Ekspor PDF"
  key_links:
    - from: "src/auth/AuthProvider.tsx"
      to: "sonner toast"
      via: "import { toast } from 'sonner'"
      pattern: "toast.error\\("
    - from: "src/auth/AuthProvider.tsx"
      to: "supabase.auth.signOut"
      via: "called inside refresh-token error handler"
      pattern: "supabase\\.auth\\.signOut\\(\\)"
    - from: "src/components/AddMoneyDialog.tsx"
      to: "investedValue prop"
      via: "remaining calculation subtracts (investedValue ?? 0)"
      pattern: "goal\\.target_amount - goal\\.current_amount - \\(investedValue \\?\\? 0\\)"
---

<objective>
Fix 6 bug frontend yang ditemukan di QA-FINDINGS.md (Bug #3, #4, #5, #6, #7, #8) dalam 1 plan paralel — semua perubahan kecil, scope-bounded, dan tidak saling overlap antar file.

Purpose: Tutup semua bug Medium dan Low frontend Phase 9 dalam satu wave eksekusi. Tidak ada perubahan ke design tokens, layout, atau komponen UI baru — semua perubahan adalah 1-baris atau 1-block edit ke file existing.

Output: 6 file frontend ter-update, semua TypeScript compile pass, semua ESLint clean, app build success.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-CONTEXT.md
@.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-PATTERNS.md
@.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dali-qa-findings-md/09-UI-SPEC.md
@QA-FINDINGS.md
@src/components/TransactionDialog.tsx

<interfaces>
<!-- Existing component contracts yang harus dihormati executor -->

From src/components/TransactionDialog.tsx (DialogDescription pattern reference):
```tsx
import {
  Dialog,
  DialogContent,
  DialogDescription,   // <-- ini yang harus ditambah ke GoalDialog + LinkInvestmentDialog
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from '@/components/ui/dialog'

// Placement (lines 96-99):
<DialogHeader>
  <DialogTitle>{editing ? 'Edit Transaksi' : 'Tambah Transaksi'}</DialogTitle>
  <DialogDescription>Catat pemasukan atau pengeluaran.</DialogDescription>
</DialogHeader>
```

From src/components/AddMoneyDialog.tsx (existing prop signature — TIDAK BERUBAH):
```tsx
interface Props {
  open: boolean
  onOpenChange: (open: boolean) => void
  goal: Goal | null
  investedValue?: number
}
```

From @supabase/supabase-js (event union):
```ts
type AuthChangeEvent = 'INITIAL_SESSION' | 'SIGNED_IN' | 'SIGNED_OUT' | 'PASSWORD_RECOVERY' | 'TOKEN_REFRESHED' | 'USER_UPDATED' | 'MFA_CHALLENGE_VERIFIED'
```
- `TOKEN_REFRESHED` event dengan `session === null` mengindikasikan refresh failure.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix Bug #3 — Add forceMount to GoalsTab TabsContent in FinansialTab</name>
  <files>src/tabs/FinansialTab.tsx</files>
  <read_first>
    - src/tabs/FinansialTab.tsx (full file — 22 lines)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-PATTERNS.md (D-08, D-09 — only goals tab gets forceMount, kekayaan does NOT)
  </read_first>
  <action>
Open `src/tabs/FinansialTab.tsx`. Locate line 16 yang berbunyi:

```tsx
        <TabsContent value="goals">
```

Replace dengan EXACT line:

```tsx
        <TabsContent value="goals" forceMount>
```

CRITICAL constraints (D-09):
- HANYA TabsContent dengan `value="goals"` yang diberi `forceMount`.
- TabsContent dengan `value="kekayaan"` (line 13) HARUS TETAP tanpa `forceMount` — biar KekayaanTab boleh unmount karena ada chart yang berat.
- Tidak ada perubahan import, tidak ada perubahan struktur lain, tidak ada perubahan ke Tabs/TabsList/TabsTrigger.

Hasil akhir file (full):
```tsx
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs'
import GoalsTab from '@/tabs/GoalsTab'
import KekayaanTab from '@/tabs/KekayaanTab'

export default function FinansialTab() {
  return (
    <div className="space-y-6">
      <Tabs defaultValue="kekayaan" className="w-full">
        <TabsList className="mb-4">
          <TabsTrigger value="kekayaan">Kekayaan</TabsTrigger>
          <TabsTrigger value="goals">Goals</TabsTrigger>
        </TabsList>
        <TabsContent value="kekayaan">
          <KekayaanTab />
        </TabsContent>
        <TabsContent value="goals" forceMount>
          <GoalsTab />
        </TabsContent>
      </Tabs>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>grep -c 'TabsContent value="goals" forceMount' src/tabs/FinansialTab.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/tabs/FinansialTab.tsx` contains exactly one occurrence of literal string `TabsContent value="goals" forceMount`.
    - File contains exactly one occurrence of literal string `TabsContent value="kekayaan">` (kekayaan TIDAK punya forceMount).
    - File does NOT contain `TabsContent value="kekayaan" forceMount` (kekayaan tidak boleh forceMount).
    - File line count tetap 22 ± 1 (minor edit only).
    - `tsc --noEmit` exits 0 (no type errors).
  </acceptance_criteria>
  <done>FinansialTab.tsx mempunyai forceMount pada Goals saja; tab Goals state filter (search + status filter) preserved saat user switch ke Kekayaan lalu balik.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix Bug #4 — AuthProvider toast + signOut on refresh-token failure</name>
  <files>src/auth/AuthProvider.tsx</files>
  <read_first>
    - src/auth/AuthProvider.tsx (full file — 78 lines)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-PATTERNS.md (D-10, D-11, D-12 — toast + signOut pattern)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-UI-SPEC.md (Bug #4 — Auth Toast Error specs)
    - QA-FINDINGS.md (Dashboard section, Bug #7 — root cause: clock skew + AuthApiError "Refresh Token Not Found")
  </read_first>
  <action>
Open `src/auth/AuthProvider.tsx` and apply 3 EXACT edits:

**Edit 1 — Add `toast` import (after line 3):**

Replace lines 1-3:
```tsx
import { createContext, useContext, useEffect, useState, type ReactNode } from 'react'
import { type Session, type User } from '@supabase/supabase-js'
import { supabase } from '@/lib/supabase'
```

With:
```tsx
import { createContext, useContext, useEffect, useState, type ReactNode } from 'react'
import { type Session, type User } from '@supabase/supabase-js'
import { supabase } from '@/lib/supabase'
import { toast } from 'sonner'
```

**Edit 2 — Replace `useEffect` block (lines 34-53) with error-handling version:**

Replace ENTIRE block from `useEffect(() => {` (line 34) through `}, [])` (line 53) with:

```tsx
  useEffect(() => {
    supabase.auth.getSession().then(({ data, error }) => {
      if (error) {
        // Refresh token gagal saat startup (e.g. clock skew / "Refresh Token Not Found")
        console.error('[AuthProvider] getSession error:', error)
        toast.error('Sesi berakhir, silakan login kembali')
        supabase.auth.signOut()
        setSession(null)
        setIsAdmin(false)
        setLoading(false)
        return
      }
      setSession(data.session)
      if (data.session?.user) {
        upsertProfile(data.session.user.id, data.session.user.user_metadata)
      }
      setLoading(false)
    })

    const { data: { subscription } } = supabase.auth.onAuthStateChange((event, session) => {
      // Token refresh failed → session becomes null (D-10, D-11)
      if (event === 'TOKEN_REFRESHED' && !session) {
        console.error('[AuthProvider] Token refresh failed')
        toast.error('Sesi berakhir, silakan login kembali')
        supabase.auth.signOut()
        setSession(null)
        setIsAdmin(false)
        return
      }
      setSession(session)
      if (session?.user) {
        upsertProfile(session.user.id, session.user.user_metadata)
      } else {
        setIsAdmin(false)
      }
    })

    return () => subscription.unsubscribe()
  }, [])
```

CRITICAL constraints:
- Toast wording HARUS persis `'Sesi berakhir, silakan login kembali'` (D-10, UI-SPEC line 96).
- Tetap log ke console (`console.error(...)`) — D-12, jangan suppress.
- Setelah `toast.error` panggil `supabase.auth.signOut()` (D-10, D-11).
- Set `setSession(null)`, `setIsAdmin(false)`, dan `setLoading(false)` (untuk getSession path) sebelum return — supaya UI tidak stuck di loading state.
- Underscore `_event` parameter di onAuthStateChange diganti dengan `event` (digunakan untuk match `'TOKEN_REFRESHED'`).
- TIDAK ada perubahan ke fungsi `upsertProfile`, `signIn`, `signOut`, `useAuthContext` — hanya useEffect block yang berubah.
- TIDAK menambah AuthApiError import — `data.error` dari getSession sudah cukup karena Supabase return PostgrestError-like di field `error`.
  </action>
  <verify>
    <automated>grep -c "Sesi berakhir, silakan login kembali" src/auth/AuthProvider.tsx && grep -c "import { toast } from 'sonner'" src/auth/AuthProvider.tsx && grep -c "TOKEN_REFRESHED" src/auth/AuthProvider.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 2 occurrences of literal string `'Sesi berakhir, silakan login kembali'` (one for getSession path, one for onAuthStateChange path).
    - File contains exactly 1 occurrence of `import { toast } from 'sonner'`.
    - File contains literal string `event === 'TOKEN_REFRESHED' && !session`.
    - File contains exactly 2 occurrences of `supabase.auth.signOut()` inside useEffect (excluding the existing one inside the `signOut` function defined later).
    - File contains exactly 2 occurrences of `console.error('[AuthProvider]` (one per error path).
    - File contains `import { type Session, type User } from '@supabase/supabase-js'` (Session/User imports unchanged — TIDAK ditambah AuthApiError).
    - `tsc --noEmit` exits 0.
    - `eslint src/auth/AuthProvider.tsx` exits 0.
  </acceptance_criteria>
  <done>AuthProvider menangkap refresh-token failure pada getSession dan TOKEN_REFRESHED event, menampilkan toast error, memanggil signOut, dan reset state — tanpa mempengaruhi flow login normal.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix Bug #5 — AddMoneyDialog remaining calculation includes investedValue</name>
  <files>src/components/AddMoneyDialog.tsx</files>
  <read_first>
    - src/components/AddMoneyDialog.tsx (full file ~150 lines — focus on lines 22, 25, 62)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-PATTERNS.md (D-13, D-14)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-CONTEXT.md (Bug #5 — investedValue prop sudah ada, tidak perlu ubah GoalsTab)
  </read_first>
  <action>
Open `src/components/AddMoneyDialog.tsx`. Locate line 62 yang berbunyi:

```tsx
  const remaining = Math.max(0, goal.target_amount - goal.current_amount)
```

Replace dengan EXACT line:

```tsx
  const remaining = Math.max(0, goal.target_amount - goal.current_amount - (investedValue ?? 0))
```

CRITICAL constraints (D-13, D-14):
- HANYA line 62 yang berubah — semua line lain (termasuk DialogDescription template string di line 72-80) tidak diubah.
- `investedValue` sudah ada sebagai prop di line 22 (`investedValue?: number`) dan sudah di-destructure di line 25 (`{ open, onOpenChange, goal, investedValue }`) — tidak perlu menambah/mengubah.
- TIDAK menambah perubahan ke GoalsTab.tsx (caller sudah pass `investedValue` per CONTEXT.md `<code_context>`).
- TIDAK menambah validasi extra atau formatRupiah — hanya kalkulasi remaining yang dikoreksi.
- Operator precedence aman: `a - b - c` left-to-right → `(a - b) - c` ≡ `a - b - c`. `(investedValue ?? 0)` dengan parentheses eksplisit menghindari ambiguity dengan `??`.

Hasil akhir line 62:
```tsx
  const remaining = Math.max(0, goal.target_amount - goal.current_amount - (investedValue ?? 0))
```

Konteks sekitar (untuk verifikasi visual):
```tsx
  if (!goal) return null

  const isPending = addMoney.isPending || withdraw.isPending
  const remaining = Math.max(0, goal.target_amount - goal.current_amount - (investedValue ?? 0))

  return (
```
  </action>
  <verify>
    <automated>grep -c 'goal.target_amount - goal.current_amount - (investedValue ?? 0)' src/components/AddMoneyDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 1 occurrence of literal string `goal.target_amount - goal.current_amount - (investedValue ?? 0)`.
    - File does NOT contain pattern `goal.target_amount - goal.current_amount\)` di line yang menghitung remaining (the OLD formula must be gone — note: `goal.current_amount}` di DialogDescription line 75 is a different context, OK).
    - File contains exactly 1 occurrence of `const remaining = Math.max(0,` (single declaration).
    - File contains exactly 1 occurrence of `investedValue?: number` (prop signature unchanged at line 22).
    - File contains `{ open, onOpenChange, goal, investedValue }: Props` (destructure unchanged at line 25).
    - `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>AddMoneyDialog kalkulasi remaining sekarang akurat — saat goal punya investasi terhubung, "Sisa yang perlu dikumpulkan" sudah memperhitungkan investedValue.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Fix Bug #6 + #7 (GoalDialog) — DialogDescription + label + helper text</name>
  <files>src/components/GoalDialog.tsx</files>
  <read_first>
    - src/components/GoalDialog.tsx (full file — 142 lines, focus on lines 1-8 imports, 92-95 DialogHeader, 109-112 label block)
    - src/components/TransactionDialog.tsx (lines 1-9 imports + lines 96-99 DialogHeader pattern — analog reference)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-PATTERNS.md (D-15, D-16, D-17, D-18, D-20)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-UI-SPEC.md (Copywriting Contract — exact strings)
  </read_first>
  <action>
Open `src/components/GoalDialog.tsx` and apply 3 EXACT edits:

**Edit 1 — Add `DialogDescription` to import (lines 2-8):**

Replace lines 2-8:
```tsx
import {
  Dialog,
  DialogContent,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from '@/components/ui/dialog'
```

With:
```tsx
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from '@/components/ui/dialog'
```

**Edit 2 — Add `<DialogDescription>` to DialogHeader (lines 93-95):**

Replace lines 93-95:
```tsx
          <DialogHeader>
            <DialogTitle>{editing ? 'Edit Goal' : 'Tambah Goal'}</DialogTitle>
          </DialogHeader>
```

With:
```tsx
          <DialogHeader>
            <DialogTitle>{editing ? 'Edit Goal' : 'Tambah Goal'}</DialogTitle>
            <DialogDescription>Buat atau edit goal keuangan Anda.</DialogDescription>
          </DialogHeader>
```

**Edit 3 — Update label + add helper text (lines 109-112):**

Replace lines 109-112:
```tsx
            <div className="grid gap-2">
              <Label htmlFor="g-current">Sudah Terkumpul (Rp)</Label>
              <Input id="g-current" inputMode="numeric" placeholder="0" value={currentStr} onChange={(e) => setCurrentStr(e.target.value)} />
            </div>
```

With:
```tsx
            <div className="grid gap-2">
              <Label htmlFor="g-current">Dana Kas Terkumpul (Rp)</Label>
              <Input id="g-current" inputMode="numeric" placeholder="0" value={currentStr} onChange={(e) => setCurrentStr(e.target.value)} />
              <p className="text-xs text-muted-foreground">Investasi terhubung dihitung otomatis dari portofolio</p>
            </div>
```

CRITICAL constraints:
- Label exact: `Dana Kas Terkumpul (Rp)` (D-15, UI-SPEC line 97).
- Helper text exact: `Investasi terhubung dihitung otomatis dari portofolio` (D-16, UI-SPEC line 98). TANPA tanda titik di akhir.
- DialogDescription exact: `Buat atau edit goal keuangan Anda.` (D-18, UI-SPEC line 99). DENGAN tanda titik di akhir (konsisten dengan TransactionDialog pattern).
- Helper text className: `text-xs text-muted-foreground` (konsisten dengan line 106 yang sudah ada).
- TIDAK menyentuh field "Target (Rp)" (line 104-107) — itu sudah punya helper text sendiri, jangan diubah.
- TIDAK menambah validasi atau logika baru.
- Indentasi: 12-space (3 levels) di dalam grid gap-4 — match existing style.
  </action>
  <verify>
    <automated>grep -c "Dana Kas Terkumpul (Rp)" src/components/GoalDialog.tsx && grep -c "Investasi terhubung dihitung otomatis dari portofolio" src/components/GoalDialog.tsx && grep -c "Buat atau edit goal keuangan Anda" src/components/GoalDialog.tsx && grep -c "DialogDescription," src/components/GoalDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 1 occurrence of literal string `Dana Kas Terkumpul (Rp)`.
    - File contains exactly 1 occurrence of literal string `Investasi terhubung dihitung otomatis dari portofolio`.
    - File contains exactly 1 occurrence of literal string `Buat atau edit goal keuangan Anda.`.
    - File does NOT contain literal string `Sudah Terkumpul (Rp)` (old label removed).
    - File contains `DialogDescription,` (with trailing comma) inside import block — proves named import added.
    - File contains exactly 1 occurrence of `<DialogDescription>` opening tag inside JSX.
    - File contains `<p className="text-xs text-muted-foreground">Investasi terhubung dihitung otomatis dari portofolio</p>` (helper text wrapped in <p>).
    - `tsc --noEmit` exits 0.
    - `eslint src/components/GoalDialog.tsx` exits 0.
  </acceptance_criteria>
  <done>GoalDialog mempunyai (1) DialogDescription untuk a11y, (2) label kas-only yang jelas, (3) helper text yang menjelaskan investasi dihitung otomatis. Console warning Missing aria-describedby saat GoalDialog terbuka hilang.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Fix Bug #7 (LinkInvestmentDialog) — Add DialogDescription</name>
  <files>src/components/LinkInvestmentDialog.tsx</files>
  <read_first>
    - src/components/LinkInvestmentDialog.tsx (full file — 178 lines, focus on lines 1-8 imports, 110-112 DialogHeader)
    - src/components/TransactionDialog.tsx (lines 1-9 imports + lines 96-99 DialogHeader pattern — analog reference)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-PATTERNS.md (D-17, D-19, D-20)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-UI-SPEC.md (Copywriting Contract — exact strings)
  </read_first>
  <action>
Open `src/components/LinkInvestmentDialog.tsx` and apply 2 EXACT edits:

**Edit 1 — Add `DialogDescription` to import (lines 2-8):**

Replace lines 2-8:
```tsx
import {
  Dialog,
  DialogContent,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from '@/components/ui/dialog'
```

With:
```tsx
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from '@/components/ui/dialog'
```

**Edit 2 — Add `<DialogDescription>` to DialogHeader (lines 110-112):**

Replace lines 110-112:
```tsx
        <DialogHeader>
          <DialogTitle>Hubungkan Investasi — {goal.name}</DialogTitle>
        </DialogHeader>
```

With:
```tsx
        <DialogHeader>
          <DialogTitle>Hubungkan Investasi — {goal.name}</DialogTitle>
          <DialogDescription>Hubungkan investasi ke goal ini dengan menentukan persentase alokasi.</DialogDescription>
        </DialogHeader>
```

CRITICAL constraints:
- DialogDescription exact: `Hubungkan investasi ke goal ini dengan menentukan persentase alokasi.` (D-19, UI-SPEC line 100). DENGAN tanda titik di akhir.
- TIDAK ada perubahan ke logic file ini — hanya import dan JSX header.
- Indentasi: 10-space (match `<DialogTitle>` indentation di line 111).
  </action>
  <verify>
    <automated>grep -c "Hubungkan investasi ke goal ini dengan menentukan persentase alokasi" src/components/LinkInvestmentDialog.tsx && grep -c "DialogDescription," src/components/LinkInvestmentDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 1 occurrence of literal string `Hubungkan investasi ke goal ini dengan menentukan persentase alokasi.`.
    - File contains `DialogDescription,` (with trailing comma) inside import block.
    - File contains exactly 1 occurrence of `<DialogDescription>` opening tag inside JSX.
    - File still contains `<DialogTitle>Hubungkan Investasi — {goal.name}</DialogTitle>` unchanged.
    - `tsc --noEmit` exits 0.
    - `eslint src/components/LinkInvestmentDialog.tsx` exits 0.
  </acceptance_criteria>
  <done>LinkInvestmentDialog memiliki DialogDescription untuk a11y; console warning Missing aria-describedby saat dialog terbuka hilang.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Fix Bug #8 — ReportsTab "Export PDF" → "Ekspor PDF"</name>
  <files>src/tabs/ReportsTab.tsx</files>
  <read_first>
    - src/tabs/ReportsTab.tsx (focus on lines 160-170 button block)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-PATTERNS.md (D-21)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-UI-SPEC.md (Copywriting Contract — Ekspor PDF)
  </read_first>
  <action>
Open `src/tabs/ReportsTab.tsx`. Locate line 168 yang berbunyi:

```tsx
            Export PDF
```

Replace dengan EXACT line (preserve 12-space indentation):

```tsx
            Ekspor PDF
```

CRITICAL constraints (D-21):
- HANYA text "Export PDF" yang diganti dengan "Ekspor PDF".
- TIDAK ada perubahan ke `<Download className="mr-2 h-4 w-4" />` icon di line 167.
- TIDAK ada perubahan ke `<Button variant="outline" size="sm" disabled={exporting} onClick={handleExport}>` di lines 161-166.
- TIDAK ada perubahan ke `handleExport` handler di file ini.
- Konsistensi dengan tab Transaksi dan Investasi yang sudah pakai "Ekspor"/"Impor".
  </action>
  <verify>
    <automated>grep -c "Ekspor PDF" src/tabs/ReportsTab.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 1 occurrence of literal string `Ekspor PDF`.
    - File does NOT contain literal string `Export PDF` anywhere (old text removed).
    - File contains `<Download className="mr-2 h-4 w-4" />` unchanged at line 167.
    - File contains `disabled={exporting}` unchanged at line 164.
    - `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Tombol di tab Laporan menampilkan "Ekspor PDF" — konsisten bahasa Indonesia dengan tombol di tab Transaksi/Investasi.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Final build + lint sweep across all 6 modified files</name>
  <files>—</files>
  <read_first>
    - package.json (verify build/lint scripts)
  </read_first>
  <action>
Run repo-wide validation untuk memastikan tidak ada breakage di file lain:

1. `npx tsc --noEmit` — confirm zero TS errors di semua file (bukan hanya 6 file di atas).
2. `npm run lint` (atau `npx eslint .`) — confirm zero lint errors di file yang dimodifikasi (Task 1-6).
3. `npm run build` (Vite) — confirm production build pass.

Jika ada error di file LAIN yang sudah ada sebelum Phase 9 (lihat STATE.md "23 lint errors di src/ pre-existing"), JANGAN fix — itu deferred ke phase berikutnya. Catat sebagai pre-existing di summary.

Jika ada error di salah satu dari 6 file Phase 9, kembali ke task sebelumnya dan perbaiki.

Tidak ada file baru yang ditulis di task ini — purely verification + report.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0.
    - `npm run build` exits with code 0 (Vite production build success).
    - Output dari build menampilkan dist/ artifacts (assets/index-*.js, dll).
    - `npx eslint src/tabs/FinansialTab.tsx src/auth/AuthProvider.tsx src/components/AddMoneyDialog.tsx src/components/GoalDialog.tsx src/components/LinkInvestmentDialog.tsx src/tabs/ReportsTab.tsx` exits with code 0 (no errors di 6 file Phase 9; pre-existing errors di file lain TIDAK diperhitungkan).
  </acceptance_criteria>
  <done>Semua 6 file frontend Phase 9 lulus tsc + eslint + build, dan tidak menyebabkan regresi di file lain.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase Auth | Refresh token disimpan di browser localStorage; expired/missing token bisa dimanipulasi user (clear storage) atau auto-expire |
| Component prop → render | `goal.target_amount`, `goal.current_amount`, `investedValue` berasal dari trusted server data tetapi rendering memengaruhi user decision |
| User input → form state | Label "Dana Kas Terkumpul" mengubah expectation user — tidak ada injection vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Spoofing | AuthProvider toast on `TOKEN_REFRESHED && !session` | accept | Toast hanya menampilkan pesan generic; user tetap di-`signOut` segera setelah toast — tidak ada window untuk attacker memalsukan toast karena code path hanya triggered oleh Supabase SDK internal event. |
| T-09-08 | Information Disclosure | console.error('[AuthProvider] ...') logs error object | accept | Error object dari Supabase Auth tidak mengandung kredensial; hanya nama error + clock skew warning. Console log adalah pre-existing pattern di app (mapSupabaseError pakai console.error). User logged-out segera. |
| T-09-09 | Tampering (XSS via DialogDescription) | Static string literals in DialogDescription | mitigate | DialogDescription menerima string literal hardcoded — tidak ada user input yang di-render. React JSX auto-escapes string children. Tidak ada `dangerouslySetInnerHTML`. |
| T-09-10 | Tampering (forceMount memory leak) | TabsContent forceMount on GoalsTab | accept | GoalsTab tetap mounted saat user di tab Kekayaan — memori cost: 1 React tree (~typical 50-100KB) untuk preserve filter state. Acceptable untuk single-user webapp. KekayaanTab tidak forceMount → chart tidak boros memori. |
| T-09-11 | Repudiation | Refresh token failure logged but not audited | accept | Untuk single-user app, audit log bukan threat utama. Console error sudah cukup untuk debug; sentry/posthog tidak dalam scope Phase 9. |

</threat_model>

<verification>
- 6 file frontend yang sudah listed di `files_modified` ter-update sesuai action verbatim.
- `npx tsc --noEmit` clean.
- `npm run build` clean.
- Tidak ada perubahan di file lain (git diff hanya menampilkan 6 file).
- Tidak ada perubahan ke design tokens (`src/index.css`), komponen UI di `src/components/ui/`, atau routing — sesuai UI-SPEC scope boundary.
</verification>

<success_criteria>
1. Bug #3: GoalsTab filter state preserved saat tab switch (Plan 09-04 verifies via UAT).
2. Bug #4: Refresh-token failure → toast "Sesi berakhir, silakan login kembali" + signOut.
3. Bug #5: AddMoneyDialog "Sisa" akurat saat ada investasi terhubung.
4. Bug #6: GoalDialog label "Dana Kas Terkumpul (Rp)" + helper text rendered.
5. Bug #7: GoalDialog dan LinkInvestmentDialog tidak lagi memunculkan console warning aria-describedby.
6. Bug #8: ReportsTab tombol "Ekspor PDF" rendered.
7. Build + tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-02-SUMMARY.md` documenting:
- 6 file paths modified with line numbers
- Build/lint output (tsc clean, build success)
- Confirmation: tidak ada perubahan di file lain
- Pre-existing lint errors di file lain (di-flag, tidak di-fix per scope)
</output>
