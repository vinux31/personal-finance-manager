---
phase: 09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified: []
autonomous: false
requirements: [QA-CRITICAL-1, QA-CRITICAL-2]
must_haves:
  truths:
    - "Migration 0025 SQL applied to production Supabase database via Studio SQL Editor"
    - "Function enforce_goal_investment_total() body in DB shows FOR UPDATE inside subquery (no FOR UPDATE+aggregate combo)"
    - "Function add_money_to_goal(BIGINT, NUMERIC) body in DB shows alias `g.` on every column reference of base table goals"
    - "Trigger goal_investments_total_check exists on goal_investments and fires BEFORE INSERT OR UPDATE"
    - "RPC add_money_to_goal callable by authenticated role (GRANT EXECUTE applied)"
  artifacts:
    - path: "Supabase Studio SQL Editor (live)"
      provides: "Deployed function bodies matching 0025_fix_goal_bugs.sql verbatim"
      contains: "matching pg_proc.prosrc inspections"
  key_links:
    - from: "Live PostgREST"
      to: "fixed RPC add_money_to_goal"
      via: "user clicking 'Tambah Uang' di Goals tab"
      pattern: "no 42702 ambiguous column error"
    - from: "Live PostgREST"
      to: "fixed trigger enforce_goal_investment_total"
      via: "user clicking 'Hubungkan Investasi' di Goals tab"
      pattern: "no 0A000 FOR UPDATE+aggregate error"
---

<objective>
[BLOCKING] Apply migration `0025_fix_goal_bugs.sql` ke production Supabase database via Studio SQL Editor (manual paste). Tanpa task ini, Critical #1 dan #2 tetap broken di production meskipun file SQL sudah ada di repo.

Purpose: Studio SQL Editor adalah de-facto migration channel untuk project ini (lihat STATE.md §"Studio fallback is the de-facto migration channel"). `supabase db push` rusak (history mismatch). Migrasi 0017-0024 semua applied via Studio paste — pattern ini berlanjut untuk 0025.

Output: 2 fungsi DB ter-update di production, trigger ter-attach, GRANT EXECUTE applied. Bug Critical #1 dan #2 resolved at runtime.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-CONTEXT.md
@supabase/migrations/0025_fix_goal_bugs.sql

<interfaces>
<!-- Studio paste protocol — same as Phases 5/6/7/8 -->

Studio SQL Editor URL pattern: `https://supabase.com/dashboard/project/<project-ref>/sql/new`

Pre-paste verification queries (run these BEFORE pasting 0025):

```sql
-- 1. Confirm 0024 already applied (prerequisite — defines withdraw_from_goal pattern Phase 9 mirror)
SELECT proname, pg_get_function_arguments(oid) AS args
FROM pg_proc
WHERE proname IN ('add_money_to_goal', 'withdraw_from_goal', 'enforce_goal_investment_total')
  AND pronamespace = 'public'::regnamespace
ORDER BY proname;
-- Expected: 3 rows (add_money_to_goal BIGINT/NUMERIC, withdraw_from_goal BIGINT/NUMERIC, enforce_goal_investment_total no args)

-- 2. Confirm trigger currently attached (will be re-attached by 0025 idempotently)
SELECT tgname, tgrelid::regclass FROM pg_trigger
WHERE tgname = 'goal_investments_total_check';
-- Expected: 1 row, tgrelid = goal_investments
```

Post-paste verification queries (run AFTER pasting 0025):

```sql
-- 1. Verify enforce_goal_investment_total now uses subquery FOR UPDATE
SELECT prosrc FROM pg_proc
WHERE proname = 'enforce_goal_investment_total'
  AND pronamespace = 'public'::regnamespace;
-- Expected: prosrc contains "FROM (\n    SELECT allocation_pct" and "FOR UPDATE\n  ) sub"

-- 2. Verify add_money_to_goal now uses alias g.
SELECT prosrc FROM pg_proc
WHERE proname = 'add_money_to_goal'
  AND pronamespace = 'public'::regnamespace;
-- Expected: prosrc contains "FROM goals g" and "WHERE g.id = p_id AND g.user_id = v_uid"

-- 3. Confirm trigger still attached
SELECT tgname FROM pg_trigger WHERE tgname = 'goal_investments_total_check';
-- Expected: 1 row

-- 4. Confirm GRANT applied
SELECT grantee, privilege_type FROM information_schema.routine_privileges
WHERE routine_name = 'add_money_to_goal' AND grantee = 'authenticated';
-- Expected: 1 row, privilege_type = EXECUTE
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] User pastes 0025_fix_goal_bugs.sql into Supabase Studio SQL Editor</name>
  <files>—</files>
  <read_first>
    - supabase/migrations/0025_fix_goal_bugs.sql (the file Plan 09-01 created — full content harus di-paste)
    - .planning/STATE.md §"Studio fallback is the de-facto migration channel"
  </read_first>
  <what-built>
    Plan 09-01 sudah membuat file `supabase/migrations/0025_fix_goal_bugs.sql` di repo. File ini perlu di-deploy ke production Supabase database. `supabase db push` BROKEN di project ini (history mismatch — lihat STATE.md). Channel deploy yang dipakai sejak Phase 5 adalah manual paste ke Studio SQL Editor. Task ini adalah human-only action — Claude TIDAK bisa otomatis paste ke Studio.
  </what-built>
  <how-to-verify>
**Step 1 — Buka file 0025 dan copy seluruh content:**
```bash
cat supabase/migrations/0025_fix_goal_bugs.sql
```
Atau buka di editor dan select-all + copy.

**Step 2 — Buka Supabase Studio SQL Editor:**
- URL: `https://supabase.com/dashboard/project/<your-project-ref>/sql/new`
- Login jika belum.

**Step 3 — Run pre-paste verification queries (one block, copy paste):**
```sql
SELECT proname, pg_get_function_arguments(oid) AS args
FROM pg_proc
WHERE proname IN ('add_money_to_goal', 'withdraw_from_goal', 'enforce_goal_investment_total')
  AND pronamespace = 'public'::regnamespace
ORDER BY proname;
```
Expected output: 3 rows. Jika kurang dari 3, STOP — laporkan ke developer (migration sebelumnya belum apply).

**Step 4 — Paste seluruh content `0025_fix_goal_bugs.sql` ke Studio SQL Editor:**
- Clear editor.
- Paste full file content.
- Click "Run" (atau Ctrl/Cmd+Enter).
- Expected: "Success. No rows returned" atau similar — TIDAK boleh ada error.

**Common errors dan resolution:**
- `function "add_money_to_goal" already exists with same argument types` → DROP FUNCTION IF EXISTS sudah ada di migration, kalau muncul: hard-refresh editor + retry.
- `syntax error at or near` → file 0025 corrupt; verify via `sha1sum supabase/migrations/0025_fix_goal_bugs.sql` dan compare dengan SUMMARY plan 09-01.
- `permission denied` → user login Studio bukan owner; gunakan Service Role.

**Step 5 — Run post-paste verification queries (one block):**
```sql
-- Critical #1 fix verification
SELECT
  CASE
    WHEN prosrc ~ 'FROM \(\s+SELECT allocation_pct' THEN 'PASS'
    ELSE 'FAIL'
  END AS critical_1_subquery_pattern,
  CASE
    WHEN prosrc ~ 'FOR UPDATE\s+\) sub' THEN 'PASS'
    ELSE 'FAIL'
  END AS critical_1_for_update_in_subquery
FROM pg_proc
WHERE proname = 'enforce_goal_investment_total'
  AND pronamespace = 'public'::regnamespace;

-- Critical #2 fix verification
SELECT
  CASE
    WHEN prosrc ~ 'FROM goals g\s' THEN 'PASS'
    ELSE 'FAIL'
  END AS critical_2_alias_in_select,
  CASE
    WHEN prosrc ~ 'WHERE g\.id = p_id AND g\.user_id = v_uid' THEN 'PASS'
    ELSE 'FAIL'
  END AS critical_2_alias_in_where
FROM pg_proc
WHERE proname = 'add_money_to_goal'
  AND pronamespace = 'public'::regnamespace;

-- Trigger + GRANT
SELECT tgname FROM pg_trigger WHERE tgname = 'goal_investments_total_check';
SELECT grantee, privilege_type FROM information_schema.routine_privileges
WHERE routine_name = 'add_money_to_goal' AND grantee = 'authenticated';
```

Expected:
- Row 1: `critical_1_subquery_pattern=PASS`, `critical_1_for_update_in_subquery=PASS`
- Row 2: `critical_2_alias_in_select=PASS`, `critical_2_alias_in_where=PASS`
- Row 3: 1 row dengan `tgname=goal_investments_total_check`
- Row 4: 1 row dengan `grantee=authenticated`, `privilege_type=EXECUTE`

**Step 6 — Smoke test runtime di production app:**
- Login ke production app (atau staging).
- Buka tab Goals.
- Klik "Hubungkan Investasi" pada salah satu goal → pilih investasi + masukkan alokasi 50% → klik Simpan.
  - **Expected (sebelum fix):** Toast error `FOR UPDATE is not allowed with aggregate functions`.
  - **Expected (sesudah fix):** Sukses tersimpan, dialog tertutup.
- Klik "Tambah Uang" pada salah satu goal → masukkan jumlah 1000 → klik Simpan.
  - **Expected (sebelum fix):** Toast error `column reference "current_amount" is ambiguous`.
  - **Expected (sesudah fix):** Sukses tersimpan, dialog tertutup.
  </how-to-verify>
  <acceptance_criteria>
    - User confirm Studio paste: "Success. No rows returned" atau equivalent (no SQL error).
    - User confirm post-paste verification: 4 PASS dari critical_1/critical_2 query, 1 row trigger, 1 row GRANT.
    - User confirm smoke test: Link Investasi sukses (no FOR UPDATE error), Tambah Uang sukses (no ambiguous column error).
    - Salin output verifikasi ke chat untuk archived (akan di-include di Plan 09-04 VERIFICATION.md).
  </acceptance_criteria>
  <resume-signal>Type "applied" jika Studio paste sukses + 4 PASS verifikasi + smoke test sukses. Type "failed: [pesan error]" jika ada error apapun di langkah Studio paste atau verifikasi.</resume-signal>
  <action>See <how-to-verify> block above — user follows Step 1 through Step 6 to paste 0025 ke Studio dan run verifikasi queries. Claude tidak dapat eksekusi langkah ini secara autonomous.</action>
  <verify>
    <automated>echo "Manual checkpoint — user must paste verification output to chat per resume-signal"</automated>
  </verify>
  <done>User typed "applied" + pasted verification output showing 4 PASS + 1 trigger row + 1 GRANT row + smoke test sukses untuk Link Investasi dan Tambah Uang.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local file → Studio editor | Manual copy-paste — risiko tampering bila file dimodifikasi setelah Plan 09-01 commit |
| Studio user → production DB | Owner/Service Role membuat perubahan irreversible (DROP FUNCTION + CREATE) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Tampering (file modified pre-paste) | `0025_fix_goal_bugs.sql` content | mitigate | Plan 09-01 SUMMARY records SHA-1 of file content. Before Studio paste, user dapat verify dengan `sha1sum supabase/migrations/0025_fix_goal_bugs.sql` dan compare ke SUMMARY hash. |
| T-09-13 | Denial of Service (production downtime selama paste) | DROP FUNCTION + CREATE FUNCTION | accept | DROP+CREATE menjalankan dalam single transaction Studio (Supabase wraps). Window broken < 1 detik. Concurrent client calls selama window dapat raise function-not-found tapi auto-retry akan sukses post-CREATE. Acceptable untuk app single-user. |
| T-09-14 | Elevation of Privilege (Studio user bypass app auth) | Direct SQL execution | accept | Studio access dibatasi ke Owner/Service Role — assumed trusted (developer running this plan). Tidak applicable ke end-user threat model. |
| T-09-15 | Information Disclosure (verification query exposes pg_proc bodies) | post-paste prosrc inspection | accept | prosrc menampilkan body fungsi yang sudah open-source di repo. Tidak ada secret di body fungsi. |
| T-09-16 | Repudiation (no audit log of who pasted) | Studio paste | accept | Studio log paste action di Supabase audit log (out-of-app). Plan 09-04 VERIFICATION.md akan record timestamp + verifier identity. |

</threat_model>

<verification>
- User explicitly typed "applied" di chat.
- 4 PASS dari critical_1/critical_2 verification queries (recorded di chat).
- Smoke test Link Investasi + Tambah Uang sukses tanpa toast error (recorded di chat dengan screenshot atau text confirmation).
</verification>

<success_criteria>
1. Migration 0025 deployed to production Supabase database.
2. Pre-paste state confirmed: 3 fungsi (add_money_to_goal, withdraw_from_goal, enforce_goal_investment_total) ada.
3. Post-paste state confirmed: prosrc bodies match expected patterns (subquery FOR UPDATE; alias `g.`).
4. Trigger goal_investments_total_check still attached.
5. GRANT EXECUTE on add_money_to_goal granted to authenticated.
6. Runtime smoke test: Link Investasi + Tambah Uang sukses tanpa error toast Critical #1/#2.
</success_criteria>

<output>
After completion, create `.planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-03-SUMMARY.md` documenting:
- Timestamp paste applied (UTC + WIB).
- Output verifikasi 4 queries (PASS/FAIL per critical).
- Output trigger query (1 row).
- Output GRANT query (1 row).
- Smoke test outcome (Link Investasi + Tambah Uang).
- Studio user (Owner email atau Service Role).
- Migration list status: `supabase migration list --linked` will show 0025 as Local-only — accepted per STATE.md (history reconciliation deferred).
</output>
