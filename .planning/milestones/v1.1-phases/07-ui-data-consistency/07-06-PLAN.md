---
phase: 07-ui-data-consistency
plan: 06
type: execute
wave: 4
depends_on: [07-04]
files_modified:
  - eslint.config.js
  - src/queries/investments.ts
autonomous: true
requirements: [CONS-02]
must_haves:
  truths:
    - "`eslint.config.js` rules block contains a `no-restricted-syntax` rule with severity `'error'` and AST selector exactly `CallExpression[callee.object.callee.object.callee.name='Date'][callee.property.name='slice']` (D-16)."
    - "`src/queries/investments.ts:111` no longer contains `new Date().toISOString().slice(0, 10)`; uses `todayISO()` imported from `@/lib/format` instead (D-19)."
    - "`pnpm lint` exits 0 — meaning the new rule does not fire on existing code (no leftover `new Date().toISOString().slice(...)` callsites in src/)."
    - "After 07:00 WIB, calling 'Refresh Harga' inserts a `price_history` row with `date` field equal to today's WIB YYYY-MM-DD (verifiable in plan 07-08 UAT-2)."
  artifacts:
    - path: eslint.config.js
      provides: "no-restricted-syntax rule with AST selector + Indonesian message"
      contains: "no-restricted-syntax"
    - path: src/queries/investments.ts
      provides: "useRefreshPrices uses todayISO() instead of new Date().toISOString().slice"
      contains: "todayISO()"
  key_links:
    - from: "eslint.config.js no-restricted-syntax rule"
      to: "all .ts/.tsx files under src/"
      via: "files: ['**/*.{ts,tsx}'] glob"
      pattern: "no-restricted-syntax"
    - from: "src/queries/investments.ts:111"
      to: "@/lib/format todayISO()"
      via: "named import"
      pattern: "import.*todayISO.*format"
---

<objective>
CONS-02 — Fix WIB-vs-UTC timezone bug at the only known callsite (`src/queries/investments.ts:111`) and prevent regression via ESLint AST-selector rule. Pure code/config — zero DB change, zero migration, runs in parallel with sibling Wave 4 plans (07-05 frontend, 07-07 View-As gate).

**Two-task plan** (both edits are tiny but logically separate):

1. **Add ESLint rule** (`eslint.config.js`) — new `no-restricted-syntax` entry blocking `new Date().toISOString().slice(...)` chain pattern via AST selector. Severity `error` (D-17 — CI block consistent with Phase 5 hardening discipline). No exceptions (D-18). Pre-existing 23 lint errors in src/ (per STATE.md) are unrelated to this rule and remain deferred to Phase 8.

2. **Fix the offending callsite** — `src/queries/investments.ts:111` uses `new Date().toISOString().slice(0, 10)` which returns UTC date; on Asia/Jakarta after midnight UTC (i.e., 07:00 WIB), this returns yesterday's WIB date causing `price_history` rows to be off-by-one-day. Replace with `todayISO()` from `@/lib/format` (D-19). The helper is local-time-based and WIB-correct in browser; server-side TZ hardening deferred to v1.2 per D-20.

**Why this is its own plan** (not bundled with 07-05): zero shared files with sibling plans; eslint config is a separate concern from React component wiring; smaller blast radius if revert needed.

Output:
- 2 files modified
- `pnpm lint` exits 0 (no new violations after the fix)
- `pnpm build` exits 0
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-data-consistency/07-CONTEXT.md
@.planning/phases/07-ui-data-consistency/07-PATTERNS.md
@eslint.config.js
@src/queries/investments.ts
@src/lib/format.ts

<interfaces>
<!-- Existing helper to use. -->

From src/lib/format.ts:
  export function todayISO(): string  // returns YYYY-MM-DD in local browser TZ (WIB-correct in Asia/Jakarta browser)

Existing eslint.config.js structure (current — flat config with `defineConfig`):
  export default defineConfig([
    globalIgnores(['dist']),
    {
      files: ['**/*.{ts,tsx}'],
      extends: [...],
      languageOptions: {...},
      // NO `rules` block currently — must be added
    },
  ])
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add no-restricted-syntax rule to eslint.config.js</name>
  <files>eslint.config.js</files>
  <read_first>
    - eslint.config.js (full file — flat config, defineConfig wrapper, no rules block currently)
    - .planning/phases/07-ui-data-consistency/07-CONTEXT.md (D-16 — exact AST selector + message; D-17 — severity error; D-18 — no exceptions)
    - .planning/phases/07-ui-data-consistency/07-PATTERNS.md (section "eslint.config.js (no-restricted-syntax rule)" — full skeleton)
  </read_first>
  <action>
Modify `eslint.config.js` by adding a `rules` block to the existing config object. Final file content (verbatim per PATTERNS.md skeleton; D-16 selector + D-17 severity):

```javascript
import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import tseslint from 'typescript-eslint'
import { defineConfig, globalIgnores } from 'eslint/config'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      js.configs.recommended,
      tseslint.configs.recommended,
      reactHooks.configs.flat.recommended,
      reactRefresh.configs.vite,
    ],
    languageOptions: {
      ecmaVersion: 2020,
      globals: globals.browser,
    },
    rules: {
      'no-restricted-syntax': ['error', {
        selector: "CallExpression[callee.object.callee.object.callee.name='Date'][callee.property.name='slice']",
        message: 'Pakai todayISO() dari @/lib/format — .toISOString().slice(0,10) returns UTC date, bukan WIB',
      }],
    },
  },
])
```

**Critical details:**
- AST selector MUST match exactly: `CallExpression[callee.object.callee.object.callee.name='Date'][callee.property.name='slice']`. This selector matches the chain `new Date().toISOString().slice(...)`:
  - Outer `CallExpression`: the `.slice(...)` call
  - `callee.object`: the `.toISOString()` call
  - `callee.object.callee.object`: the `new Date()` constructor call
  - `callee.object.callee.object.callee.name === 'Date'`: confirms it's specifically `new Date()` (not some other function returning a Date-like object)
  - `callee.property.name === 'slice'`: the final `.slice` method
- Message MUST be in Bahasa Indonesia per project constraint (Pattern F).
- Severity MUST be `'error'` (D-17 — CI block).
- Single rule entry — do NOT add additional patterns (D-18 says no exceptions; if executor finds genuine edge case at Task 2, use inline `// eslint-disable-next-line no-restricted-syntax`).

**MUST NOT** modify any other field of the config (extends, languageOptions, files glob). **MUST NOT** add `'no-restricted-syntax'` to multiple config blocks — single rule in single block. **MUST NOT** change globalIgnores.

After edit, the file should have exactly one new top-level field added (`rules: { ... }`) inside the second config object.
  </action>
  <verify>
    <automated>node --eval "import('./eslint.config.js').then(m => console.log(JSON.stringify(m.default.flatMap(c => Array.isArray(c.rules) || (c.rules && c.rules['no-restricted-syntax']) ? ['has-rule'] : []))))"</automated>
  </verify>
  <acceptance_criteria>
    - File `eslint.config.js` contains exact substring `'no-restricted-syntax'` (verifiable: `grep -F "'no-restricted-syntax'" eslint.config.js` returns ≥ 1 line).
    - File contains exact substring `CallExpression[callee.object.callee.object.callee.name='Date'][callee.property.name='slice']` (D-16 AST selector verbatim — verifiable via `grep -F`).
    - File contains exact substring `Pakai todayISO() dari @/lib/format` (Indonesian message — verifiable via `grep -F`).
    - File contains exact substring `['error', {` (severity = error — D-17).
    - File starts with the same `import` statements (verifiable: `head -7 eslint.config.js` matches the existing imports).
    - File ends with `])` (closing of defineConfig call).
    - The new `rules:` block is INSIDE the second config object (the one with `files: ['**/*.{ts,tsx}']`) — not at top level.
    - File parses as valid JS module: `node -e "import('./eslint.config.js').then(() => console.log('ok')).catch(e => { console.error(e); process.exit(1) })"` exits 0.
  </acceptance_criteria>
  <done>eslint.config.js has the no-restricted-syntax rule with exact AST selector + Indonesian message + error severity; file imports unchanged; flat config still parses.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace new Date().toISOString().slice(0, 10) with todayISO() at investments.ts:111</name>
  <files>src/queries/investments.ts</files>
  <read_first>
    - src/queries/investments.ts (full file — line 111 has the offending callsite; line 1-22 has imports)
    - src/lib/format.ts (full file — confirm `todayISO()` exists and is the WIB-correct helper)
    - src/db/investments.ts (line 94 — same file already imports + uses `todayISO()` — in-file precedent)
    - .planning/phases/07-ui-data-consistency/07-CONTEXT.md (D-19 — exact callsite fix; D-20 — todayISO() kept as-is)
    - .planning/phases/07-ui-data-consistency/07-PATTERNS.md (section "src/queries/investments.ts (line 111 — date helper fix)" — before/after diff)
  </read_first>
  <action>
Modify `src/queries/investments.ts`:

**Edit 1 — Add import:**

The current import block (lines 1-22) imports from `@/db/investments`, `@/lib/errors`, `@/auth/useTargetUserId`. Add a new import line for `todayISO`:

```typescript
import { todayISO } from '@/lib/format'
```

Place it grouped with the other `@/lib/*` imports (next to `import { mapSupabaseError } from '@/lib/errors'`).

**Edit 2 — Replace line 111:**

Current line 111 reads:
```typescript
      const today = new Date().toISOString().slice(0, 10)
```

Replace with:
```typescript
      const today = todayISO()
```

That's the only change to the function body. The rest of `useRefreshPrices` (line 106-129) stays identical — the variable name `today` is preserved, downstream `updatePrice(id, price, today)` is unchanged.

**MUST NOT** modify any other line. **MUST NOT** rename `today` variable. **MUST NOT** add eslint-disable comments — D-18 mandates no exceptions, and the new ESLint rule will not fire on `todayISO()` (it doesn't match the AST selector).

After this edit, `pnpm lint` should exit 0 (the rule doesn't fire on the new code) and the previous violation is gone.

**Sanity check after edit:** `grep -n "new Date().toISOString().slice" src/queries/investments.ts` MUST return zero matches. `grep -n "todayISO" src/queries/investments.ts` MUST return at least 2 matches (the import + the usage).
  </action>
  <verify>
    <automated>pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - File `src/queries/investments.ts` contains exact substring `import { todayISO } from '@/lib/format'` (new import).
    - File `src/queries/investments.ts` does NOT contain `new Date().toISOString().slice` ANYWHERE (verifiable: `! grep -q 'new Date().toISOString().slice' src/queries/investments.ts`).
    - File `src/queries/investments.ts` contains exact substring `const today = todayISO()` at the position of the previous offending line (verifiable: `grep -F 'const today = todayISO()' src/queries/investments.ts` returns 1 line).
    - The function `useRefreshPrices` body is structurally unchanged except for the one line replacement (verifiable: file line count delta within ±1 of original — the only change is `import` line added + 1 line replaced).
    - `pnpm lint` exits 0 — meaning the new ESLint rule doesn't fire (the offending callsite is gone, and no other callsites exist in src/).
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm build` exits 0.
    - **Codebase-wide audit (defensive):** `grep -rn "new Date()\\.toISOString()\\.slice" src/ 2>/dev/null` returns zero matches. (If matches exist beyond `investments.ts:111`, they would have been pre-existing — fix them too in this task. STATE.md records 23 pre-existing lint errors but timezone violations are NEW and must all be fixed for `pnpm lint` to exit 0.)
  </acceptance_criteria>
  <done>investments.ts:111 uses todayISO(); todayISO imported; codebase-wide grep for the offending pattern returns zero; lint passes; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → updatePrice → price_history (DB) | The `today` value is sent to PostgREST as the `date` column for the `price_history` insert. Server has no way to verify it's the "real" today; relies on client-supplied date. This is acceptable: the client owner is the user querying their own data, and an off-by-one TZ bug is a UX issue not a security issue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-23 | T (Tampering) | client-supplied `date` value | accept | A user with dev tools could call `updatePrice(id, price, '2099-01-01')` to backdate a row. RLS scopes the insert to their own user_id but the date itself is not validated. Defer server-side date validation to v1.2 if needed (REQUIREMENTS.md "Future Requirements" — `import_transactions_bulk` server-side hardening reference). The fix in this plan addresses the off-by-one BUG, not the tampering vector. |
| T-07-24 | T (Tampering) | ESLint rule bypass via inline disable | mitigate | D-18: no exceptions. If executor in this plan or future plans encounter a genuine need to use `new Date().toISOString().slice()`, they MUST add `// eslint-disable-next-line no-restricted-syntax` with comment justification — visible in code review. The rule is severity `'error'` so CI fails until inline disable is added explicitly. |
</threat_model>

<verification>
After both tasks complete:
- `grep -F "'no-restricted-syntax'" eslint.config.js` returns ≥ 1 line.
- `grep -F "callee.object.callee.object.callee.name='Date'" eslint.config.js` returns 1 line.
- `! grep -rn 'new Date().toISOString().slice' src/` succeeds (zero matches anywhere in src/).
- `pnpm lint` exits 0.
- `pnpm build` exits 0.

**No production deploy in this plan.** Plan 07-08 commits Wave 4 results to master triggering Vercel auto-deploy.
</verification>

<success_criteria>
- ESLint flat config has new `no-restricted-syntax` rule with exact AST selector from D-16, severity `error`, Indonesian message.
- src/queries/investments.ts:111 callsite fixed; `todayISO` imported.
- No other `new Date().toISOString().slice` occurrences in src/.
- `pnpm lint` exits 0.
- Build + type check pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-data-consistency/07-06-SUMMARY.md` documenting:
- 2 files modified
- ESLint rule snippet (verbatim)
- Callsite fix snippet (before/after)
- Decisions implemented: D-16, D-17, D-18, D-19, D-20
- Threats addressed: T-07-23 (accepted), T-07-24
- Note: pnpm lint exits 0 confirms zero pre-existing matches of the new pattern (the 23 deferred errors per STATE.md are unrelated rules: badge/button/tabs fast-refresh, csvInvestments any, PensiunTab refs-during-render).
- Plan 07-08 UAT-2 will verify production behavior (Refresh Harga after 07:00 WIB writes today's WIB date).
</output>
