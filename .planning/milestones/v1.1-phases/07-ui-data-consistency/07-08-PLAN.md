---
phase: 07-ui-data-consistency
plan: 08
type: execute
wave: 5
depends_on: [07-04, 07-05, 07-06, 07-07]
files_modified:
  - .planning/phases/07-ui-data-consistency/07-08-UAT.md
  - .planning/phases/07-ui-data-consistency/07-VERIFICATION.md
  - .planning/STATE.md
autonomous: false
requirements: [CONS-01, CONS-02, CONS-03, UX-01, UX-02]
must_haves:
  truths:
    - "Vercel auto-deploy of master branch GREEN with all Wave 4 frontend changes (07-05/07-06/07-07)."
    - "All 5 Browser-MCP UAT scenarios PASS against https://kantongpintar.vercel.app/ — UAT-1 (CONS-01 demo case + withdraw helper text + error MESSAGE), UAT-2 (CONS-02 timezone), UAT-3 (CONS-03 idempotent seed), UAT-4 (UX-01 reset key), UAT-5 (UX-02 View-As gate)."
    - "07-VERIFICATION.md ditulis dengan 5 ROADMAP success criteria mapped to PASS / PASS-WITH-NOTES / FAIL evidence."
    - "STATE.md updated with v1.1 Phase 7 completion + new deferred items (if any) + ready-for-Phase-8 status."
    - "All 5 REQ-IDs (CONS-01, CONS-02, CONS-03, UX-01, UX-02) marked complete in REQUIREMENTS.md traceability table."
  artifacts:
    - path: .planning/phases/07-ui-data-consistency/07-08-UAT.md
      provides: "Manual UAT script + execution log untuk 5 scenarios per CONTEXT D-29"
      contains: "UAT-1"
    - path: .planning/phases/07-ui-data-consistency/07-VERIFICATION.md
      provides: "Phase verdict (PASS / PASS-WITH-NOTES / FAIL) mapped to 5 ROADMAP criteria"
      contains: "Verdict:"
    - path: .planning/STATE.md
      provides: "Updated current position + Phase 7 complete + Phase 8 ready"
  key_links:
    - from: "git push origin master"
      to: "Vercel auto-deploy → kantongpintar.vercel.app"
      via: "15-30s build → live"
      pattern: "https://kantongpintar.vercel.app"
    - from: "Browser-MCP UAT execution"
      to: "07-08-UAT.md log"
      via: "step-by-step user observations + screenshot/timestamps"
      pattern: "UAT-[1-5]"
---

<objective>
**[BLOCKING]** Final gate for Phase 7 — production verify-before-close per PROJECT.md constraint. Mirrors Phase 5/6 close model.

**Steps:**
1. Pre-flight: confirm Wave 4 plans (07-05, 07-06, 07-07) all completed and committed locally; trigger production deploy by pushing master to origin.
2. Wait 15-30s (per memory `project_vercel_deploy_timing.md`) for Vercel auto-deploy to complete; verify via curl.
3. Execute 5 Browser-MCP UAT scenarios per CONTEXT.md D-29 against the live URL.
4. Write `07-VERIFICATION.md` mapping all 5 ROADMAP success criteria to PASS / PASS-WITH-NOTES / FAIL evidence.
5. Write `07-08-UAT.md` capturing per-step observations + screenshots/network captures.
6. Update STATE.md (Phase 7 complete, Phase 8 ready) + update REQUIREMENTS.md traceability table (mark CONS-01..UX-02 complete with plan reference).

**Mode:** `autonomous: false`. Browser-MCP UAT requires the human-in-the-loop to launch the MCP browser session and confirm visual outcomes. Network captures + DOM assertions are automated by the MCP tool itself, but session orchestration + screenshot capture is a checkpoint flow.

Output:
- 07-08-UAT.md (manual UAT script + observations)
- 07-VERIFICATION.md (verdict)
- STATE.md updated
- REQUIREMENTS.md traceability table updated
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ui-data-consistency/07-CONTEXT.md
@.planning/phases/07-ui-data-consistency/07-PATTERNS.md
@.planning/phases/07-ui-data-consistency/07-04-PLAN.md
@.planning/phases/07-ui-data-consistency/07-05-PLAN.md
@.planning/phases/07-ui-data-consistency/07-06-PLAN.md
@.planning/phases/07-ui-data-consistency/07-07-PLAN.md
@.planning/phases/06-race-atomicity/06-VERIFICATION.md
@.planning/phases/06-race-atomicity/06-05-UAT.md

<deploy_timing_note>
Per memory `project_vercel_deploy_timing.md`: Vercel auto-deploy from master takes 15-30s. Do NOT wait 90s. After `git push origin master`, poll `curl -sI https://kantongpintar.vercel.app/` for 200 OK + `x-vercel-id` header indicating new deployment. Typical sequence: push → 5s build start → 20s deploy → live.
</deploy_timing_note>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Pre-flight — confirm Wave 4 commits + push to master</name>
  <read_first>
    - .planning/phases/07-ui-data-consistency/07-05-PLAN.md (frontend wiring)
    - .planning/phases/07-ui-data-consistency/07-06-PLAN.md (eslint + investments fix)
    - .planning/phases/07-ui-data-consistency/07-07-PLAN.md (View-As CSV gate)
    - .planning/phases/07-ui-data-consistency/07-04-DEPLOY-LOG.md (Wave 3 result — DB already at 0024)
  </read_first>
  <what-built>
    Plans 07-05, 07-06, 07-07 produced 11 file modifications across frontend code. They are committed locally on master. This task confirms the working tree is clean and pushes to origin to trigger Vercel auto-deploy.
  </what-built>
  <how-to-verify>
    1. `git status` — working tree must be clean (or only contain Phase 7 SUMMARY files yet to be committed).
    2. `git log --oneline -10` — confirm Wave 1, 2, 4 commits present (commits referencing plans 07-01 through 07-07).
    3. `git rev-parse HEAD` capture commit SHA for the UAT log.
    4. **Run build locally first to catch any regression:** `pnpm build` MUST exit 0.
    5. **Run lint locally:** `pnpm lint` MUST exit 0.
    6. **Run tsc:** `pnpm tsc --noEmit` MUST exit 0.
    7. `git push origin master` — push all phase 7 commits to GitHub.
    8. Wait ~5s for GitHub webhook → Vercel.
    9. Poll: `curl -sI https://kantongpintar.vercel.app/ | head -10` repeat every 5s until response includes a fresh `x-vercel-id` header (or check Vercel dashboard for "Ready" status). Total wait: 15-30s typical.

    User confirms by replying "deployed-<commit_sha>" with the Vercel deployment URL.
  </how-to-verify>
  <resume-signal>Type "deployed-<sha>" with deploy URL.</resume-signal>
  <acceptance_criteria>
    - `git status` reports clean working tree (or only SUMMARY files staged).
    - `pnpm build`, `pnpm lint`, `pnpm tsc --noEmit` all exit 0.
    - `git push origin master` succeeds (no force-push, no rejection).
    - `curl -sI https://kantongpintar.vercel.app/` returns 200 OK with a fresh `x-vercel-id` indicating new deploy.
    - Commit SHA + deploy URL captured for UAT log.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] UAT-1 (CONS-01) — Goal progress 100% + badge "Tercapai" + withdraw helper text + split MESSAGE</name>
  <read_first>
    - .planning/phases/07-ui-data-consistency/07-CONTEXT.md (D-29 UAT-1 scenario verbatim; D-15 helper text wording; D-14 error MESSAGE wording)
    - .planning/phases/07-ui-data-consistency/07-PATTERNS.md (Pattern F — exact-match strings)
    - .planning/ROADMAP.md (Phase 7 SC#1)
  </read_first>
  <what-built>
    Migration 0024 backfill (Wave 3) flipped status to 'completed' for goals where total_amount >= target. Frontend wiring (07-05) switched GoalsTab to useGoalsWithProgress; AddMoneyDialog has investedValue prop. UAT verifies the user-facing manifestation.
  </what-built>
  <how-to-verify>
    Use Browser-MCP (Playwright) to:

    **Pre-condition setup (one-time, may already exist for admin user):**
    - Login as admin (rinoadi28@gmail.com) at https://kantongpintar.vercel.app/.
    - Navigate to Finansial → Goals. Locate goal with target Rp 10.000.000 (or pick first goal where target × 1 ≤ Σ(allocation_pct × investment value × 60% via VIEW). If no demo case exists, manually set up: create goal "Test CONS-01" target Rp 10.000.000; link 60% to an investment with current_price ≥ Rp 18.000.000 — but NOTE: backfill in 0024 ran during 07-04, so any pre-existing goal that fits the math is already 'completed'.)

    **UAT-1 steps:**
    1. Navigate to Goals tab → assert at least one goal card shows progress bar = 100% AND badge text "Tercapai" (status='completed').
       Capture: screenshot of card.
    2. Click that goal's "Tarik Dana" button → AddMoneyDialog opens with mode='tarik'.
       Capture: screenshot of dialog.
       **ASSERT** DialogDescription contains EXACT substring `Saldo kas tersedia: Rp ` followed by `(terpisah dari investasi Rp ` (D-15 + Pattern F). The Browser-MCP DOM query: `await page.locator('[data-radix-dialog-description], .DialogDescription').textContent()` includes both substrings.
    3. Enter an amount > current_amount (e.g. if cash is Rp 0, enter Rp 100). Click "Tarik" submit.
    4. Assert toast appears with EXACT substring `Saldo kas tidak cukup. Tersedia Rp ` followed by `(terpisah dari Rp ` AND `di investasi)` (D-14 wording from migration 0024 → mapSupabaseError → toast).
       Capture: screenshot of toast OR network response from withdraw RPC.
    5. Close dialog. No data should be modified (RPC raised, no UPDATE).

    Append all 5 step observations + screenshots / DOM textContent captures into `.planning/phases/07-ui-data-consistency/07-08-UAT.md` under `## UAT-1 (CONS-01)`.

    User confirms by typing "uat-1-pass" or describing failures.
  </how-to-verify>
  <resume-signal>Type "uat-1-pass" with screenshots paths or DOM text captures, or describe failures.</resume-signal>
  <acceptance_criteria>
    - At least one goal card visible on Goals tab with progress bar at 100% AND visible "Tercapai" badge.
    - AddMoneyDialog (withdraw mode) DialogDescription contains BOTH `'Saldo kas tersedia: Rp '` AND `'(terpisah dari investasi Rp '` substrings simultaneously (D-15 exact-match).
    - Submitting an over-cash withdraw produces a toast/alert containing BOTH `'Saldo kas tidak cukup. Tersedia Rp '` AND `'(terpisah dari Rp '` AND `'di investasi)'` substrings (D-14 exact-match).
    - 07-08-UAT.md `## UAT-1` section captures: step-by-step observations, screenshot paths, DOM text captures.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] UAT-2 (CONS-02) — Refresh Harga writes today's WIB date</name>
  <read_first>
    - .planning/phases/07-ui-data-consistency/07-CONTEXT.md (D-29 UAT-2)
    - .planning/phases/07-ui-data-consistency/07-06-PLAN.md (the fix at investments.ts:111)
  </read_first>
  <what-built>
    Plan 07-06 replaced `new Date().toISOString().slice(0, 10)` with `todayISO()` (local-time, WIB-correct in browser). UAT verifies the fix in production.
  </what-built>
  <how-to-verify>
    **Best run after 07:00 WIB (local Asia/Jakarta time)** — that's when UTC midnight has passed but WIB is on a new day. If running before 07:00 WIB, the fix is still observable but the bug condition (UTC vs WIB mismatch) is not yet active.

    1. Login at https://kantongpintar.vercel.app/.
    2. Navigate to Investasi tab.
    3. Open DevTools → Network tab → filter by `price_history` or `updatePrice` or look for POST/PATCH to PostgREST `investments` or `price_history`.
    4. Click "Refresh Harga" button.
    5. Inspect the network request body for at least one of the resulting `price_history` INSERTs. Look for a `date` field in the JSON body.
    6. **Compute expected:** the local browser today in YYYY-MM-DD format. Sanity check: `new Date().toLocaleDateString('en-CA')` returns this in browser console (en-CA happens to format YYYY-MM-DD).
    7. ASSERT: the `date` field in the request body matches the expected WIB local date (NOT the UTC date if test runs after 07:00 WIB).

    Append observation + screenshot of network request body into `07-08-UAT.md` under `## UAT-2 (CONS-02)`.

    User confirms by typing "uat-2-pass" with the captured `date` value + expected WIB date.
  </how-to-verify>
  <resume-signal>Type "uat-2-pass" with date evidence.</resume-signal>
  <acceptance_criteria>
    - Refresh Harga click triggers at least one network request with a JSON body containing `date: 'YYYY-MM-DD'`.
    - The captured `date` value equals the local browser date (verifiable: `new Date().toLocaleDateString('en-CA')` matches).
    - If run after 07:00 WIB: confirm the date is NOT yesterday's UTC date (the bug case).
    - 07-08-UAT.md `## UAT-2` section updated.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] UAT-3 (CONS-03) — Idempotent seed_rencana on new user signup</name>
  <read_first>
    - .planning/phases/07-ui-data-consistency/07-CONTEXT.md (D-29 UAT-3; D-02 marker dedup; D-04 5+3 seed counts)
    - .planning/phases/07-ui-data-consistency/07-02-PLAN.md (RPC behavior + count expectation)
  </read_first>
  <what-built>
    Plan 07-02 created seed_rencana RPC (idempotent via marker); plan 07-05 wired useRencanaInit to call the RPC. UAT verifies that a fresh user signup produces exactly 5 goals + 3 investments + 1 marker, and 5 reloads do not duplicate.
  </what-built>
  <how-to-verify>
    **Test user setup:**
    - Use a test email allowed in `allowed_emails` (NOT admin's primary email). If no such test email exists, this UAT can be skipped and downgraded to "verified via pgTAP" — note in UAT log.
    - Alternatively, use admin's account combined with a Studio query to manually delete the marker + goals + investments and observe re-seed.

    **Browser-MCP steps (preferred — fresh signup):**
    1. Open incognito/private window → https://kantongpintar.vercel.app/.
    2. Sign in with test Google account (must be in `allowed_emails`). Wait for redirect to Dashboard.
    3. Navigate to Goals tab → ASSERT exactly 5 goal cards visible (Dana Pernikahan, DP + Akad Kredit Xpander, Non-Budget Nikah, Dana Darurat, Buffer Cadangan — names from rencanaNames.ts).
    4. Navigate to Investasi tab → ASSERT exactly 3 investment rows visible (Reksadana Sukuk Sucorinvest Sharia, Emas Tabungan Pegadaian, Saham BMRI).
    5. Reload the page 5 times in quick succession (F5 × 5).
    6. After last reload, return to Goals → ASSERT still exactly 5 goals (no duplicates). Investasi → ASSERT still 3.
    7. **Studio cross-check (optional but stronger):** Open Supabase Studio with admin → query `SELECT COUNT(*) FROM user_seed_markers WHERE user_id = '<test_user_uuid>'` → expected 1.

    **Studio-only fallback (if no test email):**
    1. As admin in Studio, capture admin's user_id.
    2. Run: `DELETE FROM user_seed_markers WHERE user_id = <admin_uid>; DELETE FROM goals WHERE user_id = <admin_uid> AND name = ANY(ARRAY[...]); DELETE FROM investments WHERE user_id = <admin_uid> AND asset_name = ANY(ARRAY[...]);`
    3. In browser, refresh Dashboard. Inspect Network tab for `seed_rencana` RPC call → response = `true`.
    4. Reload 5x → subsequent calls (if any) get `false` from RPC (no duplicate inserts).
    5. Counts return to 5 goals + 3 investments + 1 marker.

    Append observations + counts + screenshots / network captures into `07-08-UAT.md` under `## UAT-3 (CONS-03)`.

    User confirms "uat-3-pass" with counts.
  </how-to-verify>
  <resume-signal>Type "uat-3-pass" with counts (or "uat-3-skip-with-pgtap-pass" if test email unavailable).</resume-signal>
  <acceptance_criteria>
    - Fresh signup OR admin-after-cleanup → exactly 5 goal rows with the 5 RENCANA names.
    - Exactly 3 investment rows with the 3 RENCANA names.
    - Exactly 1 user_seed_markers row for the user.
    - 5 reloads → counts unchanged (no duplicates).
    - If skipped: pgTAP test 07-seed-rencana.sql results from plan 07-04 deploy log are referenced as evidence (PASS-WITH-NOTES verdict acceptable).
    - 07-08-UAT.md `## UAT-3` section updated with counts + evidence.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: [BLOCKING] UAT-4 (UX-01) — Reset Seed Rencana clears per-user key + marker</name>
  <read_first>
    - .planning/phases/07-ui-data-consistency/07-CONTEXT.md (D-29 UAT-4; D-07.3..D-07.5 reset flow)
    - .planning/phases/07-ui-data-consistency/07-05-PLAN.md (SettingsTab doResetSeed extension)
  </read_first>
  <what-built>
    Plan 07-05 extended SettingsTab.doResetSeed to: delete RENCANA goals/investments → call reset_rencana_marker RPC → remove per-user localStorage key → cleanup legacy global key. UAT verifies all 4 effects.
  </what-built>
  <how-to-verify>
    **Pre-condition:** User must be in seeded state (5 goals + 3 investments + marker row + per-user localStorage key set).

    1. Login at production URL. Open DevTools → Application → Local Storage.
    2. **Pre-check:** key `rencana_seeded_<your_uid>` exists with value '1'.
    3. (Optional) In DevTools console, also set legacy key for cleanup test: `localStorage.setItem('rencana_seeded', '1')`.
    4. Navigate to Pengaturan (SettingsTab) → click "Reset Seed Rencana" button.
    5. Confirmation dialog appears → click "Ya, Reset" (or whatever the confirm button label is).
    6. Wait for toast `Seed direset. Buka Dashboard untuk inisialisasi ulang.`
    7. **Post-check 1:** key `rencana_seeded_<your_uid>` NOT in Local Storage (D-07.4 — key removed).
    8. **Post-check 2:** key `rencana_seeded` NOT in Local Storage (D-07.5 — legacy cleanup).
    9. **Post-check 3 (Studio):** `SELECT COUNT(*) FROM user_seed_markers WHERE user_id = '<your_uid>'` → expected 0 (D-07.3 — marker deleted).
    10. **Post-check 4:** Goals tab shows zero RENCANA goals; Investasi shows zero RENCANA investments (existing behavior preserved).
    11. **Re-seed verification:** Navigate to Dashboard → useRencanaInit fires → seed_rencana RPC called → counts return to 5+3+1.

    Append all post-checks + console screenshots into `07-08-UAT.md` under `## UAT-4 (UX-01)`.

    User confirms "uat-4-pass" with the 4 post-check states.
  </how-to-verify>
  <resume-signal>Type "uat-4-pass" with localStorage + DB observations.</resume-signal>
  <acceptance_criteria>
    - After clicking Reset Seed Rencana:
      - localStorage key `rencana_seeded_${user.id}` is GONE.
      - localStorage key `rencana_seeded` is GONE (legacy cleanup verified).
      - DB row `user_seed_markers` for the user is DELETED (count = 0).
      - 5 RENCANA goals + 3 RENCANA investments are DELETED.
    - Subsequent Dashboard load triggers useRencanaInit → counts back to 5+3+1.
    - 07-08-UAT.md `## UAT-4` section updated.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: [BLOCKING] UAT-5 (UX-02) — View-As CSV gate active</name>
  <read_first>
    - .planning/phases/07-ui-data-consistency/07-CONTEXT.md (D-29 UAT-5; D-23/D-24 exact wording)
    - .planning/phases/07-ui-data-consistency/07-07-PLAN.md (gate implementation)
  </read_first>
  <what-built>
    Plan 07-07 added Layer 1 (disabled+title) + Layer 1.5 (handler early-return) to Impor CSV button in TransactionsTab + InvestmentsTab. UAT verifies both buttons in both states (View-As ON / OFF).
  </what-built>
  <how-to-verify>
    **Pre-condition:** Logged in as admin with View-As capability + at least one other user in `allowed_emails`.

    1. Login as admin at production URL.
    2. Navigate to TransactionsTab → ASSERT Impor button is ENABLED (not disabled, no tooltip on hover).
    3. Navigate to InvestmentsTab → ASSERT Impor button is ENABLED.
    4. Open admin View-As dropdown (in Pengaturan or admin nav) → switch to viewing user X.
    5. Navigate to TransactionsTab → ASSERT Impor button is DISABLED (greyed-out). Hover → tooltip text exactly `'Tidak tersedia saat View-As'` (D-23 — verifier diff-matches).
    6. Try to click anyway (mouse) → no action (disabled).
    7. (Optional defense-in-depth check) Open DevTools console → run `document.querySelector('button:has(svg.lucide-upload)').click()` to bypass disabled — assert toast appears with text `'Impor CSV tidak tersedia saat View-As'` (D-24).
    8. Repeat steps 5-7 for InvestmentsTab Impor button.
    9. Exit View-As (back to admin's own context) → ASSERT both Impor buttons ENABLED again.

    Append all step observations + screenshots (button states + tooltip + toast) into `07-08-UAT.md` under `## UAT-5 (UX-02)`.

    User confirms "uat-5-pass" with screenshots.
  </how-to-verify>
  <resume-signal>Type "uat-5-pass" with screenshots.</resume-signal>
  <acceptance_criteria>
    - Admin's own context (no View-As): both Impor buttons enabled.
    - Admin View-As ON: both Impor buttons disabled with `title="Tidak tersedia saat View-As"` (exact wording).
    - DOM `<button>` element has `disabled` attribute when isViewAs.
    - Programmatic `.click()` (DevTools) shows toast `'Impor CSV tidak tersedia saat View-As'` (Layer 1.5).
    - Exit View-As: buttons re-enabled (no manual refresh needed).
    - 07-08-UAT.md `## UAT-5` section updated with screenshots covering all states.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Write 07-VERIFICATION.md + update STATE.md + update REQUIREMENTS.md traceability</name>
  <files>.planning/phases/07-ui-data-consistency/07-VERIFICATION.md, .planning/phases/07-ui-data-consistency/07-08-UAT.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/07-ui-data-consistency/07-08-UAT.md (the file you've been appending to in Tasks 2-6 — finalize structure)
    - .planning/phases/06-race-atomicity/06-VERIFICATION.md (analog — Phase 6 verdict format)
    - .planning/phases/06-race-atomicity/06-05-UAT.md (analog — UAT log format)
    - .planning/STATE.md (current — to update Position + Pending Todos + Deferred + Phase Summary)
    - .planning/REQUIREMENTS.md (current — to update traceability table marking CONS-01..UX-02 complete)
    - .planning/ROADMAP.md (Phase 7 success criteria 1-5 — verbatim mapping)
    - .planning/phases/07-ui-data-consistency/07-04-DEPLOY-LOG.md (for migrations applied verdict)
  </read_first>
  <action>
**Edit 1 — Finalize `07-08-UAT.md`:**

Ensure the file (built incrementally across Tasks 2-6) has this exact section structure:

```markdown
# Phase 7 UAT Log — Wave 5

**Date:** <YYYY-MM-DD>
**Production URL:** https://kantongpintar.vercel.app/
**Vercel deploy SHA:** <commit_sha from Task 1>
**Browser:** <e.g. Chrome via Browser-MCP / Playwright>
**Tester:** <user identifier>

## Pre-flight (Task 1)
<git status, build/lint/tsc results, push confirmation, deploy URL>

## UAT-1 (CONS-01)
<step-by-step observations, screenshot paths, DOM text captures, verdict>
**Verdict:** PASS / PASS-WITH-NOTES / FAIL

## UAT-2 (CONS-02)
<network capture, expected vs actual date, verdict>
**Verdict:** PASS / PASS-WITH-NOTES / FAIL

## UAT-3 (CONS-03)
<row counts, reload behavior, verdict>
**Verdict:** PASS / PASS-WITH-NOTES / FAIL

## UAT-4 (UX-01)
<localStorage states + Studio query results, verdict>
**Verdict:** PASS / PASS-WITH-NOTES / FAIL

## UAT-5 (UX-02)
<screenshots of both tabs in both states + Layer 1.5 toast, verdict>
**Verdict:** PASS / PASS-WITH-NOTES / FAIL

## Aggregate Verdict
PASS / PASS-WITH-NOTES / FAIL — <one-line summary>
```

**Edit 2 — Write `07-VERIFICATION.md`:**

Mirror Phase 6 VERIFICATION.md structure. Map each ROADMAP success criterion to evidence:

```markdown
# Phase 7 Verification: UI/Data Consistency

**Date:** <YYYY-MM-DD>
**Verdict:** PASS / PASS-WITH-NOTES / FAIL
**Plans completed:** 07-01, 07-02, 07-03, 07-04, 07-05, 07-06, 07-07
**Migrations applied:** 0022, 0023, 0024 (Studio paste — see 07-04-DEPLOY-LOG.md)

## ROADMAP Success Criteria

### SC#1 (CONS-01): Goal target Rp 10jt + 60% × Rp 18jt linked → progress 100% + badge "Tercapai" + withdraw helper text + split error MESSAGE
**Evidence:** UAT-1 (07-08-UAT.md). Migration 0024 backfill applied (DEPLOY-LOG candidates_after = 0).
**Verdict:** PASS / NOTE: <if applicable>

### SC#2 (CONS-02): pnpm lint passes with no-restricted-syntax rule + Refresh Harga writes WIB date
**Evidence:** plan 07-06 build verification (`pnpm lint` exit 0); UAT-2 network capture.
**Verdict:** PASS / NOTE

### SC#3 (CONS-03): New user signup → 5+3 atomic seed; reload 5x no duplicates; mid-failure rollback
**Evidence:** plan 07-04 pgTAP results (07-seed-rencana.sql ≥7 PASS, 0 FAIL); UAT-3 production observation.
**Verdict:** PASS / NOTE: <e.g. UAT-3 ran via admin-cleanup variant if test email unavailable>

### SC#4 (UX-01): Reset Seed → per-user key cleared, marker deleted, re-seed on reload
**Evidence:** UAT-4 4 post-checks all confirmed.
**Verdict:** PASS / NOTE

### SC#5 (UX-02): Admin View-As → Impor CSV disabled with tooltip "Tidak tersedia saat View-As"; exit → re-enabled
**Evidence:** UAT-5 screenshots in TransactionsTab + InvestmentsTab.
**Verdict:** PASS / NOTE

## Threats Mitigated (STRIDE)

T-07-01..T-07-28 — see threat_model blocks across plans 07-01..07-07. All HIGH-severity threats verified mitigated either via pgTAP function-def proofs (search_path, SECURITY DEFINER, FOR UPDATE) or via UAT exact-string matches.

## Deferred Items (new this phase)

<List anything NOT shipped — e.g. UAT-3 fresh-signup variant if skipped due to no test email; document explicitly>

## Sign-off

Phase 7 closes with verdict: <PASS / PASS-WITH-NOTES / FAIL>.
Ready for Phase 8: Dev Hygiene (DEV-02..DEV-04, no DB migrations).
```

**Edit 3 — Update `.planning/STATE.md`:**

Update the following sections:
- **Frontmatter:** `progress.completed_phases` → 3 (was 2 — Phase 5+6+7); `progress.completed_plans` → 14 + 8 = 17 (or actual current count after this plan); `progress.percent` recompute; `last_activity` → today.
- **`## Current Position`:** "Phase: 07 (ui-data-consistency) — COMPLETE". "Phase: 08 (dev-hygiene) — Not started".
- **`## v1.1 Phase Summary` table:** Phase 7 status → "**Complete (PASS / PASS-WITH-NOTES)**" with migrations 0022+0023+0024.
- **`## Accumulated Context` → `### Decisions (v1.1 execution-time)`:** add new entries (anything notable from Phase 7 execution — e.g. "5+3 seed counts vs CONTEXT.md 5+5 drift corrected at plan-time"; "Studio paste de-facto migration channel continues from Phase 4-6, no change").
- **`## Deferred Items`:** add new rows for any UAT-3 fresh-signup skip or UAT-2 timezone test ran-before-WIB-midnight conditions.
- **`## Session Continuity`:** update `Last session` timestamp + `Stopped at: Phase 7 complete` + `Resume command: /gsd-plan-phase 8`.

**Edit 4 — Update `.planning/REQUIREMENTS.md` traceability table:**

In the table starting around line 88, update these 5 rows:
- `| CONS-01 | Phase 7 — UI/Data Consistency | 07-01, 07-03, 07-05 | shipped |`
- `| CONS-02 | Phase 7 — UI/Data Consistency | 07-06 | shipped |`
- `| CONS-03 | Phase 7 — UI/Data Consistency | 07-02, 07-05 | shipped |`
- `| UX-01 | Phase 7 — UI/Data Consistency | 07-05 | shipped |`
- `| UX-02 | Phase 7 — UI/Data Consistency | 07-07 | shipped |`

Also update the requirements list at top: change `[ ]` to `[x]` for CONS-01, CONS-02, CONS-03, UX-01, UX-02.

**Final:** Confirm all 4 files saved. Run `git status` to confirm modifications staged.
  </action>
  <verify>
    <automated>grep -c "PASS\|PASS-WITH-NOTES\|FAIL" .planning/phases/07-ui-data-consistency/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-ui-data-consistency/07-08-UAT.md` exists with 5 UAT sections (UAT-1..5) plus Pre-flight + Aggregate Verdict.
    - File `.planning/phases/07-ui-data-consistency/07-VERIFICATION.md` exists.
    - VERIFICATION.md contains exact substring `### SC#1 (CONS-01)` AND `### SC#2 (CONS-02)` AND `### SC#3 (CONS-03)` AND `### SC#4 (UX-01)` AND `### SC#5 (UX-02)` — all 5 SCs mapped.
    - VERIFICATION.md contains a top-level `**Verdict:**` line (PASS / PASS-WITH-NOTES / FAIL) at the file header.
    - STATE.md `current_position` says "Phase: 07 ... COMPLETE" and "Phase: 08 ... Not started".
    - STATE.md frontmatter `completed_phases` reflects Phase 7 done.
    - STATE.md `## v1.1 Phase Summary` table row for Phase 7 says "**Complete**" or "**Complete (PASS-WITH-NOTES)**".
    - REQUIREMENTS.md traceability table rows for CONS-01, CONS-02, CONS-03, UX-01, UX-02 contain `shipped` (status column).
    - REQUIREMENTS.md top-level checkboxes for these 5 requirements changed from `[ ]` to `[x]`.
  </acceptance_criteria>
  <done>4 files written/updated; verdict captured in VERIFICATION.md; state advanced to Phase 8 ready; traceability marked shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → GitHub → Vercel → Production users | The push-to-master step is the trust boundary. After this, all users see the new code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-29 | I (Information Disclosure) | UAT log captures | mitigate | UAT log captures only the user's own data + DOM text. The exact-match string assertions ('Tidak tersedia saat View-As', 'Saldo kas tersedia: ...') do not include PII. Studio queries in UAT-3/UAT-4 use the tester's own user_id only. |
| T-07-30 | T (Tampering) | post-deploy regression | mitigate | All 5 UATs MUST pass before VERIFICATION verdict can be PASS. Any FAIL on UAT downgrades verdict to FAIL or PASS-WITH-NOTES (latter only if the failure is environmental, e.g., UAT-3 skipped due to no test email — Studio fallback documented). |
| T-07-31 | T (Tampering) | concurrent admin actions during UAT | accept | UATs run sequentially in the same browser session; no concurrent modification expected. |
</threat_model>

<verification>
After all 7 tasks complete:
- `grep -c "PASS\|PASS-WITH-NOTES" .planning/phases/07-ui-data-consistency/07-VERIFICATION.md` returns ≥ 6 (1 top-level + 5 SCs).
- `grep -c "FAIL" .planning/phases/07-ui-data-consistency/07-VERIFICATION.md` is acceptable in count IF accompanied by `PASS-WITH-NOTES` overall verdict (e.g., one SC verified via pgTAP only, downgraded to NOTE).
- `grep -c '\[x\]' .planning/REQUIREMENTS.md` increased by 5 (the 5 phase-7 requirements).
- STATE.md frontmatter advance: `completed_phases >= 3`.
</verification>

<success_criteria>
- Production deploy GREEN at https://kantongpintar.vercel.app/.
- All 5 ROADMAP SCs mapped to evidence (PASS / PASS-WITH-NOTES) in 07-VERIFICATION.md.
- 07-08-UAT.md captures all 5 UAT scenarios with screenshots / DOM text / network / Studio outputs.
- STATE.md updated: Phase 7 complete, Phase 8 ready.
- REQUIREMENTS.md traceability marks CONS-01, CONS-02, CONS-03, UX-01, UX-02 shipped.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-data-consistency/07-08-SUMMARY.md` documenting:
- Phase 7 verdict (PASS / PASS-WITH-NOTES / FAIL)
- Plans shipped: 07-01..07-07 (8 SUMMARY files)
- Migrations live: 0022, 0023, 0024
- Files modified across phase: ~16 (3 SQL + 3 pgTAP + 1 deploy log + 7 frontend files + verification + UAT)
- Decisions implemented: D-01..D-29 (full set from CONTEXT.md)
- Threats addressed: T-07-01..T-07-31 (full STRIDE set across all plans)
- Resume: `/gsd-plan-phase 8` for Phase 8: Dev Hygiene.
</output>
