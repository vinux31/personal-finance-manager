---
phase: 08-dev-hygiene
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/seed.sql
  - .planning/PROJECT.md
autonomous: true
requirements:
  - DEV-03
  - DEV-04

must_haves:
  truths:
    - "File supabase/seed.sql ada di filesystem"
    - "supabase/config.toml line 65 sql_paths = ['./seed.sql'] tetap tidak berubah"
    - "PROJECT.md Context section punya bullet Performance recentTx dengan threshold 50k rows"
  artifacts:
    - path: "supabase/seed.sql"
      provides: "Dev seed placeholder agar supabase db reset tidak warn"
      contains: "Dev seed (empty)"
    - path: ".planning/PROJECT.md"
      provides: "Performance note recentTx query"
      contains: "transactions_date_idx"
  key_links:
    - from: "supabase/config.toml line 65"
      to: "supabase/seed.sql"
      via: "sql_paths reference"
      pattern: "sql_paths = \\[\"./seed.sql\"\\]"
---

<objective>
Dua cleanup item kecil yang tidak saling bergantung:

1. **DEV-03**: Buat file `supabase/seed.sql` (kosong dengan dev comment) sehingga `supabase db reset` lokal tidak warn/fail karena missing file yang sudah direferensikan di config.toml:65.

2. **DEV-04**: Tambah bullet Performance ke section `## Context` di `.planning/PROJECT.md` — mendokumentasikan bahwa `recentTx` dashboard query cukup untuk < 50k rows dan kapan harus migrasi ke materialized view.

Purpose: Menghilangkan misconfiguration (DEV-03) dan mencatat future trigger for performance migration (DEV-04) agar future maintainer punya baseline keputusan.

Output: File `supabase/seed.sql` baru + `.planning/PROJECT.md` dengan bullet Performance tambahan.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Lokasi tepat untuk kedua perubahan -->

supabase/config.toml baris 60-65 (tidak diubah, hanya referensi):
```toml
[db.seed]
enabled = true
sql_paths = ["./seed.sql"]
```
Path `"./seed.sql"` relative dari `supabase/` directory → file target = `supabase/seed.sql`

.planning/PROJECT.md baris 74-81 (Context section — append setelah baris Migrations):
```markdown
- **Stack:** React 19 + TypeScript + ...
- **Auth:** Google OAuth only, ...
- **DB:** Supabase dengan Row Level Security ...
- **RPC:** `mark_bill_paid`, ...
- **Deployment:** Vercel auto-deploy ...
- **Existing recurring data:** Tabel `recurring_templates` ...
- **Multi-user:** Admin bisa view-as ...
- **Migrations:** 0001 → 0021 (21 migrations applied ...
```
Bullet baru ditambahkan tepat setelah baris `Migrations` (line 81), sebelum blank line menuju `## Constraints`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat supabase/seed.sql (DEV-03)</name>
  <files>supabase/seed.sql</files>

  <read_first>
    - supabase/config.toml baris 60-65 — konfirmasi path referensi `"./seed.sql"` agar tidak salah lokasi
  </read_first>

  <action>
Buat file baru `supabase/seed.sql` (di project root, bukan `supabase/supabase/seed.sql`) dengan isi persis:

```sql
-- Dev seed (empty). Add sample data here for local development.
```

Single line, tidak ada baris lain. File harus berada di `supabase/seed.sql` karena config.toml menggunakan path `"./seed.sql"` relative dari directory `supabase/`.

Jangan ubah `supabase/config.toml` — referensi `sql_paths = ["./seed.sql"]` di baris 65 tetap seperti adanya.
  </action>

  <verify>
    <automated>test -f "/c/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/pfm-web/supabase/seed.sql" && echo "EXISTS" || echo "MISSING"</automated>
  </verify>

  <acceptance_criteria>
    - File `supabase/seed.sql` ada di filesystem (bukan `supabase/supabase/seed.sql`)
    - File berisi string `Dev seed (empty)`
    - `supabase/config.toml` baris 65 masih berisi `sql_paths = ["./seed.sql"]` tidak berubah
  </acceptance_criteria>

  <done>
    - `supabase/seed.sql` ada dan berisi comment minimal
    - `supabase/config.toml` tidak berubah
    - DEV-03 requirement closed
  </done>
</task>

<task type="auto">
  <name>Task 2: Tambah performance note ke PROJECT.md (DEV-04)</name>
  <files>.planning/PROJECT.md</files>

  <read_first>
    - .planning/PROJECT.md — baca seluruh file untuk menemukan posisi tepat baris `Migrations` di section `## Context` (sekitar baris 81)
  </read_first>

  <action>
Edit `.planning/PROJECT.md` — tambahkan bullet baru tepat setelah baris `**Migrations:**` di section `## Context`.

Baris yang ditambahkan (wording exact, jangan diubah):
```markdown
- **Performance:** Dashboard `recentTx` query pakai `useTransactions({ limit: 5 })` + index `transactions_date_idx` — sufficient untuk dataset < 50k rows; pertimbangkan migrasi ke materialized view jika dataset user aktif melewati threshold tersebut.
```

Sisipkan setelah baris:
```
- **Migrations:** 0001 → 0021 (21 migrations applied to cloud — v1.1 Phase 5 added 0017+0018, v1.1 Phase 6 added 0019+0020+0021)
```

Dan sebelum blank line yang memisahkan `## Context` dari `## Constraints`.

Format mengikuti pola existing: `- **Bold key:** sentence` — konsisten dengan semua bullet lain di section ini.

Jangan ubah bagian lain di file. Jangan update timestamp `*Last updated:` di baris terakhir — itu diupdate oleh `/gsd-transition`.
  </action>

  <verify>
    <automated>grep -n "transactions_date_idx" "/c/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/pfm-web/.planning/PROJECT.md"</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/PROJECT.md` mengandung string `transactions_date_idx`
    - `.planning/PROJECT.md` mengandung string `50k rows`
    - `.planning/PROJECT.md` mengandung string `materialized view`
    - Bullet `**Performance:**` berada di section `## Context` (setelah baris `**Migrations:**` dan sebelum `## Constraints`)
    - `grep -c "Performance" .planning/PROJECT.md` mengembalikan angka >= 1
  </acceptance_criteria>

  <done>
    - Bullet `**Performance:**` ada di section `## Context` PROJECT.md
    - Wording exact sesuai D-08: "Dashboard `recentTx` query pakai `useTransactions({ limit: 5 })` + index `transactions_date_idx` — sufficient untuk dataset < 50k rows; pertimbangkan migrasi ke materialized view jika dataset user aktif melewati threshold tersebut."
    - DEV-04 requirement closed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Deskripsi |
|----------|-----------|
| supabase/seed.sql | File config lokal — tidak dieksekusi di production |
| .planning/PROJECT.md | File dokumentasi — tidak ada data user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Information Disclosure | supabase/seed.sql | mitigate | File kosong dengan single comment — tidak ada credential, tidak ada data user real, tidak ada PII. File ini untuk dev environment lokal saja. Pastikan tidak menambahkan data sensitif (password, API key, email real) ke file ini. |
| T-08-03 | Information Disclosure | .planning/PROJECT.md | accept | File dokumentasi internal planning — tidak ada PII user, tidak ada credential. Hanya metadata arsitektur teknis. |
</threat_model>

<verification>
1. `test -f supabase/seed.sql && echo EXISTS` — harus print "EXISTS"
2. `grep "Dev seed (empty)" supabase/seed.sql` — harus return baris comment
3. `grep -c "sql_paths" supabase/config.toml` — harus return 1 (tidak berubah)
4. `grep "transactions_date_idx" .planning/PROJECT.md` — harus return baris Performance bullet
5. `grep "50k rows" .planning/PROJECT.md` — harus return baris Performance bullet
</verification>

<success_criteria>
- `supabase/seed.sql` ada dengan content `-- Dev seed (empty). Add sample data here for local development.`
- `supabase/config.toml:65` `sql_paths = ["./seed.sql"]` tidak berubah
- `.planning/PROJECT.md` section Context punya bullet `**Performance:**` dengan wording lengkap termasuk `transactions_date_idx`, `50k rows`, `materialized view`
- DEV-03 dan DEV-04 requirements closed
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/08-dev-hygiene/08-02-SUMMARY.md` menggunakan template `@/root/.claude/get-shit-done/templates/summary.md`.
</output>
