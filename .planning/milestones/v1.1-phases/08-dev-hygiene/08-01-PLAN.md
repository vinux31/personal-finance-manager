---
phase: 08-dev-hygiene
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tabs/ReportsTab.tsx
autonomous: true
requirements:
  - DEV-02

must_haves:
  truths:
    - "ReportsTab.tsx tidak mengandung type cast 'as { category?: string }'"
    - "PieLabelRenderProps diimport dari 'recharts' di ReportsTab.tsx"
    - "tsc --noEmit pass tanpa error baru"
    - "Pie chart label tetap menampilkan nama kategori"
  artifacts:
    - path: "src/tabs/ReportsTab.tsx"
      provides: "Recharts pie label handler properly-typed"
      contains: "PieLabelRenderProps"
  key_links:
    - from: "src/tabs/ReportsTab.tsx line 199"
      to: "Recharts PieLabelRenderProps.name"
      via: "label prop"
      pattern: "PieLabelRenderProps"
    - from: "src/tabs/ReportsTab.tsx line 212"
      to: "Recharts PieLabelRenderProps.name"
      via: "label prop"
      pattern: "PieLabelRenderProps"
---

<objective>
Fix type cast unsafe `as { category?: string }` di dua Pie chart label handlers pada `ReportsTab.tsx` (baris 199 dan 212). Ganti ke `PieLabelRenderProps` yang merupakan proper type dari Recharts — survive major upgrade tanpa breakage.

Purpose: Menghilangkan `any`-equivalent type cast yang bisa pecah saat Recharts major upgrade mengubah internal shape. `e.name` populated dari `nameKey="category"` yang sudah terpasang — output visual tidak berubah.

Output: `src/tabs/ReportsTab.tsx` dengan import `PieLabelRenderProps` dan dua label handler yang properly typed. `tsc --noEmit` pass.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types dari Recharts yang dipakai executor. Tidak perlu eksplorasi node_modules. -->

Dari recharts (verified di node_modules/recharts/types/index.d.ts):
```typescript
export interface PieLabelRenderProps extends PieSectorDataItem {
  name?: string | number;    // populated dari nameKey prop
  // ... other fields
}
```

Dari ReportsTab.tsx (current import block baris 2-5):
```tsx
import {
  BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer,
  PieChart, Pie, Cell,
} from 'recharts'
```

Dari ReportsTab.tsx (current label props baris 199 dan 212 — kedua identik):
```tsx
label={(e) => String((e as { category?: string }).category ?? '')}
```

Kedua Pie sudah memiliki `nameKey="category"` — artinya `e.name` = nilai field `category` dari data. Tidak perlu mapping tambahan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Recharts pie label type cast (DEV-02)</name>
  <files>src/tabs/ReportsTab.tsx</files>

  <read_first>
    - src/tabs/ReportsTab.tsx — baca seluruh file untuk melihat import block saat ini (baris 2-5) dan kedua Pie label props (baris 199 dan 212)
  </read_first>

  <action>
Lakukan dua perubahan di `src/tabs/ReportsTab.tsx`:

**Perubahan 1 — Import block (baris 2-5):**

Ganti:
```tsx
import {
  BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer,
  PieChart, Pie, Cell,
} from 'recharts'
```

Dengan (tambah `type PieLabelRenderProps` di baris baru setelah `Cell,`):
```tsx
import {
  BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer,
  PieChart, Pie, Cell,
  type PieLabelRenderProps,
} from 'recharts'
```

**Perubahan 2 — Label prop baris 199 (Pie Pengeluaran per Kategori):**

Ganti:
```tsx
label={(e) => String((e as { category?: string }).category ?? '')}
```

Dengan:
```tsx
label={(e: PieLabelRenderProps) => String(e.name ?? '')}
```

**Perubahan 3 — Label prop baris 212 (Pie Pemasukan per Kategori):**

Ganti (identik dengan baris 199):
```tsx
label={(e) => String((e as { category?: string }).category ?? '')}
```

Dengan:
```tsx
label={(e: PieLabelRenderProps) => String(e.name ?? '')}
```

Jangan ubah bagian lain di file ini. Tidak ada perubahan logika — `e.name` menghasilkan string yang sama dengan `(e as {...}).category` karena Recharts memetakan `nameKey` field ke `.name` di label callback.
  </action>

  <verify>
    <automated>cd "/c/Users/Administrator/OneDrive - PT Pertamina (Persero)/Desktop/pfm-web" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `src/tabs/ReportsTab.tsx` line 2-6 berisi `type PieLabelRenderProps,` dalam import block dari 'recharts'
    - `src/tabs/ReportsTab.tsx` TIDAK mengandung string `as { category?: string }` (dapat diverifikasi: grep tidak menemukan hasil)
    - `src/tabs/ReportsTab.tsx` baris 199 mengandung `(e: PieLabelRenderProps) => String(e.name ?? '')`
    - `src/tabs/ReportsTab.tsx` baris sekitar 212 mengandung `(e: PieLabelRenderProps) => String(e.name ?? '')`
    - `npx tsc --noEmit` exit code 0 (tidak ada error TypeScript baru)
  </acceptance_criteria>

  <done>
    - Import `type PieLabelRenderProps` dari 'recharts' sudah ada
    - Kedua `label` prop Pie tidak lagi pakai `as { category?: string }` cast
    - `tsc --noEmit` pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Deskripsi |
|----------|-----------|
| Client render | Label callback dijalankan di browser — tidak ada data sensitif |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | ReportsTab.tsx label callback | accept | Pure type cleanup — tidak ada data path baru, tidak ada input dari user, tidak ada akses ke data selain yang sudah di-render. Risiko nol. |
</threat_model>

<verification>
1. `grep -n "as { category" src/tabs/ReportsTab.tsx` — harus return KOSONG (tidak ada hasil)
2. `grep -n "PieLabelRenderProps" src/tabs/ReportsTab.tsx` — harus return minimal 3 baris (1 import + 2 usage)
3. `npx tsc --noEmit` — exit code 0
</verification>

<success_criteria>
- File `src/tabs/ReportsTab.tsx` tidak mengandung `as { category?: string }` cast
- Import `type PieLabelRenderProps` dari 'recharts' ada di file
- Kedua label prop menggunakan `(e: PieLabelRenderProps) => String(e.name ?? '')`
- `tsc --noEmit` pass (exit 0)
- DEV-02 requirement closed
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/08-dev-hygiene/08-01-SUMMARY.md` menggunakan template `@/root/.claude/get-shit-done/templates/summary.md`.
</output>
