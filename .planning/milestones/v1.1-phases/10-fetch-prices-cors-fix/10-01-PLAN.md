---
phase: 10-fetch-prices-cors-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/fetch-prices/index.ts
autonomous: true
requirements: [SEC-01, CONS-02]
must_haves:
  truths:
    - "ALLOWED_ORIGINS Set di supabase/functions/fetch-prices/index.ts berisi 3 entries: kantongpintar.app, www.kantongpintar.app, kantongpintar.vercel.app"
    - "Komentar di file tetap akurat (no stale references ke 'preview deploys not in this list')"
    - "TypeScript syntax tetap valid (file parse-able oleh Deno tooling)"
  artifacts:
    - path: "supabase/functions/fetch-prices/index.ts"
      provides: "Updated CORS allowlist mendukung Vercel production domain"
      contains: "https://kantongpintar.vercel.app"
  key_links:
    - from: "supabase/functions/fetch-prices/index.ts ALLOWED_ORIGINS"
      to: "corsFor() function logic (line 12-23)"
      via: "ALLOWED_ORIGINS.has(origin) check"
      pattern: "ALLOWED_ORIGINS\\.has\\(origin\\)"
---

<objective>
Tambah `https://kantongpintar.vercel.app` ke set `ALLOWED_ORIGINS` di edge function `fetch-prices`. Ini adalah fix 1-baris murni — satu-satunya penyebab production "Refresh Harga" gagal dengan CORS rejection. Source-side code (JWT enforcement Phase 5 SEC-01, todayISO write-path Phase 7 CONS-02) sudah benar.

Purpose: Un-block live verification SEC-01 + CONS-02 di production domain `kantongpintar.vercel.app`. Tanpa fix ini, browser request "Refresh Harga" ditolak di pre-flight OPTIONS oleh `corsFor()` yang fallback ke `kantongpintar.app` saat origin tidak match — menyebabkan browser block respons.

Output: 1 file modified + 1 git commit. Plan 10-02 akan deploy + verify live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@supabase/functions/fetch-prices/index.ts

<interfaces>
<!-- Current state of ALLOWED_ORIGINS Set (lines 7-10 of supabase/functions/fetch-prices/index.ts): -->

```typescript
const ALLOWED_ORIGINS = new Set<string>([
  'https://kantongpintar.app',
  'https://www.kantongpintar.app',
])
```

<!-- Target state after this task: -->

```typescript
const ALLOWED_ORIGINS = new Set<string>([
  'https://kantongpintar.app',
  'https://www.kantongpintar.app',
  'https://kantongpintar.vercel.app',
])
```

<!-- Key consumer (corsFor function, lines 12-23 — DO NOT modify, only ALLOWED_ORIGINS Set changes): -->

```typescript
function corsFor(req: Request): Record<string, string> {
  const origin = req.headers.get('Origin') ?? ''
  const allowed = ALLOWED_ORIGINS.has(origin) ? origin : 'https://kantongpintar.app'
  return {
    'Access-Control-Allow-Origin': allowed,
    'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
    'Access-Control-Allow-Methods': 'POST, OPTIONS',
    'Vary': 'Origin',  // CDN cache poisoning mitigation — kept intact
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Vercel production domain ke ALLOWED_ORIGINS Set</name>
  <files>supabase/functions/fetch-prices/index.ts</files>
  <read_first>
    - supabase/functions/fetch-prices/index.ts (full file — fokus pada lines 1-23 untuk CORS block; lines 24+ adalah business logic yang TIDAK boleh disentuh)
  </read_first>
  <action>
Edit `supabase/functions/fetch-prices/index.ts`. Hanya 1 perubahan:

**Sebelum (lines 7-10):**
```typescript
const ALLOWED_ORIGINS = new Set<string>([
  'https://kantongpintar.app',
  'https://www.kantongpintar.app',
])
```

**Sesudah (lines 7-11):**
```typescript
const ALLOWED_ORIGINS = new Set<string>([
  'https://kantongpintar.app',
  'https://www.kantongpintar.app',
  'https://kantongpintar.vercel.app',
])
```

Tambah literal string `'https://kantongpintar.vercel.app',` (dengan trailing comma, indented 2 spaces, quote style match existing single-quote) sebagai entry ke-3 di array argumen ke `Set<string>([...])`. Posisi: setelah baris `'https://www.kantongpintar.app',` dan sebelum baris penutup `])`.

**Komentar di lines 4-6 — UPDATE juga:**

Sebelum:
```typescript
// ---------- CORS allowlist ----------
// Per-domain CORS. Vercel preview deploys are NOT in this list — test edge functions
// only via `supabase functions serve` locally OR via production. (Per research integration risk note.)
```

Sesudah:
```typescript
// ---------- CORS allowlist ----------
// Per-domain CORS. Production domain `kantongpintar.vercel.app` (Vercel) included.
// Vercel preview deploys (e.g. `<branch>-<hash>.vercel.app`) are NOT in this list — test edge
// functions only via `supabase functions serve` locally OR via production.
```

Alasan komentar update: stale comment akan menimbulkan kebingungan karena baris di bawah secara eksplisit memasukkan domain `*.vercel.app` (production). Klarifikasi: yang excluded adalah PREVIEW deploys, bukan production Vercel domain.

**JANGAN sentuh:**
- Lines 1-3 (imports).
- Lines 12-23 (corsFor function — tidak perlu logic change, fallback `'https://kantongpintar.app'` tetap aman karena hanya kena saat origin tidak ada di Set).
- Lines 24+ (Types, Handler, Helpers — semua business logic tetap utuh).

**Setelah edit, CONFIRM dengan `grep` (jangan run di Bash, cukup mental check):**
- File mengandung exact string `'https://kantongpintar.vercel.app',` tepat 1 kali.
- File mengandung exact string `'https://kantongpintar.app',` tepat 1 kali (di dalam Set, NOT di fallback line 14).
- File mengandung `ALLOWED_ORIGINS.has(origin) ? origin : 'https://kantongpintar.app'` masih intact (untreated fallback baseline).
  </action>
  <verify>
    <automated>grep -c "'https://kantongpintar.vercel.app'," supabase/functions/fetch-prices/index.ts | xargs -I{} test {} -eq 1 && grep -c "ALLOWED_ORIGINS.has(origin) ? origin : 'https://kantongpintar.app'" supabase/functions/fetch-prices/index.ts | xargs -I{} test {} -eq 1 && grep -c "Vercel preview deploys" supabase/functions/fetch-prices/index.ts | xargs -I{} test {} -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'https://kantongpintar.vercel.app',"` di file returns exactly `1`.
    - `grep -c "'https://kantongpintar.app',"` returns exactly `1` (entry asli tetap utuh).
    - `grep -c "'https://www.kantongpintar.app',"` returns exactly `1` (entry www tetap utuh).
    - `grep -n "new Set<string>"` matches line 7 (struktur Set tidak berubah).
    - `grep -c "ALLOWED_ORIGINS.has(origin) ? origin : 'https://kantongpintar.app'"` returns `1` (fallback logic intact, business logic untouched).
    - Komentar di header CORS block disebut `Vercel preview deploys` (eksplisit klarifikasi production vs preview).
    - Total line count file naik dari 178 → 179 (hanya tambah 1 baris di Set).
  </acceptance_criteria>
  <done>ALLOWED_ORIGINS Set sekarang mengandung 3 domain (production .app, www .app, vercel .app). File siap di-commit di Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit perubahan dengan message yang reference Phase 10 + audit</name>
  <files>—</files>
  <read_first>
    - supabase/functions/fetch-prices/index.ts (verify Task 1 changes already applied)
    - .planning/ROADMAP.md (Phase 10 entry untuk konteks commit message)
  </read_first>
  <action>
Setelah Task 1 selesai, jalankan git operations berikut:

1. Verify hanya 1 file yang berubah:
```bash
git status --short supabase/functions/fetch-prices/index.ts
```
Expected output: ` M supabase/functions/fetch-prices/index.ts` (modified, no other files).

2. Stage file:
```bash
git add supabase/functions/fetch-prices/index.ts
```

3. Commit dengan message berikut (gunakan HEREDOC untuk format aman):
```bash
git commit -m "$(cat <<'EOF'
feat(10): add kantongpintar.vercel.app to fetch-prices CORS allowlist

ALLOWED_ORIGINS Set di edge function fetch-prices sebelumnya hanya berisi
kantongpintar.app + www.kantongpintar.app. Production deployment di Vercel
mengakses dari kantongpintar.vercel.app — corsFor() fallback ke
kantongpintar.app menyebabkan browser block respons karena Origin mismatch.

Fix: tambah https://kantongpintar.vercel.app sebagai entry ke-3.
Komentar di-update untuk klarifikasi: yang excluded adalah Vercel PREVIEW
deploys (per-branch URL), bukan production Vercel domain.

Source-side code untuk SEC-01 (JWT enforcement) + CONS-02 (todayISO write-path)
sudah benar sejak Phase 5/Phase 7 — fix ini purely infra/CORS config.

Plan 10-02 akan deploy via supabase functions deploy + run live UAT.

Refs: v1.1-MILESTONE-AUDIT.md (tech_debt verdict), ROADMAP.md Phase 10.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

4. Verify commit succeeded:
```bash
git log -1 --oneline
```
Expected: format `<sha> feat(10): add kantongpintar.vercel.app to fetch-prices CORS allowlist`.

5. Verify nothing extra was committed:
```bash
git show --stat HEAD
```
Expected: hanya `supabase/functions/fetch-prices/index.ts` di file list dengan `+4 -2` atau serupa (3 baris konten + komentar update).

**JANGAN push.** Push akan dilakukan setelah deploy verified di Plan 10-02. Solo dev workflow — local commit dulu, push setelah live verification.
  </action>
  <verify>
    <automated>git log -1 --pretty=format:'%s' | grep -c "feat(10): add kantongpintar.vercel.app to fetch-prices CORS allowlist" | xargs -I{} test {} -eq 1 && git diff HEAD~1 HEAD --name-only | grep -c "supabase/functions/fetch-prices/index.ts" | xargs -I{} test {} -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=format:'%s'` returns exactly `feat(10): add kantongpintar.vercel.app to fetch-prices CORS allowlist`.
    - `git diff HEAD~1 HEAD --name-only` returns exactly 1 file: `supabase/functions/fetch-prices/index.ts`.
    - `git diff HEAD~1 HEAD --stat` shows ~4 insertions, ~2 deletions (1 baris CORS + komentar update).
    - `git status` reports clean working tree (no uncommitted changes).
    - Commit message body mengandung referensi `Phase 5`/`SEC-01` dan `Phase 7`/`CONS-02` (untuk audit trail).
    - Commit message body mengandung referensi `v1.1-MILESTONE-AUDIT.md`.
    - **TIDAK ada `git push`** — push diserahkan ke Plan 10-02 setelah live UAT pass.
  </acceptance_criteria>
  <done>Source change committed locally dengan message yang fully describe perubahan + konteks audit. Repo siap untuk Plan 10-02 deploy + verify gate.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → edge function fetch-prices | CORS pre-flight (OPTIONS) + actual POST request crosses untrusted-network boundary; allowlist controls which origins receive valid CORS headers |
| Edge function → Supabase auth (auth.getUser) | JWT verification crosses platform boundary — primary auth gate (already shipped Phase 5 SEC-01); CORS allowlist is secondary defense |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing (cross-origin attack via expanded allowlist) | ALLOWED_ORIGINS Set | mitigate | Hanya tambah 1 domain spesifik (`https://kantongpintar.vercel.app`) yang dimiliki/dikontrol developer. Tidak ada wildcard (`*.vercel.app`), tidak ada Vercel preview deploys (per-branch hash URL). Komentar di-update eksplisit untuk dokumentasi keputusan ini agar reviewer masa depan tidak loosen accidentally. |
| T-10-02 | Tampering (CDN cache poisoning) | Edge function response headers | mitigate | `Vary: Origin` header sudah present di `corsFor()` line 21 — TIDAK disentuh oleh Plan 10-01. Setiap origin dapat cached response berbeda; CDN tidak akan serve response untuk Origin A ke client dengan Origin B. |
| T-10-03 | Elevation of Privilege (regression breaking JWT enforcement during deploy) | Plan 10-02 deploy step | mitigate | Plan 10-02 wajib include curl-based SEC-01 regression smoke test — `curl -H 'Origin: https://kantongpintar.vercel.app' -H 'Content-Type: application/json' -d '{"investments":[]}' <function-url>` (NO Authorization header) MUST return 401. Verifies CORS allowlist change tidak accidentally bypass JWT gate. |
| T-10-04 | Information Disclosure (stale comment misleading future reviewer) | Komentar lines 4-6 | mitigate | Komentar di-update bersamaan dengan ALLOWED_ORIGINS change. Tidak boleh leave stale "Vercel deploys are NOT in this list" — sekarang akurat menyebut "preview deploys" (bukan "all Vercel deploys"). |

</threat_model>

<verification>
- File `supabase/functions/fetch-prices/index.ts` mengandung 3 entries di ALLOWED_ORIGINS Set.
- Komentar header CORS block updated untuk klarifikasi production vs preview.
- 1 commit `feat(10):` di local git history (HEAD).
- Working tree clean.
- Tidak ada perubahan di file lain (business logic + auth gate untouched).
- Tidak ada `git push` — itu kerjaan Plan 10-02.
</verification>

<success_criteria>
1. `grep` confirms `'https://kantongpintar.vercel.app',` ada 1x di file.
2. `git log` confirms 1 commit baru dengan subject `feat(10): add kantongpintar.vercel.app to fetch-prices CORS allowlist`.
3. `git diff` confirms hanya 1 file yang berubah dengan ~4 insertions / ~2 deletions.
4. Tidak ada regression di code lain (business logic, auth, types semuanya untouched).
</success_criteria>

<output>
After completion, create `.planning/phases/10-fetch-prices-cors-fix/10-01-SUMMARY.md` documenting:
- Exact diff applied (before/after of ALLOWED_ORIGINS Set + comment block).
- Commit SHA dari `git log -1 --format=%H`.
- Confirmation bahwa belum ada `git push` (push deferred ke Plan 10-02).
- Pointer ke Plan 10-02 sebagai next step (deploy + UAT).
</output>
