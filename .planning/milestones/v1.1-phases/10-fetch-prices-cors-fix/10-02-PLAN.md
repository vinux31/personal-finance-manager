---
phase: 10-fetch-prices-cors-fix
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/phases/10-fetch-prices-cors-fix/10-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SEC-01, CONS-02]
must_haves:
  truths:
    - "Edge function fetch-prices versi terbaru sudah deployed ke Supabase production (dengan ALLOWED_ORIGINS 3 entries)"
    - "Browser klik 'Refresh Harga' di tab Investasi (kantongpintar.vercel.app) returns HTTP 200, no CORS error di Network tab"
    - "Row baru muncul di price_history dengan date = todayISO() WIB date (CONS-02 live verified)"
    - "curl smoke test dengan Origin kantongpintar.vercel.app TANPA Authorization tetap return 401 (SEC-01 regression check pass)"
    - "10-VERIFICATION.md mendokumentasikan ke-4 success criteria di atas dengan evidence verbatim"
  artifacts:
    - path: ".planning/phases/10-fetch-prices-cors-fix/10-VERIFICATION.md"
      provides: "Evidence-backed verification untuk 4 ROADMAP success criteria + close-out Phase 10"
      contains: "SC #1, SC #2, SC #3, SC #4"
    - path: ".planning/STATE.md"
      provides: "Updated project state — Phase 10 marked complete + deferred 'fetch-prices CORS' item closed"
      contains: "Phase 10"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 10 row marked [x]"
      contains: "[x] Phase 10"
  key_links:
    - from: "supabase/functions/fetch-prices/index.ts (committed Plan 10-01)"
      to: "Supabase production runtime (deployed via supabase functions deploy)"
      via: "supabase functions deploy fetch-prices"
      pattern: "Deployed Function fetch-prices"
    - from: "Browser POST /functions/v1/fetch-prices (Origin: kantongpintar.vercel.app)"
      to: "Edge function corsFor() ALLOWED_ORIGINS.has(origin) check"
      via: "CORS pre-flight + Access-Control-Allow-Origin echo back"
      pattern: "Access-Control-Allow-Origin: https://kantongpintar.vercel.app"
    - from: "useRefreshPrices (src/queries/investments.ts:111)"
      to: "price_history table row insert"
      via: "todayISO() WIB date string"
      pattern: "INSERT INTO price_history.*date"
---

<objective>
Deploy versi baru `fetch-prices` ke Supabase production, lalu eksekusi 3 verifikasi live (browser UAT Refresh Harga, DB query price_history, curl JWT regression smoke), dan tulis `10-VERIFICATION.md` yang menutup Phase 10 + un-block live verification SEC-01 + CONS-02 yang sebelumnya defer dari Phase 5/Phase 7.

Purpose: Plan 10-01 hanya ubah source code lokal. Tanpa deploy, perubahan tidak active di production. Tanpa live UAT, tidak ada bukti bahwa fix benar-benar memperbaiki "Refresh Harga" flow yang dilaporkan di v1.1-MILESTONE-AUDIT.md. Tanpa curl regression smoke, kita tidak punya jaminan deploy tidak accidentally menonaktifkan JWT enforcement (SEC-01 regression risk).

Output: Edge function deployed + 10-VERIFICATION.md ditulis + STATE.md updated + ROADMAP.md row Phase 10 di-mark [x] + git push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@supabase/functions/fetch-prices/index.ts
@.planning/phases/10-fetch-prices-cors-fix/10-01-PLAN.md
@.planning/phases/05-security-hardening/

<interfaces>
<!-- Supabase CLI command surface yang akan dipakai (Task 1): -->

```bash
# Deploy edge function — pakai project linked di supabase/config.toml
supabase functions deploy fetch-prices
# Expected output (substring): "Deployed Function fetch-prices on project ref ..."
```

<!-- Edge function endpoint URL pattern (Task 3 curl smoke): -->
# https://<project-ref>.functions.supabase.co/fetch-prices
# Project ref dapat diperoleh dari `supabase status` atau `.env`/Supabase dashboard.

<!-- Expected CORS response headers (Task 2 browser DevTools verification): -->
# Access-Control-Allow-Origin: https://kantongpintar.vercel.app
# Vary: Origin
# (TIDAK boleh fallback ke 'https://kantongpintar.app' — itu indikator allowlist match gagal)

<!-- price_history schema (Task 2 step 3 — DB row verification): -->
# Columns yang relevan: investment_id, price, date (DATE WIB), recorded_at (TIMESTAMPTZ)
# `date` field harus = todayISO() output (e.g. '2026-05-02' untuk WIB 2026-05-02 walaupun UTC mungkin masih 2026-05-01)
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Deploy fetch-prices edge function ke Supabase production</name>
  <files>—</files>
  <read_first>
    - supabase/functions/fetch-prices/index.ts (verify perubahan Plan 10-01 sudah committed)
    - supabase/config.toml (verify project linked + verify_jwt = true untuk fetch-prices)
  </read_first>
  <what-built>
    Plan 10-01 sudah commit perubahan ALLOWED_ORIGINS lokal. Sekarang user perlu deploy via Supabase CLI ke project production agar perubahan active di runtime. Claude tidak bisa otomatis deploy karena `supabase functions deploy` butuh CLI access ke akun user (auth token + project link).
  </what-built>
  <how-to-verify>
**Pre-flight checks (jalankan dulu sebelum deploy):**

1. Pastikan working tree clean dan commit Plan 10-01 sudah ada:
```bash
git status
git log -1 --oneline
```
Expected: `working tree clean` + commit subject `feat(10): add kantongpintar.vercel.app to fetch-prices CORS allowlist`.

2. Pastikan Supabase CLI ter-link ke project production:
```bash
supabase status
```
Expected: menampilkan API URL + DB URL untuk project Kantong Pintar (bukan local). Jika tidak ter-link, jalankan `supabase link --project-ref <REF>` dulu (REF tersedia di Supabase Dashboard → Project Settings → General).

**Deploy step:**

3. Jalankan deploy:
```bash
supabase functions deploy fetch-prices
```

Expected output (paste verbatim ke chat):
- Baris berisi `Bundling Function: fetch-prices` atau serupa.
- Baris berisi `Deploying Function: fetch-prices` atau serupa.
- Baris akhir berisi `Deployed Function fetch-prices on project ref <ref>` atau `Function deployed successfully`.

Jika error:
- `not logged in` → jalankan `supabase login` dulu, retry.
- `project not linked` → jalankan `supabase link --project-ref <REF>`, retry.
- `bundle error` → STOP, paste error ke chat — kemungkinan syntax error dari Plan 10-01 (re-read file untuk verify).

**Post-deploy smoke (1-detik sanity check, sebelum full UAT di Task 2):**

4. Test OPTIONS pre-flight:
```bash
curl -i -X OPTIONS https://<project-ref>.functions.supabase.co/fetch-prices \
  -H "Origin: https://kantongpintar.vercel.app" \
  -H "Access-Control-Request-Method: POST" \
  -H "Access-Control-Request-Headers: authorization,content-type"
```
Expected response headers:
- `HTTP/2 200` (atau `200 OK`)
- `access-control-allow-origin: https://kantongpintar.vercel.app` (echo back, BUKAN fallback `kantongpintar.app`)
- `vary: Origin`
- `access-control-allow-methods: POST, OPTIONS`

Jika `access-control-allow-origin` returns `https://kantongpintar.app` (fallback) instead of `kantongpintar.vercel.app`, itu indikator deploy belum propagate atau allowlist masih versi lama — tunggu 30s, retry. Jika tetap, STOP dan diagnose.

Paste output `curl -i` (full headers) verbatim ke chat — akan dipakai sebagai evidence di Task 4 VERIFICATION.md.
  </how-to-verify>
  <acceptance_criteria>
    - User pastes deploy output ke chat — output mengandung literal string `Deployed Function fetch-prices` (atau Supabase CLI equivalent confirming success).
    - User pastes `curl -i -X OPTIONS ...` output ke chat.
    - OPTIONS response mengandung header `access-control-allow-origin: https://kantongpintar.vercel.app` (case-insensitive match — bukan fallback ke kantongpintar.app).
    - OPTIONS response mengandung `vary: Origin`.
    - OPTIONS HTTP status = 200.
    - Tidak ada deploy error (auth, bundle, network) — kalau ada, user diagnose dan retry sebelum lanjut.
  </acceptance_criteria>
  <resume-signal>Type "deploy-ok" + paste verbatim 2 outputs (deploy log + curl OPTIONS headers). Type "deploy-fail: [error]" jika gagal.</resume-signal>
  <action>See <how-to-verify> block — user runs `supabase functions deploy fetch-prices` + curl OPTIONS smoke test. Claude tidak dapat eksekusi karena butuh user CLI auth + project link.</action>
  <verify>
    <automated>echo "Manual deploy checkpoint — user must paste deploy output + OPTIONS curl response per resume-signal"</automated>
  </verify>
  <done>User typed "deploy-ok" + provided 2 outputs (deploy log dengan "Deployed Function fetch-prices" + curl OPTIONS yang echo Origin kantongpintar.vercel.app). Edge function active di production runtime.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Live UAT Refresh Harga + price_history row verification</name>
  <files>—</files>
  <read_first>
    - src/queries/investments.ts (around line 111 — useRefreshPrices implementation; verify todayISO() callsite)
    - .planning/REQUIREMENTS.md (CONS-02 acceptance criteria untuk konteks)
  </read_first>
  <what-built>
    Edge function deployed di Task 1. Sekarang flow end-to-end perlu di-verify dari browser real (kantongpintar.vercel.app). 3 sub-test:
    - UAT-1: Browser "Refresh Harga" returns 200 (no CORS rejection).
    - UAT-2: Network tab shows correct CORS headers.
    - UAT-3: DB row baru di price_history dengan `date = todayISO()` WIB.
  </what-built>
  <how-to-verify>
**Setup:**
1. Buka browser (Chrome/Edge/Firefox), navigasi ke `https://kantongpintar.vercel.app`.
2. Login dengan akun yang punya minimal 1 investasi Saham atau Emas.
3. Buka DevTools (F12) → tab **Network** → filter: `fetch-prices`. Centang "Preserve log".
4. Buka tab kedua → Supabase Studio → SQL Editor (untuk DB query Step 3).

**UAT-1 — Refresh Harga returns 200 (Success Criteria #2):**
1. Di app, navigasi ke tab **Investasi**.
2. Klik tombol **"Refresh Harga"** (icon refresh, biasanya di header tab atau pojok kanan atas).
3. Observe Network tab:
   - Cari request ke endpoint berisi `fetch-prices` (path lengkap: `<project>.functions.supabase.co/fetch-prices` atau `/functions/v1/fetch-prices`).
   - **Expected:** Request status = `200 OK` (atau `200`). BUKAN `(failed) net::ERR_FAILED`, BUKAN merah, BUKAN `0 (CORS error)`.
4. Klik request tersebut → tab **Headers**:
   - **Request Headers** harus include `Authorization: Bearer eyJ...` (JWT user yang sedang login).
   - **Response Headers** harus include `access-control-allow-origin: https://kantongpintar.vercel.app` (echo back, BUKAN `kantongpintar.app`).
   - **Response Headers** harus include `vary: Origin`.
5. Tab **Response** → JSON harus mengandung `{ "results": [...], "errors": [...] }` (struktur FetchPricesResponse). `results` array harus non-empty (kecuali user tidak punya investasi Saham/Emas — kalau begitu, akun salah, retry dengan akun yang punya).
6. Toast / UI feedback di app harus indikasi sukses (mis. "Harga diperbarui", harga di tabel berubah, atau loading spinner clear).

**UAT-2 — DevTools Console no CORS error:**
7. DevTools → tab **Console**:
   - **Expected:** Tidak ada error merah berisi `Access-Control-Allow-Origin`, `CORS`, `blocked by CORS policy`, atau `preflight response`.
   - Boleh ada warning lain (misalnya React DevTools, Vercel telemetry) — yang penting tidak ada CORS error.

**UAT-3 — DB row baru di price_history dengan WIB date (Success Criteria #3 + CONS-02 live verify):**
8. Di Supabase Studio SQL Editor, jalankan query berikut (substitute `<user_id>` dengan UUID user yang baru klik Refresh Harga — bisa diperoleh dari `auth.uid()` atau dari profile):
```sql
-- Ambil 5 row paling baru dari price_history untuk user ini
SELECT
  ph.id,
  ph.investment_id,
  i.asset_name,
  ph.price,
  ph.date,
  ph.recorded_at,
  -- Compare dengan WIB date (Asia/Jakarta = UTC+7)
  (NOW() AT TIME ZONE 'Asia/Jakarta')::date AS wib_today,
  CASE WHEN ph.date = (NOW() AT TIME ZONE 'Asia/Jakarta')::date THEN 'PASS — WIB today' ELSE 'FAIL — date drift' END AS cons02_check
FROM price_history ph
JOIN investments i ON i.id = ph.investment_id
WHERE i.user_id = '<user_id>'
ORDER BY ph.recorded_at DESC
LIMIT 5;
```

Expected output:
- Minimum 1 row di top dengan `recorded_at` ≈ NOW() (within last 1 menit).
- Row tersebut `date` = `wib_today` value.
- Column `cons02_check` = `PASS — WIB today` untuk row terbaru.

**Edge case:** Jika sekarang antara 17:00 UTC (00:00 WIB) dan kemudian, `wib_today` mungkin sudah hari berikutnya dibanding UTC date — ini justru point dari CONS-02 (todayISO pakai WIB, bukan UTC). PASS criterion: `ph.date` == `(NOW() AT TIME ZONE 'Asia/Jakarta')::date`, bukan == `CURRENT_DATE` (yang UTC).

**Paste ke chat:**
- Screenshot Network tab Refresh Harga request (status 200 + response headers visible).
- Screenshot/text Console tab (no CORS error).
- Verbatim output query SQL (minimal row terbaru dengan all 8 columns).
  </how-to-verify>
  <acceptance_criteria>
    - UAT-1: User pastes screenshot/text yang menunjukkan request `fetch-prices` di Network tab dengan status `200`.
    - UAT-1: Response Headers mengandung `access-control-allow-origin: https://kantongpintar.vercel.app` (echo back match Origin).
    - UAT-1: Response body adalah JSON `{"results":[...],"errors":[...]}` dengan `results` non-empty.
    - UAT-1: UI app menampilkan feedback sukses (harga updated atau toast).
    - UAT-2: User confirm Console tidak ada error berisi `CORS`, `Access-Control-Allow-Origin`, atau `blocked by CORS policy`.
    - UAT-3: User pastes verbatim SQL query result dengan minimum 1 row terbaru.
    - UAT-3: Row terbaru column `cons02_check` = `PASS — WIB today`.
    - UAT-3: Row terbaru `recorded_at` adalah dalam 5 menit terakhir (proves baru-baru ini di-insert oleh Refresh Harga).
    - Tidak ada regression di flow lain investasi (CRUD investasi tetap berfungsi — spot check 1 add/edit/delete).
  </acceptance_criteria>
  <resume-signal>Type "uat-pass" + paste 3 evidence (Network screenshot/text, Console no-CORS confirmation, SQL query output). Type "uat-fail: [UAT-N], [issue]" jika ada yang FAIL — agent akan diagnose.</resume-signal>
  <action>See <how-to-verify> block — user runs 3 UAT scenarios di production app + Supabase Studio. Claude tidak dapat eksekusi browser-based UAT ataupun query database production.</action>
  <verify>
    <automated>echo "Manual UAT checkpoint — user must paste 3 evidence per resume-signal"</automated>
  </verify>
  <done>User typed "uat-pass" + provided 3 evidence (Network 200 + correct CORS, Console clean, SQL row dengan PASS cons02_check). Refresh Harga flow live verified end-to-end.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: SEC-01 regression smoke — curl tanpa Authorization tetap return 401</name>
  <files>—</files>
  <read_first>
    - supabase/functions/fetch-prices/index.ts lines 56-82 (auth header check + auth.getUser block)
    - .planning/REQUIREMENTS.md (SEC-01 line untuk konteks regression)
    - .planning/phases/05-security-hardening/05-VERIFICATION.md jika exists (style reference untuk SEC-01 evidence format)
  </read_first>
  <action>
Eksekusi 2 curl smoke test untuk verify CORS allowlist change tidak accidentally weaken JWT enforcement (SEC-01 regression check).

**Pre-step:** Tentukan project URL. User dapat ambil dari salah satu dari:
- Output `supabase status` (API URL + path `/functions/v1/`).
- Supabase Dashboard → Project Settings → API → "Project URL".
- File `.env` lokal (`VITE_SUPABASE_URL` atau serupa).

Format URL edge function: `https://<project-ref>.functions.supabase.co/fetch-prices` ATAU `https://<project-ref>.supabase.co/functions/v1/fetch-prices` (tergantung versi CLI; coba yang `.functions.supabase.co` dulu, fallback ke `/functions/v1/` jika 404).

User akan diminta paste project URL ke chat sebelum Claude lanjut.

**Smoke #1 — POST tanpa Authorization, dengan Origin yang baru di-allowlist (HARUS tetap 401):**
```bash
curl -i -X POST "https://<project-ref>.functions.supabase.co/fetch-prices" \
  -H "Origin: https://kantongpintar.vercel.app" \
  -H "Content-Type: application/json" \
  -d '{"investments":[]}'
```

Expected response:
- HTTP status: `401` (atau `HTTP/2 401`).
- Body: JSON `{"error":"Unauthorized"}` ATAU `{"code":"UNAUTHORIZED_NO_AUTH_HEADER"...}` (tergantung apakah platform-layer atau handler-layer yang reject — keduanya valid per STATE.md decision).
- CORS response headers tetap include `access-control-allow-origin: https://kantongpintar.vercel.app` (echo back — proves CORS allowlist berfungsi tapi tidak bypass auth).
- `vary: Origin` tetap present.

Jika status = 200 → REGRESSION CRITICAL: Plan 10-01 + Task 1 deploy accidentally menghilangkan JWT enforcement. STOP, diagnose dengan re-read fetch-prices/index.ts lines 56-82 (Authorization header check + auth.getUser).

**Smoke #2 — POST tanpa Authorization, Origin yang TIDAK di-allowlist (regression baseline — 401 + fallback CORS):**
```bash
curl -i -X POST "https://<project-ref>.functions.supabase.co/fetch-prices" \
  -H "Origin: https://malicious-example.com" \
  -H "Content-Type: application/json" \
  -d '{"investments":[]}'
```

Expected response:
- HTTP status: `401`.
- Body: `{"error":"Unauthorized"}` atau platform-equivalent.
- `access-control-allow-origin: https://kantongpintar.app` (FALLBACK — corsFor() line 14 fallback baseline). BUKAN `https://malicious-example.com` (yang artinya allowlist bypass).
- `vary: Origin` tetap present.

Smoke #2 confirms 2 hal sekaligus:
1. Allowlist tidak loosened (origin yang tidak terdaftar tetap di-fallback ke baseline, tidak echo back).
2. Auth enforcement berlaku universal (origin allowed maupun tidak, tanpa JWT = 401).

**Catat verbatim output kedua curl ke chat — akan dipakai sebagai evidence Section "SEC-01 Regression" di Task 4 VERIFICATION.md.**

**Note:** Smoke ini autonomous (Claude bisa eksekusi) ASALKAN user sudah paste project URL di chat. Jika user belum paste, minta dulu.
  </action>
  <verify>
    <automated>echo "User must paste project URL first; Claude then runs 2 curl smokes and verifies response status=401 + response headers"</automated>
  </verify>
  <acceptance_criteria>
    - Project URL diperoleh dari user (paste ke chat).
    - Smoke #1 output mengandung `HTTP/2 401` atau `HTTP/1.1 401` (case-insensitive match status line).
    - Smoke #1 response body adalah JSON dengan key `error` atau `code` indicating Unauthorized.
    - Smoke #1 response header `access-control-allow-origin` = `https://kantongpintar.vercel.app` (allowlist echo back working).
    - Smoke #2 output mengandung `HTTP/2 401` atau `HTTP/1.1 401`.
    - Smoke #2 response header `access-control-allow-origin` = `https://kantongpintar.app` (fallback baseline, BUKAN `https://malicious-example.com`).
    - Both responses include `vary: Origin`.
    - Output verbatim kedua curl di-record untuk dipakai di Task 4.
  </acceptance_criteria>
  <done>SEC-01 regression smoke pass — JWT enforcement tetap intact post-deploy, allowlist berfungsi sebagai expected (echo allowed, fallback unallowed), no auth bypass introduced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write 10-VERIFICATION.md dengan evidence ke-4 ROADMAP success criteria</name>
  <files>.planning/phases/10-fetch-prices-cors-fix/10-VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 10 success criteria — 4 items)
    - .planning/phases/10-fetch-prices-cors-fix/10-01-SUMMARY.md jika sudah exist (commit SHA)
    - .planning/phases/05-security-hardening/05-VERIFICATION.md jika exist (style reference)
    - .planning/phases/09-phase-09-qa-bug-fix-fix-semua-bug-dari-qa-findings-md/09-VERIFICATION.md jika exist (style reference, lebih recent)
  </read_first>
  <action>
Tulis file `.planning/phases/10-fetch-prices-cors-fix/10-VERIFICATION.md` dengan struktur exact ini (isi placeholder `<...>` dengan data verbatim dari Task 1, 2, 3):

```markdown
# Phase 10 Verification — Fix `fetch-prices` CORS Allowlist

**Verifier:** Claude Opus 4.7 + user (manual deploy + UAT)
**Date:** <YYYY-MM-DD WIB>
**Scope:** Tutup gap CORS allowlist yang teridentifikasi di v1.1-MILESTONE-AUDIT.md.
**Verdict:** <PASS | PASS-WITH-NOTES | FAIL>

---

## Summary

| SC | Description | Verification Method | Status |
|----|-------------|---------------------|--------|
| #1 | ALLOWED_ORIGINS Set berisi 3 entries (incl. kantongpintar.vercel.app) | Static grep (Plan 10-01) + git diff | PASS |
| #2 | Browser "Refresh Harga" dari kantongpintar.vercel.app returns 200, no CORS error | Live UAT-1 + Network tab inspection | PASS |
| #3 | Row baru di price_history dengan date = todayISO() WIB (CONS-02 live un-blocked) | Live UAT-3 SQL query | PASS |
| #4 | curl POST tanpa Authorization tetap return 401 (SEC-01 regression intact) | Task 3 curl smoke (2 variants) | PASS |

---

## Evidence per Success Criteria

### SC #1 — ALLOWED_ORIGINS source change

**Commit:** `<commit_sha_from_10-01-SUMMARY>` — `feat(10): add kantongpintar.vercel.app to fetch-prices CORS allowlist`

**Diff applied:**
```diff
 const ALLOWED_ORIGINS = new Set<string>([
   'https://kantongpintar.app',
   'https://www.kantongpintar.app',
+  'https://kantongpintar.vercel.app',
 ])
```

**Static verification:**
```
$ grep -c "'https://kantongpintar.vercel.app'," supabase/functions/fetch-prices/index.ts
1
```

### SC #2 — Live UAT Refresh Harga (browser)

**Steps:** Login `kantongpintar.vercel.app` → tab Investasi → klik "Refresh Harga" dengan DevTools Network tab open.

**Network tab observation (verbatim from user):**
<paste UAT-1 evidence dari Task 2 — request status, response headers, response body snippet>

**Console tab observation:**
<paste UAT-2 confirmation — no CORS error>

**App UI feedback:**
<paste user description — toast / harga updated / spinner cleared>

**Result:** PASS — request `fetch-prices` returns 200 dengan `access-control-allow-origin: https://kantongpintar.vercel.app` echoed back; tidak ada CORS error di Console.

### SC #3 — price_history row baru dengan WIB date (CONS-02 live)

**SQL query executed (Studio SQL Editor):**
```sql
SELECT ph.id, ph.investment_id, i.asset_name, ph.price, ph.date, ph.recorded_at,
  (NOW() AT TIME ZONE 'Asia/Jakarta')::date AS wib_today,
  CASE WHEN ph.date = (NOW() AT TIME ZONE 'Asia/Jakarta')::date THEN 'PASS — WIB today' ELSE 'FAIL — date drift' END AS cons02_check
FROM price_history ph
JOIN investments i ON i.id = ph.investment_id
WHERE i.user_id = '<user_id>'
ORDER BY ph.recorded_at DESC
LIMIT 5;
```

**Result rows (verbatim from user):**
<paste UAT-3 SQL query output dari Task 2>

**Result:** PASS — row terbaru `date` = `wib_today` value, `cons02_check` = `PASS — WIB today`. CONS-02 todayISO write-path live verified setelah sebelumnya defer di Phase 7 karena CORS block.

### SC #4 — SEC-01 regression curl smokes

**Smoke #1 — Allowed origin tanpa JWT (must 401):**
```bash
curl -i -X POST "https://<project>.functions.supabase.co/fetch-prices" \
  -H "Origin: https://kantongpintar.vercel.app" \
  -H "Content-Type: application/json" \
  -d '{"investments":[]}'
```
Output:
<paste verbatim curl output dari Task 3 Smoke #1 — minimum: status line, access-control-allow-origin header, vary header, body>

Result: 401 (PASS — JWT gate intact)

**Smoke #2 — Disallowed origin tanpa JWT (must 401 + fallback CORS):**
```bash
curl -i -X POST "https://<project>.functions.supabase.co/fetch-prices" \
  -H "Origin: https://malicious-example.com" \
  -H "Content-Type: application/json" \
  -d '{"investments":[]}'
```
Output:
<paste verbatim curl output dari Task 3 Smoke #2>

Result: 401 + `access-control-allow-origin: https://kantongpintar.app` fallback (PASS — allowlist tidak loosened, no echo of malicious origin).

**Combined verdict SC #4:** SEC-01 regression intact. CORS allowlist change tidak introduce auth bypass.

---

## Deploy Evidence (Task 1)

**Command:**
```bash
supabase functions deploy fetch-prices
```

**Output (verbatim from user):**
<paste deploy log dari Task 1>

**OPTIONS pre-flight smoke:**
<paste curl -i -X OPTIONS output dari Task 1>

---

## Regression Check

Spot-check skenario non-Phase-10 (no regression):
- [ ] Login + signOut tetap berfungsi (kantongpintar.vercel.app).
- [ ] Tambah Investasi (Saham/Emas/Reksadana) tetap berfungsi.
- [ ] Edit/Hapus Investasi tetap berfungsi.
- [ ] Tab Goals + Link Investment + Add Money tetap berfungsi (Phase 7/9 work intact).
- [ ] Tab Kekayaan — Net Worth calculation tetap include investasi.
- [ ] Tab Laporan — chart pie + bar tetap render.

---

## Closes / Un-blocks

- **Closes:** "Edge Function `fetch-prices` CORS misconfiguration" deferred item di STATE.md (Deferred Items table — `infra | Edge Function fetch-prices CORS...`) ditandai resolved.
- **Un-blocks:** Phase 5 SEC-01 live re-verification dari production domain — sebelumnya bisa di-verify hanya via curl, sekarang via browser UAT real flow.
- **Un-blocks:** Phase 7 CONS-02 live verification — todayISO write-path sebelumnya hanya verified static (grep di code) + DB-side (pgTAP); sekarang verified end-to-end live.

## Deferred / Out of Scope (carried forward)

- D-14 raw NUMERIC formatting di withdraw_from_goal error message — tetap defer ke v1.2.
- net_worth_snapshots auto-insert 42501 saat View-As — tetap defer ke v1.2.
- Migration history reconciliation (`supabase migration list --linked` masih show Local-only 0014..0025) — tetap defer ke v1.2 hygiene phase.

---

## Artifacts

- `supabase/functions/fetch-prices/index.ts` (modified Plan 10-01, deployed Task 1).
- Commit `<sha>` (Plan 10-01) + Plan 10-02 commit (this VERIFICATION.md + STATE/ROADMAP updates).

---

## Sign-off

- [ ] All 4 SC PASS
- [ ] No regression
- [ ] Evidence recorded
- [ ] STATE.md updated (Phase 10 complete + deferred CORS item resolved)
- [ ] ROADMAP.md Phase 10 row marked [x]
- [ ] Verdict: PASS

Verifier: <name>
Date: <YYYY-MM-DD>
```

Isi semua placeholder `<...>` dengan data real dari Task 1/2/3. JANGAN biarkan placeholder text seperti `<paste ...>`, `<commit_sha_from...>`, `<YYYY-MM-DD>` di file final.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-fetch-prices-cors-fix/10-VERIFICATION.md && grep -c "SC #1\|SC #2\|SC #3\|SC #4" .planning/phases/10-fetch-prices-cors-fix/10-VERIFICATION.md | xargs -I{} test {} -ge 4 && grep -c "Verdict: PASS" .planning/phases/10-fetch-prices-cors-fix/10-VERIFICATION.md | xargs -I{} test {} -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/10-fetch-prices-cors-fix/10-VERIFICATION.md` exists.
    - File mengandung 4 SC references (`SC #1`, `SC #2`, `SC #3`, `SC #4`) di Summary table.
    - File mengandung literal `Verdict: PASS` (atau `PASS-WITH-NOTES`).
    - File mengandung verbatim deploy log + OPTIONS curl dari Task 1.
    - File mengandung verbatim Network tab evidence + SQL query output dari Task 2.
    - File mengandung verbatim Smoke #1 + Smoke #2 curl output dari Task 3.
    - File mengandung diff snippet untuk SC #1 yang menunjukkan tambahan `https://kantongpintar.vercel.app`.
    - File mengandung "Closes / Un-blocks" section yang reference Phase 5 SEC-01 + Phase 7 CONS-02.
    - File mengandung "Regression Check" section dengan minimum 6 checkboxes.
    - File TIDAK mengandung unfilled placeholder `<paste ...>`, `<copy ...>`, `<YYYY-MM-DD>`, `<sha>`, `<user_id>`, `<commit_sha_from_10-01-SUMMARY>`.
  </acceptance_criteria>
  <done>10-VERIFICATION.md ditulis dengan evidence lengkap untuk ke-4 SC + deploy + regression. Phase 10 closure document siap di-archive.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Update STATE.md + ROADMAP.md untuk Phase 10 complete + git push</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (current state — focus pada Current Position, Deferred Items, Decisions, Session Continuity)
    - .planning/ROADMAP.md (Phase 10 entry + checkbox state)
    - .planning/phases/10-fetch-prices-cors-fix/10-VERIFICATION.md (verdict yang baru ditulis Task 4)
  </read_first>
  <action>
**Edit 1 — STATE.md:**

1. Update `last_updated` ke timestamp ISO sekarang (UTC, format `YYYY-MM-DDTHH:MM:SS.000Z`).
2. Update `last_activity` ke `YYYY-MM-DD -- Phase 10 complete, fetch-prices CORS allowlist updated + live UAT pass`.
3. Update `progress.completed_phases` dan `progress.completed_plans`:
   - `total_plans`: tambah 2 (Phase 10's 2 plans).
   - `completed_plans`: tambah 2.
   - `completed_phases`: tambah 1 (Phase 10).
4. Update `stopped_at` ke `Phase 10 complete — fetch-prices CORS gap closed; v1.1 milestone fully verified`.
5. Update "Current Position" → set Phase ke `10 (fetch-prices CORS fix) — Complete (YYYY-MM-DD)`.
6. Tambah section baru di "Accumulated Context > Decisions" — judul `### Decisions (v1.1 execution-time, post-Phase-10)`:
   ```markdown
   - **Phase 10 PASS.** ALLOWED_ORIGINS Set di edge function fetch-prices ditambahi `https://kantongpintar.vercel.app`; deployed via `supabase functions deploy`; live UAT Refresh Harga di production browser pass; SEC-01 regression curl smoke pass (JWT gate tetap 401 untuk request tanpa Authorization). 10-VERIFICATION.md tersedia di .planning/phases/10-fetch-prices-cors-fix/10-VERIFICATION.md.
   - **Lesson Phase 10 — CORS allowlist drift saat domain baru.** Saat memutuskan deploy app ke domain baru (Vercel default vs custom domain), edge function ALLOWED_ORIGINS harus di-update bersamaan, jika tidak browser flow yang bergantung pada edge function akan silent-fail (network 200 tapi browser block respons karena CORS mismatch). Pattern: documenting domain → deploy domain di same plan/PR.
   - **CONS-02 live un-blocked.** Phase 7 CONS-02 sebelumnya hanya verified static + pgTAP karena CORS block menghalangi live UAT. Phase 10 un-block live verification — row price_history dengan `date = (NOW() AT TIME ZONE 'Asia/Jakarta')::date` confirmed. CONS-02 status di REQUIREMENTS.md tetap `shipped` (Phase 7) — Phase 10 hanya menambah live evidence.
   - **SEC-01 status di REQUIREMENTS.md.** Sebelumnya `pending` (live re-verify dari kantongpintar.vercel.app blocked oleh CORS). Sekarang dapat di-update ke `shipped` — pertimbangkan apakah masih ada gap (mis. SC #1/SC #2 destructive variant) sebelum mark shipped final. Tentative update: `live-verified Phase 10`.
   ```
7. Update "Deferred Items" table — temukan row `infra | Edge Function fetch-prices CORS misconfiguration...` dan ubah Status column ke `**resolved Phase 10**` (bold), atau pindahkan ke section baru "Resolved Items" jika ada precedent format.
8. Update "Roadmap Evolution" — tambah entry: `- Phase 10 completed (YYYY-MM-DD): fetch-prices CORS allowlist fix — un-blocks live verification SEC-01 + CONS-02. PASS verdict.`
9. Update "Session Continuity":
   - `Last session`: timestamp baru.
   - `Stopped at`: `Phase 10 complete — v1.1 milestone fully verified including live SEC-01 + CONS-02`.
   - `Resume options`: tambah option `/gsd-milestone-close v1.1` (atau equivalent — milestone close gate karena semua deferred infra item sudah resolved).

**Edit 2 — ROADMAP.md:**

1. Cari section header `### 🚧 v1.1 Hardening & Consistency (In Progress)`.
2. Cari row `- [ ] **Phase 10: Fix \`fetch-prices\` CORS Allowlist**...`.
3. Ubah `[ ]` menjadi `[x]`.
4. Cari section "### Phase 10: Fix `fetch-prices` CORS Allowlist" detail block.
5. Cari "Plans:" subsection — ubah:
   ```markdown
   - [ ] 10-01-PLAN.md — Update `ALLOWED_ORIGINS` di `supabase/functions/fetch-prices/index.ts` + commit
   - [ ] 10-02-PLAN.md — [BLOCKING] `supabase functions deploy fetch-prices` + live UAT...
   ```
   menjadi:
   ```markdown
   - [x] 10-01-PLAN.md — Update `ALLOWED_ORIGINS` di `supabase/functions/fetch-prices/index.ts` + commit
   - [x] 10-02-PLAN.md — [BLOCKING] `supabase functions deploy fetch-prices` + live UAT Refresh Harga di `kantongpintar.vercel.app` + curl SEC-01 regression smoke + write 10-VERIFICATION.md
   ```
6. Update "Progress" table di bagian bawah ROADMAP — tambah row baru atau update existing:
   ```markdown
   | 10. fetch-prices CORS fix | v1.1 | 2/2 | ✅ Complete | YYYY-MM-DD |
   ```

**Edit 3 — REQUIREMENTS.md (optional but recommended):**

1. Cari row SEC-01 di status table (sekitar line 90).
2. Update `pending` → `shipped (Phase 5 + Phase 10 live re-verify)`. Tetap conservative — jika ada doubt, biarkan `pending` dan tambah note.

**Commit + push:**

```bash
git add .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/phases/10-fetch-prices-cors-fix/10-VERIFICATION.md .planning/phases/10-fetch-prices-cors-fix/10-01-SUMMARY.md .planning/phases/10-fetch-prices-cors-fix/10-02-SUMMARY.md

git commit -m "$(cat <<'EOF'
docs(10): close Phase 10 — fetch-prices CORS allowlist verified

Phase 10 PASS — semua 4 ROADMAP success criteria evidenced:
- SC #1: ALLOWED_ORIGINS Set berisi 3 entries (commit <SHA-10-01>)
- SC #2: Browser "Refresh Harga" dari kantongpintar.vercel.app returns 200
- SC #3: price_history row baru dengan date = WIB today (CONS-02 live)
- SC #4: curl tanpa Authorization tetap return 401 (SEC-01 intact)

Closes deferred item "Edge Function fetch-prices CORS misconfiguration"
(STATE.md Deferred Items table). Un-blocks live verification SEC-01 +
CONS-02 dari production domain.

Refs: v1.1-MILESTONE-AUDIT.md (tech_debt verdict), 10-VERIFICATION.md.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

# Push BOTH commits (Plan 10-01 source change + Plan 10-02 docs close-out)
git push origin master
```

Verify push:
```bash
git status
# Expected: "Your branch is up to date with 'origin/master'."
```

Jika `git push` gagal (non-fast-forward, network), STOP dan report — jangan force-push.

CRITICAL constraints:
- TIDAK mengubah Phase 5/6/7/8/9 entries (sudah completed sebelumnya).
- Pakai backtick + plus/minus consistent dengan existing pattern di file.
- Tanggal pakai format `YYYY-MM-DD` (WIB), timestamp ISO pakai UTC dengan `Z`.
- Push hanya jika UAT-pass + curl smoke pass + VERIFICATION.md complete.
  </action>
  <verify>
    <automated>grep -c "Phase 10 PASS\|Phase 10 complete" .planning/STATE.md | xargs -I{} test {} -ge 1 && grep -c "\[x\] \*\*Phase 10:" .planning/ROADMAP.md | xargs -I{} test {} -ge 1 && grep -c "\[x\] 10-01-PLAN.md" .planning/ROADMAP.md | xargs -I{} test {} -eq 1 && grep -c "\[x\] 10-02-PLAN.md" .planning/ROADMAP.md | xargs -I{} test {} -eq 1 && git log -1 --pretty=format:'%s' | grep -c "docs(10): close Phase 10" | xargs -I{} test {} -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` mengandung literal `Phase 10 PASS` atau `Phase 10 complete`.
    - `.planning/STATE.md` `last_updated` di-update ke timestamp ISO baru.
    - `.planning/STATE.md` Decisions section mengandung "Lesson Phase 10 — CORS allowlist drift" dan "CONS-02 live un-blocked".
    - `.planning/STATE.md` Deferred Items table — row `Edge Function fetch-prices CORS` Status sekarang `resolved Phase 10` (atau dipindah ke Resolved section).
    - `.planning/ROADMAP.md` row `[x] **Phase 10:` (dengan checkbox checked).
    - `.planning/ROADMAP.md` Plans list Phase 10 mengandung `[x] 10-01-PLAN.md` dan `[x] 10-02-PLAN.md`.
    - `.planning/ROADMAP.md` Progress table mengandung row Phase 10 dengan `Complete`.
    - 1 commit baru `docs(10):` dengan subject `docs(10): close Phase 10 — fetch-prices CORS allowlist verified`.
    - `git push origin master` succeeded — `git status` reports `Your branch is up to date with 'origin/master'`.
    - Push mencakup BOTH commits (10-01 source change + 10-02 docs close-out).
  </acceptance_criteria>
  <done>STATE.md + ROADMAP.md + REQUIREMENTS.md updated; Phase 10 secara formal tercatat sebagai complete; 2 commits pushed to remote master; v1.1 milestone fully verified termasuk live SEC-01 + CONS-02.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local source → Supabase production runtime (Task 1 deploy) | `supabase functions deploy` push code via authenticated CLI token; runtime activation crosses dev → prod boundary |
| Browser (kantongpintar.vercel.app) → edge function fetch-prices (Task 2 UAT) | User browser sends authenticated request; CORS pre-flight + actual POST cross network boundary; allowlist + JWT are dual gates |
| Anonymous curl → edge function (Task 3 regression smoke) | Untrusted attacker simulation; no Authorization header should always 401 regardless of origin |
| User UAT confirmation + curl outputs → 10-VERIFICATION.md (Task 4) | User attestation + raw command output crosses into permanent project audit record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Spoofing (cross-origin attack via expanded allowlist) | Production fetch-prices runtime post-deploy | mitigate | Task 1 + Task 2 verify allowlist hanya echo `kantongpintar.vercel.app` (legitimate production), bukan wildcard. Task 3 Smoke #2 explicitly tests bahwa origin `https://malicious-example.com` di-fallback ke baseline `kantongpintar.app`, BUKAN echoed back — proves allowlist Set membership check working. |
| T-10-06 | Tampering (CDN cache poisoning post-allowlist-change) | Supabase edge runtime + CDN | mitigate | `Vary: Origin` header sudah present di corsFor() (verified Plan 10-01 untouched). Task 1 OPTIONS smoke + Task 2 Network tab UAT explicitly check `vary: Origin` di response headers. CDN akan cache per-origin separately. |
| T-10-07 | Elevation of Privilege (deploy regression breaks JWT enforcement) | Edge function auth gate post-deploy | mitigate | Task 3 dedicated SEC-01 regression smoke — 2 curl variants (allowed origin no-JWT, disallowed origin no-JWT). Both MUST return 401. Acceptance criteria explicit: status code matched, response body Unauthorized, CORS headers correct. Failure → STOP + diagnose, NO push. |
| T-10-08 | Repudiation (false PASS claim di VERIFICATION.md) | Task 4 evidence recording | mitigate | Acceptance criteria Task 2 + Task 3 require user paste verbatim Network tab data + curl outputs ke chat. Task 4 acceptance criteria forbid unfilled placeholder strings — every `<paste ...>` MUST be replaced with real data. Task 4 automated verify checks file existence + key SC markers. |
| T-10-09 | Information Disclosure (sensitive data in VERIFICATION.md) | Pasted curl/SQL outputs | accept | curl outputs adalah headers + 401 body (no PII). SQL outputs include `investment_id`, `asset_name`, `price` (user's own portfolio) — solo developer trust model, audit trail acceptable risk. JWT tokens dari browser MUST be redacted by user before paste — instruction explicit di Task 2 step 4. |
| T-10-10 | Denial of Service (push tanpa UAT pass introduces broken state to remote) | Task 5 git push | mitigate | Task 5 push hanya dijalankan SETELAH semua sebelumnya pass: deploy ok (Task 1), UAT pass (Task 2), regression smoke pass (Task 3), VERIFICATION.md complete (Task 4). Sequential blocking gates make premature push impossible. |

</threat_model>

<verification>
- Edge function `fetch-prices` deployed dengan ALLOWED_ORIGINS 3 entries (Task 1 deploy log evidence).
- Browser "Refresh Harga" dari `kantongpintar.vercel.app` returns 200 + correct CORS headers (Task 2 UAT-1 evidence).
- DevTools Console clean dari CORS errors (Task 2 UAT-2 evidence).
- price_history row baru dengan date = WIB today (Task 2 UAT-3 SQL evidence).
- 2 curl smokes confirm 401 + correct CORS fallback behavior (Task 3 evidence).
- 10-VERIFICATION.md complete dengan ke-4 SC PASS + verbatim evidence (Task 4).
- STATE.md + ROADMAP.md updated, deferred CORS item marked resolved, 2 commits pushed to remote master (Task 5).
</verification>

<success_criteria>
1. Edge function deployed ke production Supabase tanpa error.
2. Browser UAT Refresh Harga PASS — request 200 + CORS allowed + price_history row dengan WIB date.
3. SEC-01 regression curl smoke PASS — JWT gate tetap 401 untuk request tanpa Authorization (allowed maupun disallowed origin).
4. 10-VERIFICATION.md ditulis dengan evidence lengkap untuk ke-4 SC.
5. STATE.md + ROADMAP.md updated; Phase 10 ditandai complete; deferred CORS item resolved.
6. 2 commits pushed to remote master (10-01 source + 10-02 docs).
7. Tidak ada regression di flow lain (Login, CRUD investasi, Goals, Net Worth, Laporan).
</success_criteria>

<output>
After completion, create `.planning/phases/10-fetch-prices-cors-fix/10-02-SUMMARY.md` documenting:
- Verdict (PASS / PASS-WITH-NOTES / FAIL).
- 4 SC status grid.
- Files updated (10-VERIFICATION.md, STATE.md, ROADMAP.md, optionally REQUIREMENTS.md).
- Commit SHAs untuk both commits (Plan 10-01 source + Plan 10-02 docs).
- Confirmation `git push` succeeded.
- Pointer ke milestone close gate (`/gsd-milestone-close v1.1` jika appropriate) sebagai next step.
</output>
