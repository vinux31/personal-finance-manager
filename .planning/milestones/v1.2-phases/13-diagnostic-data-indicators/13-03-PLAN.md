---
phase: 13-diagnostic-data-indicators
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/db/goals.ts
  - src/queries/kesehatanTier2.ts
  - src/tabs/kesehatan/Tier2Panel.tsx
autonomous: false
requirements: [DIAG-05, DIAG-06]
must_haves:
  truths:
    - "User dengan ≥1 long-term active goal lihat #5 sebagai 'X/Y on-track' badge dengan warna hijau (≥75% on-track)/kuning (50-74%)/merah (<50%)"
    - "User TANPA long-term active goal lihat #5 sebagai cta-fallback card 'Belum punya tujuan jangka panjang' + CTA 'Buat Goals →' /goals"
    - "User dengan pension_simulations row + ≥1 ht_en_* enabled lihat #6 sebagai persentase 'X%' dengan warna hijau (≥100%)/kuning (70-99%)/merah (<70%)"
    - "User TANPA pension_simulations row lihat #6 sebagai cta-fallback 'Belum simulasi pensiun' + CTA 'Hitung di sini →' /pensiun"
    - "User dengan pension_simulations.updated_at > 6 bulan lihat badge stale 'Stale Xbln' di IndikatorCard #6 (compute variant + staleMonths field)"
    - "Goal interface extended dengan optional `created_at` field via listGoals() SELECT — backward compat (existing consumers tidak break)"
    - "computeGoalsOnTrack pakai created_at kalau ada, fallback ke `1 Jan tahun ini` kalau missing"
    - "computePensiun pakai LIFE_EXPECTANCY_YEARS=75 untuk targetTotal — formula DEVIATES dari spec §4 literal: pakai (LIFE_EXPECTANCY_YEARS − usia_pensiun) sebagai years-remaining-post-retirement (decision locked plan-checker iteration 2; rationale di code comment)"
    - "Edge case guard: usia_pensiun ≥ LIFE_EXPECTANCY_YEARS → yearsRemaining floored ke 1 (Math.max) untuk avoid divide-by-zero"
    - "Tier 2 panel CTA row: 2 button [Kelola Goals → /goals] + [Simulasi pensiun → /pensiun]"
    - "Tier 2 panel modul link: 'Pelajari: Tujuan & Risiko' → /kesehatan/tujuan"
    - "KesehatanLanding.tsx TIDAK di-edit di plan ini (file ownership Plan 13-01); Plan 13-03 cuma overwrite Tier2Panel.tsx body"
  artifacts:
    - path: "src/db/goals.ts"
      provides: "Goal interface extended dengan created_at + listGoals() SELECT include created_at"
      contains: "created_at"
    - path: "src/queries/kesehatanTier2.ts"
      provides: "computeGoalsOnTrack (DIAG-05) + computePensiun (DIAG-06) implementing spec §4"
      contains: "function computeGoalsOnTrack"
    - path: "src/tabs/kesehatan/Tier2Panel.tsx"
      provides: "Tier 2 wrapper component yang inject 2 indicators + 2 CTA + 1 modul link ke TierPanel"
      min_lines: 30
  key_links:
    - from: "src/db/goals.ts listGoals SELECT"
      to: "Goal interface created_at"
      via: "additive SELECT field"
      pattern: "created_at"
    - from: "computeGoalsOnTrack"
      to: "long-term filter (target_date > now+1y AND status='active')"
      via: "Date comparison + filter"
      pattern: "target_date"
    - from: "computePensiun"
      to: "pensiun-calc.ts compute helpers (calcBPJS, calcDPPK, calcInvestasiMandiri, etc) + LIFE_EXPECTANCY_YEARS"
      via: "import + projection sum"
      pattern: "calcInvestasiMandiri|LIFE_EXPECTANCY_YEARS"
    - from: "computePensiun staleMonths"
      to: "IndikatorResult compute variant.staleMonths field"
      via: "spread {...(isStale && { staleMonths })}"
      pattern: "staleMonths"
---

<objective>
Implement compute logic Tier 2 (DIAG-05 Goals on-track, DIAG-06 Pensiun) + smart fallback CTA card pattern + UI wrapper Tier2Panel.

**Indikator Tier 2 (per spec §4):**
- **#5 Goals on-track (DIAG-05):** % goals long-term yang `current_amount/target_amount ≥ time_elapsed/total_duration`. Filter: `target_date > NOW() + 1 year AND status='active'`. Threshold ≥75% hijau, 50-74% kuning, <50% merah. **Smart fallback:** kalau no long-term goal → cta-fallback "Belum punya tujuan jangka panjang" → /goals.
- **#6 Pensiun (DIAG-06):** Formula deviates dari spec §4 literal — implementation pakai `proyeksi total ÷ (target_bulanan × 12 × (LIFE_EXPECTANCY_YEARS − usia_pensiun))`. Spec literal "× usia_harapan" treated as ambiguous; financial semantics mengarah ke "years remaining post-retirement". Decision locked plan-checker iteration 2. Threshold ≥100% hijau, 70-99% kuning, <70% merah. **Smart fallback:** kalau no pension_simulations row → cta-fallback "Belum simulasi pensiun" → /pensiun. **Stale notice:** kalau updated_at > 6 bulan, set `staleMonths` field.

**Open questions resolved (locked di plan ini):**
1. **Usia harapan hidup (RESEARCH.md Open Q1):** **HARDCODE `LIFE_EXPECTANCY_YEARS = 75`** di `src/queries/kesehatanTypes.ts` (sudah di-commit di Plan 13-01). Justifikasi: BPS Indonesia 2024 angka harapan hidup ~74 tahun, rounded ke 75 untuk simplicity dan konservatif (kasih buffer 1 tahun). Konstanta exported supaya gampang adjust pasca-rilis tanpa rebuild compute logic.
2. **IndikatorResult variant extension (RESEARCH.md Open Q2):** **EXTEND compute variant** dengan optional `staleMonths?: number` di kesehatanTypes.ts (sudah di-commit di Plan 13-01). IndikatorCard render badge "Stale Xbln" jika field ada (sudah di-commit di Plan 13-01). Plan 13-03 cuma SET field di compute output.
3. **Goal `created_at` (RESEARCH.md Open Q3 → CONTEXT.md Plan 13-03 explicit):** **EXTEND `listGoals()` SELECT include `created_at`** — backward compat additive change, no schema migration. Goal interface tambah `created_at: string`. computeGoalsOnTrack pakai created_at untuk total_duration; fallback `new Date(now.getFullYear(), 0, 1)` (1 Jan tahun ini) kalau field missing.

Purpose: Tier 2 = layer akumulasi & risiko jangka panjang (tujuan finansial + kesiapan pensiun). Smart fallback penting karena banyak user belum punya goal long-term atau belum coba simulasi pensiun.

Output:
- Modify `src/db/goals.ts`: extend listGoals SELECT + Goal interface include `created_at`
- Replace `src/queries/kesehatanTier2.ts` (Wave 1 stub) dengan implementasi real 2 compute functions
- Modify `src/tabs/kesehatan/Tier2Panel.tsx` — replace stub body (Plan 13-01) dengan wrapper TierPanel + tier 2 specific config (2 indikator + 2 CTA + 1 modul link). Props signature TIDAK berubah.
- KesehatanLanding.tsx TIDAK di-edit (file ownership exclusive — Plan 13-01 sudah wire stub Tier2Panel)

Implements:
- DIAG-05 Goals on-track (long-term + smart fallback CTA)
- DIAG-06 Pensiun readiness (compute + smart fallback CTA + stale 6+ bulan notice)

**Decision references (from CONTEXT.md):**
- D-Compute: hybrid extend existing hooks + useMemo (locked) — Plan 13-03 implementasi via kesehatanTier2.ts
- D-LIFE_EXPECTANCY: hardcode 75 (locked oleh recommendation di CONTEXT.md "Open question 1: recommend hardcode `75` (BPS Indonesia 2024 life expectancy ~74) di constant `LIFE_EXPECTANCY_YEARS = 75` exported")
- D-Goal-created_at: extend SELECT additive change (locked oleh CONTEXT.md "Open question 3 (extend goals query): goals.listGoals() belum include `created_at`. Plan task: extend SELECT include `created_at` (additive change, no schema migration)")
- D-IndikatorResult variant: extend compute dengan optional `staleMonths?: number` (locked oleh CONTEXT.md "Open question 2: Lock in 13-03 plan: variant `{ kind: 'compute', value, color, display, staleMonths?: number }`")
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-diagnostic-data-indicators/13-CONTEXT.md
@.planning/phases/13-diagnostic-data-indicators/13-RESEARCH.md
@.planning/phases/13-diagnostic-data-indicators/13-01-SUMMARY.md
@docs/superpowers/specs/2026-05-08-framework-page-design.md
@src/db/goals.ts
@src/db/pensiun.ts
@src/lib/pensiun-calc.ts
@src/queries/kesehatanTypes.ts
@src/queries/kesehatanIndikator.ts
@src/tabs/pensiun/HitungTotalPanel.tsx
@src/tabs/kesehatan/TierPanel.tsx
@src/tabs/kesehatan/KesehatanLanding.tsx

<interfaces>
<!-- Output dari Plan 13-01 yang Plan 13-03 consume -->

```ts
// src/queries/kesehatanTypes.ts (Plan 13-01)
export type IndikatorResult =
  | { kind: 'compute'; value: number; color: IndikatorColor; display: string; staleMonths?: number }
  | { kind: 'cta-fallback'; message: string; ctaLabel: string; ctaTo: string }
  | { kind: 'placeholder-data-tipis'; ... }

export const THRESHOLDS = {
  goalsOnTrack: { green: 75, yellow: 50 },
  pensiun: { green: 100, yellow: 70 },
  // ...
}
export const LIFE_EXPECTANCY_YEARS = 75  // BPS Indonesia 2024 ~74, rounded
```

```ts
// Existing types (verified):
// src/db/goals.ts — current Goal interface (PLAN AKAN EXTEND)
export interface Goal {
  id: number
  name: string
  target_amount: number
  current_amount: number
  target_date: string | null
  status: GoalStatus  // 'active' | 'completed' | 'paused'
  // PLAN 13-03 ADD: created_at: string
}

// src/db/pensiun.ts
export interface PensionSimRow {
  id: string
  user_id: string
  updated_at: string
  created_at: string
  usia: number
  usia_pensiun: number
  target_bulanan: number  // BIGINT
  masa_kerja: number
  // 'ht_en_*' boolean flags (enable per source) + ht_*_* config fields
  ht_en_bpjs: boolean | null
  ht_en_dppk: boolean | null
  ht_en_dplk: boolean | null
  ht_en_taspen: boolean | null
  ht_en_pesangon: boolean | null
  ht_en_invest: boolean | null
  // BPJS
  ht_bpjs_upah: number | null
  // DPPK
  ht_dppk_type: string | null  // 'ppmp' | 'ppip'
  ht_dppk_phdp: number | null
  ht_dppk_faktor: number | null
  ht_dppk_iuran: number | null
  // DPLK (similar — see source)
  // Taspen, Pesangon, Invest fields
  // ... 40+ fields total
  gaji_pokok: number | null
}

// src/lib/pensiun-calc.ts (existing pure functions used di HitungTotalPanel)
export function calcBPJS(args: { upahBulanan: number; masaKerja: number }): { jht: number; jpBulanan: number; total: number }
export function calcDPPK(args: { type: 'ppmp'|'ppip'; phdp: number; faktor: number; iuranBulanan: number; masaKerja: number }): { total: number }
export function calcDPLK(args: { ... }): { total: number }
export function calcTaspen(args: { ... }): { tht: number; bulanan: number; total: number }
export function calcPesangon(args: { ... }): { total: number }
export function calcInvestasiMandiri(args: { iuranBulanan: number; returnPct: number; saldoAwal: number; kenaikanPct: number; masaKerja: number }): { total: number }
```

```tsx
// HitungTotalPanel.tsx pattern (existing — yang harus di-replicate di computePensiun):
const totalLumpSum = useMemo(() => {
  let t = 0
  if (sumber.bpjs) t += sumber.bpjs.jht + sumber.bpjs.jpBulanan * 12 * 20
  if (sumber.dppk) t += sumber.dppk.total
  if (sumber.dplk) t += sumber.dplk.total
  if (sumber.taspen) t += sumber.taspen.tht + sumber.taspen.bulanan * 12 * 20
  if (sumber.pesangon) t += sumber.pesangon.total
  if (sumber.invest) t += sumber.invest.total
  return t
}, [sumber])
```

```ts
// listGoals current SELECT (src/db/goals.ts):
.from('goals')
.select('id, name, target_amount, current_amount, target_date, status')
.eq('user_id', uid)
// PLAN 13-03 ADD `created_at` ke select string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend src/db/goals.ts — Goal interface + listGoals SELECT include created_at</name>
  <files>src/db/goals.ts</files>
  <action>
**Modify `src/db/goals.ts`** — additive change. Backward compat (existing consumers di codebase tidak break karena field optional/additional, bukan rename atau type narrowing).

**Step 1: Read existing file** (gunakan Read tool) untuk verify struktur Goal interface + listGoals function. Common shape (verified dari RESEARCH.md):

```ts
export interface Goal {
  id: number
  name: string
  target_amount: number
  current_amount: number
  target_date: string | null
  status: GoalStatus
}

export async function listGoals(filters?: GoalFilters, uid?: string): Promise<Goal[]> {
  const { data, error } = await supabase
    .from('goals')
    .select('id, name, target_amount, current_amount, target_date, status')
    .eq('user_id', uid)
    // ... possibly more
  if (error) throw error
  return (data ?? []) as Goal[]
}
```

**Step 2: Modify** dengan 2 perubahan:

1. **Goal interface** — tambah field `created_at: string` (required, bukan optional, karena DB column NOT NULL DEFAULT now()):

```ts
export interface Goal {
  id: number
  name: string
  target_amount: number
  current_amount: number
  target_date: string | null
  status: GoalStatus
  created_at: string  // NEW — additive, backward compat (additional field di response shape)
}
```

2. **listGoals SELECT** — tambah `created_at` ke SELECT clause:

```ts
.select('id, name, target_amount, current_amount, target_date, status, created_at')
```

**Verify backward compat:** existing consumers di codebase yang pakai Goal type:
- `useGoals()` in `src/queries/goals.ts` — return `Goal[]`, consumers destructure `{ id, name, ... }` — tidak break (additional field tidak override).
- GoalsTab, GoalCard, dst — kalau pakai object spread atau destructure tertentu field, tetap OK (created_at jadi available).
- Semua tempat yang construct Goal manually (mis. test fixtures) HARUS update untuk include created_at. **Spot check via grep:**
   ```bash
   grep -rn "as Goal" src/ --include="*.ts" --include="*.tsx" | head
   grep -rn "Goal = {" src/ --include="*.ts" --include="*.tsx" | head
   ```
  Kalau ada test fixture atau manual construction, tambahkan `created_at: '2026-01-01T00:00:00Z'` (sample). Kalau tidak ada manual construction (semua via DB query), tidak ada perubahan tambahan.

**Step 3: Verify** dengan `npx tsc --noEmit`. Kalau TS error muncul karena consumer tertentu construct Goal tanpa created_at:
- Search lokasi: `grep -n "{ id:.*name:.*target_amount" src/`
- Tambahkan `created_at: '...'` ke literal object.

**Step 4: Verify SQL** — kolom `created_at` exist di table goals di production. Cek migration sebelumnya:
```bash
grep -n "created_at" supabase/migrations/0009_goals.sql 2>/dev/null || grep -rn "CREATE TABLE goals" supabase/migrations/ | head
```
Expected: column `created_at TIMESTAMPTZ NOT NULL DEFAULT now()` ada di migration. Kalau tidak ada (edge case), tambah migration baru `0030_goals_add_created_at.sql` — tapi kemungkinan kecil karena ini default kolom yang biasanya di-add di awal table creation.

**Comment di code** untuk jelaskan kenapa create_at di-add:

```ts
/**
 * created_at — added 2026-05-08 for Phase 13 DIAG-05 (Tier 2 Goals on-track).
 * Used by computeGoalsOnTrack untuk total_duration calculation.
 * Additive change — backward compat dengan existing consumers.
 */
```
  </action>
  <verify>
    <automated>grep -q "created_at" src/db/goals.ts && grep -q "created_at: string" src/db/goals.ts && grep -q "created_at" src/queries/goals.ts || true && npx tsc --noEmit</automated>
  </verify>
  <done>Goal interface include created_at: string. listGoals SELECT include created_at. TypeScript compile zero error (existing consumers backward compat).</done>
</task>

<task type="auto">
  <name>Task 2: Implement compute logic real di src/queries/kesehatanTier2.ts (replace Wave 1 stubs)</name>
  <files>src/queries/kesehatanTier2.ts</files>
  <action>
**Replace seluruh isi `src/queries/kesehatanTier2.ts`** (saat ini stub) dengan implementasi real 2 compute functions.

**File: `src/queries/kesehatanTier2.ts`**

```ts
import type { Goal } from '@/db/goals'
import type { PensionSimRow } from '@/db/pensiun'
import {
  calcBPJS,
  calcDPPK,
  calcDPLK,
  calcTaspen,
  calcPesangon,
  calcInvestasiMandiri,
} from '@/lib/pensiun-calc'
import {
  THRESHOLDS,
  LIFE_EXPECTANCY_YEARS,
  type IndikatorColor,
  type IndikatorResult,
} from './kesehatanTypes'

// ============================================================
// #5 computeGoalsOnTrack (DIAG-05)
// ============================================================
//
// Long-term goal filter: target_date > NOW() + 1 year AND status = 'active'
// On-track: current_amount/target_amount >= time_elapsed/total_duration
// Smart fallback: no long-term goal → cta-fallback /goals
// ============================================================

export function computeGoalsOnTrack(goals: Goal[]): IndikatorResult {
  const now = new Date()
  const oneYearFromNow = new Date(now)
  oneYearFromNow.setFullYear(oneYearFromNow.getFullYear() + 1)

  const longTerm = goals.filter(
    g =>
      g.status === 'active' &&
      g.target_date !== null &&
      new Date(g.target_date) > oneYearFromNow,
  )

  if (longTerm.length === 0) {
    return {
      kind: 'cta-fallback',
      message: 'Belum punya tujuan jangka panjang',
      ctaLabel: 'Buat Goals →',
      ctaTo: '/goals',
    }
  }

  // Linear progress check
  const onTrack = longTerm.filter(g => {
    // Use g.created_at (Plan 13-03 Task 1 added). Fallback: 1 Jan tahun ini.
    const start = g.created_at
      ? new Date(g.created_at)
      : new Date(now.getFullYear(), 0, 1)
    const end = new Date(g.target_date!)  // non-null per filter

    const totalMs = end.getTime() - start.getTime()
    if (totalMs <= 0) {
      // Edge case: target_date <= start (mis. created in future, atau date salah)
      // Treat as on-track (avoid penalize karena data invalid).
      return true
    }

    const elapsedMs = Math.max(0, now.getTime() - start.getTime())
    const timeElapsedFrac = Math.min(1, elapsedMs / totalMs)
    const progressFrac = g.target_amount > 0 ? g.current_amount / g.target_amount : 0

    return progressFrac >= timeElapsedFrac
  })

  const pct = (onTrack.length / longTerm.length) * 100
  const color: IndikatorColor =
    pct >= THRESHOLDS.goalsOnTrack.green
      ? 'green'
      : pct >= THRESHOLDS.goalsOnTrack.yellow
      ? 'yellow'
      : 'red'

  return {
    kind: 'compute',
    value: pct,
    color,
    display: `${onTrack.length}/${longTerm.length} on-track`,
  }
}

// ============================================================
// #6 computePensiun (DIAG-06)
// ============================================================
//
// Formula DEVIATES INTENTIONALLY dari spec §4 literal "× usia_harapan":
// Implementation: proyeksi total ÷ (target_bulanan × 12 × (LIFE_EXPECTANCY_YEARS - usia_pensiun))
// Spec literal "× usia_harapan" (75) akan inflate target 3.75x — semantic incorrect.
// Decision locked Phase 13 plan-checker iteration 2 (rationale di code comment).
// Ratio ≥100% hijau, 70-99% kuning, <70% merah.
// Smart fallback: no row → cta-fallback /pensiun
// Stale notice: updated_at > 6 bulan → set staleMonths field
//
// Proyeksi total: SUM dari aktif source (BPJS/DPPK/DPLK/Taspen/Pesangon/Invest)
// reuse calc helpers dari src/lib/pensiun-calc.ts (exact pattern dari HitungTotalPanel.tsx)
// ============================================================

const STALE_THRESHOLD_MONTHS = 6

function computeProjectionTotal(sim: PensionSimRow): number {
  const masaKerja = sim.masa_kerja ?? 0
  let total = 0

  if (sim.ht_en_bpjs && sim.ht_bpjs_upah !== null) {
    const r = calcBPJS({
      upahBulanan: sim.ht_bpjs_upah ?? sim.gaji_pokok ?? 0,
      masaKerja,
    })
    // Pattern dari HitungTotalPanel: jht (lumpsum) + jpBulanan × 12 × 20 (asumsi 20 tahun pensiun draw)
    total += r.jht + r.jpBulanan * 12 * 20
  }

  if (sim.ht_en_dppk && sim.ht_dppk_type) {
    const r = calcDPPK({
      type: sim.ht_dppk_type as 'ppmp' | 'ppip',
      phdp: sim.ht_dppk_phdp ?? 0,
      faktor: sim.ht_dppk_faktor ?? 0,
      iuranBulanan: sim.ht_dppk_iuran ?? 0,
      masaKerja,
    })
    total += r.total
  }

  if (sim.ht_en_dplk) {
    // DPLK params: read dari sim.ht_dplk_* (cek source field names di src/db/pensiun.ts)
    // Skeleton: kalau field signature beda, adjust mapping. RESEARCH.md sebut "kalau ht_en_dplk, taspen, pesangon, invest — sama".
    const r = calcDPLK({
      iuranBulanan: (sim as Record<string, unknown>).ht_dplk_iuran as number ?? 0,
      returnPct: (sim as Record<string, unknown>).ht_dplk_return as number ?? 0,
      saldoAwal: (sim as Record<string, unknown>).ht_dplk_saldo as number ?? 0,
      masaKerja,
    } as Parameters<typeof calcDPLK>[0])
    total += r.total
  }

  if (sim.ht_en_taspen) {
    const r = calcTaspen({
      gajiPokok: (sim as Record<string, unknown>).ht_taspen_gaji as number ?? sim.gaji_pokok ?? 0,
      masaKerja,
    } as Parameters<typeof calcTaspen>[0])
    total += r.tht + r.bulanan * 12 * 20
  }

  if (sim.ht_en_pesangon) {
    const r = calcPesangon({
      gajiPokok: (sim as Record<string, unknown>).ht_pesangon_gaji as number ?? sim.gaji_pokok ?? 0,
      masaKerja,
    } as Parameters<typeof calcPesangon>[0])
    total += r.total
  }

  if (sim.ht_en_invest) {
    const r = calcInvestasiMandiri({
      iuranBulanan: (sim as Record<string, unknown>).ht_inv_bulanan as number ?? 0,
      returnPct: (sim as Record<string, unknown>).ht_inv_return as number ?? 0,
      saldoAwal: (sim as Record<string, unknown>).ht_inv_saldo as number ?? 0,
      kenaikanPct: (sim as Record<string, unknown>).ht_inv_kenaikan as number ?? 0,
      masaKerja,
    })
    total += r.total
  }

  return total
}

export function computePensiun(sim: PensionSimRow | null | undefined): IndikatorResult {
  if (!sim) {
    return {
      kind: 'cta-fallback',
      message: 'Belum simulasi pensiun',
      ctaLabel: 'Hitung di sini →',
      ctaTo: '/pensiun',
    }
  }

  // Stale check
  const updatedAt = new Date(sim.updated_at)
  const monthsStale = (Date.now() - updatedAt.getTime()) / (1000 * 60 * 60 * 24 * 30)
  const isStale = monthsStale > STALE_THRESHOLD_MONTHS

  // Compute proyeksi total
  const totalLumpSum = computeProjectionTotal(sim)

  // Edge case: kalau no source enabled, totalLumpSum = 0 → ratio 0
  if (totalLumpSum === 0) {
    return {
      kind: 'cta-fallback',
      message: 'Simulasi pensiun belum punya source aktif',
      ctaLabel: 'Lengkapi simulasi →',
      ctaTo: '/pensiun',
    }
  }

  /**
   * Formula DEVIATES INTENTIONALLY from spec §4 literal "× usia_harapan".
   *
   * Spec literal:    proyeksi ÷ (target_bulanan × 12 × usia_harapan)
   * Implementation:  proyeksi ÷ (target_bulanan × 12 × (LIFE_EXPECTANCY_YEARS - usia_pensiun))
   *
   * Rationale: User pensiun di usia 55 butuh dana untuk 20 tahun PASCA-pensiun
   * (75 − 55), bukan 75 tahun total. Spec wording ambiguous; financial semantics
   * mengarah ke "years remaining after retirement" — kalau dipakai literal "× 75",
   * target jadi 3.75x lebih besar dari kebutuhan riil → semua user merah artificial.
   *
   * Decision locked di Phase 13 planning, sign-off via plan-checker iteration 2.
   *
   * Edge case guard: usia_pensiun ≥ LIFE_EXPECTANCY_YEARS → years remaining ≤ 0 →
   * targetTotal = 0 → ratio = Infinity. Mitigation: floor yearsRemaining ke minimum 1
   * (Math.max) untuk avoid divide-by-zero. Kalau usia_pensiun > 75, treat sebagai
   * "sudah cover sisa hidup" — yearsRemaining=1 = ratio inflated, render compute
   * green (acceptable: user yang plan pensiun setelah usia harapan = edge case rare).
   */
  const yearsRemaining = Math.max(LIFE_EXPECTANCY_YEARS - sim.usia_pensiun, 1)
  const targetTotal = sim.target_bulanan * 12 * yearsRemaining

  // Edge case: target_bulanan 0 atau negatif → ratio undefined; render sebagai compute red
  if (targetTotal <= 0) {
    return {
      kind: 'compute',
      value: 0,
      color: 'red',
      display: '— (set target bulanan)',
      ...(isStale && { staleMonths: Math.floor(monthsStale) }),
    }
  }

  const ratio = (totalLumpSum / targetTotal) * 100
  const color: IndikatorColor =
    ratio >= THRESHOLDS.pensiun.green
      ? 'green'
      : ratio >= THRESHOLDS.pensiun.yellow
      ? 'yellow'
      : 'red'

  return {
    kind: 'compute',
    value: ratio,
    color,
    display: `${ratio.toFixed(0)}%`,
    ...(isStale && { staleMonths: Math.floor(monthsStale) }),
  }
}
```

**Penjelasan computeProjectionTotal:**
- Pattern persis dari `HitungTotalPanel.tsx` line 50-59 (RESEARCH.md):
  - BPJS: `jht (lumpsum) + jpBulanan × 12 × 20` (20 tahun pensiun draw assumption)
  - Taspen: `tht + bulanan × 12 × 20`
  - DPPK/DPLK/Pesangon/Invest: `.total`
- `ht_en_*` flags gate masing-masing source — kalau false/null, skip.
- Field names `ht_dplk_*`, `ht_taspen_*`, `ht_pesangon_*`, `ht_inv_*`: TYPE BLIND CAST via `Record<string, unknown>` karena PensionSimRow di `src/db/pensiun.ts` punya banyak field (40+) dan dalam beberapa skema field signature pensiun-calc bisa beda. **Eksekutor Task 2 HARUS verify dengan baca `src/db/pensiun.ts` PensionSimRow + `src/lib/pensiun-calc.ts` signature** sebelum commit. Adjust field reads agar match aktual signatures.
- Kalau ada field name mismatch, fallback gracefully (return 0 untuk source itu) — tetap compile tanpa runtime crash.

**View-As compatibility:** Reuse pattern dari Plan 13-01 — `usePensionSim()` already View-As-aware via useTargetUserId. RESEARCH.md flagged: pension_simulations RLS hanya `auth.uid() = user_id` (no `is_admin()` fallback) → admin View-As ke user lain → `usePensionSim()` returns null → DIAG-06 show cta-fallback. **Acceptable per CONTEXT.md** (informative, no crash). Document di SUMMARY.md.

**Stale notice rendering:** Plan 13-01 `IndikatorCard` compute variant sudah handle `staleMonths` field — render badge "Stale Xbln" kuning warna. Plan 13-03 cuma SET field; UI rendering sudah ready.

**Verify:** `npx tsc --noEmit`. Kalau type cast error karena PensionSimRow signature beda dengan asumsi di code skeleton:
- Read actual `src/db/pensiun.ts` PensionSimRow.
- Update field names di `computeProjectionTotal` sesuai signature aktual.
- Kalau calcDPLK/calcTaspen/calcPesangon signature di pensiun-calc.ts beda dari yang di-asumsi, adjust args object.

**Pragmatik:** Kalau menjelang implementasi field signatures terlalu sulit di-resolve secara generik, fallback ke MINIMAL implementasi: cuma compute BPJS + Investasi (2 source paling umum), skip DPPK/DPLK/Taspen/Pesangon dengan komentar TODO. Compute tetap valid untuk sebagian besar user; underestimate ratio acceptable (bias konservatif > over-optimistic). Document di SUMMARY.md kalau fallback path diambil.
  </action>
  <verify>
    <automated>grep -q "function computeGoalsOnTrack" src/queries/kesehatanTier2.ts && grep -q "function computePensiun" src/queries/kesehatanTier2.ts && grep -q "longTerm.length === 0" src/queries/kesehatanTier2.ts && grep -q "Belum punya tujuan jangka panjang" src/queries/kesehatanTier2.ts && grep -q "Belum simulasi pensiun" src/queries/kesehatanTier2.ts && grep -q "LIFE_EXPECTANCY_YEARS" src/queries/kesehatanTier2.ts && grep -q "staleMonths" src/queries/kesehatanTier2.ts && grep -q "calcInvestasiMandiri" src/queries/kesehatanTier2.ts && npx tsc --noEmit</automated>
  </verify>
  <done>computeGoalsOnTrack: filter long-term goals + linear progress check + smart fallback CTA. computePensiun: project total via calc helpers (reuse HitungTotalPanel pattern) + LIFE_EXPECTANCY_YEARS=75 target + smart fallback + stale 6+bulan notice. TS compile zero error.</done>
</task>

<task type="auto">
  <name>Task 3: Replace Tier2Panel.tsx stub body dengan wrapper TierPanel + Tier 2 specific config (NO KesehatanLanding edit)</name>
  <files>src/tabs/kesehatan/Tier2Panel.tsx</files>
  <action>
**File 1 (NEW): `src/tabs/kesehatan/Tier2Panel.tsx`** — wrapper TierPanel untuk Tier 2 specific config.

```tsx
import TierPanel from './TierPanel'
import type { IndikatorMap } from '@/queries/kesehatan'

type Props = {
  indicators: IndikatorMap
}

/**
 * Tier 2 — AKUMULASI panel.
 *
 * Per spec §4 + §4.5:
 * - 2 indikator: Goals on-track (#5), Pensiun (#6)
 * - 2 CTA: Kelola Goals → /goals, Simulasi pensiun → /pensiun
 * - Modul link: Tujuan & Risiko → /kesehatan/tujuan
 *
 * Smart fallback handled di compute layer (kesehatanTier2.ts):
 * - User tanpa long-term goal → indicators['5'] = cta-fallback variant
 * - User tanpa pension_simulations → indicators['6'] = cta-fallback variant
 * - Stale 6+ bulan → indicators['6'].staleMonths set, IndikatorCard render badge
 */
export default function Tier2Panel({ indicators }: Props) {
  return (
    <TierPanel
      tierId={2}
      indicators={[
        {
          label: 'Goals Long-term on-track',
          thresholdHint: '≥ 75% on-track hijau · 50-74% kuning · < 50% merah',
          result: indicators['5'],
        },
        {
          label: 'Kesiapan Pensiun',
          thresholdHint: '≥ 100% target hijau · 70-99% kuning · < 70% merah',
          result: indicators['6'],
        },
      ]}
      ctas={[
        { label: 'Kelola Goals', to: '/goals', variant: 'default' },
        { label: 'Simulasi pensiun', to: '/pensiun', variant: 'outline' },
      ]}
      modulLinks={[
        { label: 'Tujuan & Risiko', slug: 'tujuan' },
      ]}
    />
  )
}
```

---

**File ownership exclusive (Plan 13-01 sudah wire):** Plan 13-01 sudah buat stub Tier2Panel.tsx + sudah wire <Tier2Panel ... /> di KesehatanLanding.tsx renderTierContent. Plan 13-03 cuma OVERWRITE body Tier2Panel.tsx — TIDAK edit KesehatanLanding.tsx.

**Action eksekutor:**
1. Buka `src/tabs/kesehatan/Tier2Panel.tsx` (stub dari Plan 13-01).
2. **Replace ENTIRE file** dengan kode di atas (props signature `{ indicators: IndikatorMap }` IDENTIK — hanya body return yang berubah dari placeholder ke 2 IndikatorCard + 2 CTA + 1 modul link).

**Verify NO edit ke KesehatanLanding.tsx:**
```bash
git diff src/tabs/kesehatan/KesehatanLanding.tsx | wc -l   # expect 0
```

Wave 2 plans 13-02/13-03/13-04 dapat run paralel zero conflict — masing-masing modify file Panel sendiri.
  </action>
  <verify>
    <automated>test -f src/tabs/kesehatan/Tier2Panel.tsx && grep -q "Tujuan & Risiko" src/tabs/kesehatan/Tier2Panel.tsx && grep -q "/goals" src/tabs/kesehatan/Tier2Panel.tsx && grep -q "/pensiun" src/tabs/kesehatan/Tier2Panel.tsx && grep -q "tujuan" src/tabs/kesehatan/Tier2Panel.tsx && ! grep -q "STUB Wave 1" src/tabs/kesehatan/Tier2Panel.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Tier2Panel.tsx stub body REPLACED dengan real implementation: TierPanel wrap 2 indikator + 2 CTA + 1 modul link. KesehatanLanding TIDAK diedit. TS compile + production build success.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual UAT — verify Tier 2 panel render dengan smart fallback + stale notice</name>
  <files>(visual UAT — no file changes)</files>
  <action>
**What's been built:**
- `src/db/goals.ts`: Goal interface tambah `created_at`, listGoals SELECT include created_at
- `kesehatanTier2.ts`: 2 compute functions real (DIAG-05 Goals on-track + DIAG-06 Pensiun) dengan smart fallback CTA + stale notice
- `Tier2Panel.tsx`: wrapper TierPanel dengan 2 indicators + 2 CTA + 1 modul link
- KesehatanLanding wire: klik tier 2 trapezoid → Accordion slide-down menampilkan Tier2Panel

**Steps untuk human:**

**Setup:**
```bash
npm run dev
```
Open http://localhost:5173/kesehatan di Chrome desktop. Login dengan akun production.

**Test 1 — Tier 2 trapezoid color reflects aggregate:**
1. Navigate ke /kesehatan.
2. Lihat warna tier 2 (medium width, atas Tier 1).
3. Klik tier 2 → accordion slide-down menampilkan Tier2Panel.
4. Verify aggregate color: jika #5 dan #6 keduanya cta-fallback → tier color gray (semua placeholder/fallback per spec §4); jika ada compute → color sesuai aggregateTierColor logic.

**Test 2 — DIAG-05 Goals on-track scenarios:**

**Scenario A — User tanpa long-term goal:**
1. Login dengan akun yang belum punya goal `target_date > now+1 year AND status='active'`.
2. Klik tier 2.
3. Expected #5 IndikatorCard: cta-fallback variant
   - Header "Goals Long-term on-track"
   - Badge "Belum ada" gray
   - Pesan "Belum punya tujuan jangka panjang"
   - Button "Buat Goals →" → klik navigate ke /goals
4. Klik tier 2 → tier aggregate color tetap (mungkin gray atau sesuai #6).

**Scenario B — User dengan long-term goal:**
1. Login dengan akun yang punya minimal 1 goal `target_date > now+1y AND status='active'`. Bisa simulate dengan SQL Editor:
   ```sql
   INSERT INTO goals (user_id, name, target_amount, current_amount, target_date, status, created_at)
   VALUES (auth.uid(), 'Test Long-term', 100000000, 25000000, NOW() + INTERVAL '2 years', 'active', NOW() - INTERVAL '6 months');
   ```
   - Goal ini: target 100jt, current 25jt = 25% progress; created 6 bulan lalu, target 2 tahun → durasi total 30 bulan, elapsed 6 bulan → 20% time elapsed. Progress (25%) > timeElapsed (20%) → on-track.
2. Klik tier 2.
3. Expected #5: compute variant "1/1 on-track" badge hijau (≥75%).
4. Tambah goal kedua off-track:
   ```sql
   INSERT INTO goals (user_id, name, target_amount, current_amount, target_date, status, created_at)
   VALUES (auth.uid(), 'Test Off-track', 100000000, 5000000, NOW() + INTERVAL '2 years', 'active', NOW() - INTERVAL '12 months');
   ```
   - 5% progress vs 33% time elapsed → off-track.
5. Reload /kesehatan, klik tier 2.
6. Expected #5: "1/2 on-track" = 50% kuning.

**Scenario C — Smart fallback transition:**
1. Setelah punya goal long-term, klik "Buat Goals →" sebelumnya tidak relevan (di scenario A). Sekarang user lihat compute display.
2. Hapus goal:
   ```sql
   UPDATE goals SET status = 'paused' WHERE user_id = auth.uid() AND name LIKE 'Test%';
   ```
3. Reload, klik tier 2 → kembali ke cta-fallback "Belum punya tujuan jangka panjang".

**Test 3 — DIAG-06 Pensiun scenarios:**

**Scenario A — User tanpa pension_simulations row:**
1. Akun yang belum pernah simulasi pensiun.
2. Klik tier 2.
3. Expected #6 IndikatorCard: cta-fallback "Belum simulasi pensiun" + button "Hitung di sini →" → /pensiun.

**Scenario B — User dengan pension_simulations row + ht_en_invest = true:**
1. Navigate ke /pensiun, isi simulasi DCA (mis. saldo awal 10jt, iuran 1jt/bln, return 8%, masa kerja 20 tahun, target_bulanan 5jt). Save.
2. Back ke /kesehatan, klik tier 2.
3. Expected #6: compute variant dengan persen ratio.
   - Estimasi: total invest ~~ Rp 600jt-700jt setelah 20 tahun
   - Target: 5jt × 12 × (75-pensiun_age). Misal pensiun_age=55 → 5jt × 12 × 20 = 1.2M
   - Ratio ~50% → merah, atau lebih tinggi tergantung detail input.
4. Verify warna sesuai threshold ≥100/70-99/<70.

**Scenario C — Stale notice 6+ bulan:**
1. Manual update via SQL Editor:
   ```sql
   UPDATE pension_simulations SET updated_at = NOW() - INTERVAL '7 months' WHERE user_id = auth.uid();
   ```
2. Reload /kesehatan, klik tier 2.
3. Expected #6 IndikatorCard:
   - Badge utama tetap warna sesuai ratio
   - Tambahan badge kecil kuning "Stale 7bln" di samping label
4. Hover badge stale → tooltip atau visual hint "Simulasi terakhir: 7 bulan lalu".

**Test 4 — Threshold hint visible:**
1. Verify sub-label di kedua IndikatorCard:
   - "≥ 75% on-track hijau · 50-74% kuning · < 50% merah"
   - "≥ 100% target hijau · 70-99% kuning · < 70% merah"

**Test 5 — CTA + modul link navigate:**
1. Klik button "Kelola Goals" → URL `/goals` → GoalsTab render.
2. Back, klik "Simulasi pensiun" → URL `/pensiun` → PensiunTab render.
3. Back, klik link "Pelajari: Tujuan & Risiko" → URL `/kesehatan/tujuan` → wildcard redirect ke /dashboard (Phase 15 akan fill).

**Test 6 — View-As compatibility:**
1. Login admin, View-As ke user lain.
2. Navigate /kesehatan, klik tier 2.
3. Expected:
   - #5 Goals on-track pakai data viewed-user.
   - #6 Pensiun: kemungkinan besar tampil cta-fallback (RLS pension_simulations tidak punya `OR is_admin()` per RESEARCH.md pitfall #3 → admin View-As dapat null). **Acceptable** — tampil "Belum simulasi pensiun" message + CTA. Jangan crash.

**Test 7 — Edge cases:**
1. **Goal target_date null:** filter `g.target_date !== null` exclude → tidak masuk longTerm.
2. **Goal status='paused':** filter `g.status === 'active'` exclude.
3. **Goal target_amount=0:** progressFrac = 0, treat off-track. Tidak crash.
4. **Pension target_bulanan=0:** computePensiun render "— (set target bulanan)" red.
5. **Pension no source enabled (semua ht_en_*=false):** cta-fallback "Simulasi pensiun belum punya source aktif".

**Test 8 — Mobile responsive:**
1. Resize ≤640px.
2. Klik tier 2 → 2 IndikatorCard stacked + 2 CTA + 1 modul link.
3. Verify text wrap acceptable, no overflow.

**Test 9 — Build success:**
```bash
npm run build
```
Expected: zero TS error, bundle delta <15KB tambahan.

**Report back:**
- Screenshot Tier 2 panel scenario A (cta-fallback both) — Test 2A + 3A
- Screenshot Tier 2 panel scenario B (compute both) — Test 2B + 3B
- Screenshot stale notice badge — Test 3C
- Confirmation CTA navigate — Test 5
- View-As result — Test 6
- Mobile screenshot — Test 8
- Build output — Test 9

**Issues to flag:**
- Smart fallback tidak muncul saat no long-term goal / no pension row
- Stale notice tidak render saat updated_at > 6 bulan
- created_at fallback bocor (compute panic kalau field missing — should fallback ke 1 Jan tahun ini)
- Pensiun ratio terlalu tinggi/rendah karena LIFE_EXPECTANCY_YEARS asumsi salah
- DPPK/DPLK/Taspen field signature mismatch dengan pensiun-calc → compute return 0 → underestimate
  </action>
  <verify>
    <automated>echo "Manual visual UAT — verify Tier 2 panel + smart fallback + stale notice"</automated>
  </verify>
  <done>Visual UAT pass dengan screenshots semua scenario (cta-fallback, compute, stale), no regression vs Plan 13-02</done>
  <resume-signal>Type "approved" + tempel screenshots Test 2, 3, 8, atau describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → goals + pension_simulations queries | useIndikator (Plan 13-01) compose; Plan 13-03 cuma compute pure functions. |
| created_at SELECT extension | Additive change ke listGoals SELECT — no new RLS surface, RLS policy goals existing tetap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | T | computeGoalsOnTrack created_at fallback ke 1 Jan tahun ini saat field missing | accept | Backward compat — kalau ada existing row di DB tanpa created_at (unlikely karena DEFAULT now()), fallback memberikan estimasi reasonable. Document di SUMMARY.md. |
| T-13-14 | I | listGoals expose created_at di response — internal data shape change | accept | created_at tidak sensitive (timestamp pembuatan goal own user). RLS goals tetap sama (auth.uid() = user_id OR is_admin()). Tidak ada IDOR baru. |
| T-13-15 | T | computeProjectionTotal field signature mismatch dengan pensiun-calc.ts | mitigate | Type cast Record<string, unknown> + fallback ke 0 untuk source dengan field missing. Pragmatic fallback (BPJS + Invest only) acceptable, document di SUMMARY.md. |
| T-13-16 | I | View-As pension_simulations RLS leak (admin View-As → null) | accept | RLS pension_simulations `auth.uid() = user_id` (no `OR is_admin()`) per RESEARCH.md. computePensiun returns cta-fallback graceful. **Tidak fix di Phase 13** — kandidat patch Phase 14 atau migration terpisah. |
| T-13-17 | E | LIFE_EXPECTANCY_YEARS hardcode bisa jadi nilai unrealistic (mis. 75 ke depan terbukti rendah/tinggi) | accept | Konstanta exported di kesehatanTypes.ts — gampang adjust pasca-rilis. Tracked di STATE.md blockers/concerns "Threshold belum tervalidasi empiris". |
| T-13-17b | T | Formula deviasi dari spec §4 literal "× usia_harapan" → potensi mismatch interpretation user | mitigate | Code comment block dokumentasi rationale + plan-checker iteration 2 sign-off. SUMMARY.md harus dokumentasi keputusan ini. Edge case guard Math.max(yearsRemaining, 1) prevent divide-by-zero kalau usia_pensiun ≥ LIFE_EXPECTANCY_YEARS. |
| T-13-18 | T | Stale notice false positive saat sim manual update via SQL tanpa updated_at change | accept | RESEARCH.md pitfall #6 — updated_at di-set oleh app (`upsertPensionSim()`), bukan DB trigger. Manual SQL update bisa miss. Acceptable v1.2 (rare admin edge case). |

</threat_model>

<verification>
- `src/db/goals.ts`: Goal interface tambah `created_at: string`, listGoals SELECT include created_at — TS compile zero error
- `kesehatanTier2.ts` exported 2 functions: computeGoalsOnTrack, computePensiun
- computeGoalsOnTrack: filter long-term `target_date > now+1y AND status='active'`, smart fallback kalau empty, linear progress check pakai created_at + fallback 1 Jan tahun ini
- computePensiun: smart fallback kalau no row, computeProjectionTotal reuse calc helpers per HitungTotalPanel pattern, target = target_bulanan × 12 × (LIFE_EXPECTANCY_YEARS - usia_pensiun), staleMonths field set kalau updated_at > 6 bulan
- LIFE_EXPECTANCY_YEARS=75 imported dari kesehatanTypes.ts (Plan 13-01)
- Tier2Panel render 2 indikator + 2 CTA (Kelola Goals, Simulasi pensiun) + 1 modul link (Tujuan & Risiko → /kesehatan/tujuan)
- KesehatanLanding TIDAK di-edit di plan ini — sudah wired ke stub di Plan 13-01, body Tier2Panel rewrite cukup
- Visual UAT pass: cta-fallback both, compute both, stale notice render, View-As graceful, mobile responsive, build success
</verification>

<success_criteria>
1. `Goal.created_at` field ada di interface + listGoals SELECT — backward compat verified (existing consumers tidak break)
2. computeGoalsOnTrack filter long-term: `target_date > now+1y AND status='active'`
3. Smart fallback DIAG-05: no long-term goal → cta-fallback "Belum punya tujuan jangka panjang" + CTA /goals
4. Linear progress check: `current/target >= time_elapsed/total_duration` dengan created_at (fallback 1 Jan tahun ini)
5. Threshold ≥75/50-74/<50 verified visual (compute variant warna match expected)
6. Smart fallback DIAG-06: no pension_simulations row → cta-fallback "Belum simulasi pensiun" + CTA /pensiun
7. computePensiun reuse calc helpers (calcBPJS, calcDPPK, calcInvestasiMandiri, dst) per HitungTotalPanel pattern
8. LIFE_EXPECTANCY_YEARS=75 used untuk targetTotal calculation
9. Stale notice: updated_at > 6 bulan → staleMonths field set, IndikatorCard render badge "Stale Xbln"
10. Tier2Panel: 2 IndikatorCard + 2 CTA + 1 modul link (Tujuan & Risiko)
11. View-As graceful: pension_simulations RLS leak → computePensiun returns cta-fallback (no crash)
12. `npx tsc --noEmit` + `npm run build` success
</success_criteria>

<output>
After completion, create `.planning/phases/13-diagnostic-data-indicators/13-03-SUMMARY.md` dengan:
- Files modified: `src/db/goals.ts` (Goal+listGoals), `kesehatanTier2.ts` (replaced stub), `Tier2Panel.tsx` (replaced stub body — real wrapper). `KesehatanLanding.tsx` TIDAK diedit (Plan 13-01 ownership).
- Visual UAT results: screenshots scenario cta-fallback + compute + stale notice + mobile
- Open questions resolved:
  - LIFE_EXPECTANCY_YEARS=75 (BPS Indonesia 2024)
  - IndikatorResult compute variant extended dengan staleMonths (sudah Plan 13-01)
  - Goal.created_at extended (backward compat additive)
- **Formula deviation locked (computePensiun):** SUMMARY.md HARUS document keputusan deviasi dari spec §4 literal "× usia_harapan". Implementation pakai (LIFE_EXPECTANCY_YEARS − usia_pensiun) untuk years-remaining-post-retirement. Rationale: spec wording ambiguous; financial semantics. Plan-checker iteration 2 sign-off. Edge case guard yearsRemaining ≥ 1 (Math.max).
- **Pension source compute coverage (W5 advisory):** SUMMARY.md MUST list explicit per-source compute outcome — untuk masing-masing dari 6 sources (BPJS / DPPK / DPLK / Taspen / Pesangon / Investasi Mandiri) tulis: SUCCESS (real value) atau FALLBACK (returns 0 karena field signature mismatch). Format table: `| Source | ht_en_* | Compute | Field signature match | Notes |`. Kalau ada source fallback ke 0, document field name yang missing/mismatch supaya Phase 14 bisa fix.
- computeProjectionTotal pattern dari HitungTotalPanel — verify all 6 sources atau document fallback minimal (BPJS+Invest only) kalau field signature mismatch
- View-As caveat: pension_simulations RLS no `is_admin()` → admin View-As dapat cta-fallback. Document untuk Phase 14 backlog (kandidat fix RLS atau accept).
- Hand-off: Pattern Tier1Panel/Tier2Panel established. Plan 13-04 follow same pattern (Tier3Panel body rewrite — KesehatanLanding sudah wire stub di Plan 13-01).
</output>
</content>
</invoke>