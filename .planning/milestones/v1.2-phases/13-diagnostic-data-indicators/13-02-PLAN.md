---
phase: 13-diagnostic-data-indicators
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/queries/kesehatanTier1.ts
  - src/tabs/kesehatan/Tier1Panel.tsx
autonomous: false
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-10]
must_haves:
  truths:
    - "User dengan ≥3 bulan kalender data transactions lihat 4 IndikatorCard di Tier 1: Dana Darurat (X bulan), Savings Rate (Y%), DAR Konsumtif (Z%), Asuransi Kesehatan (covered/belum diisi)"
    - "User dengan <3 bulan kalender data lihat #1 dan #2 sebagai placeholder 'Butuh 3 bulan data, sudah X/3' + CTA /transaksi (tidak ikut agregasi warna tier)"
    - "Indikator #3 DAR Konsumtif: <20% hijau, 20-40% kuning, >40% merah (INVERTED — semakin kecil semakin hijau)"
    - "Tier 1 panel render DAR Total info row sebagai info tambahan (bukan indikator warna) — tampil 'X% (mayoritas KPR)' atau '(non-KPR)' tergantung kprFraction"
    - "Tier 1 panel CTA row: 2 button [Kelola akun → /kekayaan] + [Catat transaksi → /transaksi]"
    - "Tier 1 panel modul link: 'Pelajari: Pondasi & Cash Flow' → /kesehatan/arus-kas"
    - "KesehatanLanding.tsx TIDAK di-edit di plan ini (file ownership Plan 13-01); Plan 13-02 cuma overwrite Tier1Panel.tsx body"
    - "Compute functions di kesehatanTier1.ts: computeDanaDarurat, computeSavingsRate, computeDARKonsumtif, computeDARTotal, computeAsuransiShell — semua implement formula spec §4 verbatim"
    - "Asuransi Kesehatan shell: read protection_checklist.health_coverage; null/undefined/'tidak' → red 'Belum diisi'; else → green dengan label coverage"
  artifacts:
    - path: "src/queries/kesehatanTier1.ts"
      provides: "5 compute functions Tier 1 implementing DIAG-01/02/03/10 + Tier 1 #4 shell read"
      contains: "function computeDanaDarurat"
    - path: "src/tabs/kesehatan/Tier1Panel.tsx"
      provides: "Tier 1 wrapper component yang inject indicators + DAR Total infoSlot + CTAs + modulLinks ke TierPanel"
      min_lines: 40
  key_links:
    - from: "src/queries/kesehatanTier1.ts computeDanaDarurat"
      to: "transactions data calendar month grouping (date.substring(0,7))"
      via: "Set<YYYY-MM> distinct months filter"
      pattern: "substring\\(0,\\s*7\\)"
    - from: "Tier1Panel"
      to: "useIndikator().indicators + .darTotalInfo"
      via: "props passed dari KesehatanLanding"
      pattern: "indicators\\['1'\\]"
---

<objective>
Implement compute logic + UI wrapper untuk **Tier 1 — PROTEKSI**. 4 indikator + DAR Total info + 2 CTA + 1 modul link.

Indikator yang di-compute (per spec §4):
- **#1 Dana Darurat (DIAG-01):** `SUM(akun likuid) ÷ avg(expense bulanan, 3 bulan kalender)`. Threshold ≥6 hijau, 3-5 kuning, <3 merah. Edge case <3 bulan → placeholder.
- **#2 Savings Rate (DIAG-02):** `(income − expense) ÷ income`, average per bulan kalender lalu rata-rata 3 bulan. Threshold ≥20% hijau, 10-19% kuning, <10% merah. Edge case <3 bulan → placeholder.
- **#3 DAR Konsumtif (DIAG-03):** `SUM(liabilities WHERE type ≠ 'kpr') ÷ aset finansial`. Threshold <20% hijau, 20-40% kuning, >40% merah (inverted).
- **#4 Asuransi Kesehatan SHELL (DIAG, partial — Phase 14 form mutation):** read `protection_checklist.health_coverage`; null/'tidak' → red 'Belum diisi', else → green dengan label.

Plus **DAR Total info row** (bukan indikator warna): `SUM(all liabilities) ÷ aset finansial` ditampilkan dengan kontekstualisasi mayoritas KPR vs non-KPR.

Purpose: Tier 1 = pondasi proteksi finansial (likuiditas, savings, beban utang konsumtif, kesehatan). Indikator hijau di tier ini = base layer kuat sebelum ke Tier 2 akumulasi.

Output:
- Replace `src/queries/kesehatanTier1.ts` (Wave 1 stub) dengan implementasi real 5 compute functions
- Modify `src/tabs/kesehatan/Tier1Panel.tsx` — replace stub body (dari Plan 13-01) dengan wrapper TierPanel + tier 1 specific config (4 indikator + DAR Total info + 2 CTA + 1 modul link). Props signature TIDAK berubah — Plan 13-01 stub sudah set Props type stable.
- KesehatanLanding.tsx TIDAK di-edit di plan ini — file ownership exclusive. Plan 13-01 sudah wire stub Tier1Panel ke renderTierContent.

Implements:
- DIAG-01 Dana Darurat
- DIAG-02 Savings Rate
- DIAG-03 DAR Konsumtif + DAR Total info
- DIAG-10 Edge case data tipis untuk #1 dan #2 placeholder
- (DIAG-04 partial — shell only, mutation Phase 14)

**Decision references (from CONTEXT.md):**
- D-Compute: hybrid extend existing hooks + useMemo (locked) — implementasi via `kesehatanTier1.ts` compute functions yang dipanggil dari `useIndikator()` (Plan 13-01)
- D-Tier1Panel: extend TierPanel via tier-specific Tier1Panel.tsx wrapper (locked option dari CONTEXT.md "extend TierPanel via tier-specific config object atau buat Tier1Panel.tsx wrapper")
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-diagnostic-data-indicators/13-CONTEXT.md
@.planning/phases/13-diagnostic-data-indicators/13-RESEARCH.md
@.planning/phases/13-diagnostic-data-indicators/13-01-SUMMARY.md
@docs/superpowers/specs/2026-05-08-framework-page-design.md
@src/db/transactions.ts
@src/db/netWorth.ts
@src/db/investments.ts
@src/queries/kesehatanTypes.ts
@src/queries/kesehatanIndikator.ts
@src/tabs/kesehatan/TierPanel.tsx
@src/tabs/kesehatan/KesehatanLanding.tsx

<interfaces>
<!-- Output dari Plan 13-01 yang Plan 13-02 consume -->

```ts
// src/queries/kesehatanTypes.ts (Plan 13-01)
export type IndikatorColor = 'green' | 'yellow' | 'red'
export type IndikatorResult =
  | { kind: 'compute'; value: number; color: IndikatorColor; display: string; staleMonths?: number }
  | { kind: 'placeholder-data-tipis'; monthsAvailable: number; ctaTo: string }
  | { kind: 'cta-fallback'; message: string; ctaLabel: string; ctaTo: string }

export const THRESHOLDS = {
  danaDarurat: { green: 6, yellow: 3 },
  savingsRate: { green: 20, yellow: 10 },
  darKonsumtif: { green: 20, yellow: 40 },
  // ...
}

export const LIQUID_TYPES = ['tabungan', 'giro', 'cash', 'dompet_digital'] as const
export const FINANCIAL_TYPES = ['tabungan', 'giro', 'cash', 'dompet_digital', 'deposito'] as const
export const KONSUMTIF_LIAB_TYPES = ['cicilan_kendaraan', 'kartu_kredit', 'paylater', 'kta'] as const
```

```ts
// Existing types yang harus di-import:
// src/db/transactions.ts
export interface Transaction {
  id: number
  date: string         // 'YYYY-MM-DD'
  type: 'income' | 'expense'
  category_id: number
  category_name: string
  amount: number
  note: string | null
}

// src/db/netWorth.ts
export type AccountType = 'tabungan' | 'giro' | 'cash' | 'deposito' | 'dompet_digital' | 'properti' | 'kendaraan'
export type LiabilityType = 'kpr' | 'cicilan_kendaraan' | 'kartu_kredit' | 'paylater' | 'kta'
export interface NetWorthAccount { id: number; user_id: string; name: string; type: AccountType; balance: number; created_at: string }
export interface NetWorthLiability { id: number; user_id: string; name: string; type: LiabilityType; amount: number; created_at: string }

// src/db/investments.ts
export interface Investment { id: number; asset_type: string; asset_name: string; quantity: number; buy_price: number; current_price: number | null; ... }
export function currentValue(inv: Investment): number  // (inv.current_price ?? inv.buy_price) * inv.quantity
```

```tsx
// TierPanel signature (dari Plan 13-01):
type TierPanelProps = {
  tierId: 1 | 2 | 3 | 4
  indicators?: TierPanelIndicator[]    // { label, thresholdHint?, result }
  infoSlot?: ReactNode
  ctas?: TierPanelCTA[]                 // { label, to, variant? }
  modulLinks?: TierPanelModulLink[]    // { label, slug }
  placeholderText?: string
}
```

```ts
// IndikatorMap shape (dari Plan 13-01):
export type IndikatorMap = {
  '1': IndikatorResult; '2': IndikatorResult; '3': IndikatorResult; '4': IndikatorResult
  '5': IndikatorResult; '6': IndikatorResult; '7': IndikatorResult; '8': IndikatorResult
}
export type DARTotalInfo = { value: number; display: string; kprFraction: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement compute logic real di src/queries/kesehatanTier1.ts (replace Wave 1 stubs)</name>
  <files>src/queries/kesehatanTier1.ts</files>
  <action>
**Replace seluruh isi `src/queries/kesehatanTier1.ts`** (saat ini stub dari Plan 13-01) dengan implementasi 5 compute function real per spec §4 + RESEARCH.md skeleton.

**File: `src/queries/kesehatanTier1.ts`**

```ts
import type { Transaction } from '@/db/transactions'
import type { NetWorthAccount, NetWorthLiability } from '@/db/netWorth'
import type { Investment } from '@/db/investments'
import { currentValue } from '@/db/investments'
import {
  LIQUID_TYPES,
  FINANCIAL_TYPES,
  KONSUMTIF_LIAB_TYPES,
  THRESHOLDS,
  type IndikatorColor,
  type IndikatorResult,
} from './kesehatanTypes'

// Re-export ProtectionChecklistRow dari Plan 13-01 (definisi pindah ke sini supaya
// Phase 14 bisa import dari satu tempat, dan kesehatanIndikator.ts tetap import sini).
export type ProtectionChecklistRow = {
  user_id: string
  health_coverage: 'kantor' | 'bpjs' | 'pribadi' | 'kombinasi' | 'tidak' | null
  // Field lain (has_dependents, life_*, estate_*) — Phase 14 mutation form
}

// ============================================================
// Helper: distinct calendar months dari array transactions
// ============================================================
function distinctMonths(transactions: Transaction[], filterType?: 'income' | 'expense'): string[] {
  const months = new Set<string>()
  for (const t of transactions) {
    if (filterType && t.type !== filterType) continue
    months.add(t.date.substring(0, 7))  // 'YYYY-MM'
  }
  return [...months].sort()
}

// ============================================================
// #1 computeDanaDarurat (DIAG-01) — bulan dana darurat
// ============================================================
export function computeDanaDarurat(
  accounts: NetWorthAccount[],
  transactions: Transaction[],
): IndikatorResult {
  // Edge case data tipis (DIAG-10): butuh 3 bulan kalender expense distinct
  const expenseMonths = distinctMonths(transactions, 'expense')
  if (expenseMonths.length < 3) {
    return {
      kind: 'placeholder-data-tipis',
      monthsAvailable: expenseMonths.length,
      ctaTo: '/transaksi',
    }
  }

  // Avg expense 3 bulan kalender terakhir
  const last3Months = expenseMonths.slice(-3)
  const recentExpenses = transactions.filter(
    t => t.type === 'expense' && last3Months.includes(t.date.substring(0, 7)),
  )
  const totalExpense3mo = recentExpenses.reduce((s, t) => s + t.amount, 0)
  const avgExpenseBulanan = totalExpense3mo / 3

  if (avgExpenseBulanan === 0) {
    return {
      kind: 'placeholder-data-tipis',
      monthsAvailable: expenseMonths.length,
      ctaTo: '/transaksi',
    }
  }

  // SUM akun likuid
  const totalLikuid = accounts
    .filter(a => (LIQUID_TYPES as readonly string[]).includes(a.type))
    .reduce((s, a) => s + a.balance, 0)

  const bulan = totalLikuid / avgExpenseBulanan
  const color: IndikatorColor =
    bulan >= THRESHOLDS.danaDarurat.green
      ? 'green'
      : bulan >= THRESHOLDS.danaDarurat.yellow
      ? 'yellow'
      : 'red'

  return {
    kind: 'compute',
    value: bulan,
    color,
    display: `${bulan.toFixed(1)} bulan`,
  }
}

// ============================================================
// #2 computeSavingsRate (DIAG-02) — savings rate 3-bulan avg
// ============================================================
export function computeSavingsRate(transactions: Transaction[]): IndikatorResult {
  // Edge case: butuh 3 bulan kalender berbeda (income atau expense)
  const allMonths = distinctMonths(transactions)
  if (allMonths.length < 3) {
    return {
      kind: 'placeholder-data-tipis',
      monthsAvailable: allMonths.length,
      ctaTo: '/transaksi',
    }
  }

  const last3Months = allMonths.slice(-3)

  // Per-bulan savings rate, lalu rata-rata
  const rates = last3Months.map(m => {
    const mTx = transactions.filter(t => t.date.startsWith(m))
    const income = mTx.filter(t => t.type === 'income').reduce((s, t) => s + t.amount, 0)
    const expense = mTx.filter(t => t.type === 'expense').reduce((s, t) => s + t.amount, 0)
    return income > 0 ? (income - expense) / income : 0
  })

  const avgRate = rates.reduce((s, r) => s + r, 0) / rates.length
  const pct = avgRate * 100

  const color: IndikatorColor =
    pct >= THRESHOLDS.savingsRate.green
      ? 'green'
      : pct >= THRESHOLDS.savingsRate.yellow
      ? 'yellow'
      : 'red'

  return {
    kind: 'compute',
    value: pct,
    color,
    display: `${pct.toFixed(0)}%`,
  }
}

// ============================================================
// Helper: aset finansial (denominator DAR & Rasio Investasi)
// ============================================================
function totalAsetFinansial(
  accounts: NetWorthAccount[],
  investments: Investment[],
): number {
  const fromAccounts = accounts
    .filter(a => (FINANCIAL_TYPES as readonly string[]).includes(a.type))
    .reduce((s, a) => s + a.balance, 0)
  const fromInv = investments.reduce((s, inv) => s + currentValue(inv), 0)
  return fromAccounts + fromInv
}

// ============================================================
// #3 computeDARKonsumtif (DIAG-03)
// ============================================================
export function computeDARKonsumtif(
  accounts: NetWorthAccount[],
  liabilities: NetWorthLiability[],
  investments: Investment[],
): IndikatorResult {
  const asetFinansial = totalAsetFinansial(accounts, investments)

  if (asetFinansial === 0) {
    // Edge case: aset 0 → divide-by-zero. Display "—" sebagai red (high risk default).
    return {
      kind: 'compute',
      value: 0,
      color: 'red',
      display: '— (perlu aset)',
    }
  }

  const konsumtif = liabilities
    .filter(l => (KONSUMTIF_LIAB_TYPES as readonly string[]).includes(l.type))
    .reduce((s, l) => s + l.amount, 0)

  const pct = (konsumtif / asetFinansial) * 100

  // INVERTED threshold: <green hijau, >yellow merah
  const color: IndikatorColor =
    pct < THRESHOLDS.darKonsumtif.green
      ? 'green'
      : pct <= THRESHOLDS.darKonsumtif.yellow
      ? 'yellow'
      : 'red'

  return {
    kind: 'compute',
    value: pct,
    color,
    display: `${pct.toFixed(0)}%`,
  }
}

// ============================================================
// DAR Total — info-only (BUKAN indikator warna)
// ============================================================
export function computeDARTotal(
  accounts: NetWorthAccount[],
  liabilities: NetWorthLiability[],
  investments: Investment[],
): { value: number; display: string; kprFraction: number } | null {
  const asetFinansial = totalAsetFinansial(accounts, investments)
  if (asetFinansial === 0) return null

  const total = liabilities.reduce((s, l) => s + l.amount, 0)
  if (total === 0) return null

  const kpr = liabilities.filter(l => l.type === 'kpr').reduce((s, l) => s + l.amount, 0)
  const pct = (total / asetFinansial) * 100
  const kprFraction = kpr / total

  return {
    value: pct,
    display: `${pct.toFixed(0)}%`,
    kprFraction,
  }
}

// ============================================================
// #4 Asuransi Kesehatan SHELL (DIAG-04 partial — Phase 14 mutation)
// ============================================================

// Map enum value → label Indonesian
const HEALTH_COVERAGE_LABEL: Record<NonNullable<ProtectionChecklistRow['health_coverage']>, string> = {
  kantor: 'Dari kantor',
  bpjs: 'BPJS pribadi',
  pribadi: 'Asuransi pribadi',
  kombinasi: 'Kombinasi',
  tidak: 'Tidak covered',
}

export function computeAsuransiShell(
  row: ProtectionChecklistRow | null | undefined,
): IndikatorResult {
  if (!row || row.health_coverage == null) {
    return {
      kind: 'compute',
      value: 0,
      color: 'red',
      display: 'Belum diisi',
    }
  }

  if (row.health_coverage === 'tidak') {
    return {
      kind: 'compute',
      value: 0,
      color: 'red',
      display: 'Tidak covered',
    }
  }

  return {
    kind: 'compute',
    value: 1,
    color: 'green',
    display: HEALTH_COVERAGE_LABEL[row.health_coverage],
  }
}
```

**Validation logic per spec §4:**
- DIAG-01: bulan = totalLikuid ÷ avgExpense3mo. Threshold ≥6/3-5/<3.
- DIAG-02: per-bulan rate `(income − expense) / income`, avg 3 bulan. Threshold ≥20/10-19/<10. **Catatan:** kalau income 0 di bulan tertentu → rate = 0 (bukan -Infinity). Per-bulan dibatasi 0; rata-rata bisa negatif (expense > income).
- DIAG-03: konsumtif/asetFinansial × 100. Threshold INVERTED: <20 hijau, 20-40 kuning, >40 merah.
- DIAG-10: edge case <3 bulan kalender → placeholder-data-tipis kind, no color, tidak ikut tier aggregation.
- Asuransi shell: row null/health_coverage null → red "Belum diisi"; 'tidak' → red "Tidak covered"; else → green dengan label Indonesian.

**Edge case verification:**
- Aset finansial 0 (no accounts + no investments) di DIAG-03: returns red "— (perlu aset)" — degraded display, masih ikut aggregation sebagai red (acceptable: user belum punya aset = high risk).
- Total liabilitas 0 di computeDARTotal: returns null → infoSlot tidak render (optional info row).
- Distinct months persis 3: pas threshold, lolos placeholder. Tidak ada off-by-one.

**Test mental walkthrough:**
- User dengan tx Jan/Feb/Mar 2026 (3 bulan distinct): expenseMonths.length=3 → lolos placeholder. last3Months=['2026-01','2026-02','2026-03'].
- User dengan tx Jan dan Mar 2026 (skip Feb): expenseMonths.length=2 → placeholder "Sudah 2/3".
- User dengan tx 90 hari konsekutif tapi cuma 1 bulan kalender: expenseMonths.length=1 → placeholder.
  </action>
  <verify>
    <automated>grep -q "function computeDanaDarurat" src/queries/kesehatanTier1.ts && grep -q "function computeSavingsRate" src/queries/kesehatanTier1.ts && grep -q "function computeDARKonsumtif" src/queries/kesehatanTier1.ts && grep -q "function computeDARTotal" src/queries/kesehatanTier1.ts && grep -q "function computeAsuransiShell" src/queries/kesehatanTier1.ts && grep -q "expenseMonths.length < 3" src/queries/kesehatanTier1.ts && grep -q "last3Months" src/queries/kesehatanTier1.ts && grep -q "INVERTED" src/queries/kesehatanTier1.ts || grep -q "darKonsumtif.green" src/queries/kesehatanTier1.ts && grep -q "kprFraction" src/queries/kesehatanTier1.ts && npx tsc --noEmit</automated>
  </verify>
  <done>5 compute functions implemented dengan formula spec §4 verbatim. Edge case <3 bulan return placeholder-data-tipis (DIAG-10). DAR Konsumtif INVERTED threshold benar. computeDARTotal returns kprFraction untuk infoSlot kontekstualisasi. computeAsuransiShell read protection_checklist.health_coverage. TS compile zero error.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Tier1Panel.tsx stub body dengan wrapper TierPanel + Tier 1 specific config (NO KesehatanLanding edit)</name>
  <files>src/tabs/kesehatan/Tier1Panel.tsx</files>
  <action>
**File 1 (NEW): `src/tabs/kesehatan/Tier1Panel.tsx`** — wrapper TierPanel yang inject Tier 1 specific config (4 indikator + DAR Total info row + 2 CTA + 1 modul link).

```tsx
import TierPanel from './TierPanel'
import type { IndikatorMap, DARTotalInfo } from '@/queries/kesehatan'

type Props = {
  indicators: IndikatorMap
  darTotalInfo: DARTotalInfo | null
}

/**
 * Tier 1 — PROTEKSI panel.
 *
 * Per spec §4 + §4.5:
 * - 4 indikator: Dana Darurat (#1), Savings Rate (#2), DAR Konsumtif (#3), Asuransi Kesehatan (#4 shell)
 * - Info row: DAR Total (kontekstualisasi mayoritas KPR vs non-KPR)
 * - 2 CTA: Kelola akun & utang → /kekayaan, Catat transaksi → /transaksi
 * - Modul link: Pondasi & Cash Flow → /kesehatan/arus-kas
 */
export default function Tier1Panel({ indicators, darTotalInfo }: Props) {
  // DAR Total info row content (optional — null kalau aset 0 atau total liab 0)
  const infoSlot = darTotalInfo ? (
    <span>
      <strong>DAR Total kamu:</strong> {darTotalInfo.display}
      {darTotalInfo.kprFraction > 0.5
        ? ' (mayoritas KPR — beban rumah)'
        : darTotalInfo.kprFraction > 0
        ? ' (campuran KPR & utang konsumtif)'
        : ' (tanpa KPR)'}
    </span>
  ) : undefined

  return (
    <TierPanel
      tierId={1}
      indicators={[
        {
          label: 'Dana Darurat',
          thresholdHint: '≥ 6 bulan hijau · 3-5 kuning · < 3 merah',
          result: indicators['1'],
        },
        {
          label: 'Savings Rate',
          thresholdHint: '≥ 20% hijau · 10-19% kuning · < 10% merah',
          result: indicators['2'],
        },
        {
          label: 'DAR Konsumtif',
          thresholdHint: '< 20% hijau · 20-40% kuning · > 40% merah',
          result: indicators['3'],
        },
        {
          label: 'Asuransi Kesehatan',
          thresholdHint: 'Covered (kantor/BPJS/pribadi/kombinasi) hijau · belum diisi atau tidak covered merah',
          result: indicators['4'],
        },
      ]}
      infoSlot={infoSlot}
      ctas={[
        { label: 'Kelola akun & utang', to: '/kekayaan', variant: 'default' },
        { label: 'Catat transaksi', to: '/transaksi', variant: 'outline' },
      ]}
      modulLinks={[
        { label: 'Pondasi & Cash Flow', slug: 'arus-kas' },
      ]}
    />
  )
}
```

---

**File ownership exclusive (Plan 13-01 sudah wire):** Plan 13-01 sudah buat stub Tier1Panel.tsx + sudah wire <Tier1Panel ... /> di KesehatanLanding.tsx renderTierContent. Plan 13-02 cuma OVERWRITE body Tier1Panel.tsx — TIDAK edit KesehatanLanding.tsx (zero file overlap dengan Plan 13-03/13-04 → Wave 2 paralel safe).

**Action eksekutor:**
1. Buka `src/tabs/kesehatan/Tier1Panel.tsx` (stub dari Plan 13-01).
2. **Replace ENTIRE file** dengan kode di atas (props signature `{ indicators: IndikatorMap; darTotalInfo: DARTotalInfo | null }` IDENTIK — hanya body return yang berubah dari placeholder ke 4 IndikatorCard + DAR Total infoSlot + 2 CTA + 1 modul link).

**Verify NO edit ke KesehatanLanding.tsx:**
```bash
git diff src/tabs/kesehatan/KesehatanLanding.tsx | wc -l   # expect 0 — file unchanged
```

**Build verify:**
```bash
npx tsc --noEmit
npm run build
```
Expected: build success, bundle delta minimal (<10KB tambahan untuk Tier1Panel real implementation).
  </action>
  <verify>
    <automated>test -f src/tabs/kesehatan/Tier1Panel.tsx && grep -q "Pondasi & Cash Flow" src/tabs/kesehatan/Tier1Panel.tsx && grep -q "/kekayaan" src/tabs/kesehatan/Tier1Panel.tsx && grep -q "/transaksi" src/tabs/kesehatan/Tier1Panel.tsx && grep -q "arus-kas" src/tabs/kesehatan/Tier1Panel.tsx && grep -q "DAR Total kamu" src/tabs/kesehatan/Tier1Panel.tsx && ! grep -q "STUB Wave 1" src/tabs/kesehatan/Tier1Panel.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Tier1Panel.tsx stub body REPLACED dengan real implementation: TierPanel wrap 4 indicators + DAR Total infoSlot + 2 CTA + 1 modul link. KesehatanLanding TIDAK diedit (Plan 13-01 sudah wire stub). TS compile + production build success.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual UAT — verify Tier 1 panel render dengan data real production</name>
  <files>(visual UAT — no file changes)</files>
  <action>
**What's been built:**
- `kesehatanTier1.ts`: 5 compute function real (DanaDarurat, SavingsRate, DARKonsumtif, DARTotal, AsuransiShell) implementing DIAG-01/02/03/10 + Tier 1 #4 shell
- `Tier1Panel.tsx`: wrapper TierPanel dengan 4 indicators + DAR Total info row + 2 CTA + 1 modul link
- KesehatanLanding wire: klik tier 1 trapezoid → Accordion slide-down menampilkan Tier1Panel

**Steps untuk human:**

**Setup:**
```bash
npm run dev
```
Open http://localhost:5173/kesehatan di Chrome desktop. Login dengan akun production yang punya data lengkap (≥3 bulan transactions + akun bank + investments).

**Test 1 — Tier 1 trapezoid color reflect aggregate:**
1. Navigate ke /kesehatan.
2. Lihat warna tier 1 (paling lebar, paling bawah). Expected: warna sesuai aggregate dari 4 indikator (kemungkinan besar mixed → kuning atau merah karena Tier 1 #4 Asuransi shell akan red kalau protection_checklist row belum exist).
3. Verify color match dengan klik tier → indicator card colors di panel:
   - Aggregate `red` jika ada minimal 1 indikator merah.
   - Aggregate `yellow` jika tidak ada merah tapi ada minimal 1 kuning.
   - Aggregate `green` jika semua hijau.
   - Aggregate `gray` jika semua placeholder/cta-fallback (unlikely di Tier 1 karena #3 selalu compute).

**Test 2 — Klik tier 1 → Accordion slide-down:**
1. Klik trapezoid Tier 1 PROTEKSI.
2. Expected: Accordion smooth slide-down (kalau `tw-animate-css` registered) atau instant (no animation tapi tetap functional). Panel muncul di bawah tier 1.
3. Klik tier 1 lagi → panel tutup (collapsible).
4. Klik tier 2 → tier 1 auto-close (single-open behavior dari Accordion `type="single"`).

**Test 3 — 4 IndikatorCard render correctly:**

Expected di panel Tier 1:
1. **Dana Darurat:** "X.X bulan" badge dengan warna sesuai threshold.
   - User dengan tabungan Rp 30jt, expense avg Rp 5jt/bln (3 bulan): 6 bulan → hijau.
   - User dengan tabungan Rp 5jt, expense avg Rp 5jt/bln: 1 bulan → merah.
2. **Savings Rate:** "Y%" badge.
   - Income Rp 20jt, expense Rp 16jt → 20% → hijau.
   - Expense > income → negatif % → merah.
3. **DAR Konsumtif:** "Z%" badge.
   - Liabilities konsumtif Rp 5jt, aset finansial Rp 100jt → 5% → hijau.
   - Liabilities konsumtif Rp 50jt, aset finansial Rp 100jt → 50% → merah.
4. **Asuransi Kesehatan:**
   - Tanpa protection_checklist row → "Belum diisi" red.
   - Dengan health_coverage='kantor' (manual seed via Studio SQL Editor): "Dari kantor" green.

**Test 3.1 — Threshold hint text visible:** Hover atau lihat sub-label di setiap IndikatorCard. Expected text:
- "≥ 6 bulan hijau · 3-5 kuning · < 3 merah"
- "≥ 20% hijau · 10-19% kuning · < 10% merah"
- "< 20% hijau · 20-40% kuning · > 40% merah"
- "Covered (kantor/BPJS/pribadi/kombinasi) hijau · belum diisi atau tidak covered merah"

**Test 4 — Edge case data tipis (DIAG-10):**

Cara cepat: pakai akun dev dengan transaksi sedikit (atau simulate dengan delete sebagian transaksi via Studio SQL Editor):
1. Akun dengan 0 transaksi → indikator #1 dan #2: "Butuh data" badge + "Butuh 3 bulan data — sudah 0/3" + button "Catat transaksi →".
2. Akun dengan transaksi cuma 1 bulan kalender (mis. semua tanggal di Mei 2026): #1 dan #2: "sudah 1/3".
3. Akun dengan transaksi di Jan dan Mar 2026 (skip Feb): "sudah 2/3".
4. Akun dengan transaksi di 3+ bulan kalender berbeda: #1 dan #2 compute normal.
5. Tier color: jika #1 dan #2 placeholder, tier color dihitung dari #3 dan #4 saja (placeholder tidak ikut aggregation per spec §4).

**Test 5 — DAR Total info row:**

Expected info row antara indikator stack dan CTA buttons:
- Akun dengan total liabilities Rp 100jt (KPR Rp 80jt + cicilan kendaraan Rp 20jt), aset finansial Rp 200jt: "**DAR Total kamu:** 50% (mayoritas KPR — beban rumah)"
- Akun dengan total Rp 50jt non-KPR (kartu kredit + paylater), aset Rp 200jt: "**DAR Total kamu:** 25% (tanpa KPR)"
- Akun campuran (KPR Rp 30jt + non-KPR Rp 30jt = total Rp 60jt, aset Rp 200jt): "**DAR Total kamu:** 30% (campuran KPR & utang konsumtif)"
- Akun dengan total liabilities 0 ATAU aset finansial 0: info row tidak muncul (computeDARTotal returns null).

**Test 6 — CTA buttons + modul link:**
1. Klik button "Kelola akun & utang" → URL bar `/kekayaan` → KekayaanTab render.
2. Back ke /kesehatan, klik button "Catat transaksi" → URL bar `/transaksi` → TransactionsTab render.
3. Back ke /kesehatan, klik tier 1, klik link "Pelajari: Pondasi & Cash Flow" → URL bar `/kesehatan/arus-kas` → wildcard redirect ke /dashboard (Phase 15 akan fill modul, acceptable di Phase 13).

**Test 7 — View-As compatibility (kalau punya akses admin):**
1. Login sebagai admin.
2. Pengaturan → View-As → pilih user lain dengan data berbeda dari admin.
3. Navigate ke /kesehatan.
4. Expected: Tier 1 panel indikator pakai data **viewed-user**, bukan admin sendiri.
5. Spot check: kalau viewed-user punya 0 transaksi → #1 dan #2 placeholder; admin own data ≥ 3 bulan → tidak relevant.

**Test 8 — Mobile responsive:**
1. Resize browser ≤640px atau Chrome DevTools mobile emulation.
2. Klik tier 1 → Accordion slide-down. Panel content render mobile-friendly:
   - 4 IndikatorCard stacked vertically (full-width).
   - DAR Total info row text wrap acceptable.
   - 2 CTA buttons wrap ke baris kedua kalau perlu (flex flex-wrap).
3. Verify tidak ada overflow horizontal.

**Test 9 — Build success + bundle delta:**
```bash
npm run build
```
Expected:
- Build success dengan zero TS error
- Bundle size delta vs sebelum 13-02: <15KB tambahan (Tier1Panel.tsx + extended kesehatanTier1.ts).

**Report back:**
- Screenshot Tier 1 panel di akun production (Test 3 — full panel visible)
- Screenshot edge case placeholder (Test 4 — kalau bisa simulate)
- Screenshot DAR Total info row (Test 5)
- Confirmation 3 CTA navigate correct routes (Test 6)
- View-As result (Test 7) kalau available
- Mobile screenshot (Test 8)
- Build output line + bundle size (Test 9)

**Issues to flag (jangan auto-approve):**
- Tier 1 trapezoid warna tidak match aggregate panel
- Animation accordion stuck/jerky (lihat Threat T-13-05 di 13-01: tw-animate-css missing → patch follow-up `@import "tw-animate-css"` di src/index.css)
- IndikatorCard label/threshold hint typo
- Edge case <3 bulan tidak placeholder (compute even with sparse data)
- DAR Total kontekstualisasi kprFraction salah (mis. KPR 80% tampil "tanpa KPR")
- View-As bocor data (tampil indikator admin pas View-As user lain)
  </action>
  <verify>
    <automated>echo "Manual visual UAT — see action steps untuk verify Tier 1 panel render production data"</automated>
  </verify>
  <done>Visual UAT pass dengan screenshots/output reported, no regression vs Phase 12 + Plan 13-01</done>
  <resume-signal>Type "approved" + tempel screenshots Test 3, 5, 8, atau describe issues yang harus di-fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → 4 hooks (transactions/netWorth/investments/protection_checklist) | Plan 13-01 useIndikator compose. Plan 13-02 cuma compute pure functions, no new query. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | T | computeDanaDarurat divide-by-zero kalau avgExpense=0 | mitigate | Pre-check `if (avgExpenseBulanan === 0) return placeholder-data-tipis`. Verified di Task 1 source. |
| T-13-08 | T | computeDARKonsumtif divide-by-zero kalau aset=0 | mitigate | Pre-check `if (asetFinansial === 0) return red "— (perlu aset)"`. Acceptable degradation: user 0 aset = high financial risk default. |
| T-13-09 | I | computeAsuransiShell expose enum value tanpa label | accept | HEALTH_COVERAGE_LABEL map ke Indonesian label sebelum display. Kalaupun label miss case (mis. typo enum), fallback ke 'Belum diisi' red — fail-safe. |
| T-13-10 | T | INVERTED threshold DAR Konsumtif salah (treat as normal) | mitigate | Threshold logic: `pct < THRESHOLDS.darKonsumtif.green ? 'green' : pct <= THRESHOLDS.darKonsumtif.yellow ? 'yellow' : 'red'` — explicit `<` dan `<=` per spec. Visual UAT Test 3 verify edge case 19% (green), 20% (yellow), 40% (yellow), 41% (red). |
| T-13-11 | I | Tier1Panel render IndikatorCard pakai data dari indikator.indicators non-null | mitigate | Caller (KesehatanLanding) sudah skeleton-guard (early return jika `indikator.isLoading \|\| !indikator.indicators`). Tier1Panel only mounted post-guard. |
| T-13-12 | D | distinctMonths Set allocation untuk user 10k+ transactions | accept | Worst case Set<string> dengan ~36 entries (3 tahun calendar months) + 10k iteration → sub-100ms. Acceptable per CONTEXT.md trade-off. |

</threat_model>

<verification>
- `kesehatanTier1.ts` exported 5 functions: computeDanaDarurat, computeSavingsRate, computeDARKonsumtif, computeDARTotal, computeAsuransiShell
- ProtectionChecklistRow type re-exported dari `kesehatanTier1.ts` (single source of truth, kesehatanIndikator import dari sini)
- DIAG-01/02/10: distinct calendar months (`date.substring(0,7)`) <3 → placeholder-data-tipis kind dengan monthsAvailable
- DIAG-03: INVERTED threshold (<20 hijau, 20-40 kuning, >40 merah) verified di code
- DAR Total: kprFraction returned, Tier1Panel render kontekstualisasi "mayoritas KPR/campuran/tanpa KPR"
- Asuransi shell: null/null health_coverage/`tidak` → red; else → green dengan Indonesian label
- Tier1Panel.tsx wrap TierPanel dengan 4 indikator + infoSlot + 2 CTA (Kelola akun, Catat transaksi) + 1 modul link (Pondasi & Cash Flow)
- KesehatanLanding TIDAK di-edit di plan ini — sudah wired ke stub di Plan 13-01, body Tier1Panel rewrite cukup
- Visual UAT pass (Test 1-9): tier color match panel aggregate, edge case placeholder, DAR Total kontekstualisasi, CTA navigate correct, View-As, mobile, build delta <15KB
</verification>

<success_criteria>
1. Tier 1 panel klik → Accordion slide-down menampilkan 4 IndikatorCard + DAR Total info row + 2 CTA + 1 modul link
2. Compute Dana Darurat: bulan = totalLikuid ÷ avgExpense3mo, threshold ≥6/3-5/<3 verified pada akun real
3. Compute Savings Rate: per-bulan rate `(income−expense)/income` × avg 3, threshold ≥20%/10-19%/<10%
4. Compute DAR Konsumtif: konsumtif/asetFinansial INVERTED threshold <20%/20-40%/>40%
5. Edge case data tipis (DIAG-10): user dengan <3 bulan kalender expense → #1 dan #2 placeholder "Butuh 3 bulan data, sudah X/3" + CTA /transaksi; placeholder tidak ikut aggregateTierColor
6. Asuransi Kesehatan shell: read protection_checklist.health_coverage; null/null/`tidak` → red, else → green dengan Indonesian label
7. DAR Total info row render kontekstualisasi (mayoritas KPR jika kprFraction>0.5, campuran jika 0<kprFraction<=0.5, tanpa KPR jika 0)
8. 2 CTA navigate ke /kekayaan dan /transaksi correctly
9. Modul link "Pelajari: Pondasi & Cash Flow" → /kesehatan/arus-kas
10. View-As: indicators pakai data viewed-user (inherit dari useIndikator → useTargetUserId di Plan 13-01)
11. Mobile responsive: 4 IndikatorCard stacked + 2 CTA wrap acceptable di ≤640px
12. `npx tsc --noEmit` + `npm run build` success
</success_criteria>

<output>
After completion, create `.planning/phases/13-diagnostic-data-indicators/13-02-SUMMARY.md` dengan:
- Files modified: `kesehatanTier1.ts` (replaced stub), `Tier1Panel.tsx` (replaced stub body — real wrapper). `KesehatanLanding.tsx` TIDAK diedit (Plan 13-01 ownership).
- Visual UAT results: screenshot panel + edge case + DAR Total + mobile + build output
- Compute formulas verification per spec §4 (Dana Darurat, Savings Rate, DAR Konsumtif, DAR Total)
- DIAG-10 edge case behavior verified
- Tier 1 #4 Asuransi shell read-only verified (Phase 14 akan deliver mutation form)
- Hand-off: ProtectionChecklistRow type sekarang exported dari kesehatanTier1.ts (single source); Phase 14 mutation hook import dari sini
- Hand-off untuk 13-03 (Tier 2): pattern Tier1Panel/Tier2Panel/Tier3Panel sudah established — 13-03 follow same pattern (TierPanel wrap dengan tier-specific config)
</output>
</content>
</invoke>