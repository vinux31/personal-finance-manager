---
plan: "16-01"
phase: "16"
name: "Pre-condition Setup & Migration Playbook"
wave: 1
depends_on: []
autonomous: false
files_modified:
  - ".planning/PROJECT.md"
  - "docs/migration-playbook.md"
requirements_addressed:
  - VERIF-01
  - TECHDEBT-01

must_haves:
  truths:
    - "Pre-condition data untuk B1-B5 UAT tersedia di Supabase Cloud production"
    - "Template Gaji recurring memiliki next_due_date di bulan ini (bulan Mei 2026)"
    - "Minimal satu expense recurring template dengan next_due_date <= today"
    - "Minimal satu bill (recurring_templates row) dengan due_day <= today's day-of-month"
    - "Minimal satu goal active dengan current_amount > 0 dan current_amount < target_amount (untuk B3)"
    - "Minimal satu goal dengan status='completed' dan current_amount >= target_amount (untuk B4)"
    - "TECHDEBT-01: Keputusan Path (b) terdokumentasi di PROJECT.md Key Decisions table"
    - "docs/migration-playbook.md berisi 5 section minimum: current state, why broken, procedure, CLI-vs-Studio, signature changes"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "TECHDEBT-01 Key Decision entry"
      contains: "Migration history reconciliation"
    - path: "docs/migration-playbook.md"
      provides: "Future migration workflow playbook"
      contains: "Studio SQL Editor"
  key_links:
    - from: "Supabase Studio SQL Editor"
      to: "recurring_templates table"
      via: "INSERT/UPDATE SQL script"
      pattern: "next_due_date"
    - from: "Supabase Studio SQL Editor"
      to: "goals table"
      via: "INSERT/UPDATE SQL script"
      pattern: "status='completed'"
    - from: ".planning/PROJECT.md"
      to: "docs/migration-playbook.md"
      via: "Key Decisions entry reference"
      pattern: "migration-playbook.md"
---

<objective>
Setup pre-condition data state di Supabase Cloud production untuk B1-B5 UAT, dan dokumentasikan keputusan TECHDEBT-01 (Path b: document procedural alternative).

Purpose: Plan 16-02 tidak bisa dijalankan tanpa data pre-condition yang tepat di production. TECHDEBT-01 harus diselesaikan terlepas dari UAT — ini adalah keputusan ops yang sudah di-locked (D-01 CONTEXT.md).

Output:
- Data state production ready untuk B1 (template Gaji, bulan ini), B2 (bill due today), B3 (goal cash active), B4 (goal completed), B5 (tidak butuh data setup — hanya DevTools)
- PROJECT.md Key Decisions diupdate dengan entry TECHDEBT-01
- docs/migration-playbook.md berisi playbook lengkap untuk future migrations
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-v1-1-closure-ops-cleanup/16-CONTEXT.md
</context>

<tasks>

<task id="T1" type="checkpoint:human-action" autonomous="false">
  <name>Task 1: Setup Pre-condition Data via Supabase Studio SQL Editor</name>
  <read_first>
    - .planning/phases/16-v1-1-closure-ops-cleanup/16-CONTEXT.md — section "Pre-condition data needed (VERIF-01)" dan section "D-02 Pre-condition Data Setup Method"
    - .planning/ROADMAP.md — Phase 16 Success Criteria item 1 (pre-condition state)
  </read_first>
  <action>
**Buka Supabase Studio SQL Editor untuk project Kantong Pintar (https://supabase.com/dashboard).**

Jalankan script SQL berikut secara berurutan di SQL Editor. Ganti `'YOUR_USER_ID'` dengan UUID user production Anda (cari di: Authentication > Users > copy User UID).

---

**STEP 1 — Cek user_id kamu (jalankan dulu, salin hasilnya):**
```sql
SELECT id, email FROM auth.users ORDER BY created_at LIMIT 5;
```
Salin UUID user kamu, pakai sebagai `v_uid` di script berikutnya.

---

**STEP 2 — Setup Template Gaji recurring (B1 pre-condition):**

Script ini membuat atau mengupdate template income "Gaji" dengan `next_due_date` = 1 Mei 2026 (bulan ini), sehingga `process_due_recurring` akan memproses saat dipanggil hari ini.

```sql
DO $$
DECLARE
  v_uid UUID := 'YOUR_USER_ID';  -- GANTI dengan UUID kamu
  v_template_id UUID;
  v_income_cat_id UUID;
BEGIN
  -- Cari category 'Gaji' atau income pertama
  SELECT id INTO v_income_cat_id
  FROM categories
  WHERE user_id = v_uid AND type = 'income'
  ORDER BY name
  LIMIT 1;

  -- Cek apakah sudah ada template Gaji income monthly
  SELECT id INTO v_template_id
  FROM recurring_templates
  WHERE user_id = v_uid
    AND type = 'income'
    AND frequency = 'monthly'
    AND is_active = true
  LIMIT 1;

  IF v_template_id IS NOT NULL THEN
    -- Update next_due_date ke bulan ini (1 Mei 2026)
    UPDATE recurring_templates
    SET next_due_date = '2026-05-01'
    WHERE id = v_template_id AND user_id = v_uid;
    RAISE NOTICE 'Updated template % next_due_date to 2026-05-01', v_template_id;
  ELSE
    -- Buat template baru kalau belum ada
    INSERT INTO recurring_templates (user_id, name, type, category_id, amount, frequency, next_due_date, is_active)
    VALUES (v_uid, 'Gaji Bulanan', 'income', v_income_cat_id, 5000000, 'monthly', '2026-05-01', true)
    RETURNING id INTO v_template_id;
    RAISE NOTICE 'Created new Gaji template %', v_template_id;
  END IF;
END;
$$;
```

---

**STEP 3 — Setup Expense Recurring Template + Bill (B2 pre-condition):**

```sql
DO $$
DECLARE
  v_uid UUID := 'YOUR_USER_ID';  -- GANTI dengan UUID kamu
  v_template_id UUID;
  v_expense_cat_id UUID;
  v_today_day INT := EXTRACT(DAY FROM CURRENT_DATE)::INT;
BEGIN
  -- Cari category expense pertama yang ada
  SELECT id INTO v_expense_cat_id
  FROM categories
  WHERE user_id = v_uid AND type = 'expense'
  ORDER BY name
  LIMIT 1;

  -- Cek apakah sudah ada expense recurring template dengan due_day <= today
  SELECT id INTO v_template_id
  FROM recurring_templates
  WHERE user_id = v_uid
    AND type = 'expense'
    AND frequency = 'monthly'
    AND is_active = true
    AND next_due_date <= CURRENT_DATE
  LIMIT 1;

  IF v_template_id IS NOT NULL THEN
    RAISE NOTICE 'Expense template sudah ada: %', v_template_id;
  ELSE
    -- Buat expense template dengan next_due_date = awal bulan ini (sudah lewat)
    INSERT INTO recurring_templates (user_id, name, type, category_id, amount, frequency, next_due_date, is_active)
    VALUES (v_uid, 'Tagihan Internet', 'expense', v_expense_cat_id, 300000, 'monthly', '2026-05-01', true)
    RETURNING id INTO v_template_id;
    RAISE NOTICE 'Created expense template %', v_template_id;
  END IF;
END;
$$;
```

---

**STEP 4 — Setup Goal Active untuk B3 (2-tab withdraw race):**

Goal ini butuh current_amount >= 100000 (Rp 100k) agar bisa withdraw Rp 50k dua kali.

```sql
DO $$
DECLARE
  v_uid UUID := 'YOUR_USER_ID';  -- GANTI dengan UUID kamu
  v_goal_id BIGINT;
BEGIN
  -- Cek apakah sudah ada goal active dengan current_amount >= 100000
  SELECT id INTO v_goal_id
  FROM goals
  WHERE user_id = v_uid
    AND status = 'active'
    AND current_amount >= 100000
    AND current_amount < target_amount
  LIMIT 1;

  IF v_goal_id IS NOT NULL THEN
    RAISE NOTICE 'Goal active sudah ada: % (current_amount: %)', v_goal_id,
      (SELECT current_amount FROM goals WHERE id = v_goal_id);
  ELSE
    -- Buat goal baru untuk test B3
    INSERT INTO goals (user_id, name, target_amount, current_amount, status)
    VALUES (v_uid, 'UAT B3 Test Goal', 500000, 100000, 'active')
    RETURNING id INTO v_goal_id;
    RAISE NOTICE 'Created B3 test goal %, current_amount=100000', v_goal_id;
  END IF;
END;
$$;
```

---

**STEP 5 — Setup Goal Completed untuk B4 (completed→active flip):**

Goal ini perlu status='completed' dan current_amount >= target_amount.

```sql
DO $$
DECLARE
  v_uid UUID := 'YOUR_USER_ID';  -- GANTI dengan UUID kamu
  v_goal_id BIGINT;
BEGIN
  -- Cek apakah sudah ada goal completed
  SELECT id INTO v_goal_id
  FROM goals
  WHERE user_id = v_uid
    AND status = 'completed'
    AND current_amount >= target_amount
  LIMIT 1;

  IF v_goal_id IS NOT NULL THEN
    RAISE NOTICE 'Goal completed sudah ada: % (current_amount: %, target: %)',
      v_goal_id,
      (SELECT current_amount FROM goals WHERE id = v_goal_id),
      (SELECT target_amount FROM goals WHERE id = v_goal_id);
  ELSE
    -- Buat goal completed baru untuk B4
    INSERT INTO goals (user_id, name, target_amount, current_amount, status)
    VALUES (v_uid, 'UAT B4 Completed Goal', 10000, 10000, 'completed')
    RETURNING id INTO v_goal_id;
    RAISE NOTICE 'Created B4 completed goal %', v_goal_id;
  END IF;
END;
$$;
```

---

**STEP 6 — Verifikasi semua pre-condition (jalankan setelah semua step di atas):**

```sql
-- Verifikasi seluruh pre-condition B1-B5 sekaligus
SELECT
  'B1 Gaji template' AS check_name,
  CASE WHEN COUNT(*) > 0 THEN 'READY' ELSE 'MISSING' END AS status,
  STRING_AGG(id::text, ', ') AS ids
FROM recurring_templates
WHERE user_id = 'YOUR_USER_ID'  -- GANTI
  AND type = 'income'
  AND frequency = 'monthly'
  AND is_active = true
  AND next_due_date <= CURRENT_DATE

UNION ALL

SELECT
  'B2 Expense template (bill due)',
  CASE WHEN COUNT(*) > 0 THEN 'READY' ELSE 'MISSING' END,
  STRING_AGG(id::text, ', ')
FROM recurring_templates
WHERE user_id = 'YOUR_USER_ID'  -- GANTI
  AND type = 'expense'
  AND is_active = true
  AND next_due_date <= CURRENT_DATE

UNION ALL

SELECT
  'B3 Goal active (>=100k)',
  CASE WHEN COUNT(*) > 0 THEN 'READY' ELSE 'MISSING' END,
  STRING_AGG(id::text || '(Rp' || current_amount::text || ')', ', ')
FROM goals
WHERE user_id = 'YOUR_USER_ID'  -- GANTI
  AND status = 'active'
  AND current_amount >= 100000
  AND current_amount < target_amount

UNION ALL

SELECT
  'B4 Goal completed',
  CASE WHEN COUNT(*) > 0 THEN 'READY' ELSE 'MISSING' END,
  STRING_AGG(id::text || '(Rp' || current_amount::text || ')', ', ')
FROM goals
WHERE user_id = 'YOUR_USER_ID'  -- GANTI
  AND status = 'completed'
  AND current_amount >= target_amount;
```

Semua baris harus menunjukkan `READY`. B5 tidak butuh data setup (hanya DevTools network inspection).
  </action>
  <acceptance_criteria>
    - Query verifikasi STEP 6 menampilkan 4 baris, semua kolom `status` = `'READY'`
    - Baris "B1 Gaji template" READY — ada recurring_templates row dengan type='income', frequency='monthly', is_active=true, next_due_date <= CURRENT_DATE
    - Baris "B2 Expense template" READY — ada recurring_templates row dengan type='expense', is_active=true, next_due_date <= CURRENT_DATE
    - Baris "B3 Goal active" READY — ada goals row dengan status='active', current_amount >= 100000, current_amount < target_amount
    - Baris "B4 Goal completed" READY — ada goals row dengan status='completed', current_amount >= target_amount
    - Tidak ada error message dari SQL Editor saat menjalankan tiap step
  </acceptance_criteria>
</task>

<task id="T2" type="execute" autonomous="false">
  <name>Task 2: Dokumentasi TECHDEBT-01 — Update PROJECT.md + Tulis Migration Playbook</name>
  <read_first>
    - .planning/PROJECT.md — section "Key Decisions" (untuk tahu format table dan lokasi append)
    - .planning/phases/16-v1-1-closure-ops-cleanup/16-CONTEXT.md — section "D-01 TECHDEBT-01 Strategy" dan section "Specific Ideas" (minimum viable content playbook)
    - .planning/STATE.md — section "Decisions (carried from v1.1)" untuk migration state context
  </read_first>
  <action>
**Langkah 1: Update PROJECT.md Key Decisions table**

Buka `.planning/PROJECT.md`. Pada section `## Key Decisions`, tambahkan baris baru di akhir tabel (sebelum baris kosong penutup):

```
| Migration history reconciliation (0014..0030 Local-only) — Path (b) dokument procedural alternative | `supabase db push` broken karena history mismatch: 0001-0013 applied via CLI, 0014-0030 applied via Studio SQL Editor paste yang tidak update `schema_migrations`. Path (a) repair terlalu berisiko tanpa staging mirror. Path (b): Studio paste tetap default, dokumentasi playbook, opsional tambah dummy entries di `schema_migrations`. | ✓ Documented (v1.2 Phase 16 — docs/migration-playbook.md) |
```

**Langkah 2: Buat file docs/migration-playbook.md**

Buat file baru `docs/migration-playbook.md` dengan konten berikut (PERSIS — jangan singkat):

```markdown
# Migration Playbook — Kantong Pintar

> Last updated: 2026-05-15 | Author: Phase 16 TECHDEBT-01

## 1. Current State Snapshot

**Local migrations:** 0001–0030 (30 total, di `supabase/migrations/`)
**Cloud applied:**
- 0001–0013: Applied via `supabase db push` (terdaftar di `supabase_migrations` table cloud)
- 0014–0030: Applied via Supabase Studio SQL Editor manual paste (TIDAK terdaftar di `supabase_migrations`)

**Result of `supabase migration list --linked`:** 0014–0030 tampil sebagai "Local-only" karena Studio paste tidak otomatis update tabel `supabase_migrations` di cloud.

## 2. Kenapa `supabase db push` Rusak

`db push` mendeteksi mismatch antara local migration history dan cloud `supabase_migrations`. Karena 0014+ tidak tercatat di cloud, CLI menganggap schema cloud dan local diverged dan menolak push untuk mencegah data corruption.

**Keputusan (Path b — Phase 16 D-01):** Tidak diperbaiki. Repair via `supabase migration repair` berisiko jika terjadi drift antara local schema dan cloud schema (tidak ada staging mirror untuk verify). Studio paste sudah menjadi proven workflow sejak migration 0014 tanpa incident.

## 3. Prosedur untuk Future Migrations

Setiap kali perlu tambah migration baru, ikuti langkah ini:

### Step 1 — Tulis SQL file
```
supabase/migrations/NNNN_description.sql
```
Gunakan nomor urut berikutnya (cek `ls supabase/migrations/` untuk nomor terakhir).

### Step 2 — Paste di Studio SQL Editor
1. Buka [Supabase Dashboard](https://supabase.com/dashboard) → pilih project
2. Klik **SQL Editor** di sidebar kiri
3. Paste isi file `.sql` ke editor baru (klik "+ New query")
4. Klik **Run** dan verifikasi output ("Success" atau RAISE NOTICE yang diharapkan)

### Step 3 — Commit SQL file ke git
```bash
git add supabase/migrations/NNNN_description.sql
git commit -m "feat(migration): NNNN description"
```

SQL file di git = source of truth. Cloud schema = applied version (via Studio).

### Step 4 — Verifikasi post-apply
Jalankan query verifikasi di SQL Editor (tergantung migration — biasanya `SELECT` untuk verify table/function exists atau RLS policy aktif).

## 4. CLI Push vs Studio Paste — Kapan Pakai Apa

| Scenario | Method | Alasan |
|----------|--------|--------|
| Migration baru (normal) | **Studio paste** | `db push` broken, Studio proven workflow |
| Rollback / repair history | Manual SQL di Studio | Tidak pakai CLI — terlalu risky tanpa staging |
| Dev local (supabase start) | `supabase db reset` | Local only, tidak affect cloud |
| Staging environment (kalau ada di masa depan) | `supabase db push --linked` | Bisa pakai setelah history disync di staging |

**Catatan:** Kalau project mendapatkan staging environment, pertimbangkan untuk `supabase migration repair` di staging dulu sebelum repair di production. Sampai ada staging, Studio paste tetap default.

## 5. Handling Signature Changes pada PL/pgSQL Functions

Jika perlu mengubah signature (nama parameter, tipe parameter, atau RETURNS) dari function yang sudah ada:

**WAJIB emit `DROP FUNCTION IF EXISTS` dengan signature lama sebelum `CREATE OR REPLACE`.**

Contoh — mengubah `process_due_recurring`:
```sql
-- 1. Drop signature lama DULU
DROP FUNCTION IF EXISTS process_due_recurring(DATE, UUID, INT);

-- 2. Baru create dengan signature baru
CREATE OR REPLACE FUNCTION process_due_recurring(
  p_today    DATE DEFAULT CURRENT_DATE,
  p_uid      UUID DEFAULT NULL,
  p_max_iter INT  DEFAULT 12,
  p_new_param TEXT DEFAULT NULL  -- parameter baru
)
...
```

**Mengapa:** PostgreSQL key function identity berdasarkan `(name, arg_types)`. Tanpa explicit DROP, signature change menghasilkan overload kedua — old version tetap callable via PostgREST → race + IDOR vector. Lesson dari Phase 5 migration 0017→0018.

**Functions yang teraffect oleh rule ini:**
- `process_due_recurring(DATE, UUID, INT)` — migration 0019
- `withdraw_from_goal(BIGINT, NUMERIC)` — migration 0020
- `mark_bill_paid(UUID, DATE)` — migration 0014
- `add_money_to_goal_v2(BIGINT, NUMERIC, NUMERIC)` — migration 0022
- `aggregate_by_period(...)` — migration 0017
- `aggregate_by_category(...)` — migration 0017

## 6. Dummy Schema_migrations Entries (Opsional)

Jika ingin `supabase migration list --linked` menampilkan 0014–0030 sebagai "Applied" (bukan "Local-only"), jalankan di SQL Editor:

```sql
-- Tambah dummy entries untuk migrations yang sudah applied via Studio
-- HANYA jalankan kalau semua migration 0014-0030 SUDAH benar-benar applied di cloud
INSERT INTO supabase_migrations.schema_migrations (version, statements, name)
VALUES
  ('20240101000014', ARRAY['-- applied via Studio'], '0014_mark_bill_paid'),
  ('20240101000015', ARRAY['-- applied via Studio'], '0015_...'),
  -- ... dst untuk setiap migration 0014-0030
ON CONFLICT (version) DO NOTHING;
```

**PERINGATAN:** Hanya jalankan ini jika kamu yakin semua SQL sudah applied. Dummy entries tidak execute SQL — mereka hanya update tracking table. Jika schema belum applied tapi dummy entry ditambahkan, `db push` akan anggap schema sudah sync padahal belum.

**Status saat ini (2026-05-15):** Dummy entries BELUM ditambahkan. `supabase migration list --linked` masih menunjukkan 0014–0030 sebagai Local-only. Ini acceptable — Studio paste adalah source of truth, bukan `schema_migrations` table.
```
  </action>
  <acceptance_criteria>
    - File `docs/migration-playbook.md` ada dan bisa dibuka (`cat docs/migration-playbook.md` menampilkan konten)
    - File mengandung 6 section heading: "1. Current State Snapshot", "2. Kenapa", "3. Prosedur", "4. CLI Push vs Studio", "5. Handling Signature", "6. Dummy"
    - File mengandung string "Studio paste tetap default" (keputusan Path b terdokumentasi)
    - File mengandung string "DROP FUNCTION IF EXISTS" dalam section signature changes
    - File mengandung tabel "CLI Push vs Studio" dengan setidaknya 4 baris
    - `.planning/PROJECT.md` mengandung string "Migration history reconciliation" di section Key Decisions
    - `.planning/PROJECT.md` mengandung string "docs/migration-playbook.md" di baris Key Decision tersebut
    - Kedua file berhasil di-commit ke git (verifikasi: `git log --oneline -3` menampilkan commit yang relevan)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL Editor → Supabase Cloud | SQL dieksekusi langsung di production database tanpa staging buffer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | Pre-condition SQL scripts | accept | SQL scripts bersifat idempotent (INSERT ... ON CONFLICT DO NOTHING equivalent logic, cek IF EXISTS sebelum INSERT). Eksekusi ganda tidak corrupt data. |
| T-16-02 | Information Disclosure | user_id di SQL Editor | accept | SQL Editor hanya accessible oleh project admin (Supabase Dashboard auth). user_id di plan ini adalah UUID production admin — tidak expose ke public. |
| T-16-03 | Denial of Service | STEP 4 goal INSERT | accept | Goal baru hanya INSERT satu row, tidak ada bulk operation. Impact minimal. |
</threat_model>

<verification>
Setelah kedua task selesai, verifikasi:

1. **Pre-condition data:** Jalankan ulang query STEP 6 di SQL Editor — semua 4 baris `READY`
2. **PROJECT.md:** `grep "Migration history reconciliation" .planning/PROJECT.md` — output non-empty
3. **Playbook file:** `ls docs/migration-playbook.md` — file ada
4. **Git state:** `git log --oneline -2` — ada commit untuk docs task
5. **Ready for 16-02:** Semua pre-condition terpenuhi, Plan 16-02 bisa dilanjutkan
</verification>

<success_criteria>
Plan 16-01 berhasil jika:

- Semua 4 pre-condition untuk B1-B4 UAT berstatus READY di Supabase Cloud production
- TECHDEBT-01 decision (Path b) terdokumentasi di PROJECT.md Key Decisions table
- docs/migration-playbook.md ada dengan 6 section minimum dan dapat menjadi referensi standalone
- Git history mencatat perubahan documentation
- Plan 16-02 (live UAT B1-B5) siap untuk dijalankan segera setelah 16-01 complete
</success_criteria>

<output>
Setelah plan ini complete, buat `.planning/phases/16-v1-1-closure-ops-cleanup/16-01-SUMMARY.md` dengan format:

```markdown
# Plan 16-01 Summary — Pre-condition Setup & Migration Playbook

**Status:** COMPLETE
**Date:** [tanggal]

## What Was Done
- Pre-condition data B1-B4 setup via Supabase Studio SQL Editor
- TECHDEBT-01: docs/migration-playbook.md created (6 sections)
- PROJECT.md Key Decisions updated dengan migration reconciliation entry

## Pre-condition State
- B1: [template ID] — Gaji income monthly, next_due_date=2026-05-01
- B2: [template ID] — expense template, next_due_date <= today
- B3: [goal ID] — active goal, current_amount=[amount]
- B4: [goal ID] — completed goal, current_amount=[amount]
- B5: No data setup needed

## Artifacts
- docs/migration-playbook.md — created
- .planning/PROJECT.md — Key Decisions table updated

## Notes
[Catatan khusus jika ada — misal: template ID yang dipakai untuk B3/B4 UAT di Plan 16-02]
```
</output>
