---
phase: 12-kesehatan-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0029_protection_checklist.sql
  - supabase/tests/12-protection-checklist.sql
autonomous: false
requirements: [SCHEMA-01]
must_haves:
  truths:
    - "Tabel protection_checklist exist di production dengan PRIMARY KEY user_id"
    - "RLS aktif: non-admin user tidak bisa SELECT/INSERT/UPDATE row user lain"
    - "Admin bisa SELECT row user lain (View-As) tapi WITH CHECK tetap auth.uid() = user_id (admin tidak bisa WRITE atas nama user lain)"
    - "User bisa INSERT row sendiri dengan field nullable (lazy-create di Phase 14)"
    - "CHECK constraints menolak nilai out-of-range (mis. health_coverage='invalid' raises 23514)"
  artifacts:
    - path: "supabase/migrations/0029_protection_checklist.sql"
      provides: "CREATE TABLE protection_checklist + RLS policy"
      contains: "CREATE TABLE protection_checklist"
    - path: "supabase/tests/12-protection-checklist.sql"
      provides: "RLS isolation + CHECK constraint smoke test"
      contains: "RAISE NOTICE 'PASS:"
  key_links:
    - from: "protection_checklist.user_id"
      to: "auth.users.id"
      via: "FOREIGN KEY ON DELETE CASCADE"
      pattern: "REFERENCES auth\\.users\\(id\\) ON DELETE CASCADE"
    - from: "RLS policy USING clause"
      to: "auth.uid() OR is_admin()"
      via: "FOR ALL policy"
      pattern: "auth\\.uid\\(\\) = user_id OR is_admin\\(\\)"
---

<objective>
Buat tabel `protection_checklist` (per design spec §7) sebagai foundation untuk Phase 14 mutations (Tier 1 Asuransi Kesehatan + Tier 4 smart-gated checklist). Phase 12 hanya **create tabel + RLS + smoke test** — tidak ada UI yang baca/tulis data ini.

Purpose: Foundation schema. Phase 14 akan implement form UI yang lazy-create row di first interaction. Schema harus sudah live di production sebelum Phase 14 plan-phase.

Output:
- Migration file `0029_protection_checklist.sql` (continue dari 0028 yang sudah applied di prod)
- SQL test `12-protection-checklist.sql` dengan RLS isolation assertions (mengikuti convention `05-tighten-rls.sql`)
- Migration applied ke Supabase Cloud via Studio SQL Editor (manual paste, db push broken — see STATE.md decisions)

Implements:
- SCHEMA-01 (per CONTEXT.md decisions section "Schema (SCHEMA-01)")
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-kesehatan-foundation/12-CONTEXT.md
@docs/superpowers/specs/2026-05-08-framework-page-design.md
@supabase/migrations/0012_net_worth.sql
@supabase/migrations/0028_withdraw_from_goal_format_rupiah.sql
@supabase/tests/05-tighten-rls.sql

<interfaces>
RLS pattern reference dari `0012_net_worth.sql` — standard pfm-web pattern untuk user-scoped tabel:

```sql
ALTER TABLE <table_name> ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users manage own <table_name>"
  ON <table_name> FOR ALL
  USING      (auth.uid() = user_id OR is_admin())
  WITH CHECK (auth.uid() = user_id);
```

Helper `is_admin()` sudah ada di project (defined in earlier migration). Tidak perlu redefine. Tinggal panggil di USING clause.

Test convention dari `05-tighten-rls.sql`:
1. BEGIN ... ROLLBACK wrapper -> no state persists
2. SET LOCAL row_security = off untuk seeding
3. SET LOCAL ROLE authenticated + set_config('request.jwt.claim.sub', uid, true) untuk assertions
4. RAISE NOTICE 'PASS:' / 'FAIL:' format -- humans scan, CI greps for "FAIL:"
5. Synthetic UUIDs: '00000000-0000-0000-0000-000000000a01' .. 'a03'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tulis migration 0029_protection_checklist.sql</name>
  <files>supabase/migrations/0029_protection_checklist.sql</files>
  <action>
Create file `supabase/migrations/0029_protection_checklist.sql` dengan content (per design spec §7 + CONTEXT.md decisions):

```sql
-- ============================================================
-- 0029_protection_checklist: Tabel checklist proteksi (Tier 1 #4 + Tier 4)
--
-- Phase 12 SCHEMA-01: Foundation untuk Phase 14 mutation forms.
-- - Tier 1 #4 Asuransi Kesehatan: health_coverage
-- - Tier 4 gate: has_dependents
-- - Tier 4 asuransi jiwa (kalau has_dependents=true): life_coverage*
-- - Tier 4 estate (universal): estate_*
--
-- Default behavior: row dibuat lazy di first interaction (Phase 14).
-- Empty state (no row) = semua tier 1 #4 dan tier 4 questions tampil unanswered (merah).
-- ============================================================

CREATE TABLE protection_checklist (
  user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,

  -- Tier 1 #4: Asuransi Kesehatan
  health_coverage TEXT CHECK (health_coverage IN
    ('kantor','bpjs','pribadi','kombinasi','tidak')),

  -- Tier 4 gate
  has_dependents BOOLEAN,

  -- Tier 4: Asuransi Jiwa (relevant kalau has_dependents = true)
  life_coverage TEXT CHECK (life_coverage IN
    ('kantor','pribadi','keduanya','tidak')),
  life_coverage_sufficient BOOLEAN,
  life_coverage_post_employment TEXT CHECK (life_coverage_post_employment IN
    ('ya','tidak','tidak_yakin')),

  -- Tier 4: Estate (universal — applies whether has_dependents true atau false)
  estate_heirs_documented BOOLEAN,
  estate_assets_documented BOOLEAN,
  estate_will_exists BOOLEAN,

  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

ALTER TABLE protection_checklist ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users manage own protection checklist"
  ON protection_checklist FOR ALL
  USING      (auth.uid() = user_id OR is_admin())
  WITH CHECK (auth.uid() = user_id);
```

Notes:
- PRIMARY KEY langsung pada `user_id` (bukan `BIGINT GENERATED` seperti net_worth_accounts) — karena 1:1 dengan user, tidak butuh surrogate id. Lazy-create pattern: `INSERT ... ON CONFLICT (user_id) DO UPDATE SET ...`.
- Semua field business nullable supaya partial-fill di Phase 14 valid (user bisa jawab Tier 1 #4 dulu sebelum Tier 4).
- CHECK constraints ikut design spec §7 verbatim — jangan adjust enum values tanpa update spec.
- Helper `is_admin()` sudah defined di migration sebelumnya (lihat 0012); tidak perlu redefine.
- Migration filename `0029_protection_checklist.sql` confirms continue dari 0028 (per CONTEXT.md decisions).
  </action>
  <verify>
    <automated>test -f supabase/migrations/0029_protection_checklist.sql && grep -q "CREATE TABLE protection_checklist" supabase/migrations/0029_protection_checklist.sql && grep -q "ENABLE ROW LEVEL SECURITY" supabase/migrations/0029_protection_checklist.sql && grep -q "auth.uid() = user_id OR is_admin()" supabase/migrations/0029_protection_checklist.sql</automated>
  </verify>
  <done>File migration exist dengan 8 fields business + RLS policy + CHECK constraints yang match design spec §7</done>
</task>

<task type="auto">
  <name>Task 2: Tulis SQL test 12-protection-checklist.sql</name>
  <files>supabase/tests/12-protection-checklist.sql</files>
  <action>
Create file `supabase/tests/12-protection-checklist.sql` mengikuti convention `05-tighten-rls.sql`:

```sql
-- ============================================================
-- Phase 12 SQL Integration Test: protection_checklist RLS + CHECK
--
-- Run: psql "$DATABASE_URL" -f supabase/tests/12-protection-checklist.sql
--
-- Validates migration 0029_protection_checklist.sql (must be applied first).
--
-- Expected: 8 PASS notices total
--   Section 1 (RLS isolation):     5
--   Section 2 (CHECK constraints): 3
-- ============================================================

BEGIN;

SET LOCAL row_security = off;

DO $$
DECLARE
  v_admin_uid UUID := '00000000-0000-0000-0000-000000000c01';
  v_user_uid  UUID := '00000000-0000-0000-0000-000000000c02';
  v_other_uid UUID := '00000000-0000-0000-0000-000000000c03';
  v_count     INT;
BEGIN
  -- Seed 3 synthetic users (admin + 2 non-admin) — abort gracefully if env disallows
  BEGIN
    INSERT INTO auth.users (id, email, encrypted_password, created_at, updated_at, aud, role)
    VALUES
      (v_admin_uid, 'phase12-admin@example.local', '', NOW(), NOW(), 'authenticated', 'authenticated'),
      (v_user_uid,  'phase12-user@example.local',  '', NOW(), NOW(), 'authenticated', 'authenticated'),
      (v_other_uid, 'phase12-other@example.local', '', NOW(), NOW(), 'authenticated', 'authenticated')
    ON CONFLICT (id) DO NOTHING;
  EXCEPTION WHEN OTHERS THEN
    RAISE NOTICE 'SKIP ALL SECTIONS: cannot seed auth.users (%). Probably restricted environment.', SQLERRM;
    RETURN;
  END;

  INSERT INTO profiles (id, is_admin, display_name)
  VALUES (v_admin_uid, true, 'Phase12 Admin')
  ON CONFLICT (id) DO UPDATE SET is_admin = true;

  INSERT INTO profiles (id, is_admin, display_name)
  VALUES (v_user_uid, false, 'Phase12 User')
  ON CONFLICT (id) DO UPDATE SET is_admin = false;

  INSERT INTO profiles (id, is_admin, display_name)
  VALUES (v_other_uid, false, 'Phase12 Other')
  ON CONFLICT (id) DO UPDATE SET is_admin = false;

  -- Seed protection_checklist row untuk other user (target of cross-user attempts)
  INSERT INTO protection_checklist (user_id, health_coverage)
  VALUES (v_other_uid, 'bpjs')
  ON CONFLICT (user_id) DO UPDATE SET health_coverage = 'bpjs';

  -- ============================================================
  -- SECTION 1: RLS isolation (non-admin scope)
  -- ============================================================
  RAISE NOTICE 'SECTION 1: RLS isolation';

  SET LOCAL ROLE authenticated;
  PERFORM set_config('request.jwt.claim.sub', v_user_uid::TEXT, true);

  -- Test 1.1 — non-admin tidak bisa SELECT row user lain
  SELECT count(*) INTO v_count FROM protection_checklist WHERE user_id = v_other_uid;
  IF v_count = 0 THEN
    RAISE NOTICE 'PASS: non-admin cannot SELECT other user protection_checklist row';
  ELSE
    RAISE NOTICE 'FAIL: non-admin sees % rows for other user (RLS leaked)', v_count;
  END IF;

  -- Test 1.2 — non-admin bisa INSERT row sendiri
  BEGIN
    INSERT INTO protection_checklist (user_id, health_coverage)
    VALUES (v_user_uid, 'kantor');
    RAISE NOTICE 'PASS: non-admin INSERT own row succeeded';
  EXCEPTION WHEN OTHERS THEN
    RAISE NOTICE 'FAIL: non-admin INSERT own row raised: %', SQLERRM;
  END;

  -- Test 1.3 — non-admin UPDATE row user lain hits 0 rows (RLS USING filter)
  UPDATE protection_checklist SET health_coverage = 'tidak' WHERE user_id = v_other_uid;
  GET DIAGNOSTICS v_count = ROW_COUNT;
  IF v_count = 0 THEN
    RAISE NOTICE 'PASS: non-admin UPDATE other user row affected 0 rows (RLS filter)';
  ELSE
    RAISE NOTICE 'FAIL: non-admin UPDATE other user row affected % rows', v_count;
  END IF;

  -- Test 1.4 — admin bisa SELECT semua rows (View-As precondition)
  PERFORM set_config('request.jwt.claim.sub', v_admin_uid::TEXT, true);
  SELECT count(*) INTO v_count FROM protection_checklist WHERE user_id IN (v_user_uid, v_other_uid);
  IF v_count = 2 THEN
    RAISE NOTICE 'PASS: admin SELECT both seeded rows (% = 2)', v_count;
  ELSE
    RAISE NOTICE 'FAIL: admin sees % rows (expected 2)', v_count;
  END IF;

  -- Test 1.5 — admin tidak bisa INSERT row atas nama user lain (WITH CHECK enforces auth.uid() = user_id)
  BEGIN
    INSERT INTO protection_checklist (user_id, has_dependents)
    VALUES ('00000000-0000-0000-0000-000000000c99'::UUID, true);
    RAISE NOTICE 'FAIL: admin INSERT atas nama user lain SUCCEEDED (WITH CHECK leaked)';
  EXCEPTION WHEN OTHERS THEN
    IF SQLSTATE = '42501' THEN
      RAISE NOTICE 'PASS: admin INSERT cross-user raised SQLSTATE 42501 (WITH CHECK enforced)';
    ELSE
      RAISE NOTICE 'FAIL: admin INSERT cross-user raised wrong SQLSTATE % -- %', SQLSTATE, SQLERRM;
    END IF;
  END;

  -- ============================================================
  -- SECTION 2: CHECK constraints
  -- ============================================================
  RAISE NOTICE 'SECTION 2: CHECK constraints';

  RESET ROLE;
  SET LOCAL row_security = off;

  -- Test 2.1 — health_coverage='invalid' raises 23514
  BEGIN
    INSERT INTO protection_checklist (user_id, health_coverage)
    VALUES ('00000000-0000-0000-0000-000000000ce1'::UUID, 'invalid_value');
    RAISE NOTICE 'FAIL: invalid health_coverage SUCCEEDED';
  EXCEPTION WHEN check_violation THEN
    RAISE NOTICE 'PASS: invalid health_coverage raised check_violation (23514)';
  WHEN OTHERS THEN
    RAISE NOTICE 'FAIL: invalid health_coverage raised wrong SQLSTATE % -- %', SQLSTATE, SQLERRM;
  END;

  -- Test 2.2 — life_coverage='invalid' raises 23514
  BEGIN
    INSERT INTO protection_checklist (user_id, life_coverage)
    VALUES ('00000000-0000-0000-0000-000000000ce2'::UUID, 'badvalue');
    RAISE NOTICE 'FAIL: invalid life_coverage SUCCEEDED';
  EXCEPTION WHEN check_violation THEN
    RAISE NOTICE 'PASS: invalid life_coverage raised check_violation (23514)';
  WHEN OTHERS THEN
    RAISE NOTICE 'FAIL: invalid life_coverage raised wrong SQLSTATE % -- %', SQLSTATE, SQLERRM;
  END;

  -- Test 2.3 — life_coverage_post_employment='invalid' raises 23514
  BEGIN
    INSERT INTO protection_checklist (user_id, life_coverage_post_employment)
    VALUES ('00000000-0000-0000-0000-000000000ce3'::UUID, 'maybe');
    RAISE NOTICE 'FAIL: invalid life_coverage_post_employment SUCCEEDED';
  EXCEPTION WHEN check_violation THEN
    RAISE NOTICE 'PASS: invalid life_coverage_post_employment raised check_violation (23514)';
  WHEN OTHERS THEN
    RAISE NOTICE 'FAIL: invalid life_coverage_post_employment raised wrong SQLSTATE % -- %', SQLSTATE, SQLERRM;
  END;

  RESET ROLE;
END $$;

\echo '============================================================'
\echo 'Phase 12 protection_checklist test complete. Rolling back.'
\echo 'Expected: 8 PASS notices total.'
\echo '============================================================'

ROLLBACK;
```

Notes:
- Mengikuti convention persis sama dengan `05-tighten-rls.sql` (BEGIN/ROLLBACK + RAISE NOTICE PASS/FAIL).
- 8 expected PASS: 5 RLS isolation (1.1, 1.2, 1.3, 1.4, 1.5) + 3 CHECK constraint (2.1, 2.2, 2.3).
- Synthetic UUIDs pakai range `c01..c03` + `c99/ce1/ce2/ce3` untuk avoid collision dengan tests lain (Phase 5 pakai `a01..a03`).
- Test ini valid baseline untuk Phase 14 future tests yang akan add mutation flow assertions.
  </action>
  <verify>
    <automated>test -f supabase/tests/12-protection-checklist.sql && grep -q "BEGIN;" supabase/tests/12-protection-checklist.sql && grep -q "ROLLBACK;" supabase/tests/12-protection-checklist.sql && grep -q "RAISE NOTICE 'PASS:" supabase/tests/12-protection-checklist.sql</automated>
  </verify>
  <done>SQL test file exist, follow BEGIN/ROLLBACK convention, ada minimal 8 PASS assertion paths</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Apply migration 0029 ke Supabase Cloud via Studio SQL Editor</name>
  <files>supabase/migrations/0029_protection_checklist.sql (apply via Studio paste, not modify)</files>
  <action>
**What's been built:** Migration file `0029_protection_checklist.sql` dan SQL test `12-protection-checklist.sql` sudah dibuat di repo (Tasks 1-2). Belum applied ke production database.

**Why human action needed:** `supabase db push` broken di project ini (history mismatch sejak v1.1 — see STATE.md decisions + memory `project_supabase_migration_workflow`). Studio SQL Editor manual paste adalah de-facto migration channel sampai TECHDEBT-01 (Phase 16) resolve.

**Steps untuk human:**

1. **Buka Supabase Studio Cloud** → project pfm-web → SQL Editor → New Query.

2. **Paste content** dari `supabase/migrations/0029_protection_checklist.sql` (file isi penuh, includes CREATE TABLE + ALTER TABLE + CREATE POLICY).

3. **Klik "Run"**. Expected output:
   - `Success. No rows returned` atau equivalent
   - Tidak ada error message tentang `relation "protection_checklist" already exists` (kalau ada → tabel sudah dibuat di run sebelumnya, OK skip)

4. **Verifikasi table exist** — di SQL Editor jalankan:
   ```sql
   SELECT column_name, data_type, is_nullable
   FROM information_schema.columns
   WHERE table_name = 'protection_checklist'
   ORDER BY ordinal_position;
   ```
   Expected: 11 rows (user_id PK, 8 business fields, updated_at, created_at).

5. **Verifikasi RLS enabled** — di SQL Editor:
   ```sql
   SELECT relname, relrowsecurity FROM pg_class WHERE relname = 'protection_checklist';
   ```
   Expected: `relrowsecurity = true`.

6. **(Opsional, kalau psql installed)** Run SQL test:
   ```bash
   psql "$DATABASE_URL" -f supabase/tests/12-protection-checklist.sql
   ```
   Expected: 8 PASS notices, zero FAIL. Kalau psql tidak ada, skip test live; SQL test akan jalan di future audit.

7. **Report back** — tempel output Run + screenshot/text dari verifikasi step 4-5.
  </action>
  <verify>
    <automated>echo "Manual verification — see action step 4-5: SQL queries di Supabase Studio"</automated>
  </verify>
  <done>Migration applied di Supabase Cloud, table protection_checklist exist dengan 11 kolom + RLS enabled (relrowsecurity=true)</done>
  <resume-signal>Tempel output Studio Run + step 4 query result, atau type "applied" kalau sudah verified manually</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PostgREST → Supabase | Authenticated user query/mutate protection_checklist via PostgREST RPC layer |
| admin View-As | Admin auth.uid() berbeda dari `user_id` parameter saat read; harus tetap RLS-protected pada WRITE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | I (Information disclosure) | protection_checklist SELECT | mitigate | RLS USING `auth.uid() = user_id OR is_admin()` — verified via SQL test §1.1 (non-admin cross-user SELECT returns 0 rows) |
| T-12-02 | T (Tampering) | protection_checklist UPDATE | mitigate | RLS WITH CHECK `auth.uid() = user_id` — admin tidak bisa modify atas nama user lain. Verified via SQL test §1.3 (non-admin UPDATE other user → 0 rows) + §1.5 (admin INSERT cross-user → 42501) |
| T-12-03 | T | CHECK constraints health_coverage, life_coverage, life_coverage_post_employment | mitigate | Database-level CHECK enum (5/4/3 values) — frontend bug atau direct API call dengan invalid enum hits 23514. Verified via SQL test §2.1-2.3 |
| T-12-04 | E (Elevation of privilege) | RLS policy bypass via SECURITY DEFINER function | accept | Phase 12 tidak define SECURITY DEFINER function untuk write path — Phase 14 akan add (kalau perlu) dengan dedicated threat model. Phase 12 risk surface = direct PostgREST INSERT/UPDATE only. |
| T-12-05 | I | user_id field di response payload | accept | Standard pattern — user authenticated baca own user_id sendiri; admin View-As baca user_id user lain dengan permission. No leak vector beyond standard RLS scope. |
</threat_model>

<verification>
- File `supabase/migrations/0029_protection_checklist.sql` exist dengan 8 business fields + RLS + CHECK
- File `supabase/tests/12-protection-checklist.sql` exist dengan BEGIN/ROLLBACK convention
- Migration applied ke Supabase Cloud (verified via `information_schema.columns` query)
- RLS enabled (verified via `pg_class.relrowsecurity`)
- Optional: SQL test runs all PASS via psql kalau available
</verification>

<success_criteria>
1. Tabel `protection_checklist` exist di production dengan 11 kolom (1 PK + 8 business + 2 timestamps)
2. RLS aktif: `relrowsecurity = true` di `pg_class`
3. Policy `Users manage own protection checklist` exist dengan USING + WITH CHECK clauses sesuai design spec
4. Migration file committed di repo dengan filename `0029_protection_checklist.sql`
5. SQL test file committed dengan minimum 8 PASS assertion paths
</success_criteria>

<output>
After completion, create `.planning/phases/12-kesehatan-foundation/12-01-SUMMARY.md` dengan:
- Migration applied timestamp + Studio Run output
- SQL test pass count (kalau dijalankan)
- Confirmation no schema change beyond 0029
- Hand-off note: Phase 14 lazy-create row pattern → use `INSERT ... ON CONFLICT (user_id) DO UPDATE SET ...`
</output>
