---
phase: 12-kesehatan-foundation
plan: 03
type: execute
wave: 2
depends_on: [12-02]
files_modified:
  - src/queries/kesehatan.ts
  - src/tabs/kesehatan/EmptyStateCTA.tsx
  - src/tabs/kesehatan/KesehatanLanding.tsx
autonomous: false
requirements: [DIAG-11]
must_haves:
  truths:
    - "User dengan total rows transactions+accounts+goals+investments < 3 lihat piramida grayed-empty + welcome banner + 3 quick-link CTA"
    - "User dengan total rows ≥ 3 lihat landing normal (PiramidaShell default + banner kalkulator + grid modul)"
    - "Banner kalkulator + grid 6 modul TETAP accessible di empty state (modul/kalkulator tidak butuh user data)"
    - "3 CTA buttons di empty state link ke: /transaksi (Catat transaksi pertama), /kekayaan (Tambah akun bank), /goals (Bikin tujuan finansial)"
    - "Total count query support View-As — admin dengan viewingAs aktif lihat empty state berdasarkan data viewed-user, bukan admin sendiri"
    - "Saat query loading, landing render skeleton/loading state — tidak blank atau layout shift janggal"
  artifacts:
    - path: "src/queries/kesehatan.ts"
      provides: "useTotalDataCount hook — agregat count dari 4 tabel (transactions/net_worth_accounts/goals/investments) dengan View-As support"
      exports: ["useTotalDataCount", "EMPTY_STATE_THRESHOLD"]
    - path: "src/tabs/kesehatan/EmptyStateCTA.tsx"
      provides: "Welcome banner component dengan 3 quick-link CTA"
      min_lines: 30
    - path: "src/tabs/kesehatan/KesehatanLanding.tsx"
      provides: "Updated landing dengan conditional empty state vs normal render"
      contains: "useTotalDataCount"
  key_links:
    - from: "KesehatanLanding"
      to: "useTotalDataCount via useQuery"
      via: "react-query hook"
      pattern: "useTotalDataCount"
    - from: "useTotalDataCount"
      to: "useTargetUserId() (View-As pattern)"
      via: "viewingAs ?? userId"
      pattern: "useTargetUserId"
    - from: "EmptyStateCTA buttons"
      to: "/transaksi, /kekayaan, /goals"
      via: "useNavigate"
      pattern: "navigate\\(/transaksi|/kekayaan|/goals\\)"
---

<objective>
Implement DIAG-11 empty state full untuk landing `/kesehatan`. Trigger: total rows count dari 4 tabel (`transactions + net_worth_accounts + goals + investments`) < 3 untuk target user (View-As-aware).

Tampilan empty state (per CONTEXT.md decisions + design spec §4 "Empty state full"):
- Hero piramida: pakai `PiramidaShell variant="grayed-empty"` (sudah dibuat di Plan 12-02 — tinggal pass prop)
- Welcome banner: "Yuk mulai isi data" header + sub-text
- 3 quick-link CTA buttons: `/transaksi`, `/kekayaan`, `/goals`
- Banner kalkulator + grid 6 modul **tetap accessible** di bawah (modul tidak butuh user data per CONTEXT.md)

Loading state: saat query masih fetch, render skeleton untuk avoid layout shift / blank flash.

Purpose: Guide user baru ke aksi konkret pertama. Tanpa empty state, user baru lihat piramida shell tanpa konteks → bingung "ini apa?". Empty state turn landing jadi onboarding.

Output:
- 1 new query hook `useTotalDataCount` di `src/queries/kesehatan.ts` (file baru, akan extend di Phase 13 dengan indicator queries lainnya)
- 1 new component `EmptyStateCTA.tsx`
- Modify `KesehatanLanding.tsx` untuk conditional render empty vs normal

Implements:
- DIAG-11 (per CONTEXT.md decisions section "Empty State Full (DIAG-11)" + design spec §4 empty state)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-kesehatan-foundation/12-CONTEXT.md
@.planning/phases/12-kesehatan-foundation/12-02-SUMMARY.md
@docs/superpowers/specs/2026-05-08-framework-page-design.md
@src/auth/useTargetUserId.ts
@src/queries/payPeriods.ts
@src/queries/recurringDueCount.ts

<interfaces>
<!-- Existing patterns yang harus diikuti -->

```ts
// src/auth/useTargetUserId.ts — sudah exist, pattern wajib untuk View-As
import { useTargetUserId } from '@/auth/useTargetUserId'
const targetUid = useTargetUserId()  // returns viewingAs?.uid ?? user?.id
```

```ts
// src/queries/recurringDueCount.ts — pattern compose query dari hook lain
// Bisa pakai pattern ini untuk count: panggil 4 useQuery existing (transactions/netWorth/goals/investments)
// dan agregat client-side. Tapi NB: existing hooks return rows, bukan count — overhead besar untuk dataset ribuan rows.

// PILIHAN A: pakai supabase select head:true count:exact (recommended untuk lightweight count)
import { supabase } from '@/lib/supabase'
const { count } = await supabase
  .from('transactions')
  .select('*', { count: 'exact', head: true })
  .eq('user_id', uid)
// → returns count tanpa fetch rows. RLS otomatis enforce ownership.
```

```ts
// React Query pattern existing (lihat src/queries/payPeriods.ts):
import { useQuery } from '@tanstack/react-query'

export function useFoo() {
  return useQuery({
    queryKey: ['foo'],
    queryFn: async () => { /* ... */ },
  })
}
```

```tsx
// PiramidaShell sudah expose variant prop dari Plan 12-02:
import PiramidaShell from './PiramidaShell'
<PiramidaShell variant="grayed-empty" />  // empty state visual
<PiramidaShell />                           // default
```

```tsx
// shadcn/ui Skeleton existing component
import { Skeleton } from '@/components/ui/skeleton'
<Skeleton className="h-12 w-full" />
```

```tsx
// shadcn/ui Button existing
import { Button } from '@/components/ui/button'
<Button variant="outline" onClick={() => navigate('/transaksi')}>
  Catat transaksi pertama
</Button>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Buat src/queries/kesehatan.ts dengan useTotalDataCount + EMPTY_STATE_THRESHOLD</name>
  <files>src/queries/kesehatan.ts</files>
  <action>
Create file `src/queries/kesehatan.ts` (file baru — will extend di Phase 13 dengan indicator queries lainnya):

```ts
import { useQuery } from '@tanstack/react-query'
import { supabase } from '@/lib/supabase'
import { useTargetUserId } from '@/auth/useTargetUserId'

/**
 * Empty state threshold untuk DIAG-11 — total rows < 3 = welcome state.
 * Konstanta supaya gampang adjust pasca-rilis (per STATE.md blockers/concerns:
 * "simpan threshold sebagai konstanta di src/queries/kesehatan.ts (bukan inline)
 * supaya gampang adjust pasca-rilis").
 */
export const EMPTY_STATE_THRESHOLD = 3

type CountResult = {
  total: number
  transactions: number
  accounts: number
  goals: number
  investments: number
}

/**
 * useTotalDataCount — count agregat dari 4 tabel data utama.
 *
 * Tujuan: trigger empty state DIAG-11 saat user baru (total < 3).
 *
 * View-As: pakai `useTargetUserId()` supaya admin View-As lihat empty state
 * berdasarkan data user yang di-view, bukan admin sendiri.
 *
 * Implementation: 4 parallel `select count:exact head:true` queries — lightweight
 * (no row fetch), RLS otomatis enforce ownership via auth.uid(). Untuk admin
 * View-As (impersonate other user), pakai filter `.eq('user_id', targetUid)`
 * — RLS USING clause `auth.uid() = user_id OR is_admin()` allow admin baca.
 *
 * Performance trade-off (per CONTEXT.md Claude's discretion):
 *   - Parallel 4 useQuery vs single RPC agregate: pilih parallel — simpler,
 *     no RPC needed, react-query default cache OK.
 *   - count:exact head:true → server returns count via Postgres count(*),
 *     no row data transferred. Sub-100ms typical.
 */
export function useTotalDataCount() {
  const targetUid = useTargetUserId()

  return useQuery<CountResult>({
    queryKey: ['kesehatan', 'totalDataCount', targetUid],
    enabled: !!targetUid,
    queryFn: async () => {
      if (!targetUid) {
        // Defensive — `enabled` flag should prevent this path
        return { total: 0, transactions: 0, accounts: 0, goals: 0, investments: 0 }
      }

      // 4 parallel HEAD count queries
      const [txRes, accRes, goalRes, invRes] = await Promise.all([
        supabase
          .from('transactions')
          .select('*', { count: 'exact', head: true })
          .eq('user_id', targetUid),
        supabase
          .from('net_worth_accounts')
          .select('*', { count: 'exact', head: true })
          .eq('user_id', targetUid),
        supabase
          .from('goals')
          .select('*', { count: 'exact', head: true })
          .eq('user_id', targetUid),
        supabase
          .from('investments')
          .select('*', { count: 'exact', head: true })
          .eq('user_id', targetUid),
      ])

      // Defensive — kalau ada error di salah satu query, log + treat sebagai 0.
      // Avoid crashing landing — empty state lebih baik dari blank screen.
      const transactions = txRes.error ? 0 : (txRes.count ?? 0)
      const accounts = accRes.error ? 0 : (accRes.count ?? 0)
      const goals = goalRes.error ? 0 : (goalRes.count ?? 0)
      const investments = invRes.error ? 0 : (invRes.count ?? 0)

      if (txRes.error || accRes.error || goalRes.error || invRes.error) {
        console.warn('[useTotalDataCount] partial error', {
          tx: txRes.error?.message,
          acc: accRes.error?.message,
          goal: goalRes.error?.message,
          inv: invRes.error?.message,
        })
      }

      return {
        total: transactions + accounts + goals + investments,
        transactions,
        accounts,
        goals,
        investments,
      }
    },
    // staleTime 60s — total count tidak perlu re-fetch agresif; user baru baru-baru ini
    // tambah row, count update di mutation invalidation existing (transactions/accounts/etc).
    // Default react-query staleTime 0 → unnecessary refetch saat tab focus. Override jadi 60s.
    staleTime: 60_000,
  })
}
```

Notes:
- `useTargetUserId()` is the existing pattern from `src/auth/useTargetUserId.ts` — returns `viewingAs?.uid ?? user?.id`. Pas admin View-As aktif, returns viewed-user uid → empty state trigger berdasarkan viewed-user data.
- `count: 'exact', head: true` di Supabase JS client → server jalankan `SELECT count(*) FROM ...` tanpa fetch rows. Lightweight, sub-100ms.
- `Promise.all` untuk 4 parallel — bukan sequential (sequential akan 4× latency).
- `staleTime: 60_000` — count tidak berubah cepat; refetch on tab focus tidak perlu agresif.
- File ini akan extend di Phase 13 dengan `useDanaDarurat`, `useSavingsRate`, dll. Phase 12 cuma seed file dengan satu hook.
- Threshold konstanta `EMPTY_STATE_THRESHOLD = 3` exported supaya component import + bandingkan: `count.total < EMPTY_STATE_THRESHOLD`.
- TIDAK pakai single RPC agregate (pilihan B di CONTEXT.md Claude's discretion) — simpler, no migration, no SECURITY DEFINER complexity. Kalau ternyata 4 parallel jadi bottleneck (unlikely di scale pfm-web), bisa optimize ke RPC di future phase.

**View-As pattern detail untuk audit/verifier:**
- Admin View-As pattern existing: `useTargetUserId()` returns `viewingAs?.uid ?? user?.id`. Pas admin aktif View-As ke user X, returns X's uid.
- RLS `net_worth_accounts`/`transactions`/`goals`/`investments` semua punya policy USING `auth.uid() = user_id OR is_admin()` — admin authenticated can SELECT semua user data.
- Filter `.eq('user_id', targetUid)` di query → returns count untuk target user only. Tanpa `.eq` filter, admin akan dapat count global semua user (bug). **`.eq('user_id', targetUid)` adalah CRITICAL untuk View-As correctness.**
- Test verify: admin tanpa View-As → targetUid = admin own uid → count admin's data. Admin dengan View-As ke user X → targetUid = X's uid → count X's data.
  </action>
  <verify>
    <automated>test -f src/queries/kesehatan.ts && grep -q "EMPTY_STATE_THRESHOLD" src/queries/kesehatan.ts && grep -q "useTotalDataCount" src/queries/kesehatan.ts && grep -q "useTargetUserId" src/queries/kesehatan.ts && grep -q "Promise.all" src/queries/kesehatan.ts && grep -q "user_id'" src/queries/kesehatan.ts && npx tsc --noEmit</automated>
  </verify>
  <done>File exist dengan exported `EMPTY_STATE_THRESHOLD = 3` + hook `useTotalDataCount` yang pakai View-As + Promise.all 4 parallel count queries. TypeScript compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Buat EmptyStateCTA component + modify KesehatanLanding untuk conditional empty state</name>
  <files>src/tabs/kesehatan/EmptyStateCTA.tsx, src/tabs/kesehatan/KesehatanLanding.tsx</files>
  <action>
**File 1 (NEW): `src/tabs/kesehatan/EmptyStateCTA.tsx`** — welcome banner dengan 3 CTA:

```tsx
import { useNavigate } from 'react-router-dom'
import { Wallet, Landmark, Target } from 'lucide-react'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Button } from '@/components/ui/button'

/**
 * DIAG-11 empty state — welcome banner untuk user baru (total rows < 3).
 *
 * 3 quick-link CTA per CONTEXT.md decisions:
 *   - Catat transaksi pertama → /transaksi
 *   - Tambah akun bank → /kekayaan
 *   - Bikin tujuan finansial → /goals
 *
 * Tampil di atas piramida grayed (PiramidaShell variant="grayed-empty").
 * Banner kalkulator + grid 6 modul tetap render di bawah (handled by KesehatanLanding).
 */
export default function EmptyStateCTA() {
  const navigate = useNavigate()

  return (
    <Card className="border-primary/20 bg-primary/5">
      <CardHeader>
        <CardTitle className="text-xl">Mulai dari mana?</CardTitle>
        <CardDescription>
          Yuk isi data dasar dulu — minimal 3 entri (transaksi, akun, atau goal) supaya piramida kesehatan bisa kasih warna sesuai kondisi keuangan kamu.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <div className="grid gap-3 sm:grid-cols-3">
          <Button
            variant="outline"
            className="h-auto justify-start gap-3 py-4 text-left"
            onClick={() => navigate('/transaksi')}
          >
            <Wallet className="h-5 w-5 shrink-0 text-primary" />
            <span className="flex flex-col items-start gap-0.5">
              <span className="text-sm font-semibold">Catat transaksi pertama</span>
              <span className="text-xs text-muted-foreground">Income atau expense</span>
            </span>
          </Button>

          <Button
            variant="outline"
            className="h-auto justify-start gap-3 py-4 text-left"
            onClick={() => navigate('/kekayaan')}
          >
            <Landmark className="h-5 w-5 shrink-0 text-primary" />
            <span className="flex flex-col items-start gap-0.5">
              <span className="text-sm font-semibold">Tambah akun bank</span>
              <span className="text-xs text-muted-foreground">Tabungan, e-wallet, dll</span>
            </span>
          </Button>

          <Button
            variant="outline"
            className="h-auto justify-start gap-3 py-4 text-left"
            onClick={() => navigate('/goals')}
          >
            <Target className="h-5 w-5 shrink-0 text-primary" />
            <span className="flex flex-col items-start gap-0.5">
              <span className="text-sm font-semibold">Bikin tujuan finansial</span>
              <span className="text-xs text-muted-foreground">Dana darurat, beli rumah</span>
            </span>
          </Button>
        </div>
      </CardContent>
    </Card>
  )
}
```

**File 2 (MODIFY): `src/tabs/kesehatan/KesehatanLanding.tsx`** — replace dengan conditional empty/normal render:

```tsx
import PiramidaShell from './PiramidaShell'
import KalkulatorBanner from './KalkulatorBanner'
import ModulCard from './ModulCard'
import EmptyStateCTA from './EmptyStateCTA'
import { MODUL_CATALOG } from './modulCatalog'
import { useTotalDataCount, EMPTY_STATE_THRESHOLD } from '@/queries/kesehatan'
import { Skeleton } from '@/components/ui/skeleton'

/**
 * Phase 12 landing — 3 section shell + DIAG-11 empty state branching.
 *
 * Empty state (total rows < 3):
 *   - PiramidaShell variant="grayed-empty"
 *   - EmptyStateCTA welcome banner dengan 3 quick-link CTA
 *   - Banner kalkulator + grid modul tetap accessible (modul/kalkulator gak butuh data)
 *
 * Normal state (total rows ≥ 3):
 *   - PiramidaShell default (Phase 13 akan inject indicators)
 *   - Banner kalkulator + grid modul
 *
 * Loading state: skeleton untuk piramida + banner placeholder, grid modul render
 * normal (modul catalog static, no data wait needed).
 */
export default function KesehatanLanding() {
  const countQuery = useTotalDataCount()

  const isLoading = countQuery.isLoading
  const isEmpty =
    !isLoading &&
    countQuery.data !== undefined &&
    countQuery.data.total < EMPTY_STATE_THRESHOLD

  return (
    <div className="space-y-6">
      <header className="space-y-1">
        <h1 className="text-2xl font-bold tracking-tight">Kesehatan Finansial</h1>
        <p className="text-sm text-muted-foreground">
          Lihat kondisi keuangan kamu lewat piramida 4 tier, lalu pelajari konsep finansial dari modul edukasi.
        </p>
      </header>

      {/* HERO: Piramida shell — branch berdasarkan loading/empty/normal */}
      <section aria-labelledby="piramida-heading">
        <h2 id="piramida-heading" className="sr-only">Piramida Kesehatan</h2>
        {isLoading ? (
          <div className="mx-auto flex max-w-md flex-col items-center gap-2 py-4">
            <Skeleton className="h-14 w-1/2" />
            <Skeleton className="h-14 w-2/3" />
            <Skeleton className="h-14 w-4/5" />
            <Skeleton className="h-14 w-[95%]" />
          </div>
        ) : (
          <PiramidaShell variant={isEmpty ? 'grayed-empty' : 'default'} />
        )}
      </section>

      {/* EMPTY STATE: Welcome banner dengan 3 quick-link CTA (hanya saat empty) */}
      {isEmpty && (
        <section aria-labelledby="empty-state-heading">
          <h2 id="empty-state-heading" className="sr-only">Welcome — mulai isi data</h2>
          <EmptyStateCTA />
        </section>
      )}

      {/* BANNER kalkulator — selalu accessible (empty atau normal) */}
      <section aria-labelledby="kalkulator-heading">
        <h2 id="kalkulator-heading" className="sr-only">Kalkulator Compound Interest</h2>
        <KalkulatorBanner />
      </section>

      {/* GRID 6 card modul — selalu accessible */}
      <section aria-labelledby="modul-heading" className="space-y-3">
        <h2 id="modul-heading" className="text-lg font-semibold">Modul Edukasi</h2>
        <div className="grid gap-3 sm:grid-cols-2">
          {MODUL_CATALOG.map((modul) => (
            <ModulCard key={modul.slug} modul={modul} />
          ))}
        </div>
      </section>
    </div>
  )
}
```

Notes:
- `isEmpty` calculated only after `!isLoading && data !== undefined` — avoid flicker dari "false → true → false" transient state.
- Loading state: skeleton untuk piramida (4 stacked skeleton bars matching trapezoid widths). Banner kalkulator + grid modul tetap render saat loading (modul catalog static, no benefit dari hide). Acceptable trade-off — landing tidak blank di loading.
- Empty state CTA banner ditempatkan **antara** piramida grayed dan banner kalkulator — visual hierarchy: shell di atas (visual placeholder), CTA primary action di tengah, secondary content (kalkulator/modul) di bawah.
- Grid modul `sm:grid-cols-2` tetap (per Plan 12-02). Banner kalkulator + modul stay visible empty atau normal.
- `<section>` semantic + sr-only headings untuk a11y — landmark navigation works.

**Behavior verification — View-As + edge cases:**
- Total = 0 (user baru banget) → empty state.
- Total = 2 (1 transaksi + 1 akun) → empty state (< 3).
- Total = 3 → normal state (≥ EMPTY_STATE_THRESHOLD).
- Total = 100 → normal state.
- Loading (network slow) → skeleton, no flash empty state.
- Query error (network fail) → `data === undefined` + `isLoading === false` → falls to neither branch; piramida renders default (no empty state). Defensive — better to show shell than show empty state when data uncertain.
- Admin View-As ke user dengan 0 data → empty state berdasarkan user data, bukan admin.
  </action>
  <verify>
    <automated>test -f src/tabs/kesehatan/EmptyStateCTA.tsx && test -f src/tabs/kesehatan/KesehatanLanding.tsx && grep -q "useTotalDataCount" src/tabs/kesehatan/KesehatanLanding.tsx && grep -q "EMPTY_STATE_THRESHOLD" src/tabs/kesehatan/KesehatanLanding.tsx && grep -q "grayed-empty" src/tabs/kesehatan/KesehatanLanding.tsx && grep -q "/transaksi" src/tabs/kesehatan/EmptyStateCTA.tsx && grep -q "/kekayaan" src/tabs/kesehatan/EmptyStateCTA.tsx && grep -q "/goals" src/tabs/kesehatan/EmptyStateCTA.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>EmptyStateCTA file exist dengan 3 CTA buttons + correct routes. KesehatanLanding modified dengan loading/empty/normal branching. TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual UAT — verify empty state behavior + View-As compatibility</name>
  <files>(visual UAT — no file changes; sanity check only)</files>
  <action>
**What's been built:**
- New query `useTotalDataCount` di `src/queries/kesehatan.ts` — 4 parallel HEAD count queries dengan View-As support (`useTargetUserId`)
- New component `EmptyStateCTA.tsx` — welcome banner dengan 3 quick-link CTA (Catat transaksi/Tambah akun/Bikin goal)
- Modified `KesehatanLanding.tsx` — branching loading/empty/normal:
  - Loading: skeleton 4-tier piramida + banner/modul tetap render
  - Empty (< 3 rows): PiramidaShell grayed-empty + EmptyStateCTA + banner kalkulator + grid modul
  - Normal (≥ 3 rows): PiramidaShell default + banner kalkulator + grid modul

**Steps untuk human:**
**Setup:**
```bash
npm run dev
```
Open http://localhost:5173 di Chrome.

**Test 1 — Empty state visual (login dengan akun yang punya < 3 rows total):**

Cara cepat tes empty state: bikin akun test fresh atau pakai akun existing dengan minimal data. Atau hack temporary: ubah `EMPTY_STATE_THRESHOLD = 3` ke nilai besar (mis. 9999) di file `src/queries/kesehatan.ts` untuk force trigger empty state regardless of count, lalu navigate ke /kesehatan untuk verify visual. Jangan lupa revert!

Expected visual saat empty state:
1. Header "Kesehatan Finansial" + tagline (sama dengan normal state).
2. Piramida 4 trapezoid **lebih gelap/redup** dari normal (variant grayed-empty pakai `bg-gray-200 text-gray-500` vs default `bg-gray-300 text-gray-700`).
3. Caption di bawah piramida: "Yuk mulai isi data untuk lihat warna hijau/kuning/merah di setiap tier."
4. **Card "Mulai dari mana?"** muncul — light primary background dengan 3 button outline:
   - Wallet icon + "Catat transaksi pertama" + "Income atau expense"
   - Landmark icon + "Tambah akun bank" + "Tabungan, e-wallet, dll"
   - Target icon + "Bikin tujuan finansial" + "Dana darurat, beli rumah"
5. Banner kalkulator tetap muncul di bawah card empty state.
6. Grid 6 modul tetap muncul di bawah banner kalkulator.

**Test 2 — Klik 3 CTA buttons di empty state:**
1. Klik "Catat transaksi pertama" → URL bar `/transaksi` → TransactionsTab render.
2. Back ke /kesehatan, klik "Tambah akun bank" → URL bar `/kekayaan` → KekayaanTab render.
3. Back ke /kesehatan, klik "Bikin tujuan finansial" → URL bar `/goals` → GoalsTab render.

**Test 3 — Tier piramida click di empty state:**
1. Pas di empty state, klik tier "Tier 1 PROTEKSI".
2. Expected toast: "Yuk isi data dulu — Tier 1 PROTEKSI akan terbuka setelah ada minimal 3 data poin." (variant grayed-empty toast message berbeda dari default).

**Test 4 — Normal state (login dengan akun production yang punya banyak data):**
1. Login dengan akun production (data lengkap — transaksi, akun, goals, investments).
2. Navigate ke /kesehatan.
3. Expected: piramida default (tidak grayed-empty), tidak ada card "Mulai dari mana?", banner kalkulator + grid modul render normal.
4. Klik tier piramida → toast normal "Tier X: detail panel akan tersedia di update berikutnya."

**Test 5 — Loading state (slow network):**
1. Chrome DevTools → Network tab → throttle ke "Slow 3G".
2. Hard reload `/kesehatan`.
3. Expected: piramida muncul sebagai 4 skeleton bars (gray pulsing), banner kalkulator + grid modul tetap render normal. Setelah query selesai, skeleton replaced dengan piramida actual.
4. **NO flash empty state** — pas loading, jangan ada split-second tampilan empty state CTA card lalu hilang. `data === undefined` selama loading harus mencegah branch ke isEmpty=true.

**Test 6 — View-As compatibility:**

(Hanya kalau punya akses akun admin + ada user lain di production untuk View-As):
1. Login sebagai admin.
2. Pengaturan → View-As → pilih user lain.
3. Navigate ke /kesehatan.
4. Expected: empty state atau normal state berdasarkan **viewed-user** data, bukan admin sendiri.
5. Spot check di SQL Editor (kalau verifiable):
   ```sql
   SELECT
     (SELECT count(*) FROM transactions WHERE user_id = '<viewed-user-uid>') AS tx,
     (SELECT count(*) FROM net_worth_accounts WHERE user_id = '<viewed-user-uid>') AS acc,
     (SELECT count(*) FROM goals WHERE user_id = '<viewed-user-uid>') AS goal,
     (SELECT count(*) FROM investments WHERE user_id = '<viewed-user-uid>') AS inv;
   ```
   Jumlah hasil < 3 → harus tampil empty state. ≥ 3 → normal state.

**Test 7 — Mobile responsive:**
1. Resize ke ≤640px width.
2. Empty state: 3 CTA button dari `sm:grid-cols-3` jadi single kolom (3 stacked buttons). Verify tidak overflow horizontal.
3. Banner kalkulator tetap responsive.

**Test 8 — Build production:**
```bash
npm run build
```
Expected: build success, no TypeScript error.

**Report back:**
- Screenshot empty state (full page) — Test 1
- Screenshot normal state (akun real production) — Test 4
- Confirmation 3 CTA buttons navigate correct route — Test 2
- Confirmation toast message berbeda di empty vs default — Test 3
- Loading state behavior — Test 5
- View-As behavior (kalau available) — Test 6
- Mobile screenshot — Test 7
- Build output — Test 8
  </action>
  <verify>
    <automated>echo "Manual visual verification — see action steps"</automated>
  </verify>
  <done>Visual UAT pass dengan screenshots/output reported, no regression</done>
  <resume-signal>Type "approved" + tempel screenshots dari Test 1, 4, 7, atau describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → 4 Supabase HEAD count queries | Authenticated user fetch count own data + admin View-As fetch count viewed-user |
| admin View-As → useTargetUserId() | Admin spoofs viewed-user uid via context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | I | useTotalDataCount returns count cross-user (View-As leak) | mitigate | RLS policies di 4 tabel (transactions/net_worth_accounts/goals/investments) sudah enforce `auth.uid() = user_id OR is_admin()`. Filter `.eq('user_id', targetUid)` cuma narrowing — tanpa filter, admin akan dapat count semua user (information disclosure). Verified via Test 6 View-As — admin lihat count viewed-user, bukan global. |
| T-12-10 | I | Non-admin user spoof targetUid via React DevTools | mitigate | useTargetUserId pakai `viewingAs?.uid` dari ViewAsContext yang only-set saat admin authenticated — non-admin tidak bisa modify context. RLS gateway: kalaupun non-admin somehow set `targetUid = otherUid`, query `.eq('user_id', otherUid)` ditolak RLS USING clause (auth.uid() != otherUid AND not is_admin) → returns count 0. No leak. |
| T-12-11 | D | 4 parallel count queries cause Supabase quota exhaustion | accept | HEAD count queries ringan; staleTime 60s mencegah refetch agresif. Worst case (user spam reload landing): 4 queries × refresh rate, masih order-of-magnitude di bawah quota tier. Monitor pasca-rilis kalau jadi masalah. |
| T-12-12 | T | EMPTY_STATE_THRESHOLD bypass via DevTools (hide empty state) | accept | User bypass = self-defeat (kehilangan onboarding hint). Tidak ada server-side state yang bisa di-tamper. Standard SPA model. |
</threat_model>

<verification>
- File `src/queries/kesehatan.ts` exist dengan exported `EMPTY_STATE_THRESHOLD` + `useTotalDataCount`
- File `src/tabs/kesehatan/EmptyStateCTA.tsx` exist dengan 3 CTA navigate ke `/transaksi`, `/kekayaan`, `/goals`
- `KesehatanLanding.tsx` modified dengan loading/empty/normal branching
- Visual UAT pass (Test 1-8) — empty state visible saat < 3 rows, normal state saat ≥ 3 rows, no flash di loading
- View-As verified (Test 6) — admin View-As lihat data viewed-user
- Build success
</verification>

<success_criteria>
1. User dengan total `transactions + accounts + goals + investments` rows < 3 lihat empty state landing dengan piramida grayed-out + welcome card "Mulai dari mana?" + 3 CTA buttons
2. Banner kalkulator + grid 6 modul **tetap accessible** di empty state
3. User dengan total ≥ 3 lihat landing normal (PiramidaShell default)
4. 3 CTA buttons di empty state navigate ke `/transaksi`, `/kekayaan`, `/goals` correctly
5. Loading state render skeleton 4-tier (no flash empty state)
6. View-As: admin View-As lihat empty/normal berdasarkan data viewed-user
7. Mobile responsive: 3 CTA buttons single kolom di ≤640px
8. `npm run build` success
9. Threshold konstanta `EMPTY_STATE_THRESHOLD = 3` exported dari `src/queries/kesehatan.ts` (mudah adjust pasca-rilis per STATE.md blockers/concerns)
</success_criteria>

<output>
After completion, create `.planning/phases/12-kesehatan-foundation/12-03-SUMMARY.md` dengan:
- Files created/modified list
- Visual UAT results dengan screenshots empty + normal + mobile
- View-As verification result
- Confirmation: `EMPTY_STATE_THRESHOLD` exported sebagai konstanta supaya gampang adjust pasca-rilis
- Hand-off note untuk Phase 13:
  - File `src/queries/kesehatan.ts` siap di-extend dengan indicator queries (useDanaDarurat, useSavingsRate, dst)
  - PiramidaShell siap di-extend dengan props `indicators` untuk warna hijau/kuning/merah
  - Empty state behavior — Phase 13 indikator placeholder masih tampil di tier panel (tier abu-abu hanya kalau SEMUA indikator placeholder, per design spec §4 agregasi warna tier)
- Hand-off note untuk Phase 14:
  - `protection_checklist` table sudah live (Plan 12-01)
  - View-As pattern `useTargetUserId()` sudah live di Phase 12 — Phase 14 pakai pattern yang sama untuk read; mutation form harus add read-only guard (per DIAG-12 di Phase 14 scope)
</output>
