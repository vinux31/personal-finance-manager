# Rencana Jan 2027 Integration — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrasikan data blueprint `rencana-keuangan-v2.html` ke pfm-web — seed portfolio, goals, dan kategori income Pertamina; tampilkan progress banner Rp 257M di Dashboard.

**Architecture:** Hybrid approach — SQL migration untuk 5 kategori income (config data), seed functions di db layer untuk goals + investasi (idempotent by name), `useRencanaInit` hook yang berjalan sekali via localStorage flag, dan `RencanaBar` component baru di Dashboard.

**Tech Stack:** React 19, TypeScript, Supabase JS v2, TanStack Query v5, shadcn/ui, Tailwind CSS v4

---

## File Map

| File | Action | Tanggung Jawab |
|------|--------|----------------|
| `supabase/migrations/0004_kategori_pertamina.sql` | Create | 5 kategori income Pertamina |
| `src/db/goals.ts` | Modify | Tambah `RENCANA_GOALS` + `seedRencanaGoals()` |
| `src/db/investments.ts` | Modify | Tambah `RENCANA_INVESTMENTS` + `seedRencanaInvestments()` |
| `src/lib/useRencanaInit.ts` | Create | Hook seed sekali via localStorage |
| `src/components/RencanaBar.tsx` | Create | Compact progress banner |
| `src/tabs/DashboardTab.tsx` | Modify | Tambah `useRencanaInit()` + `<RencanaBar>` |

---

## Task 1: SQL Migration — Kategori Income Pertamina

**Files:**
- Create: `supabase/migrations/0004_kategori_pertamina.sql`

- [ ] **Step 1.1: Buat file migration**

```sql
-- supabase/migrations/0004_kategori_pertamina.sql
-- Tambah 5 kategori income khusus Pertamina
INSERT INTO categories (name, type, icon) VALUES
  ('THR Keagamaan',          'income', '💰'),
  ('IKI (Insentif Kinerja)', 'income', '📈'),
  ('Jaspro / Tantiem',       'income', '🏭'),
  ('Gaji ke-13',             'income', '💵'),
  ('Tunjangan Cuti',         'income', '🏖️')
ON CONFLICT (name, type) DO NOTHING;
```

> UNIQUE constraint di DB adalah `(name, type)` — bukan hanya `(name)`. `ON CONFLICT (name, type)` wajib.

- [ ] **Step 1.2: Jalankan migration via Supabase dashboard**

Buka Supabase project → SQL Editor → paste isi file → Run.

Atau via CLI:
```bash
npx supabase db push
```

- [ ] **Step 1.3: Verifikasi kategori muncul**

Di SQL Editor Supabase:
```sql
SELECT name, type, icon FROM categories WHERE type = 'income' ORDER BY name;
```

Expected: 5 baris baru muncul (THR Keagamaan, IKI, Jaspro, Gaji ke-13, Tunjangan Cuti) plus kategori income yang sudah ada sebelumnya.

- [ ] **Step 1.4: Commit**

```bash
git add supabase/migrations/0004_kategori_pertamina.sql
git commit -m "feat(db): add Pertamina income categories migration"
```

---

## Task 2: Seed Function — Goals

**Files:**
- Modify: `src/db/goals.ts`

- [ ] **Step 2.1: Tambah konstanta `RENCANA_GOALS` dan fungsi `seedRencanaGoals` ke `src/db/goals.ts`**

Tambahkan di bagian bawah file, setelah fungsi `goalProgress`:

```ts
// --- Rencana Jan 2027 seed data ---

const RENCANA_GOALS: GoalInput[] = [
  {
    name: 'Dana Pernikahan',
    target_amount: 100_000_000,
    current_amount: 0,
    target_date: '2027-01-01',
    status: 'active',
  },
  {
    name: 'DP + Akad Kredit Xpander',
    target_amount: 118_000_000,
    current_amount: 0,
    target_date: '2027-01-01',
    status: 'active',
  },
  {
    name: 'Non-Budget Nikah',
    target_amount: 10_000_000,
    current_amount: 0,
    target_date: '2027-01-01',
    status: 'active',
  },
  {
    name: 'Dana Darurat',
    target_amount: 24_000_000,
    current_amount: 0,
    target_date: '2026-12-01',
    status: 'active',
  },
  {
    name: 'Buffer Cadangan',
    target_amount: 5_000_000,
    current_amount: 0,
    target_date: '2027-01-01',
    status: 'active',
  },
]

export async function seedRencanaGoals(): Promise<void> {
  const existing = await listGoals()
  const existingNames = new Set(existing.map((g) => g.name))
  const toInsert = RENCANA_GOALS.filter((g) => !existingNames.has(g.name))
  for (const g of toInsert) await createGoal(g)
}
```

- [ ] **Step 2.2: Verifikasi TypeScript tidak error**

```bash
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 2.3: Commit**

```bash
git add src/db/goals.ts
git commit -m "feat(db): add seedRencanaGoals — 5 goals dari blueprint Jan 2027"
```

---

## Task 3: Seed Function — Investasi

**Files:**
- Modify: `src/db/investments.ts`

- [ ] **Step 3.1: Tambah konstanta `RENCANA_INVESTMENTS` dan fungsi `seedRencanaInvestments` ke `src/db/investments.ts`**

Tambahkan di bagian bawah file, setelah fungsi `gainLossPercent`:

```ts
// --- Rencana Jan 2027 seed data ---

const RENCANA_INVESTMENTS: InvestmentInput[] = [
  {
    asset_type: 'Reksadana',
    asset_name: 'Reksadana Sukuk Sucorinvest Sharia',
    quantity: 1,
    buy_price: 100_000_000,
    current_price: 100_000_000,
    buy_date: '2026-04-01',
    note: 'Seeded dari rencana-keuangan-v2.html',
  },
  {
    asset_type: 'Emas',
    asset_name: 'Emas Tabungan Pegadaian',
    quantity: 5.5278,
    buy_price: 2_683_000,
    current_price: 2_683_000,
    buy_date: '2026-04-01',
    note: 'Seeded dari rencana-keuangan-v2.html',
  },
  {
    asset_type: 'Saham',
    asset_name: 'Saham BMRI',
    quantity: 1,
    buy_price: 6_129_180,
    current_price: 6_129_180,
    buy_date: '2026-04-01',
    note: 'Seeded dari rencana-keuangan-v2.html',
  },
]

export async function seedRencanaInvestments(): Promise<void> {
  const existing = await listInvestments()
  const existingNames = new Set(existing.map((i) => i.asset_name))
  const toInsert = RENCANA_INVESTMENTS.filter((i) => !existingNames.has(i.asset_name))
  for (const inv of toInsert) await createInvestment(inv)
}
```

> **Catatan Reksadana:** Disimpan qty=1, buy_price=100.000.000 (nilai total unit). Ini karena Sucorinvest tidak tracking unit NAV secara individual — nilai total lebih mudah di-update langsung.
>
> **Catatan Emas:** qty=gram (5.5278), buy_price=harga per gram (Rp 2.683.000). Total cost basis = 5.5278 × 2.683.000 ≈ Rp 14.831.117 (sedikit berbeda dari blueprint Rp 14.836.000 karena pembulatan).

- [ ] **Step 3.2: Verifikasi TypeScript tidak error**

```bash
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 3.3: Commit**

```bash
git add src/db/investments.ts
git commit -m "feat(db): add seedRencanaInvestments — 3 instrumen dari blueprint"
```

---

## Task 4: useRencanaInit Hook

**Files:**
- Create: `src/lib/useRencanaInit.ts`

- [ ] **Step 4.1: Buat file `src/lib/useRencanaInit.ts`**

```ts
import { useEffect } from 'react'
import { useQueryClient } from '@tanstack/react-query'
import { seedRencanaGoals } from '@/db/goals'
import { seedRencanaInvestments } from '@/db/investments'

const SEED_KEY = 'rencana_seeded'

export function useRencanaInit() {
  const qc = useQueryClient()

  useEffect(() => {
    if (localStorage.getItem(SEED_KEY)) return

    Promise.all([seedRencanaGoals(), seedRencanaInvestments()])
      .then(() => {
        localStorage.setItem(SEED_KEY, '1')
        qc.invalidateQueries({ queryKey: ['goals'] })
        qc.invalidateQueries({ queryKey: ['investments'] })
      })
      .catch(console.error)
  }, [])
}
```

> `useEffect` dengan deps `[]` berjalan sekali setelah mount. `localStorage` check di awal mencegah re-seed saat Dashboard di-kunjungi ulang. `invalidateQueries` memastikan Goals dan Investasi di-refetch agar UI langsung update.

- [ ] **Step 4.2: Verifikasi TypeScript tidak error**

```bash
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 4.3: Commit**

```bash
git add src/lib/useRencanaInit.ts
git commit -m "feat(lib): add useRencanaInit hook — seed goals + investasi sekali"
```

---

## Task 5: RencanaBar Component

**Files:**
- Create: `src/components/RencanaBar.tsx`

- [ ] **Step 5.1: Buat file `src/components/RencanaBar.tsx`**

```tsx
import { Progress } from '@/components/ui/progress'
import { formatRupiah } from '@/lib/format'

const TARGET_RENCANA = 257_000_000

interface RencanaBarProps {
  totalNilai: number
}

export default function RencanaBar({ totalNilai }: RencanaBarProps) {
  const progress = Math.min(100, (totalNilai / TARGET_RENCANA) * 100)
  const gap = Math.max(0, TARGET_RENCANA - totalNilai)

  const deadline = new Date('2027-01-01')
  const now = new Date()
  const bulanLagi =
    (deadline.getFullYear() - now.getFullYear()) * 12 +
    (deadline.getMonth() - now.getMonth())

  return (
    <div className="flex items-center gap-3 rounded-lg border border-blue-200 bg-blue-50 px-4 py-3">
      <div className="flex shrink-0 flex-col items-center rounded-md bg-blue-600 px-3 py-1.5 text-white">
        <span className="text-xs font-medium opacity-80">Jan 2027</span>
        <span className="text-lg font-bold leading-tight">{progress.toFixed(0)}%</span>
      </div>

      <div className="min-w-0 flex-1">
        <div className="mb-1.5 text-sm font-medium text-blue-900">
          {formatRupiah(totalNilai)} / {formatRupiah(TARGET_RENCANA)}
          {bulanLagi > 0 && (
            <span className="ml-1 font-normal text-blue-700">· {bulanLagi} bulan lagi</span>
          )}
        </div>
        <Progress value={progress} className="h-2" />
      </div>

      {gap > 0 && (
        <div className="shrink-0 text-right">
          <div className="text-xs text-muted-foreground">Gap</div>
          <div className="text-sm font-semibold text-red-600">{formatRupiah(gap)}</div>
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 5.2: Verifikasi TypeScript tidak error**

```bash
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 5.3: Commit**

```bash
git add src/components/RencanaBar.tsx
git commit -m "feat(components): add RencanaBar — compact progress banner Jan 2027"
```

---

## Task 6: Integrasi ke DashboardTab

**Files:**
- Modify: `src/tabs/DashboardTab.tsx`

- [ ] **Step 6.1: Tambah imports di `src/tabs/DashboardTab.tsx`**

Tambahkan 2 import setelah baris import yang sudah ada:

```tsx
// Tambahkan setelah import { ArrowUpCircle, ... } from 'lucide-react'
import RencanaBar from '@/components/RencanaBar'
import { useRencanaInit } from '@/lib/useRencanaInit'
```

- [ ] **Step 6.2: Panggil `useRencanaInit()` di dalam komponen**

Tambahkan satu baris setelah deklarasi hooks yang sudah ada di `DashboardTab`:

```tsx
export default function DashboardTab() {
  const today = todayISO()
  const monthStart = firstDayOfMonth()

  const { data: periodData = [] } = useAggregateByPeriod('month', monthStart, today)
  const { data: invRows = [] } = useInvestments()
  const { data: goals = [] } = useGoals()
  const { data: recentTx = [] } = useTransactions({ limit: 5 })

  useRencanaInit() // ← tambahkan di sini

  // ... sisa kode tidak berubah
```

- [ ] **Step 6.3: Tambah `<RencanaBar>` di antara metric cards dan panel grid**

Cari bagian `return` di DashboardTab. Tambahkan `<RencanaBar>` setelah closing tag `</div>` dari grid metric cards dan sebelum `<div className="grid grid-cols-1 gap-6 md:grid-cols-2">`:

```tsx
  return (
    <div className="space-y-6">
      {/* Summary cards */}
      <div className="grid grid-cols-2 gap-3 sm:grid-cols-4">
        <MetricCard ... />
        <MetricCard ... />
        <MetricCard ... />
        <MetricCard ... />
      </div>

      {/* Rencana Jan 2027 banner — hanya tampil jika ada data investasi */}
      {inv.totalNilai > 0 && <RencanaBar totalNilai={inv.totalNilai} />}

      <div className="grid grid-cols-1 gap-6 md:grid-cols-2">
        {/* Recent transactions */}
        ...
```

- [ ] **Step 6.4: Verifikasi TypeScript tidak error**

```bash
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 6.5: Jalankan dev server dan buka Dashboard**

```bash
npm run dev
```

Buka `http://localhost:5173` → login → tab Dashboard.

Expected:
- RencanaBar muncul di antara metric cards dan panel bawah
- Badge biru kiri menampilkan persentase + "Jan 2027"
- Progress bar terisi sesuai `totalNilai / 257M`
- Gap merah muncul di kanan (selama totalNilai < 257M)
- Cek console browser: tidak ada error TypeScript/runtime

- [ ] **Step 6.6: Verifikasi seed berjalan sekali**

Setelah DashboardTab pertama kali dibuka:
1. Buka tab Investasi → pastikan 3 instrumen muncul (Sukuk, Emas, BMRI)
2. Buka tab Goals → pastikan 5 goals muncul dengan `current_amount = 0`
3. Refresh halaman → buka Dashboard lagi → cek console, tidak ada log seed ulang
4. Buka DevTools → Application → Local Storage → pastikan key `rencana_seeded = 1` ada

- [ ] **Step 6.7: Verifikasi kategori income tersedia di Transaksi**

Buka tab Transaksi → klik "Tambah Transaksi" → set type "Pemasukan" → dropdown Kategori harus menampilkan 5 kategori baru: THR Keagamaan, IKI, Jaspro/Tantiem, Gaji ke-13, Tunjangan Cuti.

- [ ] **Step 6.8: Commit final**

```bash
git add src/tabs/DashboardTab.tsx
git commit -m "feat(dashboard): integrate RencanaBar + useRencanaInit for Jan 2027 tracking"
```

---

## Verifikasi End-to-End

Setelah semua task selesai, lakukan full check:

- [ ] Dashboard menampilkan RencanaBar dengan progress ~47% (Rp 120,9Jt / Rp 257Jt)
- [ ] Tab Investasi: 3 instrumen terseed (Sukuk Rp 100M, Emas Rp 14,8M, BMRI Rp 6,1M)
- [ ] Tab Goals: 5 goals aktif dengan target total Rp 257M
- [ ] Tab Transaksi → Tambah Transaksi → Pemasukan: 5 kategori Pertamina tersedia
- [ ] Refresh app: tidak ada duplikat goals/investasi
- [ ] Reset seed (hapus `rencana_seeded` dari localStorage) → refresh → seed berjalan lagi dengan benar
